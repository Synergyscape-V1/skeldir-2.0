/** Default document title — single source of truth for root layout + OG fallbacks */
export const SITE_DOCUMENT_TITLE =
  "Skeldir — Verified Ad Revenue for AI-Native Stacks" as const;

export const SITE_DESCRIPTION =
  "Skeldir shows you the gap between what ad platforms claim and what actually hits your bank account-so you can move budget with confidence, not guesswork." as const;
