/** Category differentiation anchor - home + pricing (single source of truth). */
export const CATEGORY_ANCHOR_BODY =
  "Popular tools like Triple Whale model which channel probably drove your revenue using probabilistic ML - Skeldir verifies whether your ad platform revenue claims are true using transaction records, then outputs audited proof your AI agents can act on." as const;

export const CATEGORY_ANCHOR_CLOSER_PARTS = [
  "Different operation.",
  "Different layer.",
  "Different product.",
] as const;
