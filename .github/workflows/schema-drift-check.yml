name: Schema Drift Check (Disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Schema drift check disabled
        run: echo "Schema drift check disabled pending migration system readiness."
