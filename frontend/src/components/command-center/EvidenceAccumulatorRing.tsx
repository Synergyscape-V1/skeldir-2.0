/**
 * Evidence Accumulator Ring — A4-ATLAS signature animation
 *
 * 14-segment circular SVG. Each segment = 1 day of Bayesian model window.
 * Filled segments glow with brand primary. Ring breathes (1.0 → 1.02, 3000ms).
 * On update, newest segment pulses over 500ms.
 *
 * Sourced directly from A4-ATLAS iteration (selected component).
 */
export { EvidenceAccumulatorRing } from '@/agents/a4-atlas/animations/EvidenceAccumulatorRing';
