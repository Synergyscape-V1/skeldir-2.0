# A1-SENTINEL Asset Attribution Log

## Icons
- **Lucide React** (lucide.dev) — MIT License
  - Used: LayoutDashboard, Layers, Database, PiggyBank, Search, Settings, Menu, X, HelpCircle, RefreshCw, Bell, Activity, TrendingUp, TrendingDown, Minus, AlertTriangle, AlertCircle, Info, ChevronRight, ArrowUpDown, Radio, Plug, Clock, Lock
  - Modification: none — used as provided
  - Location: shell/, command-center/

## SVG Animations
- **RadarConfidenceSweep** — Original creation for A1-SENTINEL
  - No external assets sourced
  - Procedurally generated SVG arcs and gradients
  - All colors reference CSS custom properties (no hardcoded values)

## Typography
- **Syne** (Google Fonts) — OFL License
  - Used for headings via `--font-sans` CSS variable
- **IBM Plex Sans** (Google Fonts) — OFL License
  - Used for body text via `--font-sans` CSS variable
- **IBM Plex Mono** (Google Fonts) — OFL License
  - Used for all numeric/data values via `font-mono` class

## Notes
All visual assets are either part of the existing design system (D0–D1 atoms)
or procedurally generated SVG. No external stock imagery is used. The industrial/aerospace
aesthetic is achieved through geometric SVG primitives and controlled token usage,
not sourced raster assets.
