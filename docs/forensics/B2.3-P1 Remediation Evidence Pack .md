# B2.3-P1 Remediation Evidence Pack

Generated: 2026-04-29  
Primary branch authority target: `main`  
Directive cycle: **B2.3-P1 Context-Robust, Hypothesis-Anchored Remediation Directive**

## 1. Baseline Convergence

1. Work executed from `main` baseline and scoped strictly to B2.3-P1 schema authority substrate.
2. No P2/P3/P4 implementation surfaces were introduced (no match kernel, extraction registry runtime, state-transition jobs, read APIs, queue routing, or benchmark harness additions).
3. P0 and Pre-P1 invariant surfaces were preserved as inherited constraints.

## 2. Initial Findings (Validated)

1. **Validated (P1-FH01):** no authoritative B2.3-P1 match verdict substrate existed with exact five-state DB constraint and quality constraint.
2. **Validated (P1-FH02):** no tenant-scoped B2.3-P1 table set existed with explicit DB-layer isolation policy set for all required P1 tables.
3. **Validated (P1-FH03):** no first-class exception workflow table existed with DB-enforced resolved/dismissed requires-code behavior.
4. **Validated (P1-FH04):** no dedicated B2.3 post-capture revenue event ledger substrate existed with DB-enforced taxonomy including `reversal` and tenant-scoped provider idempotency uniqueness.
5. **Validated (P1-FH05):** no dedicated SQL-queryable B2.3 ingestion telemetry substrate existed under P1 authority scope.
6. **Validated (P1-FH06/FH08/FH09/FH10):** no P1 constants module, no P1 schema enforcer, no P1 reversibility harness, and no canonical-schema P1 authority mirror existed.

## 3. Remediations Implemented

### R01 - B2.3-P1 schema migration substrate

Files:
- `alembic/versions/007_skeldir_foundation/202604291200_b23_p1_schema_authority_lock.py`

Changes:
1. Added tenant-scoped `b23_match_verdicts` with DB-enforced exact five-state lifecycle:
   - `pending`
   - `matched_provisional`
   - `matched_confirmed`
   - `adjusted`
   - `unmatched`
2. Added DB-enforced match quality constraint:
   - `high`
   - `medium`
   - `low`
3. Added first-class `b23_exception_records` with DB-enforced workflow and severity taxonomy:
   - workflow: `open`, `acknowledged`, `resolved`, `dismissed`
   - severity: `flagged`, `alert`
   - resolved/dismissed requires non-null `resolution_code` at DB constraint level.
4. Added tenant-scoped append-only `b23_revenue_events` with DB-enforced event taxonomy:
   - `payment_capture`
   - `partial_refund`
   - `full_refund`
   - `chargeback_opened`
   - `chargeback_won`
   - `chargeback_lost`
   - `reversal`
5. Added tenant/provider idempotency uniqueness:
   - `uq_b23_revenue_events_tenant_provider_event_ref (tenant_id, provider, provider_native_event_reference)`.
6. Added SQL-queryable `b23_webhook_ingestion_logs` with required telemetry columns:
   - `provider`,
   - nullable `failure_reason`,
   - `received_at`.
7. Added RLS + FORCE RLS + tenant isolation policies on all four P1 tables.
8. Added indexes for tenant/status/provider/reference/timestamp access patterns required by P1 substrate.

Outcome:
- P1 schema primitives are now DB-authoritative, tenant-isolated, and match-input semantics are explicitly locked.

### R02 - Named timing constants authority module

Files:
- `backend/app/revenue_verification/timing_constants.py`
- `backend/app/revenue_verification/__init__.py`

Changes:
1. Added single importable module exporting duration-typed constants:
   - `WEBHOOK_ARRIVAL_WINDOW = timedelta(minutes=30)`
   - `PROVISIONAL_MATCH_WINDOW = timedelta(hours=24)`
   - `REFUND_REOPENING_WINDOW = timedelta(days=30)`
2. Exported constants through package init for downstream phase imports.

Outcome:
- Timing authority is centralized and typed, with Pre-P1 values preserved exactly.

### R03 - Canonical schema parity lock

Files:
- `db/schema/canonical_schema.sql`

Changes:
1. Added canonical schema definitions for all P1 tables, constraints, indexes, FORCE RLS markers, and tenant-isolation policies.
2. Added canonical DB constraint mirrors for:
   - match status exact five-state set,
   - match quality set,
   - exception resolution-code requirement,
   - revenue-event taxonomy including `reversal`,
   - revenue-event idempotency uniqueness.

Outcome:
- Canonical schema now mirrors migration authority for B2.3-P1 surfaces.

### R04 - P1 governance contract + enforcer

Files:
- `contracts-internal/governance/b23_p1_schema_authority_lock.main.json`
- `scripts/ci/enforce_b23_p1_schema_authority_lock.py`

Changes:
1. Added P1 governance contract encoding authoritative table names, status/taxonomy sets, timing constants, forbidden payload tokens, and required CI wiring.
2. Added enforcer checks for:
   - exact status set (missing/extra detection),
   - match quality constraint,
   - exception resolution constraint presence,
   - revenue taxonomy including `reversal`,
   - tenant/provider event idempotency uniqueness token,
   - required webhook telemetry columns,
   - timing constants type/value checks,
   - canonical schema parity tokens,
   - RLS/FORCE RLS/policy presence,
   - migration hooks and reversibility-script contract tokens.
3. Added explicit forced-regression fail path.

Outcome:
- P1 closure is machine-adjudicated and fail-closed under contract drift.

### R05 - Migration reversibility proof harness

Files:
- `scripts/ci/verify_b23_p1_migration_reversibility.py`

Changes:
1. Added reproducible script that runs:
   - `alembic upgrade head`
   - `alembic downgrade -1`
   - `alembic upgrade head`
2. Captures structural signatures (columns/indexes/policies) for the four P1 tables before and after re-upgrade.
3. Fails if signatures diverge.

Outcome:
- P1 migration reversibility is executable and deterministic.

### R06 - Non-vacuous proof harness + CI wiring

Files:
- `backend/tests/test_b23_p1_schema_authority_enforcer.py`
- `backend/tests/test_b23_p1_schema_authority_runtime.py`
- `.github/workflows/ci.yml`

Changes:
1. Added enforcer negative controls for:
   - missing required match status,
   - undocumented extra match status contract drift,
   - missing quality constraint,
   - missing `reversal`,
   - missing webhook log required column,
   - timing constants mismatch,
   - canonical schema omission,
   - missing RLS policy token.
2. Added runtime DB proofs for:
   - resolved exception with null code fails (DB constraint),
   - duplicate tenant/provider event reference fails (DB unique),
   - cross-tenant visibility blocked under RLS,
   - missing-GUC write fails closed under RLS.
3. Added CI steps for P1 enforcer, negative controls, runtime proofs, and reversibility script.

Outcome:
- P1 evidence is non-vacuous and integrated into merge-blocking CI surfaces.

## 4. Falsifiable Local Validation (This Cycle)

Executed commands and outcomes:

1. `python scripts/ci/enforce_b23_p1_schema_authority_lock.py`  
Result: **PASS**

2. `pytest backend/tests/test_b23_p1_schema_authority_enforcer.py -q`  
Result: **PASS (10 passed)**

3. `python scripts/database/prepare_migration_authority_boundary.py --admin-dsn "postgresql://postgres:postgres@127.0.0.1:5432/postgres" --database-name "skeldir_b23_p1_ci" --runtime-user "app_user" --runtime-password "app_user" --migration-user "migration_owner" --migration-password "migration_owner"`  
Result: **PASS**

4. `alembic upgrade head` (with `DATABASE_URL=postgresql+asyncpg://app_user:app_user@127.0.0.1:5432/skeldir_b23_p1_ci`, `MIGRATION_DATABASE_URL=postgresql://migration_owner:migration_owner@127.0.0.1:5432/skeldir_b23_p1_ci`)  
Result: **PASS**

5. `pytest backend/tests/test_b23_p1_schema_authority_runtime.py -q` with `SKELDIR_B23_P1_REQUIRE_DB_PROOFS=1` on isolated DB  
Result: **PASS (3 passed)**

6. `python scripts/ci/verify_b23_p1_migration_reversibility.py` (same isolated DB authority boundary)  
Result: **PASS**

7. `python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`  
Result: **PASS**

8. `python scripts/ci/enforce_b23_pre_p1_spec_gate.py`  
Result: **PASS**

## 5. Non-Vacuous Break Cases Covered

1. missing one required match status fails enforcer;
2. introducing undocumented match status fails enforcer;
3. missing match-quality constraint fails enforcer;
4. resolved exception with null resolution code fails at DB constraint;
5. revenue event taxonomy drift (including missing `reversal`) fails enforcer;
6. duplicate tenant/provider event reference fails DB unique constraint;
7. cross-tenant visibility blocked and missing-GUC writes fail under DB RLS;
8. missing webhook ingestion required column fails enforcer;
9. timing constant mismatch fails enforcer;
10. canonical schema omission fails enforcer.

## 6. Files Changed (This Iteration)

- `.github/workflows/ci.yml`
- `alembic/versions/007_skeldir_foundation/202604291200_b23_p1_schema_authority_lock.py`
- `backend/app/revenue_verification/__init__.py`
- `backend/app/revenue_verification/timing_constants.py`
- `backend/tests/test_b23_p1_schema_authority_enforcer.py`
- `backend/tests/test_b23_p1_schema_authority_runtime.py`
- `contracts-internal/governance/b23_p1_schema_authority_lock.main.json`
- `db/schema/canonical_schema.sql`
- `docs/forensics/B2.3-P1 Remediation Evidence Pack .md`
- `scripts/ci/enforce_b23_p1_schema_authority_lock.py`
- `scripts/ci/verify_b23_p1_migration_reversibility.py`

## 7. Landing + Main CI Status

Final closure status: **Closed (landed on `main` with protected-branch checks green).**

Landing proof:
1. PR merged to `main`: [PR #420](https://github.com/Synergyscape-V1/skeldir-2.0/pull/420)
2. Merge commit on `main`: `1b5a3a89f466050090af0a0f4d5ba4140f46d875`
3. Merge timestamp (UTC): `2026-04-29T21:14:35Z`

Required-check adjudication proof (landed `main` SHA):
1. Protected branch required status contexts on `main`: **51**
2. Required contexts present on landed SHA: **51 / 51**
3. Required contexts failing or pending: **0**

Representative governed run evidence URLs for landed SHA:
1. CI: [run 25134255696](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25134255696)
2. Production Schema Deployment: [run 25134255666](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25134255666)
3. Schema Validation: [run 25134255729](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25134255729)
4. Phase 8 Regression Gate (Full Physics): [run 25136433224](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25136433224)

Result:
1. B2.3-P1 schema authority remediation is landed on authoritative `main`.
2. Protected-branch required checks are fully green for the landed commit.
3. P1 directive exit-gate readiness is established with falsifiable local and CI-level proof.
