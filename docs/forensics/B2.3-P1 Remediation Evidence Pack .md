# B2.3-P1 Remediation Evidence Pack

Generated: 2026-04-30  
Primary branch authority target: `main`  
Directive cycle: **B2.3-P1 Follow-up Corrective Action Remediation Directive**

## 1. Baseline Convergence

1. Follow-up corrective work was executed after prior P1 landing on `main` at merge commit `1b5a3a89f466050090af0a0f4d5ba4140f46d875`.
2. Scope remained strictly P1-closure: lifecycle authority, financial operand authority, discrepancy persistence readiness, deterministic object naming enforcement, and reversibility proof expansion.
3. No P2/P3/P4 logic surfaces were introduced.

## 2. Initial Findings (Validated)

1. **Validated:** durable P1 tables had no database-native lifecycle/data-aging authority.
2. **Validated:** `b23_revenue_events.amount_minor` remained semantically generic and did not encode event-type operand authority at schema level.
3. **Validated:** match-verdict canonical discrepancy outputs were not yet fully materialized/indexed as explicit P2 write-surface slots.
4. **Validated:** governance/enforcer/reversibility surfaces did not fully encode lifecycle and operand closure laws required by this follow-up directive.

## 3. Remediations Implemented

### R01 - Corrective migration for lifecycle + operand closure

Files:
- `alembic/versions/007_skeldir_foundation/202604301030_b23_p1_followup_lifecycle_operands.py`

Changes:
1. Added canonical match-verdict operand columns:
   - `canonical_expected_gross_amount_minor`
   - `canonical_captured_gross_amount_minor`
   - `canonical_net_verified_amount_minor`
   - `discrepancy_amount_minor`
   - `discrepancy_ratio_bps`
   - `discrepancy_band`
2. Added deterministic discrepancy constraints and indexes:
   - amount consistency,
   - ratio consistency/range,
   - discrepancy-band enum set,
   - indexed band/ratio query paths.
3. Replaced generic revenue-event money authority with event-type-specific operands:
   - `captured_amount_minor`
   - `refund_amount_minor`
   - `chargeback_amount_minor`
   - `reversal_amount_minor`
   - `net_effect_sign`
4. Removed generic `amount_minor` in upgrade path and restored it only in downgrade path for reversibility.
5. Added DB constraints enforcing:
   - legal event_type-to-operand mapping,
   - exactly one operand column populated,
   - deterministic sign convention by event type,
   - non-negative operand values.
6. Added database-native lifecycle function:
   - `public.fn_b23_p1_apply_lifecycle(max_delete integer DEFAULT 5000)`
7. Added governed `pg_cron` schedule path:
   - job name: `b23_p1_apply_lifecycle_daily`
   - schedule: `30 3 * * *`
   - command: `SELECT public.fn_b23_p1_apply_lifecycle(5000);`
8. Lifecycle retention windows encoded in function:
   - `b23_webhook_ingestion_logs`: 365 days
   - `b23_exception_records`: 1825 days
   - `b23_match_verdicts`: 1825 days
   - `b23_revenue_events`: 2555 days

Outcome:
- P1 now has activity-independent, database-native lifecycle authority plus schema-level financial operand closure.

### R02 - Governance contract expansion for follow-up closure laws

Files:
- `contracts-internal/governance/b23_p1_schema_authority_lock.main.json`

Changes:
1. Added structured sections:
   - `lifecycle_requirements`
   - `financial_operand_requirements`
   - `match_verdict_operand_requirements`
   - `revenue_event_operand_requirements`
   - `discrepancy_persistence_requirements`
   - `p2_write_surface_requirements`
   - `deterministic_db_object_name_requirements`
2. Bound contract surfaces to both base and corrective P1 migrations.
3. Encoded lifecycle retention rationale and mechanism requirements per durable P1 table.

Outcome:
- Follow-up closure obligations are machine-readable and enforceable.

### R03 - Enforcer upgraded to non-vacuously enforce follow-up closure

Files:
- `scripts/ci/enforce_b23_p1_schema_authority_lock.py`
- `backend/tests/test_b23_p1_schema_authority_enforcer.py`

Changes:
1. Enforcer now validates lifecycle mechanism, retention bindings, `pg_cron` scheduling tokens, and forbidden primary mechanisms.
2. Enforcer validates elimination of unresolved generic event money authority and presence of split operand model constraints.
3. Enforcer validates match-verdict canonical/discrepancy operand surfaces and required discrepancy indexes.
4. Enforcer validates P2 write-surface destination bindings and deterministic naming requirements.
5. Added/updated negative controls to fail on:
   - lifecycle section removal,
   - comment-only/external-worker lifecycle mechanism substitution,
   - lifecycle table omission,
   - reintroduction of generic money column,
   - operand/sign/currency constraint drift,
   - missing canonical match/discrepancy operands,
   - missing discrepancy index,
   - invalid/missing P2 write-surface destinations,
   - named-object drift,
   - legacy P1 constraint regression.

Outcome:
- CI adjudication is non-vacuous against the prior defect class.

### R04 - Canonical schema parity update

Files:
- `db/schema/canonical_schema.sql`

Changes:
1. Added `fn_b23_p1_apply_lifecycle` canonical function definition.
2. Updated `b23_match_verdicts` canonical columns/constraints for canonical/discrepancy operands.
3. Updated `b23_revenue_events` canonical columns/constraints to split-operand model and sign rules.
4. Added canonical indexes for discrepancy queryability and event-effect filtering.

Outcome:
- Canonical schema mirrors corrective migration authority for follow-up P1 closure surfaces.

### R05 - Reversibility proof expansion

Files:
- `scripts/ci/verify_b23_p1_migration_reversibility.py`

Changes:
1. Expanded schema signature capture beyond columns/indexes/policies to include:
   - constraints,
   - FORCE RLS flags,
   - lifecycle function definitions,
   - `pg_cron` job capture where available.
2. Preserved one-environment sequence:
   - upgrade head,
   - downgrade one step,
   - re-upgrade,
   - structural signature equality check.

Outcome:
- Follow-up objects are covered by deterministic downgrade/re-upgrade parity proof.

### R06 - Runtime DB proof harness update

Files:
- `backend/tests/test_b23_p1_schema_authority_runtime.py`

Changes:
1. Updated DB inserts for new canonical match-verdict and revenue-event operand schema.
2. Added runtime proof that:
   - event operand mapping violations fail,
   - sign convention violations fail,
   - generic `amount_minor` is absent,
   - lifecycle function exists.
3. Preserved existing DB proofs for:
   - exception resolution-code requirement,
   - revenue-event idempotency,
   - cross-tenant read/write isolation under RLS.

Outcome:
- Runtime validation directly exercises follow-up defect surfaces.

## 4. Falsifiable Local Validation (This Cycle)

Executed and passing:

1. `python scripts/ci/enforce_b23_p1_schema_authority_lock.py`  
Result: **PASS**

2. `pytest backend/tests/test_b23_p1_schema_authority_enforcer.py -q`  
Result: **PASS (24 passed)**

3. `python scripts/database/prepare_migration_authority_boundary.py --admin-dsn "postgresql://postgres:postgres@127.0.0.1:5432/postgres" --database-name "skeldir_b23_p1_followup_ci" --runtime-user "app_user" --runtime-password "app_user" --migration-user "migration_owner" --migration-password "migration_owner"`  
Result: **PASS**

4. `alembic upgrade head` with follow-up isolated DB authority-boundary environment  
Result: **PASS**

5. `pytest backend/tests/test_b23_p1_schema_authority_runtime.py -q` with `SKELDIR_B23_P1_REQUIRE_DB_PROOFS=1`  
Result: **PASS (4 passed)**

6. `python scripts/ci/verify_b23_p1_migration_reversibility.py` (same isolated DB)  
Result: **PASS**

7. `python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`  
Result: **PASS**

8. `python scripts/ci/enforce_b23_pre_p1_spec_gate.py`  
Result: **PASS**

## 5. Non-Vacuous Break Cases Covered

1. lifecycle requirements removed,
2. comment-only lifecycle substitution,
3. external-worker lifecycle substitution,
4. per-table lifecycle omission,
5. generic `amount_minor` reintroduction,
6. event-type operand mapping constraint drift,
7. sign convention constraint drift,
8. currency binding drift,
9. canonical expected/captured/net operand omission,
10. discrepancy amount/ratio omission,
11. discrepancy index omission,
12. invalid/missing P2 write-surface destination,
13. invalid P2 write-surface classification,
14. named constraint/index/policy/job drift,
15. prior P1 match-quality constraint regression.

## 6. Files Changed (This Iteration)

- `alembic/versions/007_skeldir_foundation/202604301030_b23_p1_followup_lifecycle_operands.py`
- `backend/tests/test_b23_p1_schema_authority_enforcer.py`
- `backend/tests/test_b23_p1_schema_authority_runtime.py`
- `contracts-internal/governance/b23_p1_schema_authority_lock.main.json`
- `db/schema/canonical_schema.sql`
- `docs/forensics/B2.3-P1 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`
- `scripts/ci/enforce_b23_p1_schema_authority_lock.py`
- `scripts/ci/verify_b23_p1_migration_reversibility.py`

## 7. Landing + Main CI Status

Status at time of this update: **Closed / complete for this follow-up corrective cycle.**

Landing evidence:
1. Corrective branch PR merged to `main`: `#422`
   - PR URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/422`
   - Merged at: `2026-04-30T15:57:26Z`
   - Merge commit on `main`: `00a36c1f90cfc8952a3108c4740fee2d96ec7092`
2. Authoritative protected-branch CI run on landed `main` SHA:
   - Workflow: `CI`
   - Run URL: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25175599673`
   - Event: `push`
   - Head SHA: `00a36c1f90cfc8952a3108c4740fee2d96ec7092`
   - Conclusion: **success**
3. Additional landed-main governance/deploy evidence:
   - `Schema Validation`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25175599619` (**success**)
   - `Empirical Validation - Directive Compliance`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25175599609` (**success**)
   - `Production Schema Deployment`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25175599663` (**success**)
