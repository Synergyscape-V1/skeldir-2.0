# B2.1-P4 Remediation Evidence Pack

Generated: 2026-04-17  
Primary branch authority: `main`  
Directive cycle: **Phase B2.1-P4 — Queue Isolation + Performance Semantics Lock**  
Investigated from commit: `a765d2ab708f9e62bc72126d249486b7a1f131fb`

## 1. Scope

This remediation cycle was limited to B2.1-P4 closures:
- deterministic vs Bayesian queue/worker isolation with real capacity boundaries,
- faithful 10K enqueue-to-commit benchmark semantics,
- real contention negative control (co-routed lane must fail),
- mounted read-path recompute-free verification under benchmark execution,
- non-vacuous CI governance lock for P4 regressions.

Out-of-scope:
- deterministic model mathematics (P2),
- persisted projection semantics and channels contract shape (P3),
- new infrastructure tiers (Redis/RabbitMQ/Kafka/Memcached).

## 2. Initial Findings (H01-H09 / RC01-RC05)

1. H01 validated.
- `backend/app/celery_app.py` routed both `app.tasks.attribution.*` and `app.tasks.bayesian.*` to `QUEUE_ATTRIBUTION` on the investigated baseline.

2. H02 partially validated.
- Existing worker-start surfaces did not guarantee dedicated Bayesian capacity; queue-name separation alone was insufficient.

3. H03 validated.
- No B2.1-P4 faithful benchmark harness existed for deterministic 10K enqueue-to-commit adjudication under contention.

4. H04 validated.
- No lock surface enforced queue-submit to durable-commit measurement semantics for P4.

5. H05 validated.
- No governance artifact required CPU+DB-real Bayesian contention evidence for B2.1-P4.

6. H06 refuted on current baseline.
- Mounted `/api/attribution/channels` read path from P3 remained persisted-read and projection-bound.

7. H07 validated.
- CI had no dedicated B2.1-P4 runtime adjudication surface.

8. H08 validated as risk.
- Shared lane topology could still permit starvation by co-routing deterministic and Bayesian tasks into one worker capacity pool.

9. H09 addressed via anti-cheat governance.
- New P4 adjudication/negative controls explicitly fail on timing-cheat, fake-contention, and recompute-on-read regression patterns.

## 3. Remediations Implemented

### R01 - Queue topology split at Celery routing layer

Files:
- `backend/app/core/queues.py`
- `backend/app/celery_app.py`

Changes:
- Added `QUEUE_BAYESIAN = "bayesian"` and added it to allowed queue set.
- Added explicit Bayesian kombu queue declaration.
- Re-routed `app.tasks.bayesian.*` to `QUEUE_BAYESIAN` (`bayesian.task`).
- Kept deterministic attribution on `QUEUE_ATTRIBUTION` (`attribution.task`).

### R02 - Real Bayesian contention task for CPU+DB physics

Files:
- `backend/app/tasks/bayesian.py`
- `backend/app/observability/metrics_policy.py`

Changes:
- Added `app.tasks.bayesian.run_resource_contention` task.
- Task consumes CPU cycles and executes repeated DB aggregate queries over `attribution_events`.
- Added canonical task-name allowlist entries for Bayesian task metrics policy.

### R03 - Worker-capacity isolation in runtime surfaces

Files:
- `Procfile`
- `docker-compose.e2e.yml`
- `backend/tests/integration/test_b07_p5_bayesian_timeout_runtime.py`
- `backend/tests/test_b052_queue_topology_and_dlq.py`

Changes:
- Procfile now defines dedicated deterministic/core worker and dedicated Bayesian worker.
- E2E compose now has separate `worker` and `worker_bayesian` services with disjoint queue subscriptions.
- Bayesian runtime integration test worker queue subscriptions updated to `bayesian,housekeeping`.
- Queue topology test suite updated for Bayesian queue/routing rules and deterministic router coverage.

### R04 - Faithful P4 benchmark harness (real queue/worker/DB path)

File:
- `scripts/benchmarks/b21_p4_queue_isolation_benchmark.py`

Capabilities:
- Seeds deterministic 10K event workload shape.
- Starts real Celery worker topologies:
  - `isolated` (deterministic worker + Bayesian worker),
  - `corouted_shared_worker` (negative-control co-routed capacity).
- Runs Bayesian contention using `app.tasks.bayesian.run_resource_contention`.
- Measures stopwatch boundary exactly as enqueue-to-durable-commit.
- Confirms deterministic recompute completion from persisted job state + allocation persistence.
- Executes mounted `/api/attribution/channels` and verifies recompute counters do not mutate on GET.

### R05 - Benchmark adjudication lock (fail-closed semantics)

File:
- `scripts/ci/enforce_b21_p4_benchmark_adjudication.py`

Enforced conditions:
- isolated topology must pass `<5s` at required event volume,
- corouted topology must fail threshold (non-vacuous contention negative control),
- dispatch must be real Celery (`send_task`) with eager mode disabled,
- contention must be resource-real (`run_resource_contention` with DB+iteration floors),
- read-path must remain recompute-free during mounted GET probe.

### R06 - Structural governance lock + negative controls

Files:
- `scripts/ci/enforce_b21_p4_queue_isolation_semantics_lock.py`
- `backend/tests/test_b21_p4_queue_isolation_semantics_lock_enforcer.py`
- `backend/tests/test_b21_p4_benchmark_adjudication_enforcer.py`
- `.github/workflows/ci.yml`

Changes:
- Added structural enforcer for queue split, worker surfaces, benchmark harness/adjudicator presence, and CI wiring.
- Added non-vacuous negative-control tests for:
  - co-route regression in Celery routing,
  - missing CI benchmark hooks,
  - eager/direct-call timing cheat,
  - fake contention substitute,
  - recompute-on-read regression indicators.
- Added dedicated runtime CI job: `B2.1-P4 Queue Isolation + Performance Semantics Lock`.
- Made required `Contract Semantic Drift Gate` depend on P4 runtime job and run P4 enforcer negative controls.

## 4. Falsifiable Validation (Local)

Executed from `C:\Users\ayewhy\II SKELDIR II`.

1. P4 structural enforcer pass
- Command: `python scripts/ci/enforce_b21_p4_queue_isolation_semantics_lock.py`
- Result: `PASS`.

2. P4 enforcer negative-control suites
- Command: `pytest backend/tests/test_b21_p4_queue_isolation_semantics_lock_enforcer.py backend/tests/test_b21_p4_benchmark_adjudication_enforcer.py -q`
- Result: `10 passed`.

3. Queue topology regression suite (post-isolation updates)
- Command: `pytest backend/tests/test_b052_queue_topology_and_dlq.py -q`
- Result: `13 passed`.

4. Benchmark harness integrity mode
- Command: `python scripts/benchmarks/b21_p4_queue_isolation_benchmark.py --mode integrity --output artifacts/b21_p4_benchmark/local_integrity.json`
- Result: completed successfully and emitted integrity artifact.

5. Local runtime benchmark attempt note
- A local run against pre-existing `skeldir_validation` failed on schema drift (`attribution_recompute_jobs.replay_event_created_ceiling` missing), indicating local DB was not aligned to current head migration state.
- Authoritative runtime adjudication is therefore anchored to the new CI B2.1-P4 job that provisions fresh DB + migrations before benchmark execution.

## 5. Exit Gate Adjudication

1. Exit Gate 1 (Compute Isolation): **PASS (implemented)**  
- Queue routing split + dedicated worker surfaces for deterministic vs Bayesian lanes are now explicit.

2. Exit Gate 2 (Faithful Stopwatch): **PASS (implemented)**  
- Harness measures enqueue to durable commit/job-success state.

3. Exit Gate 3 (Resource-Real Contention): **PASS (implemented)**  
- Bayesian contention task enforces CPU+DB resource usage evidence.

4. Exit Gate 4 (Faithful 10K/5s Benchmark): **PENDING authoritative CI execution**  
- Runtime job and adjudicator now enforce this gate on fresh migrated CI substrate.

5. Exit Gate 5 (Read-Path Decoupling): **PASS (implemented)**  
- Mounted GET probe in benchmark asserts no recompute mutation.

6. Exit Gate 6 (Non-Vacuous Governance): **PASS (implemented)**  
- Structural lock + benchmark adjudication + explicit negative controls are wired into required gate topology.

7. Exit Gate 7 (Non-Regression of P3/P2/P1/P0): **PASS on exercised local suites**  
- P3/P2/P1 surfaces were not reopened; queue/perf scope remained bounded.

## 6. Files Changed in This Remediation Cycle

- `.github/workflows/ci.yml`
- `Procfile`
- `docker-compose.e2e.yml`
- `backend/app/core/queues.py`
- `backend/app/celery_app.py`
- `backend/app/tasks/bayesian.py`
- `backend/app/observability/metrics_policy.py`
- `backend/tests/test_b052_queue_topology_and_dlq.py`
- `backend/tests/integration/test_b07_p5_bayesian_timeout_runtime.py`
- `scripts/benchmarks/b21_p4_queue_isolation_benchmark.py`
- `scripts/ci/enforce_b21_p4_queue_isolation_semantics_lock.py`
- `scripts/ci/enforce_b21_p4_benchmark_adjudication.py`
- `backend/tests/test_b21_p4_queue_isolation_semantics_lock_enforcer.py`
- `backend/tests/test_b21_p4_benchmark_adjudication_enforcer.py`
- `docs/forensics/B2.1-P4 Remediation Evidence Pack.md`

## 7. Protected-Branch Mainline Landing Evidence

Status: **PENDING (to be updated after PR merge to `main`)**

- PR URL: pending
- Merge commit on `main`: pending
- Required checks on PR: pending
- Authoritative post-merge `main` CI run URL/ID: pending
