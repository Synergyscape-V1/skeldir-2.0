# B2.3-Pre P1 Remediation Evidence Pack

Generated: 2026-04-29  
Primary branch authority target: `main`  
Directive cycle: **B2.3-Pre P1 Follow-up Corrective Action Remediation Directive**

## 1. Baseline Convergence

1. Work executed from branch `codex/b23-pre-p1-followup-corrective` rooted on `main` commit `5247e8dbd`.
2. Scope intentionally constrained to Pre-P1 semantic authority closure hardening (contract/spec/enforcer/tests/evidence).
3. No P1 schema artifacts, no extraction registry implementation, no constants module implementation, no match kernel, and no migrations were introduced.

## 2. Initial Findings (Validated)

1. **Validated (PreP1-FH01):** provider-origin payload fields and B2.3 runtime authority fields were not explicitly separated as machine-enforced boundary semantics.
2. **Validated (PreP1-FH02/FH03):** binary-float prohibition did not fully cover parsing/deserialization/normalization/conversion boundaries, and exact-decimal parsing requirements were under-enforced structurally.
3. **Validated (PreP1-FH04):** `b23_pre_p1_spec_gate.main.json` lacked strict schema-binding/typed validation with explicit extra-field rejection and enum/type safety.
4. **Validated (PreP1-FH05):** enforcer coverage omitted required semantics (including `reversal` and full float-ban scope coverage).
5. **Validated (PreP1-FH06):** negative controls were substantive but incomplete for canonical-envelope deletion, strict schema malformation, enum/type errors, unknown extra fields, and full float-path drift.

## 3. Remediations Implemented

### R01 - Provider-origin vs canonical-envelope authority boundary lock

Files:
- `contracts-internal/governance/b23_pre_p1_spec_gate.main.json`
- `docs/forensics/B2.3-Pre-P1 Specification Gates A-B.md`
- `scripts/ci/enforce_b23_pre_p1_spec_gate.py`

Changes:
1. Replaced ambiguous provider authority terms with explicit provenance fields:
   - `provider_origin_revenue_field`
   - `provider_origin_currency_field`
2. Added canonical-envelope authority fields:
   - `b23_authority_amount_field`
   - `b23_authority_currency_field`
3. Added explicit boundary controls:
   - `source_boundary = verified_b22_b23_ingress_envelope`
   - `raw_webhook_payload_forbidden_as_b23_authority = true`
   - `provider_origin_fields_are_provenance_only = true`
4. Enforcer now fails when runtime authority is reclassified back to provider-origin field paths.

Outcome:
- B2.3 authority input is machine-locked to verified/canonicalized ingress-envelope semantics.

### R02 - Full money-path float safety + exact decimal parsing closure

Files:
- `contracts-internal/governance/b23_pre_p1_spec_gate.main.json`
- `docs/forensics/B2.3-Pre-P1 Specification Gates A-B.md`
- `scripts/ci/enforce_b23_pre_p1_spec_gate.py`

Changes:
1. Expanded float-ban scopes to include:
   - provider parsing,
   - deserialization normalization,
   - decimal-to-minor conversion,
   - currency exponent application,
   - storage and all authority arithmetic paths,
   - test fixture restrictions.
2. Added explicit exact-decimal parsing policy:
   - required for decimal-string providers (`paypal`, `shopify`, `woocommerce`),
   - binary float conversion guards (`python_float`, `javascript_number`, `javascript_parseFloat`, `double`).
3. Enforcer now fails when parsing-boundary float scopes or exact-decimal rules are removed.

Outcome:
- Binary-float contamination is fail-closed across the full money path.

### R03 - Strict contract schema-binding layer

Files:
- `scripts/ci/validate_b23_pre_p1_spec_contract.py`
- `scripts/ci/enforce_b23_pre_p1_spec_gate.py`

Changes:
1. Added strict typed validator with no-extra-field checks and nested-shape constraints.
2. Enforced typed fields, required structure, and enum-constrained values for:
   - provider keys and units,
   - money storage units,
   - timing units,
   - adjustment events (including `reversal`),
   - privacy lifecycle class posture/retention/lifecycle enums.
3. Enforcer now invokes strict schema validation and emits `schema:*` failures on structural drift.

Outcome:
- Governance contract is now schema-bound rather than token/spot-check bound.

### R04 - Enforcer completeness hardening

Files:
- `scripts/ci/enforce_b23_pre_p1_spec_gate.py`

Changes:
1. Added semantic checks for:
   - canonical-envelope boundary fields,
   - raw payload forbidden law,
   - full float-ban scope set,
   - exact-decimal provider set and conversion guards,
   - required `reversal` event.
2. Preserved and revalidated:
   - P0 inherited truth correction,
   - privacy/lifecycle class coverage,
   - timing constants and `<10 second` scoping.

Outcome:
- Contract semantics and enforcer semantics are aligned and fail-closed.

### R05 - Non-vacuous negative-control expansion

Files:
- `backend/tests/test_b23_pre_p1_spec_gate_enforcer.py`

Changes:
1. Expanded tests to cover:
   - canonical-envelope authority field deletion,
   - provider-origin/runtime-authority boundary regression,
   - raw payload forbidden flag removal,
   - parsing/conversion/test-fixture float scope removals,
   - exact-decimal rule removal,
   - `reversal` removal,
   - wrong provider enum,
   - wrong money unit enum,
   - wrong timing unit enum,
   - wrong boolean type,
   - unknown extra field,
   - malformed nested provider object shape.
2. Retained forced regression control and CI wiring/spec token regressions.

Outcome:
- Fail-closed behavior is demonstrated against the exact prior defect class.

## 4. No-Premature-P1 Guard

Validated in this cycle:
1. No P1 schema migrations added.
2. No verdict/exception/revenue-event table implementation added.
3. No extraction registry implementation added.
4. No timing constants module implementation added.
5. No match kernel/service/read API implementation added.

## 5. Falsifiable Local Validation

Commands executed in this cycle:

1. `python scripts/ci/enforce_b23_pre_p1_spec_gate.py`  
Result: **PASS**

2. `pytest backend/tests/test_b23_pre_p1_spec_gate_enforcer.py -q`  
Result: **PASS (18 passed)**

3. `python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`  
Result: **PASS**

4. `python scripts/ci/enforce_b23_p0_typed_boundary_source_alignment.py`  
Result: **PASS**

5. `python scripts/ci/enforce_b15_p3_runtime_route_binding.py`  
Result: **PASS**

6. `python scripts/ci/enforce_b15_p4_mock_sdk_boundary.py`  
Result: **PASS**

7. `gh pr view 417 --json statusCheckRollup,mergeCommit,mergedAt` + required-context reconciliation against branch protection  
Result: **PASS** (`required=51`, `missing=0`, `nonSuccess=0`)

8. `gh run list --branch main --commit d345f7a01cac59c4b314fd94538de9f7f9859fef --workflow CI --json ...`  
Result: **PASS** (`CI` workflow run `25124269363` concluded `success` on `main`)

9. `gh pr view 418 --json mergeCommit,mergedAt,state`  
Result: **PASS** (evidence-finalization PR merged to `main` at `86736c5c5db345ff12c1f2fa3b99c079b483852b`)

10. `gh run view 25125834635 --json headSha,status,conclusion`  
Result: **PASS** (`headSha=86736c5c5db345ff12c1f2fa3b99c079b483852b`, `status=completed`, `conclusion=success`)

## 6. Non-Vacuous Break Cases Covered (Follow-up)

1. Missing canonical-envelope authority fields fails.
2. Runtime authority reclassified to provider-origin path fails.
3. Raw-payload forbidden law deletion fails.
4. Parsing-boundary float-scope deletion fails.
5. Decimal conversion float-scope deletion fails.
6. Exact-decimal rule deletion fails.
7. Test-fixture float-scope deletion fails.
8. `reversal` event deletion fails.
9. Wrong provider enum fails.
10. Wrong money unit enum fails.
11. Wrong timing unit enum fails.
12. Wrong boolean type fails.
13. Unknown extra field fails.
14. Malformed nested provider object shape fails.

## 7. Files Changed (This Iteration)

- `backend/tests/test_b23_pre_p1_spec_gate_enforcer.py`
- `contracts-internal/governance/b23_pre_p1_spec_gate.main.json`
- `docs/forensics/B2.3-Pre-P1 Specification Gates A-B.md`
- `docs/forensics/B2.3-Pre P1 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`
- `scripts/ci/enforce_b23_pre_p1_spec_gate.py`
- `scripts/ci/validate_b23_pre_p1_spec_contract.py`

## 8. Landing + Main CI Status

Status at time of this evidence update: **Closed / Landed on `main`.**

Landing proof:
1. PR: `#417` (`codex/b23-pre-p1-followup-corrective` -> `main`)
2. PR URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/417`
3. Merge time (UTC): `2026-04-29T17:37:30Z`
4. Landed commit on `main`: `d345f7a01cac59c4b314fd94538de9f7f9859fef`
5. Evidence finalization PR: `#418` (`codex/b23-pre-p1-followup-evidence-finalize` -> `main`)
6. Evidence finalization PR URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/418`
7. Evidence finalization merge time (UTC): `2026-04-29T18:10:49Z`
8. Final landed commit on `main`: `86736c5c5db345ff12c1f2fa3b99c079b483852b`

Protected-branch required checks proof:
1. Branch protection required contexts: `51`
2. Required-context reconciliation on PR #417 at merge: `missing=0`, `nonSuccess=0`
3. Representative required-check run anchor: `CI` run `25123506086` (required contexts completed `success`)

Mainline CI proof:
1. Post-merge `main` CI workflow run: `25124269363`
2. Conclusion: `success`
3. Final post-merge `main` CI workflow run for final landed commit: `25125834635`
4. Conclusion: `success`
