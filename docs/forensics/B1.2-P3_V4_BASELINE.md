# B1.2-P3 v4 Baseline (Lock Semantics, Lock-Miss Behavior, Enforcement Plane)

Date: 2026-03-02  
Branch under inspection: `b12-p3-pg-stampede-corrective`  
Baseline intent: validate/refute H01-H05 before corrective edits.

## 1) Lock function and scope (H01)
- **Validated true (pre-fix):** verifier refresh used session-scoped advisory lock/unlock.
- Evidence from pre-fix code path in `backend/app/core/secrets.py`:
  - `SELECT pg_try_advisory_lock(...)`
  - `SELECT pg_advisory_unlock(...)`
- Risk: liveness coupling to session/connection handling.

## 2) Timeout semantics (H02)
- **Validated true (pre-fix):** no `SET LOCAL statement_timeout` guard was set for refresh transaction work.
- `lock_timeout` was not meaningfully part of try-lock path.

## 3) Lock-miss behavior (H03)
- **Validated true (pre-fix):** lock miss returned cached/fallback behavior without bounded polling for unknown-`kid` update propagation.
- Risk: avoidable unknown-`kid` auth failures while another process refreshes.

## 4) Steady-state DB queries (H04)
- Baseline did not include an explicit “0 DB queries per request/task” assertion.
- Existing tests proved “no secret source fetch” but not direct DB-connect interdiction in hot path.

## 5) Enforcement-plane required checks (H05)
- Required checks on `main` collected via:
  - `gh api repos/Synergyscape-V1/skeldir-2.0/branches/main/protection --jq '.required_status_checks.contexts[]'`
- Current required contexts include:
  - `JWT Tenant Context Invariants`
  - `Phase 1 Negative Controls`
  - `Phase 8 Regression Gate (Full Physics)`
  - plus the full branch-protection required set.
- Baseline gap: v4-specific lock-semantics/lock-miss/0-DB proofs were not explicitly enumerated in required test execution.

## Baseline verdict
- H01-H05 remained materially open before this v4 corrective cycle.
- Corrective objective: transaction-scoped xact try-lock + bounded lock-miss polling + explicit 0-DB steady-state proofs + required-check enrollment.
