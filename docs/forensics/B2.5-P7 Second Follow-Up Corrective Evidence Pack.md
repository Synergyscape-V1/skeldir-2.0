# B2.5-P7 Second Follow-Up Corrective Evidence Pack

Directive: B2.5-P7 Follow-Up Corrective Action Directive II - rollback witnessing, timestamp truthfulness, and CI non-vacuity closure.

Current status: implementation PR evidence pending protected-main merge and post-merge main CI.

## Identity

- Rejected baseline SHA: `58b35ecea14f86e22b7fe794bae605775b40951d`.
- Rejected first follow-up implementation SHA: `d74b739aa93645a9af7d8e791db48e34365d2746`.
- Rejected first follow-up docs-only descendant: `e038e2cc2dffe2cf3b691a328d4061cf61113f80`.
- Starting main SHA for this iteration: `e038e2cc2dffe2cf3b691a328d4061cf61113f80`.
- Implementation branch: `codex/b25-p7-second-follow-up-rollback-witness`.
- Final branch SHA: pending.
- PR number: pending.
- Protected main merge SHA: pending.
- Hosted CI run IDs/job IDs: pending.

## Files Changed

- `.github/workflows/b2_5-p7-provenance-audit.yml`
- `backend/app/trust/audit.py`
- `backend/tests/trust/test_b25_p7_provenance_audit.py`
- `scripts/ci/validate_b25_p7_provenance_audit.py`
- `scripts/ci/validate_m0_scope_lock.py`
- `scripts/ci/validate_m1_local_dev_authority.py`
- `docs/ci/enforcer_registry.yaml`
- `docs/ci/gate_subsumption_matrix.yaml`
- `docs/forensics/B2.5-P7 Remediation Evidence Pack.md`
- `docs/forensics/B2.5-P7 Second Follow-Up Corrective Evidence Pack.md`
- `docs/forensics/INDEX.md`

## Hypothesis Ledger

H-P7-SFU-01: CONFIRMED pre-remediation. The existing PostgreSQL proof had no independent non-audit canary. Remediation adds a test-only PostgreSQL table witness whose canary row is written inside the caller transaction and asserts it disappears after rollback.

H-P7-SFU-02: CONFIRMED pre-remediation. Same-session audit disappearance was not an independent rollback witness. Remediation proves, in one scenario, canary disappearance, rollback-prone audit disappearance, and durable audit survival.

H-P7-SFU-03: CONFIRMED pre-remediation. Live-clock rejection existed, but static sentinel fallback mutations were not first-class controls. Remediation adds validator rejection for epoch/min/max/default/sentinel/static `created_at` fallbacks and `request.created_at or static` patterns.

H-P7-SFU-04: CONFIRMED pre-remediation. `created_at` required a timezone-aware datetime but not source authority. Remediation adds `created_at_source`, accepts only `request_issuance_context` or `persisted_original`, validates before audit hash construction, and binds source authority into audit material.

H-P7-SFU-05: CONFIRMED pre-remediation. The four Directive II markers were absent. Remediation emits nonzero markers from real checks and makes the hosted workflow grep for them.

H-P7-SFU-06: REFUTED post-remediation by scope review. No signing, verification, JWKS, routes, machine auth, export, MCP, LLM generation, rate-limit enforcement, action authority, or artifact lifecycle expansion was introduced.

## Proof Design

Rollback witness/canary:
The PostgreSQL proof creates `public.b25_p7_rollback_witness` as a test-only table through the migration-owner connection, grants only select/insert/delete to the runtime caller role, and drops it through migration-owner teardown after the proof. The canary row `caller-transaction-rollback-observed` is inserted inside the caller transaction, proven visible before rollback, then proven absent after rollback. The same scenario writes a rollback-prone same-session audit row, writes a durable audit row through the dedicated audit session, simulates `post_audit_serialization_failure`, rolls back the caller transaction, then proves the witness row and same-session audit row are absent while the durable audit row remains.

Timestamp truthfulness:
`TrustAuditRequest` now requires `created_at_source`. The accepted values are `request_issuance_context` and `persisted_original`. Missing or invalid authority fails with `audit_created_at_source_authority_required`. Audit hash material includes `created_at_source`, so source-authority mutation changes the audit identity.

CI non-vacuity:
The P7 validator emits:

- `rollback_witness_canary_controls_passed=`
- `caller_transaction_rollback_observed_controls_passed=`
- `static_sentinel_created_at_rejection_controls_passed=`
- `created_at_source_authority_controls_passed=`

The hosted workflow greps all four markers as nonzero output.

## Local Validation

Command:
`python -m pytest backend/tests/trust/test_b25_p7_provenance_audit.py -q`

Output summary:
`10 passed, 1 skipped in 64.58s`.

Command:
`python scripts/ci/validate_b25_p7_provenance_audit.py --negative-control-only`

Output summary:
`B25_P7_PROVENANCE_AUDIT_NEGATIVE_CONTROL_PASS`; `rollback_witness_canary_controls_passed=8`; `caller_transaction_rollback_observed_controls_passed=8`; `static_sentinel_created_at_rejection_controls_passed=19`; `created_at_source_authority_controls_passed=13`; `meta_negative_controls_passed=25`.

Command:
`python scripts/ci/validate_b25_p7_provenance_audit.py --negative-control --skip-prior-phase-subprocesses`

Output summary:
`B25_P7_PROVENANCE_AUDIT_VALIDATION_PASS`; all baseline P7 markers passed; new markers were `rollback_witness_canary_controls_passed=8`, `caller_transaction_rollback_observed_controls_passed=8`, `static_sentinel_created_at_rejection_controls_passed=19`, `created_at_source_authority_controls_passed=13`.

Command:
`python -m pytest backend/tests/trust -q`

Output summary:
`93 passed, 1 skipped in 128.63s`.

Command:
`python -m ruff check backend/app/trust/audit.py backend/tests/trust/test_b25_p7_provenance_audit.py scripts/ci/validate_b25_p7_provenance_audit.py scripts/ci/validate_m0_scope_lock.py scripts/ci/validate_m1_local_dev_authority.py`

Output summary:
`All checks passed!`

Command:
`python scripts/ci/validate_b25_p1_contracts.py --negative-control`

Output summary:
`B25_P1_CONTRACT_VALIDATION_PASS`.

Command:
`python scripts/ci/validate_b25_p1_trust_drift.py --negative-control`

Output summary:
`B25_P1_TRUST_DRIFT_VALIDATION_PASS`.

Command:
`python scripts/ci/validate_b25_p2_canonicalization.py --negative-control`

Output summary:
`B25_P2_CANONICALIZATION_VALIDATION_PASS`.

Command:
`python scripts/ci/validate_b25_p3_text_disposition.py --negative-control`

Output summary:
`B25_P3_TEXT_DISPOSITION_VALIDATION_PASS`.

Command:
`python scripts/ci/validate_b25_p4_money_authority.py --negative-control`

Output summary:
`B25_P4_MONEY_AUTHORITY_VALIDATION_PASS`.

Command:
`python scripts/ci/validate_b25_p5_builder.py --negative-control`

Output summary:
`B25_P5_BUILDER_VALIDATION_PASS`.

Command:
`python scripts/ci/validate_b25_p6_reason_truth_matrix.py --negative-control`

Output summary:
`B25_P6_REASON_TRUTH_MATRIX_VALIDATION_PASS`.

Local unavailable/blocked checks:

- `python -m mypy backend/app/trust/audit.py` could not run because this local interpreter has no `mypy` module installed.
- `python scripts/ci/validate_m0_scope_lock.py` failed on pre-existing M0 baseline scope findings unrelated to this P7 diff: marketing e2e/assets were reported as outside the M0 allowed surface.
- `python scripts/ci/validate_m1_local_dev_authority.py` failed on pre-existing M1 baseline SHA availability, unrelated to this P7 diff.

## Gates

Gate 1 - New Protected-Main Corrective SHA:
Status: pending protected-main merge.
Evidence: implementation branch is based on `origin/main` at `e038e2cc2dffe2cf3b691a328d4061cf61113f80`; final SHA pending.
Residual risk: branch CI and protected-main CI still required.

Gate 2 - Rollback Witness / Canary Proof:
Status: locally implemented, hosted proof pending.
Evidence: `test_postgres_audit_durability_survives_caller_rollback` includes independent temporary PostgreSQL canary and after-rollback absence assertion.
Residual risk: full PostgreSQL execution is hosted-CI authoritative.

Gate 3 - Static Sentinel Timestamp Rejection:
Status: locally passed.
Evidence: validator rejects epoch, `datetime.min`, `datetime.max`, `DEFAULT_CREATED_AT`, `SENTINEL_CREATED_AT`, and `STATIC_CREATED_AT` fallback mutants.
Residual risk: none known before hosted CI.

Gate 4 - Created_at Source Authority:
Status: locally passed.
Evidence: implementation requires and validates `created_at_source`; validator rejects missing/non-authoritative authority and binds authority into audit hash material.
Residual risk: future callers must supply one of the approved authority labels.

Gate 5 - CI Non-Vacuity:
Status: locally passed, hosted proof pending.
Evidence: workflow now greps the four required nonzero markers.
Residual risk: hosted CI still required.

Gate 6 - Baseline Preservation:
Status: locally passed for P1-P6, P7-local, and trust test suite; hosted preservation pending.
Evidence: P1 contract, P1 drift, P2 canonicalization, P3 text disposition, P4 money authority, P5 builder, P6 reason truth matrix, `backend/tests/trust`, and the P7 validator pass locally.
Residual risk: full protected-main CI still required.

Gate 7 - Scope Preservation:
Status: locally passed.
Evidence: changed files remain P7 audit/test/validator/CI/evidence paths; no P8/P9/P10/P11/MCP/LLM surfaces were added.
Residual risk: none known.

## Residual Risks

- Protected-main merge SHA and final main CI run IDs are pending.
- Local PostgreSQL proof was not executed in this Windows workspace because the proof is intentionally hosted via the CI PostgreSQL service; the workflow requires `SKELDIR_B25_P7_POSTGRES_PROOF=1`.

VERDICT: B2.5-P7 SECOND FOLLOW-UP FAIL — ROLLBACK WITNESSING AND CHRONOLOGICAL TRUTHFULNESS INCOMPLETE
