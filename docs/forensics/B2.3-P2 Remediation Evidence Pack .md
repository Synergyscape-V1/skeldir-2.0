# B2.3-P2 Remediation Evidence Pack

Generated: 2026-05-01  
Directive: **B2.3-P2 Corrective Action Remediation Directive**  
Authority branch target: `main`

## 1. Initial Findings (This Corrective Cycle)

1. **CFH01 confirmed:** `contract-semantic-drift-gate` in `.github/workflows/ci.yml` did not set `SKELDIR_B23_P2_REQUIRE_DB_PROOFS=1` for B2.3-P2 runtime tests.
2. **CFH02 confirmed:** no explicit B2.3-P2 preflight assertion proved migrated Postgres + required P1/B2.3 tables before the B2.3-P2 runtime pytest step.
3. **CFH03 confirmed:** `backend/app/revenue_verification/extraction_registry.py` still contained Stripe zero-fallback semantics (`int(payload.gross_captured_minor or 0)`).
4. **CFH04 confirmed:** `scripts/ci/enforce_b23_p2_match_engine_kernel.py` used mostly token checks and did not enforce semantic fallback families.
5. **CFH05 partially confirmed:** authenticated malformed canonical payloads already routed to durable DLQ in webhook paths, but monetary/currency fallback coercions still allowed malformed financial inputs to degrade into zero/default semantics before ingestion.
6. **CFH06 confirmed:** prior evidence document mixed closure claims with skipped local runtime context and did not enforce runtime DB proof-lock semantics.

## 2. Remediations Implemented

### R1. Runtime DB proof-lock in CI

Updated: `.github/workflows/ci.yml`

1. Added `SKELDIR_B23_P2_REQUIRE_DB_PROOFS: "1"` to the authoritative closure job env.
2. Added step `Verify B2.3-P2 runtime DB proof-lock prerequisites`:
   - asserts `SKELDIR_B23_P2_REQUIRE_DB_PROOFS == "1"`,
   - verifies DB connectivity,
   - asserts required tables via `to_regclass`:
     - `public.b23_match_verdicts`
     - `public.b23_exception_records`
     - `public.b23_revenue_events`
     - `public.b23_webhook_ingestion_logs`.
3. Hardened runtime pytest step:
   - runs `pytest backend/tests/test_b23_p2_match_engine_kernel.py -q -rA --junitxml=...`,
   - parses JUnit and fails if any test in this suite is skipped.

### R2. Zero-fallback elimination in authority extraction

Updated: `backend/app/revenue_verification/extraction_registry.py`

1. Removed Stripe fallback coercion:
   - from `int(payload.gross_captured_minor or 0)`
   - to `int(payload.gross_captured_minor)`.
2. Tightened money models to fail closed:
   - `ExtractedRevenue.amount_minor` => `gt=0`,
   - `PersistedIngressExtractionInput.verified_amount_minor` => `gt=0`,
   - Stripe gross/net fields => `gt=0`.
3. Enforced decimal-major positive amount invariant:
   - zero/non-positive normalized minor values now raise `ValueError`.

### R3. Failure-boundary contract + implementation consumption

Added: `backend/app/revenue_verification/failure_boundary.py`  
Updated: `backend/app/revenue_verification/__init__.py`  
Updated: `backend/app/revenue_verification/match_engine_kernel.py`  
Updated: `backend/app/api/webhooks.py`

1. Added explicit boundary classes:
   - `unauthenticated_malformed_webhook`
   - `authenticated_malformed_canonical_payload`
   - `valid_post_capture_unresolved_order_identity`
   - `unsupported_authenticated_provider_event_type`.
2. Kernel now consumes boundary classification for unresolved and unsupported post-capture failures.
3. Unsupported authenticated post-capture events now emit durable failure telemetry in `b23_webhook_ingestion_logs` before raising.
4. Authenticated malformed payload routing and unsupported event-family handling now carry boundary-class markers and durable DLQ telemetry.

### R4. Eliminate falsey/default money coercion in authenticated webhook ingress

Updated: `backend/app/api/webhooks.py`

1. Removed provider handlers’ fallback semantics (`or 0`, default currency fallback on missing required values).
2. Added fail-closed routing for missing/malformed money/currency:
   - Stripe: missing/non-positive amount, missing currency.
   - PayPal: missing amount object/total/currency, invalid/non-positive amount.
   - Shopify: missing currency/total_price, invalid/non-positive amount.
   - WooCommerce: missing currency/total, invalid/non-positive amount.
3. Contract-path Stripe endpoint also rejects non-positive amounts explicitly.

### R5. Enforcer upgraded from pattern-only to invariant-oriented detection

Updated: `scripts/ci/enforce_b23_p2_match_engine_kernel.py`  
Updated: `contracts-internal/governance/b23_p2_match_engine_kernel.main.json`

1. Added AST-based semantic fallback detection families:
   - boolean `or` fallback,
   - ternary fallback,
   - dict `.get(..., 0)` fallback,
   - indirect zero default variables,
   - helper-based zero coercion,
   - keyword defaults (`default=0`),
   - falsey-to-zero before `int()` / `Decimal()` conversion.
2. Added failure-boundary and webhook-consumption contract checks.
3. Added CI runtime DB proof-lock token checks.

### R6. Behavioral runtime tests expanded

Updated: `backend/tests/test_b23_p2_match_engine_kernel.py`

1. Added explicit DB proof-mode test with env assertion and required table checks.
2. Added fail-hard test for missing-table behavior in proof mode (`pytest.fail`, not skip).
3. Added malformed-money fail-closed behavioral tests:
   - missing/none/invalid/zero paths reject,
   - no extraction zero fallback acceptance.
4. Added authenticated malformed canonical payload durability test (DLQ durable row, no ingestion authority rows).
5. Added unsupported authenticated post-capture event durability test (ingestion failure row persisted, no revenue-event row).

### R7. Negative controls expanded to mutation-proof semantic coverage

Updated: `backend/tests/test_b23_p2_match_engine_kernel_enforcer.py`

Added enforced failing mutations for:
1. `int(... or 0)`
2. ternary fallback to zero
3. indirect default fallback
4. `.get("amount", 0)`
5. `or Decimal("0")`
6. helper fallback to zero
7. runtime DB proof env removal in CI workflow
8. concurrency downgrade to `asyncio.Lock`
9. kernel LLM import reachability
10. unresolved boundary classification removal
11. malformed-boundary webhook classification removal

## 3. Falsifiable Local Validation

Executed on 2026-05-01:

1. `python scripts/ci/enforce_b23_p2_match_engine_kernel.py` => **PASS**
2. `pytest backend/tests/test_b23_p2_match_engine_kernel_enforcer.py -q` => **PASS (17 passed)**
3. `pytest backend/tests/test_b23_p2_match_engine_kernel.py -q` (non-proof local mode) => **PASS with controlled skips (10 passed, 7 skipped)**
4. `SKELDIR_B23_P2_REQUIRE_DB_PROOFS=1 pytest backend/tests/test_b23_p2_match_engine_kernel.py -q` => **FAIL-HARD as designed** when required B2.3 tables are absent locally.
5. `python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py` => **PASS**
6. `python scripts/ci/enforce_b23_pre_p1_spec_gate.py` => **PASS**
7. `python scripts/ci/enforce_b23_p1_schema_authority_lock.py` => **PASS**

Interpretation:
- Proof mode now rejects missing DB/table prerequisites as failures (not skips).
- Semantic zero-fallback families are detected by enforcer negative controls.
- Failure-boundary telemetry behavior is explicitly test-covered.

## 4. Files Changed in This Corrective Iteration

1. `.github/workflows/ci.yml`
2. `backend/app/api/webhooks.py`
3. `backend/app/revenue_verification/__init__.py`
4. `backend/app/revenue_verification/extraction_registry.py`
5. `backend/app/revenue_verification/failure_boundary.py`
6. `backend/app/revenue_verification/match_engine_kernel.py`
7. `backend/tests/test_b23_p2_match_engine_kernel.py`
8. `backend/tests/test_b23_p2_match_engine_kernel_enforcer.py`
9. `contracts-internal/governance/b23_p2_match_engine_kernel.main.json`
10. `scripts/ci/enforce_b23_p2_match_engine_kernel.py`

## 5. Authoritative Merge / Mainline CI Chronology

1. PR merged to `main`:
   - PR: `#432`
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/432`
   - Merge SHA on `main`: `fbb4f634625567f3926db9d16959301fa1edbcf2`
2. First authoritative `main` CI run for merge SHA:
   - Workflow: `CI`
   - Run ID: `25218107914`
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25218107914`
   - Result: **failure**
   - Failure cause: `Governance Guardrails` -> `Enforce forensics INDEX governance` because `docs/forensics/INDEX.md` lacked an entry for this evidence pack.
3. Corrective follow-up from this run:
   - Added `docs/forensics/INDEX.md` entry for `docs/forensics/B2.3-P2 Remediation Evidence Pack .md`.
   - Re-run merge cycle required to satisfy the "full green main CI once" closure criterion.

## 6. Final Closure Evidence (Follow-up Merge)

1. Follow-up PR merged to `main`:
   - PR: `#433`
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/433`
   - Merge SHA on `main`: `007c819b79403f34067f71175ee7d06b129f91be`
2. Successful authoritative `main` CI run:
   - Workflow: `CI`
   - Run ID: `25219392128`
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25219392128`
   - Conclusion: **success**
3. Runtime DB proof-lock confirmation from the successful CI logs:
   - `SKELDIR_B23_P2_REQUIRE_DB_PROOFS: 1` visible in the contract semantic drift job environment and runtime proof step.
   - Step `Verify B2.3-P2 runtime DB proof-lock prerequisites` executed and printed `B2.3-P2 runtime DB proof-lock preflight passed`.
   - Required table checks executed in preflight for:
     - `b23_match_verdicts`
     - `b23_exception_records`
     - `b23_revenue_events`
     - `b23_webhook_ingestion_logs`
   - Runtime suite executed in CI:
     - `pytest backend/tests/test_b23_p2_match_engine_kernel.py -q -rA --junitxml=artifacts/b23_p2_runtime_proofs.junit.xml`
   - No skip-only success path remains:
     - CI script contains explicit fail condition: `B2.3-P2 runtime proofs reported skipped tests: ...`.

## 7. Closure Verdict

B2.3-P2 corrective closure criteria for this directive are now met with:
1. merged remediation implementation (`PR #432`) on `main`,
2. follow-up governance fix (`PR #433`) on `main`,
3. authoritative `main` `CI` success for the follow-up merge commit,
4. explicit runtime DB-proof execution evidence,
5. semantic zero-fallback elimination + invariant enforcement + behavioral proof coverage preserved.
