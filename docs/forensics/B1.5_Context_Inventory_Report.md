# B1.5 Context Inventory Report

Date: 2026-03-24  
Authoring mode: Forensic inventory only (no feature build, no code changes)  
Scope: B1.5 Centaur UX control-pattern substrate readiness

## Section 1 - Repo Truth Snapshot

- Primary developer workspace branch at start: `b14-p7-e2e-closure-pack` (`6ac5ad30a228e97f4dcfc1b8c53e0d0226c48a36`), dirty.
- Forensic inventory executed in detached audit worktree at local `main` commit:
  - `C:\Users\ayewhy\_b15_main_audit`
  - `HEAD` = `5704d0366adf01f5fdf44acd7af01600ccc18af8`
  - worktree status: clean.
- Local `main` is not identical to `origin/main`:
  - `main` = `5704d0366adf01f5fdf44acd7af01600ccc18af8`
  - `origin/main` = `192312c61b874a142396d6a14f2e40853f665860`
- `main..origin/main` delta is B1.4/privacy CI/evidence files and does not include B1.5 LLM contract/router/state substrate files.

Verdict for EG-1 Repo Authority Gate: **PASS**

## Section 2 - B1.5 Objective Restated from Code Truth

`main` currently contains partial B1.5-related substrate artifacts (LLM OpenAPI docs, bundled specs, generated TS types, async-oriented persistence tables, and a standalone centaur review state machine service), but no mounted FastAPI runtime handlers for investigation/budget flows, no backend approval/rejection/note mutation surface, and no routed frontend pages wired to real async review APIs; therefore the product surface is not yet an enforceable asynchronous review-and-approve control plane.

## Section 3 - Hypothesis Matrix

### H01 - B1.5 already has a concrete target integration layer in `main`.
- Evidence searched:
  - OpenAPI: `api-contracts/openapi/v1/llm-investigations.yaml`, `llm-budget.yaml`
  - Bundling: `api-contracts/redocly.yaml`, `scripts/contracts/bundle.sh`
  - Mock topology: `Procfile`, `scripts/start-mocks.sh`, `docker-compose.mock.yml`
  - Frontend routed pages: `frontend/src/pages/BudgetOptimizerPage.tsx`, `InvestigationQueuePage.tsx`
- Verdict: **REFUTED**
- Proof:
  - LLM contracts are present and bundled.
  - Routed budget/investigation pages are static/local state and not bound to LLM contract clients.
- Impact:
  - Practical sprint path is not encoded as mock-first integration in routed UX.

### H02 - Existing Prism mocks already cover investigation-state polling.
- Evidence searched:
  - `llm-investigations.yaml`, `llm-budget.yaml`
  - `Procfile`, `scripts/start-mocks.sh`, `scripts/health-check-mocks.sh`
- Verdict: **REFUTED**
- Proof:
  - Investigation status polling contract exists: `GET /api/investigations/{investigation_id}/status`.
  - Budget contract includes `status_url` example pointing to `/api/budget/recommendations/{job_id}/status`, but no such path exists in the spec.
  - Default mock startup scripts do not start LLM mocks.
- Impact:
  - Polling validation for budget async flow is contract-inconsistent and operationally partial.

### H03 - A canonical job lifecycle registry endpoint already exists.
- Evidence searched:
  - Route scans across `backend/app/api`, `api-contracts/openapi/v1`, `frontend/src`
- Verdict: **REFUTED**
- Proof:
  - No `/api/v1/jobs/{id}` or equivalent generic jobs endpoint in contract or runtime.
- Impact:
  - No canonical async lifecycle ownership endpoint for shared UI polling.

### H04 - Budget optimization results retrieval is already contract-defined.
- Evidence searched:
  - `api-contracts/openapi/v1/llm-budget.yaml`
- Verdict: **PROVEN**
- Proof:
  - `GET /api/budget/recommendations/{job_id}` is explicitly defined.
- Impact:
  - Retrieval surface is contract-defined (though runtime-unmounted).

### H05 - Review-and-Approve has a real backend mutation surface.
- Evidence searched:
  - LLM contracts and backend API route scan
- Verdict: **REFUTED**
- Proof:
  - No approve/reject/note endpoints exist in relevant contracts or mounted API modules.
- Impact:
  - Review rail is not durably enforceable at API level.

### H06 - Job lifecycle persistence ownership is explicit in `main`.
- Evidence searched:
  - `backend/app/models/llm.py`
  - `backend/app/services/investigation.py`
  - Alembic migrations for `investigation_jobs`, `investigations`, `budget_optimization_jobs`
  - `backend/app/workers/llm.py`
- Verdict: **REFUTED**
- Proof:
  - Multiple lifecycle stores exist with different grammars and writers.
  - `investigation_jobs` (centaur state machine) is separate from LLM worker tables.
- Impact:
  - Ownership is fragmented, increasing drift risk.

### H07 - The state machine needed for B1.5 already exists at least partially.
- Evidence searched:
  - `backend/app/services/investigation.py`
  - `alembic/.../202512231020_add_investigation_jobs.py`
- Verdict: **PROVEN**
- Proof:
  - Explicit transitions and constraints: `PENDING -> READY_FOR_REVIEW -> APPROVED -> COMPLETED` (+ `CANCELLED`).
- Impact:
  - A bounded review state grammar exists and is enforceable in DB/service layer.

### H08 - No synchronous chat/streaming substrate exists on `main`.
- Evidence searched:
  - Backend and frontend scans for `WebSocket`, `EventSource`, `StreamingResponse`, `chat`, `assistant`, `conversation`
- Verdict: **REFUTED**
- Proof:
  - Backend has no websocket/SSE route primitives.
  - Frontend contains websocket substrate for verification sync (`verificationWebSocket.ts`, `VerificationSyncContext.tsx`).
- Impact:
  - "No realtime substrate exists" is false repo-wide.

### H09 - No hidden auto-commit path exists for financially meaningful recommendations.
- Evidence searched:
  - `backend/app/workers/llm.py`
  - `backend/app/services/investigation.py`
  - Budget scenario/detail frontend pages
- Verdict: **REFUTED**
- Proof:
  - LLM workers persist `status="completed"` into investigation/budget job tables without approval API gates.
  - No approval mutation endpoints exist to enforce mandatory review before completion in those tables.
- Impact:
  - Non-skippable review is not technically guaranteed in active LLM job substrate.

### H10 - Frontend/backend are already aligned on generated types for B1.5 contracts.
- Evidence searched:
  - Generated types: `frontend/src/types/api/llm-investigations.ts`, `llm-budget.ts`
  - Service wrappers: `frontend/src/api/services/llm-investigations.ts`, `llm-budget.ts`
  - Typegen scripts and contract compile gate
- Verdict: **REFUTED**
- Proof:
  - Service wrappers reference nonexistent operation/type keys (`startInvestigation`, `optimizeBudget`, `InvestigationRequest`, `InvestigationStatusResponse`) and wrong path `/api/budget/optimization`.
  - Generated types define `createInvestigation`, `createBudgetOptimization` and `/api/budget/optimize`.
  - Contract compile gate includes only `contract-consumption-gate.ts` + type files, not service wrappers.
- Impact:
  - Contract drift exists now and can pass CI.

### H11 - Anti-cyborg guardrails exist in code-review or CI topology.
- Evidence searched:
  - Workflows under `.github/workflows`
  - CI scripts under `scripts/ci`
  - contract scope/governance configs
- Verdict: **REFUTED**
- Proof:
  - No explicit workflow/lint gate found that blocks adding chat/streaming UX without escalation.
  - `scripts/ci/enforce_llm_provider_boundary.py` enforces provider SDK boundary, not product chat prohibition.
- Impact:
  - Governance does not mechanically prevent anti-Centaur UX drift.

### H12 - Budget Optimizer async review flow is partially wired end-to-end.
- Evidence searched:
  - Routed frontend pages
  - Backend route mounting
  - LLM services usage
- Verdict: **REFUTED**
- Proof:
  - `BudgetOptimizerPage` action navigates directly to static scenario ID.
  - `BudgetScenarioDetailPage` approve/reject buttons have no backend call wiring.
  - No mounted runtime endpoints for LLM budget/investigation flows in FastAPI app.
- Impact:
  - End-to-end async review path is not wired.

### H13 - Investigation-state primitives are distinct from full investigation productization.
- Evidence searched:
  - `investigation_jobs` service/migration
  - LLM worker tables and contracts
  - Routed investigation UI
- Verdict: **UNRESOLVED**
- Proof:
  - Bounded control primitive exists (`investigation_jobs`), but disconnected from routed runtime/API.
  - No full queue/detail backend productization surfaced.
  - Ownership split makes it unclear whether B1.5 can remain bounded without substrate unification.
- Impact:
  - Scope bleed risk remains unless ownership is made explicit first.

### H14 - Deterministic-core supremacy is preserved in current contracts.
- Evidence searched:
  - `llm-budget.yaml`, `llm-investigations.yaml`
  - `backend/app/workers/llm.py`, `backend/app/tasks/llm.py`
- Verdict: **UNRESOLVED**
- Proof:
  - Budget contract says "hybrid deterministic + LLM workflow".
  - Worker persists provider summary text directly into recommendations payload fields.
  - No mounted runtime endpoint exists to validate deterministic-vs-LLM authority semantics in practice.
- Impact:
  - Deterministic supremacy cannot be proven from runtime truth.

### H15 - B1.5 is blocked on contract work, not just UI work.
- Evidence searched:
  - Contract route inventory and inconsistencies
  - Runtime route absence
  - Review mutation absence
  - Frontend type/service drift
- Verdict: **PROVEN**
- Proof:
  - Missing/ambiguous backend surfaces and contract mismatches prevent honest UI-only implementation.
- Impact:
  - Requires contract/mock addendum and runtime alignment before B1.5 implementation.

## Section 4 - Contract Inventory

| Method | Path | Source | Request schema | Response schema | Auth requirements | Persistence owner | Mock present | Runtime handler | Tests present |
|---|---|---|---|---|---|---|---|---|---|
| POST | `/api/investigations` | `api-contracts/openapi/v1/llm-investigations.yaml` | inline object (`question`, optional `context`) | 202 inline object (`investigation_id`, `status`, `estimated_duration_seconds`, `status_url`) | Correlation + Authorization params | Intended `investigations` table (not routed) | Yes (bundle + Procfile) | No | No route-level runtime conformance coverage (allowlisted skip) |
| GET | `/api/investigations/{investigation_id}/status` | `api-contracts/openapi/v1/llm-investigations.yaml` | path param | `#/components/schemas/InvestigationStatus` | Correlation + Authorization params | Ambiguous (`investigations` vs `investigation_jobs`) | Yes | No | No route-level runtime conformance coverage (allowlisted skip) |
| POST | `/api/budget/optimize` | `api-contracts/openapi/v1/llm-budget.yaml` | inline object (`total_budget`, `optimization_goal`, optional constraints) | 202 inline object (`job_id`, `estimated_duration_seconds`, `status_url`) | Correlation + Authorization params | Intended `budget_optimization_jobs` table | Yes | No | No route-level runtime conformance coverage (allowlisted skip) |
| GET | `/api/budget/recommendations/{job_id}` | `api-contracts/openapi/v1/llm-budget.yaml` | path param | 200 `BudgetRecommendation`; 202 running object | Correlation + Authorization params | Intended `budget_optimization_jobs` table | Yes | No | No route-level runtime conformance coverage (allowlisted skip) |
| GET | `/api/budget/recommendations/{job_id}/status` | Referenced only in `status_url` example | N/A | N/A | N/A | N/A | No | No | No |
| GET | `/api/v1/jobs/{id}` (or equivalent canonical jobs) | Not found | N/A | N/A | N/A | N/A | No | No | No |
| POST | `.../approve`, `.../reject`, `.../notes` | Not found | N/A | N/A | N/A | N/A | No | No | No |

## Section 5 - Mock & Runtime Inventory

### Mock topology truth
- LLM mock commands exist in:
  - `Procfile` (`mock_llm_investigations`, `mock_llm_budget`, `mock_llm_explanations`)
  - `scripts/contracts/mock_registry.json` and `scripts/contracts/mock-registry.json`
- Default startup/health scripts do not include LLM mocks:
  - `scripts/start-mocks.sh` only starts core/webhooks/privacy ports.
  - `scripts/health-check-mocks.sh` only checks 4010-4018.
  - `docker-compose.mock.yml` excludes LLM mock services.
- CI mock usability gate in `.github/workflows/ci.yml` checks only attribution + health mock paths.

### Runtime truth
- FastAPI mount list in `backend/app/main.py` includes auth/attribution/platform/revenue/privacy/reconciliation/export/health/webhooks only.
- Runtime introspection (importing `app.main` with `DATABASE_URL` set) reports:
  - `TOTAL_ROUTES 44`
  - `B15_ROUTE_HITS 0`
  - `OPENAPI_PATH_COUNT 40`
  - `B15_OPENAPI_HITS 0`

### Runtime audit feasibility
- Attempted `bash scripts/start-mocks.sh` failed: `/bin/bash` unavailable on this host.
- Attempted `docker compose -f docker-compose.mock.yml up -d` failed: Docker Linux engine unavailable.
- Discovery-only test run:
  - `pytest backend/tests/value_traces/test_value_05_centaur_enforcement.py -q`
  - failed due Postgres connection refused (`WinError 1225`), not semantic assertion mismatch.

Verdict for EG-3 Mock Truth Gate: **PASS** (classified as partial/drifted support, not complete support)

## Section 6 - Persistence & State Ownership

### Async jobs and centaur review entities
- `investigation_jobs`:
  - Migration: `alembic/versions/003_data_governance/202512231020_add_investigation_jobs.py`
  - Service owner: `backend/app/services/investigation.py`
  - States: `PENDING`, `READY_FOR_REVIEW`, `APPROVED`, `COMPLETED`, `CANCELLED`
  - Constraints enforce review/approval ordering.
- `investigations`:
  - Migration: `alembic/versions/004_llm_subsystem/202512081500_create_llm_tables.py`
  - ORM: `backend/app/models/llm.py`
  - Writer: `backend/app/workers/llm.py::run_investigation`
  - States: `pending`, `running`, `completed`, `failed`
- `budget_optimization_jobs`:
  - Migration: same LLM subsystem migration.
  - ORM: `backend/app/models/llm.py`
  - Writer: `backend/app/workers/llm.py::optimize_budget`
  - States: `pending`, `running`, `completed`, `failed`
- LLM audit:
  - `llm_call_audit` and related audit controls exist in migrations and models.

### Ownership finding
- There is no single authoritative lifecycle owner exposed through runtime API.
- Review-enforced `investigation_jobs` is not the same persistence path as LLM worker job completion records.

Verdict for EG-4 State Ownership Gate: **PASS** (fragmentation proven)

## Section 7 - Anti-Cyborg Audit

### Presence/absence proof
- Backend:
  - No FastAPI websocket/SSE/streaming response endpoints found in `backend/app`.
- Frontend:
  - WebSocket substrate exists for verification sync:
    - `frontend/src/services/verificationWebSocket.ts`
    - `frontend/src/contexts/VerificationSyncContext.tsx`
  - No dedicated chat/conversation component file names found in `frontend/src`.

### Auto-apply/auto-commit check
- No explicit budget "apply" API endpoint was found.
- LLM workers can mark job records completed without explicit review mutation endpoints.

Verdict for EG-6 Anti-Cyborg Gate: **PASS** (forbidden patterns are not absent repo-wide; realtime substrate exists in frontend)

## Section 8 - Blocking Gaps

1. Missing mounted runtime handlers for B1.5-relevant investigation/budget contract routes.
2. Missing backend mutation surfaces for approve/reject/notes.
3. Budget contract inconsistency: `status_url` references nonexistent status endpoint path.
4. Fragmented lifecycle ownership across `investigation_jobs`, `investigations`, and `budget_optimization_jobs`.
5. Mock topology drift and partial operational wiring (default scripts omit LLM mock services).
6. Frontend contract-consumption drift in LLM service wrappers (wrong operation IDs/types/paths).
7. CI enforcement gaps:
  - LLM contract dynamic semantics allowlisted for 404 skips.
  - Contract scope excludes LLM prefixes.
  - Static conformance scripts are B0.1 stubs.
  - Contract compile gate does not type-check LLM service wrapper usage.
8. No explicit anti-chat/anti-streaming governance gate for product UX changes.

## Section 9 - Transition Ruling

**READY ONLY AFTER CONTRACT ADDENDUM**

### Binary gate closure
- EG-1 Repo Authority Gate: **PASS**
- EG-2 Endpoint Truth Gate: **PASS**
- EG-3 Mock Truth Gate: **PASS**
- EG-4 State Ownership Gate: **PASS**
- EG-5 Review Enforcement Gate: **PASS**
- EG-6 Anti-Cyborg Gate: **PASS**
- EG-7 Scope Boundary Gate: **PASS** (bounded primitives exist; ownership ambiguity remains)
- EG-8 Orchestration Decision Gate: **PASS**

## Section 10 - Appendix

### Commands run (selected)

1. `git rev-parse --abbrev-ref HEAD`
2. `git rev-parse HEAD`
3. `git status --porcelain=v1`
4. `git rev-parse origin/main`
5. `git diff --name-only main..origin/main`
6. `rg -n "include_router\\(" backend/app/main.py`
7. `rg -n "@router\\.(get|post|put|patch|delete)\\(" backend/app/api`
8. `rg -n "^\\s*/api/|operationId|status_url|status" api-contracts/openapi/v1/llm-investigations.yaml api-contracts/openapi/v1/llm-budget.yaml`
9. `rg -n "mock_llm|4024|4025|4026|llm" Procfile scripts/... docker-compose.mock.yml`
10. `rg -n "createInvestigation|createBudgetOptimization|startInvestigation|optimizeBudget|/api/budget/optimization" frontend/src/api/services/... frontend/src/types/api/...`
11. `rg -n "WebSocket|EventSource|StreamingResponse|chat|assistant|conversation" backend/app frontend/src`
12. Runtime introspection:
    - `python` import of `backend/app/main.py` app object with `DATABASE_URL` set
13. Runtime attempts:
    - `bash scripts/start-mocks.sh` (failed: no `/bin/bash`)
    - `docker compose -f docker-compose.mock.yml up -d` (failed: Docker engine unavailable)
14. Discovery-only test:
    - `python -m pytest backend/tests/value_traces/test_value_05_centaur_enforcement.py -q` (failed: DB connection refused)

### Files inspected (selected)

- `backend/app/main.py`
- `backend/app/api/*`
- `backend/app/models/llm.py`
- `backend/app/services/investigation.py`
- `backend/app/tasks/llm.py`
- `backend/app/workers/llm.py`
- `api-contracts/openapi/v1/llm-investigations.yaml`
- `api-contracts/openapi/v1/llm-budget.yaml`
- `api-contracts/redocly.yaml`
- `scripts/contracts/bundle.sh`
- `scripts/contracts/generate_frontend_types.sh`
- `scripts/contracts/mock_registry.json`
- `scripts/contracts/mock-registry.json`
- `scripts/start-mocks.sh`
- `scripts/health-check-mocks.sh`
- `docker-compose.mock.yml`
- `tests/contract/semantics_skip_allowlist.yaml`
- `tests/contract/test_contract_semantics.py`
- `scripts/contracts/dump_routes.py`
- `scripts/contracts/dump_contract_ops.py`
- `scripts/contracts/check_static_conformance.py`
- `frontend/src/types/api/llm-investigations.ts`
- `frontend/src/types/api/llm-budget.ts`
- `frontend/src/api/services/llm-investigations.ts`
- `frontend/src/api/services/llm-budget.ts`
- `frontend/src/hooks/use-async-job-poller.ts`
- `frontend/src/pages/BudgetOptimizerPage.tsx`
- `frontend/src/pages/BudgetScenarioDetailPage.tsx`
- `frontend/src/pages/InvestigationQueuePage.tsx`
- `frontend/src/services/verificationWebSocket.ts`
- `frontend/src/contexts/VerificationSyncContext.tsx`

## Unvalidated Hypothesis Register (Explicit)

The following remain unvalidated by runtime conformance in current host conditions and must be treated as hypotheses until enforced by mounted routes + passing contract/runtime gates:

- "LLM budget/investigation routes are implemented in FastAPI runtime."
- "`/api/budget/recommendations/{job_id}/status` exists as a contract+runtime status surface."
- "Approve/reject/note review actions are durable backend mutations."
- "Frontend LLM services are type-safe aligned with generated OpenAPI types."
- "Deterministic-core authority is runtime-enforced for budget recommendation outputs."
- "CI topology currently blocks anti-cyborg UX drift by construction."

