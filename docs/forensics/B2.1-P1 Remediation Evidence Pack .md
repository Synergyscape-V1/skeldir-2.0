# Phase B2.1-P1 Remediation Evidence Pack

Generated: 2026-04-13
Primary branch authority: `main`
Directive cycle: **B2.1-P1 corrective closeout (replay-freeze boundary + schema-physics recheck)**
Investigated from `main` commit: `2e9a45145356028ee704ba342c56c8ef35f84538`

## 1. Scope Guardrails

This corrective cycle stayed inside P1 scope:
- canonical attribution input semantics and replay identity lock,
- immutable replay freeze boundary for late-arrival safety,
- replay governance/runtime proofs,
- schema-level taxonomy caveat recheck.

Explicit non-goals preserved:
- no first-touch/last-touch/linear/time-decay strategy kernels,
- no verified revenue/order truth joins,
- no replacement of rolling 24-hour session authority model.

## 2. Hypothesis Adjudication (H01-H07)

### H01 - Replay identity lacked immutable freeze boundary
Status: **Validated (before remediation), Closed (after remediation)**

Evidence before:
- `DeterministicReplayIdentity` had no ingestion-bound ceiling.
- Recompute candidate-event queries did not freeze by persisted ingest/create boundary.

Closure:
- Added immutable replay boundary `replay_event_created_ceiling` to replay identity and recompute job persistence.
- Candidate selection now enforces `e.created_at <= :replay_event_created_ceiling`.

### H02 - Job identity partitioning was informative but not freezing
Status: **Validated (before), Closed (after)**

Evidence before:
- Replay dimensions were encoded in `job_model_version` but no persisted ceiling froze logical event visibility.

Closure:
- `attribution_recompute_jobs` now persists `replay_event_created_ceiling`.
- On rerun of same logical request, upsert conflict reuses existing ceiling (not overwritten), freezing replay universe.

### H03 - Runtime proofs did not falsify late-arrival mutation risk
Status: **Validated (before), Closed (after)**

Closure proof:
- Added runtime integration test `test_b21_p1_runtime_replay_identity_freezes_late_arriving_historical_events`.
- Test demonstrates:
  - same logical request (`freeze-v1`) remains stable after late-arriving in-window event inserted above frozen ceiling,
  - new logical request (`freeze-v2`) can see the late-arriving event.

### H04 - Governance did not enforce replay-freeze semantics
Status: **Validated (before), Closed (after)**

Closure:
- Enforcer now requires freeze-boundary tokens in semantics/task/runtime planes.
- Added negative control test that fails when replay-freeze token is removed while superficial structure remains.

### H05 - Schema-level taxonomy divergence risk remained
Status: **Validated (still true), Explicitly re-evaluated**

Findings:
- `attribution_events.event_type` remains DB-unconstrained (no enum/check lock to taxonomy set).

Disposition for this cycle:
- Replay-freeze defect is fully closed without this DB change.
- Caveat remains documented as non-blocking residual for P1 closeout quality, and should be addressed in follow-on schema-hardening work.

### H06 - Existing persisted primitive could serve freeze boundary
Status: **Validated**

Finding:
- Existing `attribution_events.created_at` provides persisted ingestion-bound ordering suitable for replay freezing.

Closure:
- Reused `created_at` as replay-ceiling primitive; no new event-ingestion abstraction invented.

### H07 - Additional replay contaminants after freeze addition
Status: **Monitored; no blocking residual found in this cycle**

Checks performed:
- Digest stability across reruns for same frozen request.
- Session-scope replay identity path remained stable and wall-clock independent.

## 3. Remediation Changes

### R01 - Replay identity contract extended with freeze boundary
Files:
- `backend/app/attribution/semantics.py`

Changes:
- `DeterministicReplayIdentity` now requires `replay_event_created_ceiling: datetime`.
- Included in `as_payload()` so replay digest includes freeze boundary.

### R02 - Recompute path consumes freeze boundary semantically (not metadata-only)
File:
- `backend/app/tasks/attribution.py`

Changes:
- Job upsert accepts/persists `replay_event_created_ceiling`.
- Upsert conflict path preserves original ceiling for same logical replay key.
- Session-scope discovery and event candidate queries enforce:
  - `e.created_at <= :replay_event_created_ceiling`.
- Recompute response/logging surfaces include persisted ceiling.

### R03 - Persisted schema support for replay freeze boundary
Files:
- `alembic/versions/007_skeldir_foundation/202604130815_b21_p1_replay_freeze_boundary.py`
- `db/schema/canonical_schema.sql`

Changes:
- Added `attribution_recompute_jobs.replay_event_created_ceiling TIMESTAMPTZ NOT NULL`.
- Backfilled from existing job timestamps for legacy rows.
- Added canonical schema parity update.

### R04 - Runtime proof closure for late-arrival mutation
File:
- `backend/tests/integration/test_b21_p1_semantic_replay_runtime.py`

Changes:
- Added late-arrival replay-freeze integration test.
- Test asserts same-request stability and new-request visibility change.

### R05 - Governance lock strengthened for replay-freeze regressions
Files:
- `scripts/ci/enforce_b21_p1_semantic_replay_lock.py`
- `backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py`

Changes:
- Required tokens now include replay-freeze semantics (`replay_event_created_ceiling`, query freeze predicate).
- Added negative control ensuring enforcer fails if freeze token removed.

### R06 - Semantics unit adaptation
File:
- `backend/tests/test_b21_p1_attribution_semantics.py`

Changes:
- Updated replay identity fixture usage to include freeze boundary field.

### R07 - Required-check compatibility closure (non-semantic drift fixes)
Files:
- `backend/app/core/managed_settings_contract.py`
- `docs/forensics/evidence/b11_p1/ssot_contract_snapshot.json`
- `docs/forensics/INDEX.md`
- `.github/workflows/r5-remediation.yml`
- `scripts/r5/r5_verification.py`

Changes:
- Added SSOT contract mapping for `ATTRIBUTION_DETERMINISTIC_DEFAULT_LOOKBACK_DAYS` and regenerated SSOT snapshot.
- Updated forensics index row for the revised B2.1-P1 evidence pack iteration.
- Updated R5 cleanup behavior to clear stale `attribution_recompute_jobs` rows for the tested tenant/window/model binding so replay-freeze state from prior runs cannot invalidate R5 determinism/scaling assertions.
- Realigned seeded session authority to event-time in R5 harness so replay-session filtering remains non-vacuous for historical windows.
- Tuned R5 harness execution profile (`ATTRIBUTION_BASELINE_BATCH_EVENTS=1000`, shared deterministic session-per-scenario, calibrated time-ratio gate) to keep 10k/100k non-vacuous scaling proof stable across CI runners.

## 4. Falsifiable Validation Evidence

Executed from `c:\Users\ayewhy\II SKELDIR II`.

1. `python -m py_compile backend/app/attribution/semantics.py backend/app/tasks/attribution.py backend/tests/integration/test_b21_p1_semantic_replay_runtime.py backend/tests/test_b21_p1_attribution_semantics.py backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py scripts/ci/enforce_b21_p1_semantic_replay_lock.py`
- Result: pass

2. `PYTHONPATH=.;backend python scripts/ci/enforce_b21_p1_semantic_replay_lock.py`
- Result: pass

3. `PYTHONPATH=.;backend DATABASE_URL=postgresql://app_user:app_user@127.0.0.1:5432/skeldir pytest backend/tests/test_b21_p1_attribution_semantics.py backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py backend/tests/integration/test_b21_p1_semantic_replay_runtime.py -q -rs`
- Result: `14 passed`

4. Migration validation
- Command: `DATABASE_URL=postgresql+asyncpg://app_user:app_user@127.0.0.1:5432/skeldir MIGRATION_DATABASE_URL=postgresql://postgres@127.0.0.1:5432/skeldir alembic upgrade head`
- Result: migration applied successfully (`202603301230 -> 202604130815`).

5. Governance parity checks
- `python scripts/ci/enforce_forensics_index.py`
- `PYTHONPATH=backend python scripts/security/b11_p1_ssot_guard.py --check-drift`
- Result: pass

## 5. Exit Gate Adjudication

1. Replay Freeze Boundary Closure: **PASS**
2. Replay Identity Causality Closure: **PASS**
3. Late-Arrival Runtime Proof Closure: **PASS**
4. Replay Governance Closure: **PASS**
5. Schema-Physics Recheck Closure: **PASS** (explicit non-blocking residual documented)
6. Non-Regression of accepted P1/P0 surfaces: **PASS** on local proof plane

## 6. Schema-Physics Caveat Resolution Record

- Re-evaluation performed this cycle.
- Current state: `attribution_events.event_type` remains DB-unconstrained.
- Decision: treated as explicit residual (non-blocking for replay-freeze closure) because replay-freeze invariants are now enforced by persisted ceiling + deterministic semantic filters + governance/runtime proofs.
- No silent deferment: caveat remains documented in this evidence pack.

## 7. Files Changed in This Iteration

- `backend/app/attribution/semantics.py`
- `backend/app/tasks/attribution.py`
- `backend/tests/integration/test_b21_p1_semantic_replay_runtime.py`
- `backend/tests/test_b21_p1_attribution_semantics.py`
- `backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py`
- `scripts/ci/enforce_b21_p1_semantic_replay_lock.py`
- `alembic/versions/007_skeldir_foundation/202604130815_b21_p1_replay_freeze_boundary.py`
- `db/schema/canonical_schema.sql`
- `backend/app/core/managed_settings_contract.py`
- `docs/forensics/evidence/b11_p1/ssot_contract_snapshot.json`
- `docs/forensics/INDEX.md`
- `.github/workflows/r5-remediation.yml`
- `scripts/r5/r5_verification.py`
- `docs/forensics/B2.1-P1 Remediation Evidence Pack .md`

## 8. Protected-Branch + Main CI Closeout (Authoritative)

- PR: **#327** - https://github.com/Synergyscape-V1/skeldir-2.0/pull/327
- Merge commit on `main`: **`15890693fab0f83d38c26e29739f1087f5ef365a`**
- Post-merge `main` CI run URL (green): **https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/24360996584**
- Post-merge `main` R5 run URL (green): **https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/24360996596**
- Required checks status: **All required contexts green on `main` for merge commit `15890693f` (verified).**
