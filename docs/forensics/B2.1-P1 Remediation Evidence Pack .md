# Phase B2.1-P1 Remediation Evidence Pack

Generated: 2026-04-13  
Primary branch authority: `main`  
Corrective directive: **Phase B2.1-P1 - Canonical Attribution Input Semantics + Replay Identity Lock**

## 1. Investigative Baseline and Scope

- Investigated from commit: `a04d72b3347895e7d21112cd6fd0784850817f17` on `main`.
- Scope constrained to P1 semantic/replay closure only:
  - canonical touchpoint/conversion taxonomy,
  - canonical attribution input/output row semantics,
  - replay identity closure for recompute requests,
  - removal of wall-clock replay pollution (`expires_at > authority_now`),
  - explicit deterministic default lookback lock (30 days),
  - non-vacuous P1 CI/runtime governance lock.
- Explicit non-goals preserved:
  - no first-touch/last-touch/linear/time-decay strategy implementation,
  - no verified-revenue semantics pull-forward,
  - no queue isolation/performance/P3+ API expansion work.

## 2. Initial Findings (Hypothesis Adjudication)

### H01/H02 - Canonical touchpoint/conversion taxonomy absent in backend authority path

Status: **Validated**

Evidence:
- Baseline deterministic worker consumed broad events with only `event_type <> 'privacy_tombstone'` filters.
- No authoritative backend taxonomy module defined touchpoint/conversion classes as code-truth.

### H03/H04 - Attribution identity/replay identity were ambiguous at deterministic input boundary

Status: **Validated**

Evidence:
- Job identity previously keyed only by `(tenant_id, window_start, window_end, model_version)`.
- No explicit replay dimension lock for session scope/lookback/taxonomy semantics.

### H05/H06 - Replay path polluted by wall-clock session activeness

Status: **Validated**

Evidence:
- Recompute path used `sa.expires_at > :authority_now`.
- Historical replay depended on current wall-clock instead of persisted session facts.

### H07 - Deterministic default lookback not explicit in code-truth

Status: **Validated**

Evidence:
- No authoritative deterministic lookback setting in runtime/task semantics.

### H08/H09/H10 - Output contract and proof plane were under-specified for semantic replay closure

Status: **Validated**

Evidence:
- No canonical input/output row contract objects used by deterministic worker path.
- No P1-specific runtime/enforcer plane proving semantic weakening failure.

## 3. Remediations Implemented

### R01 - Canonical attribution semantics authority module

Files:
- `backend/app/attribution/semantics.py`
- `backend/app/attribution/__init__.py`

Changes:
- Added authoritative semantic surfaces for:
  - `TOUCHPOINT_EVENT_TYPES`
  - `CONVERSION_EVENT_TYPES`
  - `AttributionInputRow`
  - `AttributionOutputRow`
  - `DeterministicReplayIdentity`
  - deterministic lookback normalization and replay-window derivation.
- Locked semantics version: `ATTRIBUTION_SEMANTICS_VERSION = "b2.1-p1-v1"`.

Result:
- Touchpoint/conversion and input/output row semantics are explicit backend code-truth, not inferred.

### R02 - Replay identity lock and wall-clock pollution removal in deterministic worker

File:
- `backend/app/tasks/attribution.py`

Changes:
- Replaced wall-clock session filters with persisted-session overlap semantics:
  - `sa.issued_at < :replay_window_end`
  - `sa.expires_at > :replay_window_start`
- Removed `sa.expires_at > :authority_now` replay gating.
- Filtered deterministic input candidates to canonical conversion taxonomy.
- Introduced replay identity object in recompute path; replay dimensions now include:
  - taxonomy version,
  - lookback days,
  - session scope identity,
  - replay anchor/window semantics.
- Encoded replay dimensions into deterministic job identity model token (`job_model_version`) for `attribution_recompute_jobs` idempotency partitioning.
- Added input/output/replay identity digests to recompute result and structured logs.

Result:
- Historical replay stability is anchored to persisted session facts and explicit replay semantics.

### R03 - Explicit deterministic default lookback lock (30 days)

Files:
- `backend/app/core/config.py`
- `backend/app/services/attribution.py`
- `backend/app/tasks/attribution.py`

Changes:
- Added authoritative config: `ATTRIBUTION_DETERMINISTIC_DEFAULT_LOOKBACK_DAYS` (default `30`, validated `1..365`).
- Added task/service lookback propagation and normalization.
- Deterministic replay window now clamps by authoritative lookback default unless explicitly overridden.

Result:
- 30-day deterministic lookback is now executable runtime truth.

### R04 - P1 governance and non-vacuous proof-plane lock

Files:
- `scripts/ci/enforce_b21_p1_semantic_replay_lock.py`
- `backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py`
- `backend/tests/test_b21_p1_attribution_semantics.py`
- `backend/tests/integration/test_b21_p1_semantic_replay_runtime.py`
- `.github/workflows/ci.yml`
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`

Changes:
- Added semantic enforcer with negative controls for meaningful weakening:
  - reintroducing wall-clock replay gating,
  - removing required runtime semantic proofs,
  - dropping required protected-branch context.
- Added unit/runtime proofs for taxonomy lock, historical replay stability, lookback lock, and replay identity partitioning.
- Added dedicated CI runtime job: `B2.1-P1 Semantic Replay Lock`.
- Added required status-check context in governance contract.

Result:
- P1 closure is merge-blocking and non-vacuous under semantic weakening.

### R05 - Post-merge compatibility remediation for legacy proof planes

Files:
- `backend/tests/test_b0533_revenue_input_contract.py`
- `backend/tests/integration/test_b057_p5_full_chain_e2e.py`

Changes:
- Updated B0.5.3.3 contract fixtures to seed `session_authority` rows for synthetic
  sessions used by deterministic recompute assertions.
- Updated B0.5.7 E2E recompute job probe to accept replay-identity encoded
  `model_version` rows (`1.0.0::...`) in `attribution_recompute_jobs`, while
  preserving compatibility with plain `1.0.0`.

Result:
- Prevents false-negative CI failures introduced by the B2.1-P1 replay identity
  encoding and session-authority eligibility lock.

### R06 - Mainline closeout remediation for remaining phase-gate regressions

Files:
- `backend/tests/integration/test_b21_p1_semantic_replay_runtime.py`
- `backend/tests/integration/test_b057_p5_full_chain_e2e.py`

Changes:
- Added explicit `RAW_SQL_ALLOWLIST` marker to the deterministic runtime seed
  SQL block in B2.1-P1 replay runtime integration proof, satisfying
  `SCHEMA_GUARD` contract enforcement without weakening semantic assertions.
- Aligned B0.5.7 full-chain webhook event timestamp to runtime (`datetime.now(timezone.utc)`),
  removing stale historical timestamp dependence that triggered session-locality
  rejection under authority-window enforcement.

Result:
- Closed the remaining post-B2.1-P1 regressions on `main`:
  `B0.5.7 P6 E2E (Least-Privilege)`, `Phase Gates (SCHEMA_GUARD)`, and
  transitively `Phase Chain (B0.4 target)`.

## 4. Local Validation Evidence (Falsifiable)

All commands executed from repository root `c:\Users\ayewhy\II SKELDIR II`.

### Enforcement and semantic unit planes

1. `python scripts/ci/enforce_b21_p1_semantic_replay_lock.py`  
Result: `PASS`

2. `PYTHONPATH=.;backend pytest backend/tests/test_b21_p1_attribution_semantics.py backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py -q`  
Result: `9 passed`

### Runtime semantic replay proofs

3. `PYTHONPATH=.;backend DATABASE_URL=postgresql+asyncpg://app_user:app_user@127.0.0.1:5432/skeldir_validation MIGRATION_DATABASE_URL=postgresql://app_user:app_user@127.0.0.1:5432/skeldir_validation pytest backend/tests/integration/test_b21_p1_semantic_replay_runtime.py -q -rs`  
Result: `3 passed`

### Regression compatibility check on prior idempotency suite touched by replay-identity encoding

4. `PYTHONPATH=.;backend DATABASE_URL=postgresql+asyncpg://app_user:app_user@127.0.0.1:5432/skeldir_validation MIGRATION_DATABASE_URL=postgresql://app_user:app_user@127.0.0.1:5432/skeldir_validation pytest backend/tests/test_b0532_window_idempotency.py -q`  
Result: `4 passed`

### Post-merge compatibility proofs for legacy CI surfaces

5. `pytest backend/tests/test_b0533_revenue_input_contract.py -q -rs`  
Result: `2 passed`

6. `python -m py_compile backend/tests/integration/test_b057_p5_full_chain_e2e.py backend/tests/test_b0533_revenue_input_contract.py`  
Result: `pass`

7. `PYTHONPATH=.;backend pytest backend/tests/test_no_raw_inserts_core_tables.py -q -rs`  
Result: `pass`

8. `pytest backend/tests/integration/test_b21_p1_semantic_replay_runtime.py -q -rs`  
Result: `1 passed`

9. `python -m py_compile backend/tests/integration/test_b21_p1_semantic_replay_runtime.py backend/tests/integration/test_b057_p5_full_chain_e2e.py`  
Result: `pass`

## 5. Exit Gate Adjudication

1. Canonical Input Taxonomy Lock: **PASS**  
2. Immutable Attribution Identity Lock: **PASS**  
3. Replay Identity Closure (P1 scope): **PASS**  
4. Rolling 24-Hour Replay Stability Lock (no wall-clock pollution): **PASS**  
5. Default Lookback Lock (30-day authoritative default): **PASS**  
6. Non-Vacuous Replay Governance: **PASS**  
7. Non-Regression of Accepted P0 Surfaces: **PASS**

## 6. Files Changed in This Iteration

- `backend/app/attribution/__init__.py`
- `backend/app/attribution/semantics.py`
- `backend/app/core/config.py`
- `backend/app/services/attribution.py`
- `backend/app/tasks/attribution.py`
- `backend/tests/integration/test_b21_p1_semantic_replay_runtime.py`
- `backend/tests/integration/test_b057_p5_full_chain_e2e.py`
- `backend/tests/test_b0533_revenue_input_contract.py`
- `backend/tests/test_b0532_window_idempotency.py`
- `backend/tests/test_b21_p1_attribution_semantics.py`
- `backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py`
- `scripts/ci/enforce_b21_p1_semantic_replay_lock.py`
- `.github/workflows/ci.yml`
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- `docs/forensics/B2.1-P1 Remediation Evidence Pack .md`

## 7. Protected-Branch Closeout Record

1. PR `#323` merged to `main`  
Merge commit: `0e9ac64abfbee9db1db46c7bd5acee83bd2a52af`  
Post-merge `main` CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/24318734176`  
Result: `failure` (regressions remediated in R05/R06)

2. PR `#324` merged to `main`  
Merge commit: `9557bd01d9493094c1e726d3a972dbedc59b1bfb`  
Post-merge `main` CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/24319500828`  
Result: `failure` (residual regressions remediated in R06)

3. PR `#325` merged to `main`  
Merge commit: `871c71a18d5e9c09749365345282dfcdc59aeaf4`  
Post-merge `main` CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/24320202795`  
Result: `success`

4. Protected-branch enforcement evidence at final merge  
- PR `#325` merged via protected-branch workflow.
- Required `main` branch contexts were green at merge time (non-required `ssot-contract-gate` remained non-blocking).
