# B2.5-P13 Corrective Action Remediation XX Report

**Phase:** B2.5-P13 — Internal End-to-End Trust Closure Harness
**Corrective:** XX (directive `B2.5-P13 FOLLOW-UP CORRECTIVE ACTION REMEDIATION DIRECTIVE XX-2`)
**Engineering agent:** Agent 2
**Audited predecessor:** `main` = `87836b60cd2d8a23dcd6def203c14e3ae3b3477b`, tree `a922924e4e2ab7c7a98fb503f7bea69d5718effd`, Alembic head `202609011200`
**Governing audits:** `62. Github Independent Audit Report` (Nicholas), `63. Github Independent Audit Report` (Trey)

> Engineering may write `Corrective XX = CANDIDATE COMPLETE`. It may not write
> `B2.5-P13 = COMPLETE`. That remains blind-audit authority.

---

## 1. Succession reconstruction

`git fetch origin --prune` at session start resolved:

| coordinate | value |
|---|---|
| branch | `codex/b25-p13-directive-xx` |
| HEAD | `87836b60cd2d8a23dcd6def203c14e3ae3b3477b` |
| tree | `a922924e4e2ab7c7a98fb503f7bea69d5718effd` |
| `origin/main` | identical to HEAD |
| ahead / behind | `0 / 0` |
| merge-base | HEAD |
| stashes | none |

**No technical drift since the audited state**, so per directive §4.2 no second full C19 baseline was established before coding. Agent 1's passing baseline was accepted as entry evidence.

The working tree was **not** clean. It carried 27 modified files and 4 untracked files that Agent 1's handoff log does not evidence (`A1-XX-07` states no substantive remediation was completed). Reconstructed disposition:

| inherited material | disposition |
|---|---|
| `202609021200_..._runtime_tenant_and_verdict_authority.py` — verdict grant fence **plus** an HMAC-verified tenant-context redesign rewriting every public tenant RLS policy | **verdict fence kept and rewritten; HMAC redesign rejected** |
| `walk_window_predicate` override on `attribution_allocations` | **kept**, but only after running the experiment the directive requires *before* deciding |
| divergent-clock test in `test_b25_p13_c9_feature_cardinality_physics.py` | **kept and rewritten** so its assertions state what the experiment actually found |
| `pull_request` / `merge_group` triggers + job names on R2–R7 | **kept**, plus the H-XX2-08 repairs the inherited version lacked |
| `test_b25_p13_c20_runtime_authority.py` (HMAC-shaped) | **rewritten** against the narrow fence |

### Why the HMAC tenant-context redesign was rejected

The inherited migration stored a deployment HMAC key in a runtime-inaccessible table and rewrote every `public` tenant policy from `current_setting('app.current_tenant_id')` to a `SECURITY DEFINER` verifier, so a holder of the shared runtime credential could not select its own tenant. It is a coherent idea. It is also out of scope and disproportionately risky:

- the repository has **1,392 occurrences of `app.current_tenant_id` across 202 files** — migrations, tests, CI workflows, ops scripts. Every one of them is a call site the redesign changes the meaning of, under 75 required contexts;
- neither audit raised it. Audit 62 §7 recorded the live C19 probe returning `{"rls":"ok","tenant_guc":"ok"}`; audit 63 recorded RLS/GUC tenant fencing behaving correctly and cross-tenant refusal proven. The two dispositive findings were Gate K and Gate R;
- directive `A1-XX-02` states: *"Do not broaden Corrective XX into a new P13 redesign unless new evidence demands it"*, and §15: *"Do not turn Corrective XX into a full security audit."*
- H-XX2-02 defines a sibling overgrant as *"process A can manufacture process B's load-bearing premise."* A different **tenant** is not a different **process**; the API's tenant scoping is the API's own premise, derived from server-side auth. It is not another process's.

The work is preserved in a git stash (`xx2: inherited HMAC tenant-context redesign (out of Corrective XX scope, archived)`) and recorded in the debt register below, not discarded silently.

**The specific hazard the redesign was reaching for is nevertheless closed by the narrow fence:** §15's `app_user cross-tenant verdict mutation` probe is refused, because after the fence `app_user` holds no verdict write authority under *any* claimed tenant (§5, probe 4).

---

## 2. First-red ledger

| # | experiment | first observed error | environment or substrate? | causal explanation | remediation | restoration | subsequent result |
|---|---|---|---|---|---|---|---|
| 1 | H-XX2-01 direct principal test at `202609011200` | none — `UPDATE 1` succeeded | **PRODUCT** | `app_user` held `INSERT, UPDATE` on `b23_match_verdicts` directly and by inheritance through `app_rw` | C20 migration, two layers | n/a | refused, `SQLSTATE 42501` |
| 2 | first hosted PR run (`ce5a4dc5`) — R3, m2, b0545, b057_p4 | `role "app_worker" does not exist` during `alembic upgrade` | **PRODUCT (mine)** | the C20 migration named runtime roles unconditionally; several lanes migrate an empty cluster before any role exists | `_if_role_exists` guard, matching the idiom `202604291200` already uses; trigger additionally admits the relation owner | `2116cbfa` | those lanes green |
| 3 | first hosted PR run — `B2.5-P4 Money Authority` | `B25_P1_TRUST_DRIFT_VIOLATION test_b25_p13_c20_runtime_authority.py` | **PRODUCT (mine)** | the B2.5-P1 trust-drift tripwire allowlists post-P1 closure proofs by path; the new proof was not listed | allowlisted beside its C19 siblings | `2116cbfa` | green |
| 4 | `2116cbfa` — `M3 CI Governance` | `M3_BLOCKED_BY_UNREGISTERED_ENFORCERS` + `M3_BLOCKED_BY_BRANCH_PROTECTION_CONTEXT_DRIFT` | **PRODUCT (mine)** | M3 keeps three registries that must agree with live protection; the new enforcers and the five R contexts were in none | registered in `enforcer_registry.yaml`, `gate_subsumption_matrix.yaml`, `ci_topology_map.md` | `3624f621` | green |
| 5 | `2116cbfa` — `R7 Final Winning State`, phase R3c | `S6_MixedStorm_N1000` `HTTP_CONNECTION_ERRORS=2`, `UNIQUE_CANONICAL_ROWS_CREATED=296/300` | **SUBSTRATE (harness)** | uvicorn `--workers 4` against 1000 requests at concurrency 200 on a 2-core runner also hosting Postgres; the standalone R3 lane calibrates to CPU count and passed on the same commit | classified as harness debt H-R7-02 and left open; R7 demoted out of the merge boundary, §8.4/§8.6 | n/a | R7 is post-merge again |
| 6 | `593d5279` — `R7 Final Winning State`, phase R2 | `Expected exactly 1 occurrence of marker R2_WINDOW_START::…, found 0` (14 events parsed, 0 in window) | **SUBSTRATE (harness)** | `podman logs --since` bounded by a second-granular host timestamp can drop a marker emitted in the same second; the window is already delimited by markers inside the log, which is why the standalone R2 lane reads it whole | full-log read, poll for the window-end marker (H-R7-01), §8.6 | `4cc10b93` | repaired |
| 7 | `be3dc817` — `R7 Final Winning State`, phase R3c | `HTTP_5XX_COUNT=217`, `HTTP_CONNECTION_ERRORS=136`, `UNIQUE_CANONICAL_ROWS_CREATED=262/300` | **SUBSTRATE (harness)** | undiagnosed; a different signature from entry 5, and the uvicorn log is not captured on this path | classified as H-R7-03 and left open; R7 demoted, §8.6 | n/a | R7 is post-merge again |
| 8 | `be3dc817` — `B2.5-P13 C20 Verdict Authority Conservation`, step NC-C20-01 | `UPDATE 0` then "NC-C20-01 did not fire" | **PRODUCT (mine)** | the control issued its historical UPDATE without binding a tenant; the table forces RLS on that GUC, so the statement matched zero rows and the BEFORE ROW trigger never ran. A control measuring nothing looked exactly like a fence that failed | bind a real provisional verdict's tenant, require refusal, confirm the row survived | `1ab4bf06` | see §15 |
| 9 | `be3dc817` — `B2.5-P13 E2E Trust Closure Core`, C6 | `InsufficientPrivilegeError: permission denied for table b23_match_verdicts` | **PRODUCT (mine)** | C6's stimulus helper wrote events, allocations *and* verdicts through one connection as the API principal | split across production authors; the allocation now lands before the verdict confirms it, which is the real order | `1ab4bf06` | see §15 |
| 10 | `be3dc817` — C12/C13/C14 closure validators, C16/C17 migration round trips | `required_aggregator_dependency_drift`; `alembic current \| grep -q '202609011200 (head)'` | **PRODUCT (mine)** | three validators assert the P13 aggregator's dependency set as an equality, and two steps hard-code the head revision. Adding the C20 job changed both | registered C20 in all three sets; head assertions moved to `202609021200` | `1ab4bf06` | see §15 |

Entries 2–4 and 8–10 are worth stating plainly: **the pre-merge governance change caught seven defects in its own candidate before any of them could land.** Under the historical topology, every one of them would have merged and been reported afterwards — which is precisely the failure mode this corrective exists to close.

---

## 3. Process × role authority matrix

Reconstructed mechanically from a fresh PostgreSQL 15 provisioned by the production script `scripts/database/prepare_migration_authority_boundary.py` — 120 public tables × 8 principals × 4 operations, using `has_table_privilege` so inheritance is accounted for.

Role graph as provisioned:

```
app_user   ∈ {app_rw, app_ro}
app_worker ∈ {app_rw, app_ro}
migration_owner ∈ {app_user, app_worker}          (governed exception, C7 topology)
app_dispatch_publisher, app_celery_transport,
app_trust_issuer, app_trust_signer                (member of nothing)
```

`b23_match_verdicts`, before and after:

| principal | at `202609011200` | at `202609021200` |
|---|---|---|
| `app_user` | `SELECT, INSERT, UPDATE` | `SELECT` |
| `app_rw` | `SELECT, INSERT, UPDATE` | — |
| `app_ro` | `SELECT` | `SELECT` |
| `app_worker` | `SELECT, INSERT, UPDATE` | `SELECT, INSERT, UPDATE` |

Full matrix: `docs/forensics/evidence/b25_p13_c20_local_physics_evidence.json`.

---

## 4. Verdict-grant root cause and repair

Two heads, which is why a single grant edit would not have held:

1. `202604291200_b23_p1_schema_authority_lock.py:363` — direct grant to `app_user`;
2. same migration `:367` — grant to `app_rw`, which `app_user` inherits;
3. `202605061200_b23_p4_queue_performance_indexes.py:110` — **re-granted** the direct privilege after an earlier revoke in the same lineage.

Point 3 is the empirical reason the repair does not rely on grants alone. Authority is now expressed twice:

- **privilege** — `b23_match_verdicts` revoked from `PUBLIC`, `app_user`, `app_rw`, `app_ro`; `SELECT` back to `app_user`/`app_ro`; `SELECT, INSERT, UPDATE` to `app_worker`;
- **consequence** — `trg_b23_verdict_authorship_insert` / `_update` call `b23_enforce_verdict_authorship()`, which refuses unless the `session_user` is a superuser, holds the relation owner's role, or holds `app_worker`.

The trigger fires on INSERT, and on UPDATE only when a column that *is* verdict truth changes (13 columns: status, the four transition timestamps, match quality, and the six money/discrepancy columns). `attribution_events` and `webhook_ingress_identities` reference the table `ON DELETE SET NULL`; that referential action is a governed consequence of deleting the referenced row, not an assertion of verdict truth, and §5 proves it still works.

`session_user` rather than `current_user` is deliberate: `SET ROLE` changes `current_user` only, and `app_user` cannot `SET ROLE app_worker` in any case (§5, probe 6).

---

## 5. Sibling privilege battery (directive §15)

Eighteen probes, each connecting as a real production login against the migrated database.

| # | probe | principal | result |
|---|---|---|---|
| 1 | INSERT verdict | `app_user` | REFUSED `42501` |
| 2 | UPDATE verdict amount | `app_user` | REFUSED `42501` |
| 3 | `matched_provisional → matched_confirmed` | `app_user` | REFUSED `42501` |
| 4 | cross-tenant verdict mutation (tenant GUC substituted) | `app_user` | REFUSED `42501` |
| 5 | DELETE verdict | `app_user` | REFUSED `42501` |
| 6 | `SET ROLE app_worker` | `app_user` | REFUSED `42501` |
| 7 | `SET ROLE postgres` | `app_user` | REFUSED `42501` |
| 8 | partial tuple, NULL authority columns | `app_user` | REFUSED `42501` |
| 9 | **`matched_provisional → matched_confirmed`** | **`app_worker`** | **ALLOWED — allocation projected `verified=true, source='b23_match_verdict'`** |
| 10 | mutate worker-owned fit | `app_dispatch_publisher` | REFUSED |
| 11 | mutate verdict | `app_dispatch_publisher` | REFUSED |
| 12 | mutate financial ledger | `app_celery_transport` | REFUSED |
| 13 | read attribution events | `app_celery_transport` | REFUSED |
| 14 | mutate verdict | `app_trust_issuer` | REFUSED |
| 15 | mutate verdict | `app_trust_signer` | REFUSED |
| 16 | mutate allocation verification | `app_trust_signer` | REFUSED |
| 17 | schedule a fit (planner wakeup) | `app_user` | REFUSED |
| 18 | `SELECT b24_next_active_worker_generation()` | `app_user` | REFUSED — `EXECUTE` denied |

Probe 18 was expected to be *allowed and return NULL* (audit 63 §13 reported fail-closed-by-principal behaviour). The observed fence is **stronger**: `app_user` cannot execute the function at all. Recorded because the expectation was wrong, not the system.

The contract these express is asserted as an equality — not a subset — over six authority-bearing relations in `test_c20_process_role_authority_contract_holds`, and `test_c20_sibling_overgrant_is_detected` grants `app_user` `INSERT` on `bayesian_model_fits`, requires the assertion to go red, revokes, and requires green.

---

## 6. Both-directions authority physics, and the layer falsifier

`backend/tests/trust/test_b25_p13_c20_runtime_authority.py`, five tests, run inside the required `B2.5-P13 E2E Trust Closure` aggregate and again inside the C19 production topology.

**Forbidden direction.** `app_user` INSERT, benign UPDATE, transition and cross-tenant transition all refused; verdict remains `matched_provisional`; allocation remains `(verified=false, source=NULL)` — no downstream consequence occurred.

**Lawful direction.** `app_worker` performs the same transition: `UPDATE 1`, verdict `matched_confirmed`, allocation projected `(true, 'b23_match_verdict')`.

**The falsifier, which is the part that makes the two green layers mean something:**

| step | action | required outcome | observed |
|---|---|---|---|
| 1 | restore the historical grant to `app_user` | still REFUSED (trigger holds) | REFUSED `42501` |
| 2 | revoke, then drop both guard triggers | still REFUSED (privilege holds) | REFUSED |
| 3 | **sever both layers** | historical RED reproduces | **ALLOWED, `UPDATE 1`, allocation `verified=true`** |
| 4 | restore grant and both triggers from `pg_get_triggerdef` | REFUSED again, allocation back to `(false, NULL)` | REFUSED; restored triggerdefs byte-identical |

Step 3 is what forbids a vacuous pass: if either layer alone were doing nothing, steps 1 and 2 would still be green and step 3 would be indistinguishable. The workflow additionally runs NC-C20-01, which restores the historical grant on the live CI database with `psql` and requires the API principal's transition to fail.

---

## 7. B2.4 temporal coherence — the experiment, then the decision

Both audits left this open: 62 classified it `INCONCLUSIVE — BLOCKING` and refused to accept the inherited "bounded hardening debt" label without the experiment; 63 declared it a coverage limitation. Directive §11 forbids patching `feature_cardinality` before running it.

**Construction.** Two observations of one authoritative source universe. Held constant: tenant, financial event times, event contents, verified allocation contents, channel/provider identity, B2.3 authority, event-time source window. Varied: `attribution_allocations.created_at`, moved from inside the window to `window_end + 2 days`. Cohort: 129 distinct channels against a channel cap of 128, so the overflow decision is live.

**Result against the unremediated code (`87836b60`):**

| observable | before | after |
|---|---|---|
| `channel_count` / `currency_count` / `provider_count` / `campaign_or_feature_count` | 129 / 1 / 1 / 1 | **identical** |
| `channel.overflowed` | true | **identical** |
| `source_snapshot_hash` | `be325f56…` | **identical** |
| feature authority produced | yes | **identical** |
| B2.4 resource decision | `fallback` / `feature_width_exceeded` | **identical** |
| allocation relation's own bounded walk | 129 keys, overflowed | **0 keys, not overflowed** |

**Adjudication under §12.** Same semantic source + different write timing → **identical load-bearing decision**. The classification is therefore `BOUNDED HARDENING DEBT`, not `CONFIRMED PRODUCT DEFECT`. Two independent structural reasons explain why:

1. `eligibility.py` is event-clocked end to end — every relation, allocations included, joins `attribution_events.occurred_at`; `channel_count` and `currency_count` come from that preflight, not from the walk;
2. `DERIVED_DIMENSION_SOURCES` routes only `b23_match_verdicts`, `b23_revenue_events` (provider) and `attribution_events` (campaign) through `render_bounded_key_walk`. **`attribution_allocations` is never walked in production**, so its write-clock bound was unreachable from any decision.

**It was remediated anyway, and the reason is stated rather than assumed.** The unreachable bound is one line from being load-bearing: adding `("attribution_allocations", "channel_code")` to the channel dimension would hand a resource-refusal decision to a persistence clock. The codebase already carries the scar — `b23_match_verdicts` has this exact override because an earlier bug of this shape lost every provider reconciled after the window closed. The override touches `walk_window_clause` only; `window_key` still governs `member_predicate` and the dirty-event day bucket, which are unchanged. Post-remediation, re-running the same experiment changes nothing at all (`changed_keys: []`).

`NC-C20-02` reverts the override with `scripts/ci/_b25_p13_c20_revert_allocation_walk.py`, requires `allocation membership drifted from the event-clock source contract` to appear, restores exactly, and requires 12 passed. The mutator fails loudly if the override has moved, so the control can never appear to fire while having mutated nothing.

---

## 8. Merge governance

### 8.1 The historical state, reconstructed

`R2/R3/R4/R6/R7` declared `workflow_dispatch` + `push: [main]` only. PR #697 (`82f528ce`) merged through the ALLGREEN queue at `2026-09-02T09:20:30Z`; the `R2: Data-Truth Hardening` run against that exact commit started at `2026-09-02T09:20:33Z` and concluded `failure`. Three seconds. The lane was structurally incapable of blocking.

### 8.2 The repair, and why triggers alone are not it

H-XX2-06 is explicit that a workflow can execute pre-merge and still block nothing. The full conjunction is now enforced in four places:

| clause | enforced by |
|---|---|
| runs on `pull_request` **and** `merge_group` | `validate_p13_merge_governance.py` (structural, no token needed) |
| those events carry no `paths` / `branches` filter | same |
| the job publishing the context exists and is unconditional | same |
| the context is in the required-status contract | same |
| the contract matches **live GitHub** | `validate_live_branch_protection.py --mode main`, already a required context |
| every declared lane actually *executed and succeeded* on this SHA | `adjudicate_p13_adversarial_physics.py`, in its own required context (§8.4) |

`validate_p13_merge_governance.py` runs inside `CI Physics Guard` — itself a required context that runs on `pull_request`, `merge_group` and `push`. Its negative controls detach one clause at a time:

| control | validator |
|---|---|
| `pull_request` trigger removed (the historical state) | RED → restore → GREEN |
| `merge_group` trigger removed | RED → restore → GREEN |
| `pull_request` narrowed by `paths` | RED → restore → GREEN |
| publishing job made conditional | RED → restore → GREEN |
| context detached from `required_contexts` | RED → restore → GREEN |
| publishing job renamed | RED → restore → GREEN |

The pre-existing `validate_live_branch_protection.py --negative-control` battery, run inside the required `B2.4 Gate Dry Run`, independently refuses the same detachment from the other side:

```
B24_P11_NEGATIVE_CONTROL_PASS pr_required_context_removal:
  PR required-status governance delta removes or renames required contexts: ['R7 Final Winning State']
B24_P11_NEGATIVE_CONTROL_PASS main_missing_new_required_context:
  live branch protection missing required contexts: ['New Required Context']
```

So Exit Gate XX2-E's active falsifier — *detach one load-bearing lane* — fires in two independent required contexts: structurally in `CI Physics Guard`, and against live GitHub state in `B2.4 Gate Dry Run`.

### 8.3 H-XX2-08: push-only semantics inside the lanes

Inspected before changing triggers. R3 and R4 each carry an *"Open PR to anchor proof in repo"* step that creates a branch, pushes it with `GITHUB_TOKEN`, and opens a PR. Left alone, every PR run would have produced branch and PR spam about a candidate that has not landed. Both are now gated to `github.event_name == 'push' && github.ref == 'refs/heads/main'`. R2, R6 and R7 carry no push-only side effects; R6's SHA guard compares `git rev-parse HEAD` to `GITHUB_SHA`, which holds on `pull_request` and `merge_group` because `actions/checkout` checks out exactly that ref.

### 8.4 Which lanes hold the boundary, and why not R7

The first attempt made all five historical lanes required. `R7 Final Winning State` did not survive contact: three of four pre-merge runs failed for harness reasons, each with a different profile. Rather than either force it or waive it, its contribution was mapped mechanically.

| R7 phase | invokes | already covered by |
|---|---|---|
| Phase R2 | `scripts/r2/{runtime_scenario_suite, db_log_window_audit, orm_window_audit, instrument_consistency_gate}.py` | **R2 Data-Truth Hardening** — the same four, *plus* `static_behavioral_audit.py` |
| Phase R3c | `scripts/r3/ingestion_under_fire.py` | **R3 Ingestion Under Fire** |
| Phase R4b–f | `scripts/r4/worker_failure_semantics.py` | **R4 Worker Failure Semantics** |
| Phase R6 | `scripts/r6/r6_context_gathering.py` | **R6 Worker Resource Governance** |
| Phase R1, R5 | inline / `scripts/r5/r5_verification.py` | liveness and determinism, proven far more strongly by the required `B2.5-P13 E2E Trust Closure` topology journey |

R7's assertions are therefore a strict **subset** of the required standalone lanes'. The remaining claim — "all of it on one substrate" — was tested against the workflow rather than assumed: the bootstrap step issues `DROP DATABASE IF EXISTS; CREATE DATABASE` for each of `r1`…`r6`, every phase addresses only its own database, and no phase reads another's. What the phases share is a PostgreSQL **server process** and a runner VM, not conducted state. That is a resource soak, not compositional physics. The compositional physics — one financial universe carried across nine real processes with real state conduction — is `B2.5-P13 C19 Context-Robust Production Closure`, inside the required P13 aggregate.

So R7 returns to `workflow_dispatch` + `push: [main]` as a post-merge diagnostic, and the merge boundary is held by the four stable lanes plus a thin deterministic adjudicator.

**`P13 Adversarial Physics Adjudication`** exists because branch protection answers the wrong question. It answers *"is this named context green"*; GitHub counts a `skipped` check-run as satisfying a branch rule, which is verbatim the directive's automatic NOT-COMPLETE condition — *"required context is green because the real adversarial job skipped"*. The adjudicator reads the check-runs API for the SHA under adjudication and requires, for every lane declared in the contract, a check-run that exists, is `completed`, and concluded exactly `success`. It waits for the lanes, bounded at 35 minutes — inside the queue's 60-minute check-response timeout — and fails closed if the bound is reached. On `push` the lanes carry path filters, so there it observes rather than asserts.

Its seven negative controls each feed one way a lane can fail to have proved anything and require a refusal, then restore success and require acceptance:

| fed observation | adjudicator |
|---|---|
| lane concluded `skipped` | REFUSED → restore → ACCEPTED |
| lane concluded `neutral` | REFUSED → restore → ACCEPTED |
| lane concluded `cancelled` | REFUSED → restore → ACCEPTED |
| lane concluded `timed_out` | REFUSED → restore → ACCEPTED |
| lane concluded `failure` | REFUSED → restore → ACCEPTED |
| lane never reported at all | REFUSED → restore → ACCEPTED |
| lane still running when the wait expires | REFUSED → restore → ACCEPTED |
| lane absent on a `push` event | ACCEPTED (observation, not assertion) |

### 8.5 Live governance state

| | audited state | now |
|---|---|---|
| required contexts (live) | 75 | **80** |
| newly required | — | `R2 Data-Truth Hardening`, `R3 Ingestion Under Fire`, `R4 Worker Failure Semantics`, `R6 Worker Resource Governance`, `P13 Adversarial Physics Adjudication` |
| contract `contract_version` | 1.18.0 | 1.20.0 |
| `strict` / `enforce_admins` | true / true | unchanged |
| ruleset | `main-merge-queue`, ALLGREEN, min 5 / max 10, `check_response_timeout_minutes: 60` | unchanged |

### 8.6 R7's harness hypotheses, kept separate from the truth gate

Promoting R7 from "reports after a merge" to "must pass before one" surfaced three distinct non-product failures that had been invisible for as long as the lane could only ever report on history. None is the C20 change: no verdict-authority refusal, no `42501` and no reference to `b23_match_verdicts` appears anywhere in the R7 logs, and the failing phases do not write verdicts inline. They are recorded as open harness hypotheses rather than waived, and R7 stays out of the merge boundary until they are closed.

| # | observed | phase | hypothesis | disposition |
|---|---|---|---|---|
| H-R7-01 | `Expected exactly 1 occurrence of marker R2_WINDOW_START::…, found 0`, with 14 statement events parsed and 0 captured in window | R2 | the log was read as `podman logs --since "$LOG_SINCE"`, a second-granular host timestamp taken just before the suite runs. The window is already delimited *inside* the log by the `R2_WINDOW_START`/`R2_WINDOW_END` markers, so `--since` adds no bounding — only a chance of dropping a marker emitted in the same second. The standalone R2 lane reads the whole log for exactly this reason | **repaired**: read the whole log, poll for the window-end marker instead of a fixed two-second sleep. A real race regardless of blocking status |
| H-R7-02 | `HTTP_CONNECTION_ERRORS=2`, `UNIQUE_CANONICAL_ROWS_CREATED=296/300` at `S6_MixedStorm_N1000` | R3 | contention: uvicorn `--workers 4` plus a load generator plus PostgreSQL on one runner, against 1000 requests at concurrency 200 | **open**. Recalibrating workers to `cpu-1` was tried and reverted — the hosted runner reports four cores, so the premise was wrong, and the recalibrated run was strictly worse |
| H-R7-03 | `HTTP_5XX_COUNT=217`, `HTTP_CONNECTION_ERRORS=136`, `UNIQUE_CANONICAL_ROWS_CREATED=262/300` at `S6_MixedStorm_N1000` | R3 | not diagnosed. 217 server-side 500s is a different signature from H-R7-02's connection refusals; the uvicorn log is not captured on this failure path. A pool-exhaustion hypothesis is plausible (`DATABASE_POOL_SIZE=40`, `MAX_OVERFLOW=40` per worker process against a default `max_connections`) but unverified | **open** |

H-R7-02 and H-R7-03 are explicitly *not* classified as flake-and-move-on: they are unresolved harness defects in a lane that still runs after every merge to main, and R7's restore condition is recorded in the contract itself (`p13_merge_blocking_physics.demoted`). What they are not is a reason to let runner nondeterminism sit on the path of a financial-truth gate.

---

## 9. Gate XX2-F — the historical defect class is now unmergeable

PR **#702** (`p13/xx2-gate-r-falsifier`) is Corrective XX with the early-validation DLQ escape restored exactly: `_coerce_event_timestamp` raises before the DLQ-routing block, so a contractually dead-lettered ingestion failure persists no durable consequence.

Observed, **before merge, on the `pull_request` event**:

```
R2 Data-Truth Hardening            = failure
  EG-R2-FIX-5 ORM runtime innocence  = failure
  EG-R2-FIX-6 instrument consistency = failure
  S3_NON_MARKER_ORM_STATEMENTS_COUNT = 0     (scenario S3 produced no database write at all)
  FAILURES=EG-R2-FIX-5=failure;EG-R2-FIX-6=failure

mergeStateStatus                   = BLOCKED
statusCheckRollup                  = FAILURE
isInMergeQueue                     = false
```

The causal signature is exact: S3 is the malformed-payload scenario, and with the defect restored it emits **zero** ORM statements, because the validation error escapes before anything is persisted. R2's ORM runtime-innocence cross-check requires each scenario to have produced a non-marker statement — that is the durable consequence the contract demands, and its absence is precisely the historical defect.

Two independent required lanes caught it, with the identical signature. `R7 Final Winning State` failed at its own embedded *Phase R2: Data-Truth hardening* on the same commit, reporting the same `FAILURES=S3_NON_MARKER_ORM_STATEMENTS_COUNT<1; SUITE_REPORTED:S3_NON_MARKER_ORM_STATEMENTS_COUNT=0`. Detaching either lane alone would not have let this candidate through.

Under the audited topology this candidate would have merged and R2 would have reported afterwards. It is now refused before main. **PR #702 is closed unmerged.**

---

## 10. Migration and database physics (Gate XX2-G)

| | value |
|---|---|
| empty PostgreSQL 15 → `alembic upgrade head` | single head `202609021200` |
| audited predecessor `202609011200` → `alembic upgrade head` | single head `202609021200` |
| public tables, both paths | 120 / 120 |
| privilege graph (120 tables × 8 principals × 4 ops) | **identical** |
| trigger definitions (`pg_get_triggerdef`, relation-qualified) | **158 / 158 identical** |
| guard triggers present in both | `trg_b23_verdict_authorship_insert`, `trg_b23_verdict_authorship_update` |

The migration is additive — no already-applied historical migration was rewritten — and every privilege statement is guarded by `_if_role_exists`, the idiom `202604291200` already uses, because several proof lanes migrate an empty cluster before any runtime role is provisioned. `db/schema/canonical_schema.sql` was regenerated with the pinned `pg_dump` digest and the deterministic drift gate normalizes to identical.

---

## 11. Ontological preservation (Gate XX2-J)

The corrective changes grants, one trigger, one walk predicate, workflow triggers, and governance registries. It does not touch B2.1 replay freeze, integer-cent money, tenant isolation, B2.4 statistical policy, B2.4/B2.13 separation, Trust read-only projection, remote signer custody, or LLM exclusion. The B2.3 proof harnesses were re-pointed from `app_user` to `app_worker` — the principal that owns the truth they author in production — which narrows rather than widens what those proofs assume.

---

## 12. Debt register

| item | class | status |
|---|---|---|
| `attribution_allocations` write-clock walk | BOUNDED HARDENING DEBT | **closed** — experiment executed, decision unchanged in both directions, override added, `NC-C20-02` retains the falsifier |
| HMAC-verified tenant context (inherited) | DEFERRED SCOPE | stashed with reasoning in §1. Not a sibling overgrant under H-XX2-02's definition; would touch 1,392 call sites across 202 files under 75 required contexts |
| R7 R2 log-window capture race (H-R7-01) | HARNESS-QUALITY DEBT | **closed** — reads the whole log and polls for the window-end marker, §8.6 |
| R7 R3 ingestion instability under load (H-R7-02, H-R7-03) | HARNESS-QUALITY DEBT | **open** — R7 is demoted to a post-merge diagnostic until closed; restore condition recorded in `p13_merge_blocking_physics.demoted` |
| merge-queue `check_response_timeout_minutes` = 60 | GOVERNANCE DEBT | left at 60 by explicit decision. No longer load-bearing against R7 (demoted); the slowest required lane is R3 at ~11 min and the adjudicator waits at most 35 |
| `b11-p4-ci-audit-gate` substantive evidence is `main`-only (audit 62, H-AUD-03) | GOVERNANCE DEBT | out of the P13 required-edge list; not addressed by this corrective |
| beat single-process staleness, dual-B23-worker crash (audit 62 H-AUD-04/05) | COVERAGE LIMITATION | not addressed by this corrective |

---

## 12b. Open-world hypotheses (Gate W)

Derived from this session's own investigation, not supplied by the directive. None is a replay of a known historical defect.

- **H-XX2-N01 — the migration principal is a universal verdict author.** `migration_owner` is a member of both `app_user` and `app_worker` (PostgreSQL requires membership to transfer object ownership, and the planner's `SECURITY DEFINER` functions must be owned by `app_worker`). Anything holding the migration DSN can therefore author verdict truth through both layers of the C20 fence. The only thing standing between a leaked migration credential and verdict authorship is the C7 topology gate's assertion that the migration DSN is never issued to an API or worker process. That assertion was not re-exercised here. `UNRESOLVED — NON-BLOCKING` (it is the documented, governed exception, not a new hole).
- **H-XX2-N02 — ownership is the trigger layer's escape hatch.** The guard admits the relation's owner. An ownership transfer of `b23_match_verdicts` to a runtime role, or a membership grant into the migration role, would open the trigger layer silently and leave only the privilege layer. **Closed in this corrective**: `test_c20_no_runtime_principal_inherits_the_relation_owner` asserts no production login can act as the owner.
- **H-XX2-N03 — allocation membership is still API-authored.** `app_user` retains `SELECT, INSERT, UPDATE, DELETE` on `attribution_allocations`, because the attribution recompute worker legitimately runs under it. `verified` / `verification_source` are trigger-derived and re-derived (audit 63 proved the caller value is discarded), but *which channels an event has an allocation for* is authored by the API principal, and B2.4 source membership reads that set. Whether a rogue API path could move a B2.4 decision by rewriting allocation rows without touching `verified` is unexamined. `UNRESOLVED`.
- **H-XX2-N04 — invalidation is a resource surface, not a truth surface.** `app_user` holds `SELECT, INSERT, UPDATE` on `b24_dirty_events` and `b24_feature_authority_build_requests`. Those are pull signals rather than truth, but a principal that can mark arbitrary windows dirty can force unbounded B2.4 replanning. Not a fabrication primitive; a cost primitive. `UNRESOLVED — NON-BLOCKING`.
- **H-XX2-N05 — a timeout is not an admission, but it is pressure.** The merge queue's `check_response_timeout_minutes` is 60 while R7 has historically run 24–76 minutes. A timed-out merge group refuses the candidate, so no unproven code lands — but repeated timeouts create exactly the operational pressure to shrink the required set that produced the historical gap. The §8.5 alignment reduces R7's runtime variance; the timeout itself was left at 60 by explicit decision and is in the debt register.

---

## 13. Coverage limitations

Stated rather than folded into a pass:

- the C19 production-topology journey was executed in the authoritative hosted Linux environment, not re-driven a second time on this workstation. Directive §4.1–4.3 explicitly classify local Windows C19 impedance as non-authoritative and forbid spending context on it;
- the sibling battery covers the relations H-XX2-02 names plus the ones the authority contract declares. It is not a full privilege audit of all 120 tables — §15 forbids turning this corrective into one;
- Population A (authenticated-purchase-only) semantics, the full H-P13-XIX-03 permutation set, and broker-outage induction were not re-driven here; they remain as the audits left them.

---

## 14. Final evidence pack index

| artifact | location |
|---|---|
| local physics capsule (first red, post-fix authority, sibling battery, clock experiment, migration graphs) | `docs/forensics/evidence/b25_p13_c20_local_physics_evidence.json` |
| C19 topology capsule | `docs/forensics/evidence/b25_p13_c19_context_robust_evidence.json` |
| C20 runtime authority capsule (produced by the C19 journey) | `docs/forensics/evidence/b25_p13_c20_runtime_authority_evidence.json` |
| authority migration | `alembic/versions/007_skeldir_foundation/202609021200_b25_p13_c20_verdict_authority_conservation.py` |
| authority proof | `backend/tests/trust/test_b25_p13_c20_runtime_authority.py` |
| temporal-coherence experiment | `backend/tests/trust/test_b25_p13_c9_feature_cardinality_physics.py::test_c20_allocation_write_clock_cannot_change_b24_resource_decision` |
| merge-governance validator + controls | `scripts/ci/validate_p13_merge_governance.py`, `scripts/ci/test_p13_merge_governance_negative_controls.py` |
| adversarial-physics adjudicator + controls | `scripts/ci/adjudicate_p13_adversarial_physics.py`, `scripts/ci/test_p13_adversarial_physics_negative_controls.py`, `.github/workflows/p13-adversarial-physics-adjudication.yml` |
| governance contract | `contracts-internal/governance/b03_phase2_required_status_checks.main.json` |

---

## 15. Engineering status

*(completed in §16 once exact-main truth exists)*
