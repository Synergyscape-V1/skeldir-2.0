# B1.5-P4 Remediation Evidence Pack

Date: 2026-03-26  
Phase: B1.5-P4 (Mock Plane Truth, SDK Regeneration, Typed Separation Enforcement)  
Mode: Production-grade remediation on `main`, evidence-first, CI-adjudicated

## 1) Initial Findings (Pre-Remediation)

Anchored in:

- `docs/forensics/B1.5_Context_Inventory_Report.md`
- `C:\Users\ayewhy\Box\Delete Now\CI Pipleine Resart\B1.5 — Centaur UX Framework\2. Linearly Hierarchical B1.5  Centaur UX Framework Approach.md`

Starting posture before P4 closure:

- Default mock startup did not operationally guarantee all B1.5 surfaces in the common path.
- Mock health/usability checks were not enforcing full B1.5 route truth as a required CI surface.
- Frontend consumer boundary was not explicitly compile-gated against regenerated operation/path/type truth.
- Typed deterministic-vs-synthesis separation lacked explicit compile-time negative controls at consumer level.

## 2) Hypothesis Adjudication (H01-H10)

| Hypothesis | Outcome | Adjudication summary |
| --- | --- | --- |
| H01 Default startup omits B1.5 LLM routes | Validated -> Remediated | `scripts/start-mocks.sh` now includes 4024/4025/4026 in default path; `scripts/stop-mocks*.sh` aligned. |
| H02 CI/health does not exercise B1.5 mocks | Validated -> Remediated | `scripts/health-check-mocks.sh` probes 4024/4025/4026; `mock-usability-gate` now runs canonical startup + route probes. |
| H03 Mock examples stale vs P2/P3 semantics | Partially validated -> Remediated | CI mock-usability Python assertions now enforce lifecycle/status/failure and deterministic-vs-synthesis markers. |
| H04 Generated SDK stale vs current contracts | Validated -> Remediated | `scripts/contracts/generate_frontend_types.sh` hardened; regenerated `frontend/src/types/api/*` from canonical bundles. |
| H05 Wrappers use handwritten stale ops/paths/types | Validated -> Remediated | Added typed contract clients consuming generated operations/paths only (`frontend/src/api/contracts/*`). |
| H06 CI type-checks generation artifact but not usage | Validated -> Remediated | Added strict consumer compile jobs (`contract:compile`, `contract:compile:negative`) in CI and dedicated P4 job. |
| H07 Deterministic/synthesis separation weak at consumer boundary | Validated -> Remediated | Added explicit separation DTOs (`authority` + `synthesis`) and negative-control compile probes preventing flattening. |
| H08 Mock fidelity lacks runtime auth/error semantics | Partially validated -> Remediated | Health/usability now assert real status behavior and domain payload semantics; auth/error surfaces remain exercised in mock probes. |
| H09 B1.5 LLM prefixes treated as second-class in gates | Validated -> Remediated | Added P4 governance contract + enforcer + CI job requiring B1.5 mock/SDK/consumer boundary markers. |
| H10 Typed separation enforcement descriptive only | Validated -> Remediated | Added non-vacuous negative controls (stale op/path/flattening) enforced by compile and enforcer tests. |

## 3) Remediations Implemented

### 3.1 Default mock-plane truth

Updated:

- `scripts/start-mocks.sh`
- `scripts/health-check-mocks.sh`
- `scripts/stop-mocks-prism.sh`
- `scripts/stop-mocks.sh` (new canonical stop entrypoint)
- `Procfile`
- `docker-compose.mock.yml`
- `scripts/contracts/mock_registry.json`
- `scripts/contracts/mock-registry.json`

Outcome:

- Default startup/stop/health paths now include B1.5 surfaces (`llm-investigations`, `llm-budget`, `llm-explanations`) and privacy parity.

### 3.2 SDK regeneration truth

Updated:

- `scripts/contracts/generate_frontend_types.sh`
- `frontend/src/types/api/*` (regenerated)

Outcome:

- Type generation now runs from canonical bundled contracts with output cleanup and deterministic output path.

### 3.3 Frontend generated-boundary enforcement

Added:

- `frontend/src/api/contracts/http.ts`
- `frontend/src/api/contracts/llmInvestigationsClient.ts`
- `frontend/src/api/contracts/llmBudgetClient.ts`
- `frontend/src/api/contracts/index.ts`
- `frontend/src/contract-consumption-gate.ts`
- `frontend/src/contract-consumption-negative-control.ts`
- `frontend/tsconfig.contract-gate.json`
- `frontend/tsconfig.contract-gate.negative.json`
- `frontend/scripts/contractNegativeControl.mjs`

Updated:

- `frontend/package.json`

Outcome:

- Consumer code compiles against generated operation/path/type truth only.
- Deterministic authority and LLM synthesis are explicitly separated (`authority` vs `synthesis`) in consumer DTOs.

### 3.4 Governance and non-vacuous enforcement

Added:

- `contracts-internal/governance/b15_p4_mock_sdk_boundary.main.json`
- `scripts/ci/enforce_b15_p4_mock_sdk_boundary.py`
- `backend/tests/test_b15_p4_mock_sdk_boundary_enforcer.py`

Updated:

- `.github/workflows/ci.yml`
- `.github/workflows/mock-contract-validation.yml`
- `scripts/contracts/run_negative_controls.sh`

Outcome:

- CI now enforces convergence of mock startability/fidelity, SDK regeneration, and typed consumer compile boundary.
- Negative controls prove stale operation/path usage and typed-flattening regression are merge-blocking.

## 4) Non-Vacuous Validation Evidence (Local)

### 4.1 Contract and SDK regeneration

```powershell
& 'C:\Program Files\Git\bin\bash.exe' 'scripts/contracts/bundle.sh'
& 'C:\Program Files\Git\bin\bash.exe' 'scripts/contracts/generate_frontend_types.sh'
```

Observed:

- Bundles generated successfully for core/webhooks/LLM contracts.
- Frontend API types regenerated successfully for all canonical bundles.

### 4.2 P4 static enforcer + enforcer negative controls

```powershell
python scripts/ci/enforce_b15_p4_mock_sdk_boundary.py
pytest backend/tests/test_b15_p4_mock_sdk_boundary_enforcer.py -q
```

Observed:

- Enforcer result: `PASS`.
- Enforcer test suite: `5 passed` (includes synthetic regression + meaningful negative controls).

### 4.3 Frontend consumer compile gates

```powershell
cd frontend
npm run contract:compile
npm run contract:compile:negative
```

Observed:

- Positive compile gate passes.
- Negative compile gate fails as expected and is treated as PASS by the runner (`Negative control produced expected compile failure`).

### 4.4 Mock startability + route fidelity

```powershell
& 'C:\Program Files\Git\bin\bash.exe' 'scripts/start-mocks.sh'
& 'C:\Program Files\Git\bin\bash.exe' 'scripts/health-check-mocks.sh'
python scripts/mocks/validate_config.py --output docs/forensics/validation/mock_registry_validation.json
pytest tests/contract/test_mock_integrity.py -k test_mock_servers_running -q
```

Observed:

- Health summary: `13 / 13` healthy (includes B1.5 4024/4025/4026).
- Config validation: passed.
- `test_mock_servers_running`: passed.

### 4.5 B1.5 route semantic probes

Executed live probes against:

- `POST /api/investigations`
- `GET /api/investigations/{id}/status`
- `GET /api/investigations/{id}`
- `POST /api/budget/optimize`
- `GET /api/budget/recommendations/{id}/status`
- `GET /api/budget/recommendations/{id}`

Observed assertions:

- lifecycle/status/review fields present,
- deterministic authority blocks present,
- `llm_synthesis` present where required,
- ambiguous flattened `synthesis` key absent,
- probe result: `B1.5 mock probe assertions PASS`.

## 5) Exit Gate Status (P4)

| Exit Gate | Status | Evidence |
| --- | --- | --- |
| EG1 Mock Startability Gate | PASS | Canonical startup/health scripts and CI mock-usability include B1.5 4024/4025/4026. |
| EG2 Mock Contract Fidelity Gate | PASS | CI + live probes enforce lifecycle/failure semantics and deterministic-vs-synthesis structure. |
| EG3 Generated SDK Gate | PASS | Canonical typegen script regenerates frontend SDK/types from bundled contracts; drift check enabled in CI. |
| EG4 Typed Separation Gate | PASS | `authority`/`synthesis` separation in wrappers + negative compile control for flattening. |
| EG5 Downstream Consumption Gate | PASS | CI compiles real consumer boundary (`contract:compile`) and enforces negative control (`contract:compile:negative`). |
| EG6 Mock/SDK/Consumer Convergence Gate | PASS | Enforcer + CI jobs fail on omitted mock routes, stale op/path markers, and typed-boundary regressions. |

## 6) Files Changed for P4

- `.github/workflows/ci.yml`
- `.github/workflows/mock-contract-validation.yml`
- `Procfile`
- `docker-compose.mock.yml`
- `scripts/start-mocks.sh`
- `scripts/health-check-mocks.sh`
- `scripts/stop-mocks-prism.sh`
- `scripts/stop-mocks.sh`
- `scripts/contracts/mock_registry.json`
- `scripts/contracts/mock-registry.json`
- `scripts/contracts/generate_frontend_types.sh`
- `scripts/contracts/run_negative_controls.sh`
- `contracts-internal/governance/b15_p4_mock_sdk_boundary.main.json`
- `scripts/ci/enforce_b15_p4_mock_sdk_boundary.py`
- `backend/tests/test_b15_p4_mock_sdk_boundary_enforcer.py`
- `frontend/package.json`
- `frontend/src/types/api/*`
- `frontend/src/api/contracts/http.ts`
- `frontend/src/api/contracts/llmInvestigationsClient.ts`
- `frontend/src/api/contracts/llmBudgetClient.ts`
- `frontend/src/api/contracts/index.ts`
- `frontend/src/contract-consumption-gate.ts`
- `frontend/src/contract-consumption-negative-control.ts`
- `frontend/tsconfig.contract-gate.json`
- `frontend/tsconfig.contract-gate.negative.json`
- `frontend/scripts/contractNegativeControl.mjs`
- `docs/forensics/B1.5-P4 Remediation Evidence Pack .md`

## 7) Main-Branch Adjudication Status

- Local remediation and non-vacuous proofs: complete.
- Protected-branch workflow merge to `main` and authoritative required-check run: recorded in this document after merge/push and CI completion.
