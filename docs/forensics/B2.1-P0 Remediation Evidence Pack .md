# Phase B2.1-P0 Remediation Evidence Pack

Generated: 2026-04-12  
Primary branch authority: `main`  
Corrective-cycle investigation baseline commit: `193cfd4d04339c71125a7aa5ac4aa070a788cb1f`  
Directive: **Phase B2.1-P0 - Runtime Authority Convergence + Contract Surface Lock (Corrective Closeout)**

## 1. Corrective-Cycle Intent (System Physics + Information Flows)

This closeout cycle is scoped to remove the last two residual authority gaps left after prior P0 remediation:

1. Migration authority closure must be machine-enforced and least-privilege real.
2. Tenant isolation closure must be proven at the Postgres boundary on:
   - mounted channels endpoint, and
   - worker/recompute path touching restored B1.4 substrate.

The target information flow is:

`formal migration authority gate -> runtime principal boundary -> mounted runtime behavior -> Postgres RLS/GUC negative controls -> CI merge blocking`.

## 2. Hypotheses and Forensic Adjudication

### H01/H02 - Migration authority only conditionally real
- **Validated** before corrective patch: authority lock existed, but closeout proofs were not yet merge-blocking in CI.
- **Closure action**: added a dedicated CI runtime closeout job that runs schema setup/migrations via `migration_owner`, then enforces runtime deny on `public.alembic_version`.

### H03 - DSN/driver convergence may rely on local luck
- **Validated defect**: runtime identity parity fixture attempted sync `create_engine()` against `postgresql+asyncpg://...`, causing `sqlalchemy.exc.MissingGreenlet`.
- **Closure action**: normalized DSNs to sync scheme inside the parity fixture before sync engine creation.

### H04 - Channels route lacked Postgres-boundary negative proof
- **Validated as open pre-closeout**: no dedicated merge-blocking runtime negative-control test for `/api/attribution/channels`.
- **Closure action**: closeout runtime test includes same-tenant positive and cross-tenant negative controls plus RLS policy verification anchored to `current_setting('app.current_tenant_id', true)`.

### H05/H06 - Worker substrate tenant safety unproven at DB boundary
- **Validated as open pre-closeout**: worker path lacked dedicated merge-blocking cross-tenant negative control on restored substrate tables.
- **Closure action**: closeout runtime test now proves worker recompute positive path and fails cross-tenant misuse (`session locality violation`) with DB-boundary cross-tenant zero-visibility checks.

### H07 - CI does not adjudicate remaining defect classes
- **Validated** before patch: enforcer checked contract authority chain but did not require runtime closeout proof wiring/tokens.
- **Closure action**: expanded class-level enforcer to fail on missing workflow/runtime-proof closeout artifacts and required tokens.

### H08 - Corrective work could regress accepted P0 surfaces
- **Refuted** in this cycle: canonical attribution authority lock and channels contract surface were preserved; no queue topology redesign introduced.

## 3. Remediations Implemented

## R01 - CI merge-blocking closeout job for migration + tenant authority
File: `.github/workflows/ci.yml`

Added job `b21-p0-runtime-authority-closeout` with:
- dedicated Postgres service,
- runtime DSN set to `postgresql+asyncpg://app_user:app_user@127.0.0.1:5432/skeldir_b21_p0`,
- migration DSN set to `postgresql://migration_owner:migration_owner@127.0.0.1:5432/skeldir_b21_p0`,
- explicit CI parity enforcement:
  - `ENFORCE_RUNTIME_IDENTITY_PARITY: "1"`
  - `EXPECTED_RUNTIME_DB_USER: app_user`
- migration execution under migration authority (`alembic upgrade head` with migration-owner DSN),
- explicit metadata least-privilege boundary:
  - `REVOKE ALL ON TABLE public.alembic_version FROM app_user`
- merge-blocking runtime proof execution:
  - `pytest backend/tests/integration/test_b21_p0_runtime_authority_closeout.py -q`

## R02 - Governance hardening for remaining defect classes
File: `scripts/ci/enforce_b21_p0_authority_convergence.py`

Extended enforcement to fail-close when:
- runtime closeout proof file is missing,
- CI workflow lacks required B2.1 closeout runtime step/tokens,
- migration authority/identity parity boundary tokens are absent,
- runtime proof file does not contain required migration/channels/worker closeout proof markers.

## R03 - Enforcer negative controls upgraded
File: `backend/tests/test_b21_p0_authority_convergence_enforcer.py`

Added negative controls for:
- missing runtime closeout workflow step token,
- missing runtime proof token.

Also corrected baseline test to pass the new enforcer parameters.

## R04 - Runtime identity parity fixture corrected for async runtime DSN
File: `backend/tests/conftest.py`

Added `_to_sync_postgres_dsn()` and applied it in the parity fixture before creating sync SQLAlchemy engines.  
This fixes deterministic execution of CI identity checks when runtime DSN is `postgresql+asyncpg://...`.

## R05 - Runtime closeout proof suite added
File: `backend/tests/integration/test_b21_p0_runtime_authority_closeout.py`

Added merge-blocking proofs:
- `test_b21_p0_migration_authority_is_privileged_and_runtime_fails_closed`
- `test_b21_p0_channels_route_is_tenant_safe_with_cross_tenant_negative_control`
- `test_b21_p0_worker_substrate_path_is_tenant_safe_with_cross_tenant_negative_control`

These cover:
- migration-owner read authority on `alembic_version`,
- runtime principal deny on `alembic_version`,
- channels route positive/negative tenant controls,
- worker recompute tenant-boundary negative control,
- Postgres-level RLS/GUC boundary assertions on restored substrate tables.

## 4. Validation Evidence (Executed)

## Authority enforcer
- Command: `python scripts/ci/enforce_b21_p0_authority_convergence.py`
- Result: `result=PASS`

## Enforcer negative controls
- Command: `PYTHONPATH=.;backend pytest backend/tests/test_b21_p0_authority_convergence_enforcer.py -q`
- Result: `4 passed`

## Runtime closeout proofs (local CI-equivalent environment)
- Runtime DB: `skeldir_b21_p0`
- Runtime principal: `app_user`
- Migration principal: `migration_owner`
- Command sequence:
  - create roles/database and grants,
  - `alembic upgrade head` as `migration_owner`,
  - `REVOKE ALL ON TABLE public.alembic_version FROM app_user`,
  - `pytest backend/tests/integration/test_b21_p0_runtime_authority_closeout.py -q`
- Result: `3 passed`

## 5. Exit Gate Adjudication (Corrective Cycle)

1. Exit Gate 1 - Migration Authority Model Closure: **PASS** (formal privileged migration authority encoded + runtime deny check)
2. Exit Gate 2 - DSN/Driver Durability Closure: **PASS** (async runtime + sync migration paths encoded; parity fixture corrected for async DSN)
3. Exit Gate 3 - Channels Route Postgres-Boundary Tenant Isolation Closure: **PASS** (positive + negative controls)
4. Exit Gate 4 - Worker/Substrate Postgres-Boundary Tenant Isolation Closure: **PASS** (worker path negative control + DB-boundary checks)
5. Exit Gate 5 - Governance Closure for Remaining Defect Classes: **PASS** (enforcer now fails on missing closeout governance surfaces)
6. Exit Gate 6 - Non-Regression of Accepted P0 Surfaces: **PASS** (no canonical-root/channel-contract rollback; no topology redesign)

## 6. Changed Files in This Corrective Iteration

- `.github/workflows/ci.yml`
- `scripts/ci/enforce_b21_p0_authority_convergence.py`
- `backend/tests/test_b21_p0_authority_convergence_enforcer.py`
- `backend/tests/integration/test_b21_p0_runtime_authority_closeout.py`
- `backend/tests/conftest.py`
- `docs/forensics/B2.1-P0 Remediation Evidence Pack .md`

## 7. Protected-Branch Landing + Main CI Closure

Pending in this local evidence snapshot until merge execution:
- feature branch + PR for this corrective iteration,
- protected merge to `main`,
- one authoritative post-merge green `main` CI run ID and URL.

This section is updated immediately after protected-branch merge and CI adjudication complete.
