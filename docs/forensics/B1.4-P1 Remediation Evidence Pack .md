# B1.4-P1 Remediation Evidence Pack

Date: 2026-03-18 (updated)  
Scope: B1.4-P1 - Ingress Contract Sanitization + Universal PII Boundary Enforcement

## 1. Hypothesis Ledger (Validated/Refuted)

- H01 non-universal ingress enforcement: **Validated (pre-fix), remediated**.
  - Evidence: webhook-only middleware scope and unsanitized service/DLQ/quarantine writes were present.
  - Remediation: shared write-boundary enforcement added in ingestion service + DLQ + quarantine.
- H02 contract/runtime drift allowed direct PII persistence: **Validated (pre-fix), remediated**.
  - Evidence: `raw_payload` assignment paths accepted direct fields.
  - Remediation: boundary sanitizer now strips canonical payloads and redacts exception payloads before write.
- H03 webhook-only stripping insufficient: **Validated (pre-fix), remediated**.
  - Remediation: webhook handlers now pass identity payload + headers into universal boundary logic.
- H04 DLQ/quarantine bypass risk: **Validated (pre-fix), remediated**.
  - Remediation: redaction mode enforced for DLQ/quarantine payload writes.
- H05 OpenAPI/generated drift implied direct PII legitimacy for internal storage: **Partially validated**.
  - External webhook contracts still accept provider PII (intended).
  - Internal contract surfaces now covered by semantic scanner (`enforce_b14_p1_ingress_privacy.py`).
- H06 no merge-blocking drift prevention: **Validated (pre-fix), remediated**.
  - Remediation: new CI enforcer + negative-control tests + dedicated CI job + required-check contract entry.
- H07 possible hidden ingress surfaces beyond initial inventory: **Partially validated**.
  - Additional paths confirmed/covered: ingestion transaction wrapper, DLQ retry, unresolved-tenant quarantine path.
- H08 local/CLI network disconnection interrupted adjudication flow: **Refuted as root cause**.
  - Evidence: GitHub run metadata for PR #209 shows contiguous `createdAt`/`updatedAt` execution and completed jobs.
  - Adjudication: interruption was local watch-session loss, not CI/network execution failure.

## 2. Root Cause Adjudication

- R01 localized middleware instead of universal invariant: **Confirmed**.
- R02 contract-first identity semantics persisted: **Confirmed (internal drift risk)**.
- R03 error paths prioritized observability over privacy rebinding: **Confirmed**.
- R04 missing semantic-enforcement lane: **Confirmed**.
- R05 P0 taxonomy existed but P1 mapping incomplete: **Confirmed**.

## 3. Files Changed

- `.github/workflows/ci.yml`
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- `alembic/versions/007_skeldir_foundation/202603161130_b14_p1_dead_events_redaction_guardrail.py`
- `db/schema/canonical_schema.sql`
- `backend/app/ingestion/privacy_boundary.py`
- `backend/app/ingestion/event_service.py`
- `backend/app/ingestion/dlq_handler.py`
- `backend/app/api/webhooks.py`
- `backend/app/models/attribution_event.py`
- `backend/app/models/dead_event.py`
- `scripts/ci/enforce_b14_p1_ingress_privacy.py`
- `backend/tests/test_b14_p1_ingress_privacy_enforcer.py`
- `backend/tests/integration/test_b14_p1_ingress_privacy_runtime.py`
- `backend/tests/integration/test_b057_p3_webhook_ingestion_unblocking.py`
- `backend/tests/test_b044_dlq_handler.py`

## 4. Internal vs External Contract Surfaces

- External webhook contracts remain tolerant of provider payloads containing PII.
- Internal storage contract behavior is enforced at runtime boundary and drift-checked by CI scanner.
- Required-check contract now includes `B1.4 P1 Ingress Contract Sanitization`.

## 5. Ingress Sequence Implementation

Implemented sequence at shared ingress boundary:
1. Receive payload (no 400/422 for provider PII by key presence alone).
2. Compute global hash from identity payload (`global_idempotency_hash`).
3. Derive daily-peppered pseudonymous `session_id` from identifiers + header entropy.
4. Strip (canonical) or redact (DLQ/quarantine) banned direct PII.
5. Write sanitized/redacted payload only.

## 6. Exception Path Hardening

- DLQ writes now use redaction mode with `[REDACTED_B1.4]` values.
- Unresolved-tenant quarantine writes now use redaction mode and preserve payload structure.
- Trigger semantics migration added so dead_events can store redacted keys while still blocking unredacted values.

## 7. CI and Semantic Enforcers

Added:
- `scripts/ci/enforce_b14_p1_ingress_privacy.py`
- CI job: `B1.4 P1 Ingress Contract Sanitization`
- Required context contract entry in `b03_phase2_required_status_checks.main.json`

Negative controls:
- synthetic regression mode (`--simulate-regression`)
- contract regression fixture injection (`--additional-contract-file`)

## 8. DB-Backed Proof Harnesses Used

- `backend/tests/integration/test_b14_p1_ingress_privacy_runtime.py`
  - canonical ingress: seeded email/IP/name not persisted in `attribution_events.raw_payload`
  - DLQ path: seeded identifiers redacted to `[REDACTED_B1.4]` with structure preserved
  - quarantine path: seeded identifiers redacted with structure preserved
- `backend/tests/test_b14_p1_ingress_privacy_enforcer.py`
  - non-vacuous negative controls for drift scanner

## 9. Commands Executed and Outcomes

- `python scripts/ci/enforce_b14_p1_ingress_privacy.py` -> PASS
- `pytest backend/tests/test_b14_p1_ingress_privacy_enforcer.py -q` -> 3 passed
- `pytest backend/tests/integration/test_b14_p1_ingress_privacy_runtime.py -q` -> 3 passed (DB-backed)

## 10. Constraints / Residual Gaps

- Local `alembic upgrade head` in this checkout fails due missing historical revisions (`Can't locate revision identified by '202602282000'` and other missing chain refs), so full migration-chain verification from scratch was not possible in this environment.
- Full authoritative merge-to-main, protected-branch merge-path validation, and complete green main CI run were **not** completed from this workspace session.

## 11. Direct Status Statement

B1.4-P1 remediation code, CI enforcers, and DB-backed proof harnesses are implemented in this workspace and pass the targeted proofs listed above.

`B1.4-P1 passing on main`: **Not yet validated in this session** (merge + protected-branch checks + full main CI still pending authoritative execution).

## 12. Continuation Adjudication (2026-03-18)

- Breakpoint identified: PR #209 was blocked by required check `Phase 8 Regression Gate (Full Physics)`.
- Scientific finding: failing verdict was deterministic in `S4_PIIStorm_N50`, not transport/network-related.
  - Failure payload showed:
    - `CANONICAL_ROWS_CREATED=50`
    - `DLQ_ROWS_CREATED=0`
    - `PII_KEY_HIT_COUNT_IN_DB=0`
    - `passed=false`
- Root cause: Phase-8 scenario logic in `scripts/r3/ingestion_under_fire.py` still enforced legacy expectation `canonical==0 && dlq==n` for PII storm.
- Remediation landed:
  - Updated `scenario_s4_pii_storm` pass condition to `canonical==n && dlq==0` while retaining zero-PII-hit requirement.
- Secondary root cause (revealed by rerun): EG3.4 performance-profile gate still modeled PII as DLQ traffic.
  - Legacy expectations in `scenario_s8_perf_gate` required `pii_dlq_rows_created == len(pii_keys)` and excluded PII keys from expected canonical totals.
- Secondary remediation landed:
  - Updated EG3.4 expectations to require canonical acceptance of PII workload after sanitization:
    - `pii_canonical_rows_created == len(pii_keys)`
    - `pii_dlq_rows_created == 0`
    - `expected_canonical_rows_for_all_profile_keys = 1 + unique + pii`
    - `expected_dlq_rows_for_all_profile_keys = malformed only`
- Local verification completed:
  - `python scripts/ci/enforce_b14_p1_ingress_privacy.py` -> PASS
  - `python scripts/ci/enforce_b14_p0_privacy_authority.py` -> PASS
  - `python scripts/ci/enforce_forensics_index.py` -> PASS
  - `pytest backend/tests/integration/test_b14_p1_ingress_privacy_runtime.py::test_b14_p1_canonical_ingress_strips_pii_before_write -q` -> PASS
- Limitation in this workspace:
  - Full local `scripts/phase8/run_phase8_closure_pack.py --full-physics` could not be executed due unavailable local Docker engine; authoritative validation delegated to PR CI.
