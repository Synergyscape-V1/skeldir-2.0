# B2.2-P5 Remediation Evidence Pack

Generated: 2026-04-22  
Primary branch authority target: `main`  
Directive cycle: **B2.2-P5 - Performance Semantics Lock + Negative-Control Completion**  
Investigated from commit: `e0899f6de7af123f6d69c1841f63475f27b6808b` (`origin/main` at start)

## 1. Scope

This remediation cycle is constrained to P5 closure:
- credible composed-path webhook latency proof for mounted B2.2 ingress routes,
- anti-cheat closure so benchmark surfaces cannot silently bypass auth/tenant/persistence/orchestration,
- explicit route-owned provider-retry-safe unsupported-event-family semantics on mounted routes,
- Stripe canonical/alias parity expansion across success/failure/unsupported-family classes,
- merge-blocking CI wiring that separates structural anti-cheat checks from hard latency adjudication.

Out of scope:
- reopening P0-P4 architecture decisions except where needed for non-regression compatibility,
- B2.3 reconciliation or matching behavior,
- non-webhook attribution strategy changes.

## 2. Initial Findings (validated at start)

1. No B2.2-P5 governance contract/enforcer/CI lane existed on `main`.
2. No dedicated webhook benchmark/adjudication surface existed for mounted ingress hot-path p95 proof.
3. Unsupported-event-family handling remained mostly framework-route-fallthrough (`404`) rather than explicit route-owned semantics on mounted routes.
4. Stripe canonical/alias parity proofs existed for core ACK/auth/malformed classes (P4) but not explicit unsupported-family parity + benchmark parity adjudication.
5. No anti-cheat lock existed for benchmark hollowing (mock/patch/stub bypass of critical path).

## 3. Remediations Implemented

### R01 - Route-owned unsupported-event-family semantics (provider-safe ACK)

Files:
- `backend/app/api/webhooks.py`

Changes:
- Added explicit provider-aware unsupported-family detection:
  - Shopify via `X-Shopify-Topic`,
  - WooCommerce via `X-WC-Webhook-Topic` and status semantics,
  - Stripe via payload `type` / `event_type` / status hints,
  - PayPal via payload `event_type`.
- Added route-owned 200 ACK response shape:
  - `status: unsupported_event_family_ignored`
  - `error: unsupported_event_family`
  - deterministic idempotency correlation token for observability.
- Preserved malformed authenticated payload path (`dlq_routed`) and auth/tenant failure precedence.

### R02 - Deterministic PayPal benchmark cert override (testing-only, no monkeypatch)

Files:
- `backend/app/webhooks/signatures.py`

Changes:
- Added testing-only cert override path in `_fetch_paypal_certificate_pem`:
  - `SKELDIR_PAYPAL_TEST_CERT_URL`
  - `SKELDIR_PAYPAL_TEST_CERT_PEM`
  - active only when `TESTING=1`.
- Added test-override-aware validation plumbing so benchmark auth remains deterministic in hermetic CI:
  - `_paypal_testing_override_matches(...)`
  - testing override acceptance in `_validate_paypal_cert_url(...)`
  - DNS-token intersection bypass only when testing override is active in `_resolve_paypal_public_key(...)`.
- Preserves production verification semantics while removing CI flakiness caused by public-DNS dependency during benchmark integrity probes.

### R03 - Composed-path webhook benchmark harness (real mounted path)

Files:
- `scripts/benchmarks/b22_p5_webhook_ingress_benchmark.py`

Coverage:
- Hits mounted routes:
  - `/api/webhooks/shopify/order_create`
  - `/api/webhooks/stripe/payment_intent_succeeded`
  - `/api/webhooks/stripe/payment_intent/succeeded`
  - `/api/webhooks/paypal/sale_completed`
  - `/api/webhooks/woocommerce/order_completed`
- Measures `mounted_http_request_to_ack_response` latency with p95 output.
- Verifies runtime component integrity (signature verifier modules, tenant resolution module, ingestion transaction module).
- Includes integrity probes for:
  - auth-failure (`401`) negative control,
  - duplicate replay stability,
  - mounted-route unsupported-event-family semantics across providers.
- Emits persistence/minimization evidence counters (`attribution_events`, `webhook_ingress_identities`, `raw_event_payloads`, latest minimized raw payload flags).

### R04 - Benchmark adjudication enforcer

Files:
- `scripts/ci/enforce_b22_p5_webhook_benchmark_adjudication.py`
- `backend/tests/test_b22_p5_webhook_benchmark_adjudication_enforcer.py`

Enforced:
- schema/version/timing-boundary integrity,
- anti-cheat integrity flags (including `task_always_eager == false`),
- mounted-route unsupported-family semantics present in integrity probe output,
- hard `p95 <= 500ms`,
- Stripe canonical/alias p95 delta bound.

### R05 - Structural P5 lock enforcer + governance contract

Files:
- `contracts-internal/governance/b22_p5_performance_negative_control_lock.main.json`
- `scripts/ci/enforce_b22_p5_performance_negative_control_lock.py`
- `backend/tests/test_b22_p5_performance_negative_control_lock_enforcer.py`

Enforced:
- mounted-route scope closure,
- required negative-control class matrix,
- route-owned unsupported-family contract,
- benchmark anti-cheat token bans,
- CI wiring integrity for structural and latency adjudication surfaces.

### R06 - Runtime negative-control and parity proofs

Files:
- `backend/tests/test_b22_p5_performance_negative_controls.py`

Added runtime proofs:
- `test_b22_p5_negative_control_matrix_includes_route_owned_provider_safe_unsupported_family_semantics`
- `test_b22_p5_stripe_alias_and_canonical_routes_have_equivalent_success_failure_and_unsupported_family_semantics`

Coverage includes:
- forged signature,
- missing tenant key,
- wrong tenant key,
- malformed payload,
- oversized payload,
- duplicate replay,
- unsupported event family (explicit mounted-route semantics for all providers),
- Stripe canonical/alias parity for success + failure + unsupported-family classes.

### R07 - Merge-blocking CI split model (structural vs authoritative latency)

Files:
- `.github/workflows/ci.yml`

Changes:
- Added dedicated latency adjudication job:
  - `B2.2-P5 Webhook Latency Adjudication`
  - runs benchmark integrity + measure + adjudicator.
- Made required `Contract Semantic Drift Gate` depend on that latency job (`needs`), preserving merge-blocking behavior through existing protected required context.
- Added P5 structural enforcer + runtime/adjudication negative-control tests to `Contract Semantic Drift Gate`.

### R08 - B2.1-P4 enforcer compatibility hardening (non-regression unlock)

Files:
- `scripts/ci/enforce_b21_p4_queue_isolation_semantics_lock.py`
- `backend/tests/test_b21_p4_queue_isolation_semantics_lock_enforcer.py`

Changes:
- Replaced brittle exact-string `needs` matching with YAML-structural validation for `contract-semantic-drift-gate` dependency requirements.
- Preserved mandatory B2.1-P4 dependency guarantees while allowing additive dependency expansion (required by B2.2-P5 latency adjudication integration).
- Added a dedicated negative-control test to fail when the contract gate loses required B2.1-P4 dependency linkage.

## 4. Falsifiable Local Validation

Executed locally:

1. `python scripts/ci/enforce_b22_p5_performance_negative_control_lock.py` -> PASS  
2. `pytest backend/tests/test_b22_p5_performance_negative_control_lock_enforcer.py backend/tests/test_b22_p5_webhook_benchmark_adjudication_enforcer.py -q` -> PASS (10 passed)  
3. `python scripts/ci/enforce_b22_p1_authenticity_semantics_lock.py` -> PASS (non-regression)  
4. `python scripts/ci/enforce_b22_p4_idempotent_ack_orchestration.py` -> PASS (non-regression)  
5. `pytest backend/tests/test_b22_p5_performance_negative_controls.py -q` -> skipped locally (authoritative Postgres runtime-proof boundary not provisioned in local run).

## 5. Exit Gate Adjudication (post-remediation design state)

1. Exit Gate 1 - Credible Latency Proof Gate: **Implemented** (dedicated benchmark + hard adjudicator).  
2. Exit Gate 2 - Anti-Cheat Composition Gate: **Implemented** (runtime component integrity + structural anti-cheat enforcer + negative controls).  
3. Exit Gate 3 - Negative-Control Completeness Gate: **Implemented** (runtime test matrix and contract lock).  
4. Exit Gate 4 - Route-Variant Consistency Gate: **Implemented** (Stripe canonical/alias parity runtime + benchmark adjudicator delta check).  
5. Exit Gate 5 - Non-Regression + Merge-Blocking Gate: **Implemented in CI wiring** (P5 surfaces wired; P1/P4 enforcers pass locally).

## 6. Files Changed in This Remediation Cycle

- `.github/workflows/ci.yml`
- `backend/app/api/webhooks.py`
- `backend/app/webhooks/signatures.py`
- `contracts-internal/governance/b22_p5_performance_negative_control_lock.main.json`
- `scripts/benchmarks/b22_p5_webhook_ingress_benchmark.py`
- `scripts/ci/enforce_b22_p5_performance_negative_control_lock.py`
- `scripts/ci/enforce_b22_p5_webhook_benchmark_adjudication.py`
- `backend/tests/test_b22_p5_performance_negative_controls.py`
- `backend/tests/test_b22_p5_performance_negative_control_lock_enforcer.py`
- `backend/tests/test_b22_p5_webhook_benchmark_adjudication_enforcer.py`
- `docs/forensics/B2.2-P5 Remediation Evidence Pack .md`

## 7. Protected-Branch Mainline Landing Evidence

Status: **COMPLETE**

- PR: `#373` merged via protected branch workflow  
  URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/373`
- Merge commit on `main`: `f2135d5fd520c61206434c8f6c6b7f21e0e591bb`
- Final pre-merge head run (PR head `1b3512514ed1badc3a2fadfab24b8585e3fa84f1`):
  - `CI` run `24801217544` -> `completed/success`
  - `Contract Semantic Drift Gate` -> `success`
  - `B2.2-P5 Webhook Latency Adjudication` -> `success`
- Post-merge `main` CI validation (single full green run):
  - `CI` run `24801773504` (head `f2135d5fd520c61206434c8f6c6b7f21e0e591bb`) -> `completed/success`
  - includes `Contract Semantic Drift Gate` -> `success`
  - includes `B2.2-P5 Webhook Latency Adjudication` -> `success`
