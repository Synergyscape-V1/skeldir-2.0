# B2.5-P13 Corrective Action XII — Remediation Evidence

**Directive**: B2.5-P13 Context-Robust Follow-Up Corrective Action XII —
authority closure, producing-regime correspondence, and merge-governing proof
**Context**: Independent Audit Reports 50 (Nicholas) and 51 (Trey)
**Implementation branch**: `codex/b25-p13-c12-authority-closure`
**Protected main at investigation start**:
`e570815a86ddec2c8c8b188a5fa882583f13b527`
**Final protected-main SHA**: pending protected merge
**Pull request**: pending protected merge

## 1. Evidence authority and starting state

The starting state was reconstructed independently before changing it:

| Coordinate | Observed value |
|---|---|
| Primary branch | `main` |
| Protection | classic branch protection: required PR, strict required checks, administrators enforced; active `main-merge-queue` ruleset |
| Required P13 context | `B2.5-P13 E2E Trust Closure` |
| Independently useful but not separately required contexts | `B2.5-P13 C9 Positive Confidence Composition`; `B2.5-P13 C10 Executable Artifact Topology` |
| Starting Alembic head | `202608251200` |
| Directive SHA-256 | `B868D689DF3A69824029D15B1A046DBF9FCFF979922D70BE5933D4DE0B412E93` |
| Audit 50 SHA-256 | `095505DAAB052AD5B882E47CF0C505B349183726691B521DFDB964A96079090F` |
| Audit 51 SHA-256 | `C9E3BBA06ADA1C25675CF6E790F7C21E3B0D0D8DA2D3B1E0C8CC397525005BC2` |

Audit 50's positive/degraded runtime results and C11's semantic, publisher,
lineage, runtime, signing, and artifact properties were treated as preservation
requirements, not as substitutes for re-testing the new authority defect.

## 2. Pre-remediation falsifier and root cause

Two isolated PostgreSQL 15 databases were migrated from the exact starting
main and the candidate branch. Against the starting database, ordinary
principals manufactured global authority using transaction-local custom GUCs:

```text
app_worker, tenant A, app.b24_recovery_reconciler=on
  foreign dispatch SELECT -> 2 visible foreign-capable rows
  foreign dispatch UPDATE -> UPDATE 1

app_worker, tenant A, app.b24_dispatch_claim_access=on
  foreign dispatch SELECT -> 2 visible foreign-capable rows
  foreign dispatch UPDATE -> UPDATE 1

app_worker, tenant A, app.b24_fit_resolution_id=<tenant-B-fit>
  foreign fit SELECT -> 1

app_user
  direct SELECT and INSERT grants on b24_worker_process_authority -> present
```

The effective cause was compositional. PostgreSQL combines permissive RLS
policies with OR. A correct tenant predicate therefore did not constrain a
sibling policy whose complete predicate was a caller-writable setting. The GUC
was context syntactically and authority semantically; no independently
authenticated principal or unforgeable capability stood between the two.

The class inventory found seven consequence-bearing privileged-GUC policies:

1. dispatch claim-capability `SELECT`;
2. dispatch claim-capability `UPDATE`;
3. dispatch claim-function global access;
4. recovery dispatch `SELECT`;
5. recovery dispatch `UPDATE`;
6. recovery-outbox `ALL`;
7. worker-process-authority function access.

The fit parent and all 16 fit partitions separately OR'd tenant identity with
knowledge of `app.b24_fit_resolution_id`. Dormant claim-digest columns reduced
current exploitability of two policies but did not make the authority design
valid.

## 3. Remediation: authority is authenticated and bounded

Revision `202608261200` removes every privileged-GUC policy from the effective
schema and replaces the behavior with three distinct mechanisms:

- fit rows are tenant-only at the parent and every partition; resolution SQL
  binds both `tenant_id` and `fit_id`;
- global publisher access remains tied to the independent
  `app_dispatch_publisher` login established by C11;
- worker-global operations execute only inside narrowly granted
  `SECURITY DEFINER` state-machine functions. Their internal RLS policies require
  both `current_user = migration_owner` and `session_user = app_worker`, a
  conjunction an ordinary direct SQL session cannot manufacture.

The new recovery functions only lease bounded due rows, mark one identified
publishing row successful, or mark one identified publishing row failed. They
validate row identity and state, cap batch/timeout/attempt/error inputs, have a
fixed search path, revoke `PUBLIC`, and grant execution only to `app_worker`.
The successful recovery transition updates the recovery and dispatch rows in
one database transaction; the prior application implementation performed two
separate statements.

All direct table privileges on `b24_worker_process_authority` are revoked from
ordinary roles. Existing register/query behavior remains reachable only through
the already bounded process-authority functions.

## 4. Effective authority graph after remediation

| Principal / source | Operation | Tenant scope | Why PostgreSQL permits it |
|---|---|---|---|
| `app_user` | normal application DML | current tenant only | tenant RLS; no registry-table grant |
| `app_worker` direct SQL | fit/dispatch/recovery DML | current tenant only | tenant RLS; arbitrary privileged GUCs have no policy consumer |
| `app_worker` through claim/register/recovery functions | validate worker, claim or recover eligible work | bounded global state transition | function grant + `SECURITY DEFINER`; internal policy requires owner execution and worker session identity |
| `app_dispatch_publisher` | select and lease publishable dispatches; read active generation through bounded function | global, publisher duties only | distinct login and C11 select/update policies; no insert/delete policy |
| `migration_owner` migration session | schema ownership | global | non-login-equivalent migration authority; ordinary roles cannot `SET ROLE` to it |
| tenant context GUC | normal row selection | named tenant only | row predicate compares `tenant_id`; it is not treated as privileged capability |

Runtime catalog evidence found no remaining RLS expression consuming any of:
`app.b24_recovery_reconciler`, `app.b24_dispatch_claim_access`,
`app.b24_worker_authority_access`, `app.b24_claim_capability_digest`, or
`app.b24_fit_resolution_id`.

## 5. Hypothesis ledger

| Hypothesis | Pre-remediation result / cause | Post-remediation experiment | Result | Classification |
|---|---|---|---|---|
| H-XII-01 self-issued recovery | foreign read and `UPDATE 1`; bare permissive GUC policy | set every legacy privilege GUC as tenant A, read/update tenant B | zero rows; `UPDATE 0` | PASS-RUNTIME |
| H-XII-02 fit-resolution authority | tenant-B fit visible by identifier | set foreign fit ID while bound to tenant A | zero rows | PASS-RUNTIME |
| H-XII-03 larger authority class | seven GUC-based privileged policies | enumerate `pg_policies`; semantic canonical-schema scan rejects privileged setting in any policy | none remain | PASS-RUNTIME / PASS-SOURCE |
| H-XII-04 permissive composition | sibling policies OR'd into a bypass | install an alternate permissive global policy | isolation test turns red with one foreign row visible; restore turns green | PASS-RUNTIME |
| H-XII-05 transitivity | `app_user` had registry grants; elevated functions required audit | inspect grants/memberships, attempt direct table DML and `SET ROLE` | permission denied; no membership in owner or publisher | PASS-RUNTIME |
| H-XII-06 session contamination | session state was itself sufficient authority | contaminate one transaction, open a new nonpooled backend, retry | residue has no consequence; different backend PID | PASS-RUNTIME |
| H-XII-07 non-governing proofs | C9 and C10 were green-capable but not required contexts | required context converted into aggregator over core+C9+C10 | any dependency non-success makes required context red | PASS-SOURCE; protected-merge proof pending |
| H-XII-08 no causal authority control | older controls targeted publisher instances | alternate policy, not application variable rename, reintroduces class | physical red→green and four semantic controls fire | PASS-RUNTIME / PASS-CI pending |
| H-XII-09 producing-regime concurrency | broader competing-delivery proposition not explicit | two simultaneous claims against one dispatch and old-policy fit | exactly one lease, one `ACTIVE_LEASE`; one durable replan and current sampler bounds | PASS-RUNTIME |
| H-XII-10 unknown seams | open-world search | see §8 | two additional defects closed; bounded residuals listed | PASS-SOURCE / PASS-RUNTIME |

## 6. Legitimate-path and preservation evidence

Local candidate results before protected merge:

```text
C12 PostgreSQL authority/concurrency suite             6 passed
C11 semantic/publisher/lineage suite                   7 passed
C10 schedule/payload/replan subset                     3 passed
B2.4 P6 + P9 regression suites                         43 passed, 2 skipped
C12 semantic negative controls                         4 fired
C11 semantic negative controls                         3 fired
P1 contract negative controls                          110 fired
P1 trust-drift negative controls                       4 fired
P12 Trust-isolation negative controls                  4 fired
P12 CI-gate negative controls                          7 fired
Alembic downgrade/upgrade round trip                    pass
canonical schema = migrations-derived schema           pass
Ruff / compile / git diff check                         pass
```

The two P6 skips are the suite's opt-in full real-fit tests; they are exercised
by the P13 workflow's production jobs rather than claimed from this host run.
The protected workflow re-runs the complete positive and degraded journeys,
real image/default-CMD worker, actual four-chain sampler, Trust projection,
Ed25519 signing, public JWKS verification, and P1–P12 negative controls.

## 7. Causal negative controls and merge governance

The required branch-protection context now belongs to a final `always()`
aggregator with exact dependencies:

```text
B2.5-P13 E2E Trust Closure Core
AND B2.5-P13 C9 Positive Confidence Composition
AND B2.5-P13 C10 Executable Artifact Topology
-> required context: B2.5-P13 E2E Trust Closure
```

The core includes the C12 database proof and its physical alternate-policy
mutation. The existing load-bearing jobs retain controls for production-artifact
execution, worker/sampler divergence, positive confidence composition, and
policy/provenance contradiction. The C12 validator also mutates the dependency
graph itself: detaching C9 makes the validator red. Thus each property is both
causally attacked and mechanically upstream of the context required by `main`.

## 8. Open-world search

Several new hypotheses were generated from seams rather than auditor wording:

- **Recovery transition atomicity**: moving authority into a bounded function
  exposed that application success previously updated recovery then dispatch in
  separate statements. Closed by one transactional function with strict source
  states.
- **Inherited validators could require the vulnerability**: the P6 and P9
  source gates asserted the presence of fit-resolution and recovery GUCs. They
  were updated to assert tenant-bound resolution and bounded database functions,
  with their own negative controls preserved.
- **A required aggregator could hide dependency failure by skipping**: closed
  with `if: always()` plus explicit equality-to-`success` for every dependency.
- **Pooled residue could remain an application security boundary**: direct
  residue and a second-backend experiment establish that database policy does
  not depend on cleanup; the Bayesian engine remains `NullPool` as defense in
  depth.
- **Dormant capability columns could be mistaken for live security**: the
  claim-digest policies are removed. Compatibility columns remain data only and
  confer no policy authority.

## 9. Residual limitations and debt

- **BOUNDED HARDENING DEBT**: historical function bodies still assign several
  legacy GUC values internally. No effective policy consumes them, the C12
  canonical-schema validator rejects any future policy that does, and direct
  caller assignment has been physically proven consequence-free. Removing the
  inert assignments would reduce archaeological noise but does not alter
  effective authority.
- **BOUNDED HARDENING DEBT (inherited from Audit 50)**: historical policy-bundle
  rows are retained but have no dedicated historical-semantics reader. Current
  production correspondence, signing, and lineage do not depend on such a
  reader.
- **BOUNDED HARDENING DEBT (inherited from Audit 50)**: local compose files do
  not constitute the production publisher/Beat deployment contract. The
  production artifact and real CI orchestration remain the P13 deployment
  authority and are included in the required aggregator.
- **AUDIT-COVERAGE-LIMITATION**: this report is the implementation agent's
  evidence submission, not the next blind independent adjudication.
- **AUDIT-COVERAGE-LIMITATION (local host)**: an exploratory opt-in P6 real-fit
  run on the Windows host reached the governed 240-second sampler timeout; it is
  not counted as a positive result. The source-mismatch case passed, and the
  required Linux production-image C9/C10 jobs perform the authoritative physical
  posterior adjudication before merge.

No residual item restores ordinary cross-tenant authority, permits a producing
regime contradiction, or allows a load-bearing P13 proof to be green-skipped by
the required context.

## 10. Protected-main closure

Pending. This section will be replaced with the protected PR, merge-queue
evidence, final main SHA, exact-main workflow run URLs, check counts, and the
final required-context result after the implementation lands.
