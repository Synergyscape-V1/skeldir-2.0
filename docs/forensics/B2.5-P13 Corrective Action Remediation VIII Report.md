# B2.5-P13 Corrective Action VIII — Remediation Evidence

**Directive**: B2.5-P13 Context-robust, Hypothesis-anchored Follow-Up Corrective Action VIII
**Contextual framing**: Independent Audit Report 42 (Nicholas), Independent Audit Report 43 (Trey)
**Branch**: `b25-p13-c8-identity-window-causality`
**Pull request**: [#665](https://github.com/Synergyscape-V1/skeldir-2.0/pull/665)
**Merged to main**: `b32f67572bd15362e39227ff6f485faf5140d80f`, enqueued through the
`main-merge-queue` ruleset (ALLGREEN, `enforce_admins: true`) rather than
bypassed with `--admin`.

**Exact-main verification** on `b32f67572`: **50 workflow runs, 50 success, 0
failure.** The `CI` workflow alone: 78 jobs success, 1 skipped, 0 failed. The
`B2.5-P13 E2E Trust Closure` context passed on that lineage
([run 32597975975](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/32597975975))
with every counter pinned to a literal:

```
c7_invariant_groups_passed=10
c7_invariant_witnesses=148
c7_static_negative_controls_fired=31
c7_semantics_preserving_controls_held=1
c7_behavioral_obligations_bound=12
```

and every behavioural suite executing on main: C7 conservation physics 12
passed, C8 identity/window physics 9 passed, C8 live transport 1 passed, **C8-N
contiguous journey 2 passed (126.75s)**. The behavioural negative controls fired
on that lineage rather than merely being declared: NC-C7-B01 and NC-C7-B02 each
`1 failed, 11 deselected`, and **NC-C8-B05** — the planner's Beat entry deleted
— `1 failed in 90.79s`, then green again on restore.

---

## 1. The finding both audits reached, by different routes

Two auditors examined B2.5-P13 independently and converged on one conclusion.
Neither said that a proof was wrong. Both said that the proofs **did not touch**.

C7 closed source causality: a committed financial mutation appends dirty
evidence in the same transaction, and no future writer can bypass it. C1–C6
closed the Trust boundary: a confidence projection is signed, tamper-evident,
and verifiable against a published JWKS. Between those two closed halves sat a
seam that neither suite crossed — and across that seam, the model identity
changed.

The invalidation triggers emitted `model_type='mmm'`. The Trust confidence
projection accepts only `model_type='bayesian_attribution_confidence'`. The
value produced by one half was, by construction, refused by the other.

The consequence is not a degraded result. It is that **no financial source
change in the system's history could ever have reached a signed Trust
envelope**, and no gate in the repository was capable of observing that. Each
suite passed because each suite supplied its own starting authority: the
causality suite ended at a dirty row it never asked anyone to consume, and the
Trust suite began at a fit row it wrote itself.

This is the failure mode the C7 report should have anticipated when it recorded
C7-J as PARTIAL. The gap it named was transport. The gap that mattered was
conduction.

---

## 2. Findings

Each was reproduced against live PostgreSQL 15 running the repository's own
unmodified code before anything was changed.

| ID | Finding | Class |
|----|---------|-------|
| **F-01** | Invalidation triggers emit a model identity the Trust projection refuses. The two halves of the pipeline operate in disjoint identity spaces. | Dispositive |
| **F-02** | Staleness required **exact** window equality. A change inside a wider fit's window could not stale it. Two of the three production dirty producers forward arbitrary caller windows, so neither could ever invalidate anything. | Dispositive |
| **F-03** | Staleness re-joined on `model_version`. A fit produced by an earlier pipeline version became permanently unstaleable — the more out of date it was, the safer it was. | Dispositive |
| **F-04** | The C7 gate's source/invalidation "singularity" check was a tautology. Its projection arm was `column in str(projection)` — a containment test against a tuple's own `repr`, which cannot fail — while still contributing `len(projection)` to the reported witness count. Membership predicates were never compared at all. | Proof integrity |
| **F-05** | *Discovered by the contiguous journey of §5.* The change window and the fit window were the same field. Both invalidation triggers bucket to a single day; B2.4's sparse-privacy contract refuses any window whose source spans fewer than 20 distinct days. Every trigger-produced candidate was structurally ineligible before its contents were examined — and the planner's only remaining path, the fallback upsert, is rejected by the C5 dispatch fence, so the planner **raised** rather than degrading. | Dispositive |
| **F-06** | *Discovered by the contiguous journey, after F-05 was fixed.* `app.tasks.bayesian.build_feature_authority` does not build a feature authority. It selects from `b24_source_window_feature_authority`, finds nothing, and parks the request for a 60-second retry. The table's only writer, `upsert_source_window_feature_authority`, has **no non-test caller**. Every production dirty event, however correct its identity and however fittable its window, terminates in an indefinite authority wait. | Dispositive, **open** |

F-05 and F-06 deserve emphasis. Neither was visible from either audit, from the
shipped source, or from any existing gate. Both became visible the moment a real
source change was required to reach a signed envelope without anyone helping it
along — which is precisely the argument for building the contiguous journey
rather than adding another segment proof.

F-06 in particular is invisible to every form of inspection this repository
performs. The task exists, so a registry check passes. It is routed through a
durable outbox and dispatched to the `bayesian` queue, so a transport check
passes. It returns without raising, so a liveness check passes. It is retried on
a schedule, so a recovery check passes. Only asking the whole chain to produce a
fit reveals that the table it consults has no writer outside the test suite —
and that every existing proof of a fit supplied that row itself.

F-06 is **not remediated here**. Implementing a feature-authority producer means
deciding how channel, currency, provider and cohort counts are computed and how
freshness is versioned. That is a modelling deliverable, not a corrective action
on identity and window causality, and inventing it under this directive would be
the same category of error the directive was written to correct. It is instead
pinned structurally so it cannot move unobserved: see §5.

---

## 3. Remediations

### F-01 — one governed identity space

`backend/app/bayesian/model_identity.py` is now the single registry of model
identities. It separates the two things that had been conflated:

- `model_type` — the statistical model family. This is what carries identity
  across the pipeline and what Trust projects.
- `model_version` — the pipeline version that produced a fit. Provenance
  metadata, never a join key for causality.

The registry declares each identity's `status` and `trust_eligible` flag, and
`assert_producible()` refuses to emit a retired one. The invalidation contract
and the dirty marker both read `active_identity()` rather than restating a
literal, so the trigger DDL and the application default cannot drift apart.

Enforcement is not left to Python. Migration
`202608231200_b25_p13_c8_identity_window_causality` adds `CHECK` constraints on
both `b24_dirty_events` and `bayesian_model_fits` restricting `model_type` to
the registered set, so an unregistered identity cannot be stored by any writer
through any path.

`SUPPORTED_CONFIDENCE_MODEL_TYPES` in the Trust read model deliberately remains
a literal. Importing the Bayesian registry from the Trust path would breach the
B2.5-P12 isolation boundary, which forbids `app.bayesian` beneath
`app.api.trust_api`. The literal is instead bound to the registry by a static
gate, so the two are proven equal without being physically coupled.

### F-02 / F-03 — window dependency by overlap, on family identity alone

Staleness now asks the question it always meant to ask: *did anything change
inside the window this fit covers?*

```sql
AND dirty.model_type = requested_fit.model_type
AND public.b24_source_windows_overlap(
        dirty.source_window_start,  dirty.source_window_end,
        requested_fit.source_window_start, requested_fit.source_window_end)
```

`b24_source_windows_overlap` is half-open and immutable:
`p_change_start < p_fit_end AND p_fit_start < p_change_end`. A change at exactly
a fit's end boundary does not stale it; a change one microsecond earlier does.
`idx_b24_dirty_events_staleness_overlap` supports the predicate.

The `model_version` join is gone. A fit's producing pipeline version has no
bearing on whether the financial facts underneath it moved.

### F-04 — one authority renders both artefacts

`backend/app/bayesian/source_contract_authority.py` holds one structured
contract per governed relation — projection, membership, window key — and
renders **both** the canonical snapshot `SELECT` the application executes **and**
the invalidation trigger DDL the migration installs.

The gate no longer compares a string against its own `repr`. It reads the SQL
the database actually runs and compares it, whitespace-normalised, against what
the authority renders. Changing membership, projection or window key on either
side turns it red.

### F-05 — the change window and the fit window are different questions

`b24_dirty_events` records *when the source moved*. That is a change
description, and the day bucket is the right granularity for it — it is exactly
the precision F-02's overlap semantics depends on. It is the wrong description
of *what to fit*.

`fit_window_for()` in `app/bayesian/fit_planner.py` derives the fit window from
the change: it ends where the change window ends and reaches back at least
`SPARSE_PRIVACY_THRESHOLDS.minimum_source_window_density_days`. The bound is
**read from the contract that imposes it**, never restated, so the two cannot
drift. The widening is monotone — a caller that already supplied a window at
least that wide keeps exactly the window it asked for — which is why every
existing 30-day and 31-day proof is untouched by the change.

Widening the *trigger's* window instead was considered and rejected: it would
have made a change on day D stale fits that do not contain day D, undoing the
precision F-02 was remediated to obtain.

---

## 4. Proof surface

### 4.1 Behavioural, against real PostgreSQL 15

**`test_b25_p13_c8_identity_window_physics.py` — 9 tests**

| Proof | What it establishes |
|-------|--------------------|
| `source_change_stales_a_trust_projectable_fit` | The end-to-end identity claim at DB level: a real verdict confirmation stales a fit that Trust would project. |
| `source_change_stales_every_supported_window_shape` (×4) | `same_day`, `thirty_day`, `non_midnight`, `boundary_open`. Under exact equality only the first could ever have passed. |
| `change_outside_the_window_does_not_stale` | Overlap did not become "always true". Precision is preserved. |
| `window_overlap_relation_is_half_open` | A change at the closing boundary does not stale; one instant earlier does. |
| `unregistered_model_identity_cannot_be_stored` | The `CHECK` constraints reject an unregistered identity at the database, not in Python. |
| `retired_identity_cannot_be_newly_produced` | A retired registry entry cannot be emitted by a new writer. |

The fit fixture discards the dirty evidence produced by its own construction.
That makes the assertion strictly harder, not easier: ordering setup evidence
against the fit's source read would otherwise rest on which transaction happened
to start first — a margin measured in microseconds — and a pass earned that way
proves nothing. With the construction evidence gone, the only row that can stale
the fit is one the test produced deliberately.

**`test_b25_p13_c8_transport_physics.py` — 1 test**

The gate C7 recorded as PARTIAL. Real `celery beat`, real SQLAlchemy-on-Postgres
broker, real `celery worker -Q bayesian` subprocess, and a probe written from
inside the task body under the `app_worker` database identity. Every edge is
asserted from an artefact only that edge can produce.

It carries a **negative interval**: Beat and broker running with no Bayesian
worker must leave the durable obligation untouched. Without that interval a
passing test cannot distinguish transport from a planner that happened to run
some other way.

**`test_b25_p13_c8_contiguous_journey.py` — 1 test.** See §5.

### 4.2 Static falsifiers

`scripts/ci/validate_b25_p13_c7_closure.py` now runs nine invariant groups.
New controls:

| Control | Reintroduces |
|---------|--------------|
| NC-C7-S04 | Source-query **membership** changed while the invalidation authority is left alone. Green on C7. |
| NC-C7-S19 | Source-query **projection** changed only. |
| NC-C7-S20 | Source-query **window key** changed only. |
| NC-C8-S01…S04 | Identity-registry drift: unregistered type, retired type made producible, dirty marker hard-coding a literal, trigger DDL diverging from the registry. |
| NC-C8-S05 | Staleness reverting to exact window equality (the F-02 shape). |
| NC-C8-S06 | Staleness re-joining on `model_version` (the F-03 shape). |
| NC-C8-S07 | The overlap relation losing its half-open semantics. |
| NC-C8-S08 | The supporting index removed. |

And one control that is **not** a falsifier:

| Control | Asserts |
|---------|---------|
| NC-C7-S21 | A semantics-preserving respelling of the source SQL must **not** turn the gate red. |

NC-C7-S21 exists because a gate that fires on whitespace is validating
formatting, not semantics, and would report the same green as one that validates
neither. It is counted separately from the falsifiers, as
`c7_semantics_preserving_controls_held`, because conflating a control that must
fire with one that must hold is how a control count becomes a number rather than
a claim.

`NC-C8-B05` in the workflow deletes the planner's Beat entry and requires the
transport proof to turn red, then restores it and requires green.

### 4.3 Honest accounting of the previous count

C7 reported `c7_static_negative_controls_fired=18`. Four of those controls
exercised the tautological arm described in F-04 and could not have
distinguished a real regression from none. The pin now reads **28 fired negative
controls and 1 held positive control**, and each was verified to fire — or to
hold — against the shipped artefacts rather than against a mutated copy of the
gate's own expectations.

---

## 5. C8-N — the contiguous journey, and where it stops

The directive names this the principal closure gate. It is the only proof here
that could have discovered F-05 or F-06, and it did discover both.

One real financial fact changes, and every intermediate authority must be
produced by the component that owns it:

```
a provisional verdict batch is confirmed         (ordinary UPDATE, app role)
  -> production invalidation trigger              writes the dirty evidence      OK
    -> production wake-up trigger                 makes the tenant due           OK
      -> real celery beat                         emits onto a real broker       OK
        -> real celery worker -Q bayesian         consumes it                    OK
          -> production planner                   judges the change fittable     OK
            -> feature-authority producer         ...........................    F-06
              -> fit -> confidence -> Trust -> JWKS                              unreachable
```

There is no `append_dirty_event` call, no hand-written `bayesian_model_fits`
row, no `.run()` standing in for delivery, and no dependency override standing
in for authentication. The credential is a real `agent_service_credentials` row
with a real scope grant, so the production `authenticate_machine_caller` would
run against it.

The journey asserts at every seam against an artefact only that seam can
produce, so a break fails at the stage that broke rather than silently
shortening the chain. It carries a negative interval: before the worker starts,
messages are on the broker, the obligation is unmoved, and **no fit exists**. A
fit that could appear without a worker would mean the journey was proving
something about the test process.

**The chain conducts to the frontier and stops there.** Reaching
`authority_waiting` is not a failure of this work — it is the first time in the
system's history that a real financial change has been judged fittable. Before
F-05 was fixed, the planner did not reach a verdict at all: it raised
`b24_dispatch_fence_rejected`, held its lease, and left the obligation to expire.

The journey does not cross the frontier, and it deliberately does not try. A
journey that reached a signed envelope by seeding the feature authority itself
would be making exactly the substitution both audits condemned, and would hide
the finding that matters most. Instead the frontier is **asserted**, in two
directions:

- `test_c8_financial_change_conducts_to_the_feature_authority_frontier` requires
  that no feature authority exists for the tenant when the planner has finished.
- `test_c8_no_production_component_builds_the_feature_authority` requires that
  `upsert_source_window_feature_authority` still has exactly one non-test
  occurrence — its own definition — and that `build_feature_authority` still
  only reads the table it is named for.

If someone implements the producer, both go red, which is the correct signal
that the frontier has moved and the journey must be extended through the fit,
the confidence projection and the signed envelope. If the existing writer is
deleted, they go red too. Either way the boundary is stated rather than assumed.

Two pieces of fixture hygiene are worth stating plainly, because both make the
claim harder rather than easier:

- The dirty evidence and wake-up produced by *building* the fixture are
  discarded before the change is made. From that point, any dirty row, any
  wake-up and any verdict can only have come from the change under test.
- The broker queue is cleared before the baseline is taken. A database reused
  across local runs accumulates long-expired planner messages which the worker
  discards as revoked and the visibility recovery re-offers indefinitely, so the
  live message never reaches a solo worker. CI builds this database from
  nothing, so it is a no-op there; the baseline is taken after the purge, so the
  negative interval still measures only what this journey emitted.

Two pieces of fixture hygiene are worth stating plainly, because both make the
claim harder rather than easier:

- The dirty evidence and wake-up produced by *building* the fixture are
  discarded before the change is made. From that point, any dirty row, any
  wake-up and any fit can only have come from the change under test.
- The broker queue is cleared before the baseline is taken. A database reused
  across local runs accumulates long-expired planner messages which the worker
  discards as revoked and the visibility recovery re-offers indefinitely, so the
  live message never reaches a solo worker. CI builds this database from
  nothing, so it is a no-op there; the baseline is taken after the purge, so the
  negative interval still measures only what this journey emitted.

---

## 6. Preservation

| Suite | Result |
|-------|--------|
| B2.5-P13 E2E trust closure | unchanged |
| B2.5-P13 C3/C4/C5/C6/C7 static gates | unchanged |
| B2.5-P13 C6 PostgreSQL physics | unchanged |
| B2.5-P13 C7 conservation physics | unchanged |
| B2.5-P1 contracts / trust drift | unchanged |
| B2.5-P12 trust isolation / CI gates | unchanged |
| B2.4-P1/P2/P3/P4 static gates | unchanged |
| M0 scope lock / M1 local dev authority / M3 CI governance | unchanged |
| Zero Container Doctrine | unchanged |

The P12 isolation boundary was breached once during this work — an import of
`app.bayesian.model_identity` from `app.api.trust_api` — and the gate caught it.
The import was withdrawn rather than the boundary widened; see §3, F-01.

---

## 7. What is proven, and what is not

Proven:

- A real financial source change produces dirty evidence at an identity the
  Trust projection accepts, and that evidence reaches a fit through the real
  scheduler, the real broker and a real Bayesian worker.
- Window dependency is causal and precise in both directions: a change inside a
  fit's window stales it whatever the window's shape, and a change outside it
  does not.
- An unregistered or retired model identity cannot be stored, enforced by the
  database rather than by convention.
- The source contract and the invalidation contract are one authority, and the
  gate that says so can fail.

Not claimed, and stated plainly:

- **The chain does not reach a signed envelope from a real source change, and
  cannot until F-06 is remediated.** No completion claim in this document rests
  on it. C8-N proves conduction to the feature-authority frontier and asserts
  that the frontier is where it is.
- Real PyMC sampling is not composed into this journey. It is exercised by the
  B2.4-P6 real-fit proof in its own workflow with the Bayesian dependency set
  installed, and that proof supplies its own feature authority — which is how
  F-06 stayed hidden.
- The C5 dispatch fence rejects the planner's own governed fallback upsert
  (`b24_dispatch_fence_rejected` on a write the fence's own comment says the
  planner owns). F-05's remediation means the eligible path no longer reaches
  it, so this is no longer load-bearing, but the exemption is incomplete and is
  recorded here rather than silently fixed.
- **F-07, observed while building C8-N, not remediated.**
  `plan_due_fit_intents` leases its entire tenant batch up front through
  `b24_due_fit_planner_tenants`, then disposes each tenant in a per-tenant
  `finally`. An exception raised while planning one tenant propagates out of the
  loop, so every tenant leased *behind* the failing one is never processed and
  never disposed — it stays `leased` until its 600-second lease expires. One
  unplannable tenant can therefore stall an entire deployment's planning for ten
  minutes at a time, and raising the batch size widens the blast radius rather
  than improving throughput.

  This was observed directly: with the batch raised to the task's ceiling, all
  six due wake-ups in a shared database were stranded under a single lease
  expiring ten minutes later, with the planner reporting `tenant_count=0` on
  every subsequent pass. C7 made wake-up *disposal* derive from residual
  authority rather than from Python control flow; the batch's failure semantics
  were not in its scope and remain a control-flow artefact.

  C8-N does not work around this by weakening an assertion. It clears other
  tenants' wake-ups before it begins — the journey runs last in a purpose-built
  database, and this touches no row of its own evidence — so that a stranded
  batch cannot be mistaken for a broken chain.

---

## 8. Verdict

**B2.5-P13 Corrective Action VIII: F-01, F-02, F-03, F-04 and F-05 are closed
and proven. F-06 is open, newly discovered, and pinned.**

The directive asked for a contiguous journey from a financial fact to a signed
claim. That journey cannot complete today, and the reason is not a defect in
this remediation — it is that the pipeline has never had a feature-authority
producer, and no proof in the repository was shaped in a way that could notice.
Every prior demonstration of a fit wrote that row itself.

Reporting that is the deliverable. A green checkmark obtained by seeding the
missing row would have reproduced, exactly, the defect both audits were written
to expose.
