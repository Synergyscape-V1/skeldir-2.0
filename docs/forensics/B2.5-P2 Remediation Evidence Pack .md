# B2.5-P2 Remediation Evidence Pack

Phase: B2.5-P2 - Canonicalization, Hash Identity, and Schema-Version Registry

Corrective action: Context Robust Hypothesis Anchored B2.5-P2 Follow-Up Corrective Action Remediation Directives I and II

## Repository State

Original implementation PR: [#583](https://github.com/Synergyscape-V1/skeldir-2.0/pull/583)

Original protected main SHA: `2547f2a6b03d4d218c9ab3dfe50c4fe20f568ec0`

Corrective base main SHA: `fa30988608d80db1c7d18178200245d58364cc06`

Corrective branch: `codex/b25-p2-ca1-canonical-identity-proof`

Corrective PR: [#585](https://github.com/Synergyscape-V1/skeldir-2.0/pull/585)

Corrective branch commit SHA: `c0073abc8a79edfe67f81b7eb45684f14f5729c6`

Protected main SHA after corrective merge: `ae9523946096048ddf930928751f4db0e3c9a292`

Corrective merge time: `2026-06-25T15:12:28Z`

Main CI check-run census after corrective merge: `145` total, `144` success, `1` skipped/neutral, `0` pending, `0` failed.

Main CI evidence:
- B2.5-P2 Canonicalization: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28180300155/job/83467816921
- B2.5-P1 Contract Authority: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28180298414/job/83467811418
- Governance Guardrails: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28180298674/job/83468025619
- M0 Maintainability Scope Lock: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28180298526/job/83467811544
- M1 Local Development Authority: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28180298456/job/83467811299

Main CI note: the initial main run for `ae9523946096048ddf930928751f4db0e3c9a292` had one unrelated `r6-worker-resource-governance` runtime-probe failure. The failed workflow was rerun for the same SHA; the final authoritative census above is after that rerun and has `0` failed checks.

## Corrective Remediation II Repository State

Directive II starting main SHA: `f76d13baf85afab0035757cf55adb7fc7edfa523`

Directive II corrective branch: `codex/b25-p2-ca2-declarative-wrapper-standalone-pytest`

Directive II corrective branch commit SHA: `735a3e26337db3654f69e9593b4562f40a03befe`

Directive II corrective PR: [#587](https://github.com/Synergyscape-V1/skeldir-2.0/pull/587)

Directive II protected main SHA after implementation merge: `d6b37c1c595beb6359b314fc743587647ef7431c`

Directive II merge time: `2026-06-25T18:47:58Z`

Directive II main CI check-run census after implementation merge: `134` total, `133` success, `1` skipped/neutral, `0` pending, `0` failed.

Directive II main CI evidence:
- B2.5-P2 Canonicalization: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28192905146/job/83511915083
- Governance Guardrails: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28192905164
- M0 Maintainability Scope Lock: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28192905139
- M1 Local Development Authority: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28192905183

Directive II files changed:
- `.github/workflows/b2_5-p2-canonicalization.yml`
- `backend/app/trust/hash_identity.py`
- `backend/tests/trust/test_b25_p2_hash_identity.py`
- `scripts/ci/validate_b25_p2_canonicalization.py`

## Corrective Hypothesis Ledger

| Hypothesis | Finding | Disposition |
| --- | --- | --- |
| H-CA-01: non-ASCII semantic canonical bytes are not proven | Confirmed. Prior Unicode controls mutated `untrusted_display_data.display_text`, a display-only field excluded from semantic identity. | Remediated with a schema-valid semantic Unicode probe, a second golden byte fixture, semantic-hash participation tests, and `unicode_semantic_controls_passed=4`. |
| H-CA-02: `ensure_ascii=True` is not mechanically blocked | Confirmed. The canonicalizer used `ensure_ascii=False`, but the validator only required `allow_nan=False` at approved `json.dumps` sites. | Remediated with static rejection of `ensure_ascii=True`, approved-call enforcement of `ensure_ascii=False`, and `ensure_ascii_controls_passed=4`. |
| H-CA-03: HashDomainWrapper closure is not mechanically closed | Confirmed. Hash wrappers were constructed consistently but not validated as closed objects before canonical byte encoding. | Remediated with `validate_hash_domain_wrapper()` and 9 negative controls, including extra/missing wrapper keys, unsupported algorithms, unknown domains, wrong payload types, signature/display fields in semantic payloads, wrapper-field collisions, and invalid artifact-byte hashes. |
| H-CA-04: Trust pytest execution is not visible in CI proof | Partially confirmed. The validator executed `backend/tests/trust` by default, but captured successful output and did not emit a passed-count marker. | Remediated by printing `pytest_command=C:\Python311\python.exe -m pytest backend/tests/trust -q`, streaming pytest output, parsing the pass count, and emitting `pytest_trust_tests_passed=38`. |
| H-CA-05: Updated-audit pass markers are absent | Confirmed. Required markers were not present. | Remediated with `ensure_ascii_controls_passed`, `hash_wrapper_closure_controls_passed`, `manual_mapper_controls_passed`, `unicode_semantic_controls_passed`, and `pytest_trust_tests_passed`. |

## Corrective Remediation II Hypothesis Ledger

| Hypothesis | Status | Evidence | Root Cause | Remediation | Post-remediation proof | Residual risk |
| --- | --- | --- | --- | --- | --- | --- |
| H-CA2-01: HashDomainWrapper closure is still procedural | CONFIRMED | `backend/app/trust/hash_identity.py` used `HASH_WRAPPER_KEYS`, `set(wrapper)`, and missing/extra key arithmetic. | Behavioral negative controls were present, but wrapper closure was not declared as a runtime schema/model boundary. | Replaced procedural closure with `HASH_DOMAIN_WRAPPER_SCHEMA` and `Draft202012Validator(HASH_DOMAIN_WRAPPER_SCHEMA)` using `additionalProperties: False`, exact `required`, domain enum, algorithm const, and object payload schema. | `hash_wrapper_closure_controls_passed=10`; `declarative_hash_wrapper_schema_controls_passed=7`; `test_hash_domain_wrapper_schema_is_declaratively_closed` passes in standalone CI. | None for P2 scope. |
| H-CA2-02: `backend/tests/trust` is not a standalone CI YAML step | CONFIRMED | `.github/workflows/b2_5-p2-canonicalization.yml` had P1 steps and the P2 validator step, but no separate `python -m pytest backend/tests/trust -q` step. | CI visibility was treated as validator-emitted proof, not independent CI adjudication. | Added `Run backend trust pytest` before `Validate B2.5-P2 canonicalization and negative controls`. | Main CI job step 7 ran `python -m pytest backend/tests/trust -q` and reported `39 passed in 33.08s`; step 8 then ran the P2 validator. | None for P2 scope. |
| H-CA2-03: pytest subprocess remains primary proof | CONFIRMED | `scripts/ci/validate_b25_p2_canonicalization.py` contained `run_pytest_suite()`, `subprocess.run(...)`, and emitted `pytest_trust_tests_passed`. | The validator carried too much proof responsibility. | Removed embedded pytest subprocess execution, removed `--pytest`/`--no-pytest`, and removed `pytest_trust_tests_passed` output. | Required ownership search over the P2 validator/workflow/Makefile returns no `pytest_trust_tests_passed`, no validator `subprocess.run`, and no `pytest_command`; primary proof is the standalone CI step. | None for P2 scope. |
| H-CA2-04: declarative wrapper closure may accidentally introduce Pydantic serialization | REFUTED AFTER REMEDIATION | Remediation uses JSON Schema, not Pydantic. | No Pydantic wrapper model was needed because `jsonschema` was already a trust-path dependency. | Used JSON Schema validation only; no Pydantic serialization path was introduced. | Static serializer hook search shows only negative-control strings in tests/validator; no trust preimage path uses `model_dump`, `model_dump_json`, `TypeAdapter.dump_*`, serializers, or `json_encoders`. | None for P2 scope. |
| H-CA2-05: fixing the two blockers may regress already-proven P2 invariants | REFUTED | All existing local and CI markers remain nonzero and passing. | No regression observed. | Preserved canonicalization, Unicode, numeric, serializer, hash-domain, version, null, manifest, array-ordering, manual-mapper, and scope controls. | Main P2 validator passes with `B25_P2_CANONICALIZATION_VALIDATION_PASS`, existing markers, and `scope_overreach_controls_passed=4`. | None for P2 scope. |

## Root-Cause Findings

The original P2 proof established deterministic canonicalization, hash-domain manifests, serializer boundaries, and schema/canonicalization registries, but its Unicode evidence was anchored to display-only text. That left a semantic proof gap: non-ASCII UTF-8 bytes could be canonicalized without proving they participated in semantic hash identity.

The original implementation also relied on disciplined construction of hash input wrappers rather than validating wrapper closure as a first-class invariant. That made wrapper-key and payload-domain attacks testable only indirectly.

The CI validator already invoked `backend/tests/trust`, but successful pytest output was hidden by `capture_output=True`; authoritative CI logs could therefore prove the validator passed, but not visually prove the executed pytest command or passed count.

## Corrective Remediations

Added a narrow optional contract field `semantic_unicode_probe` with const value `Café-東京`. It is intentionally fixture-only, schema-valid, and registered under `semantic_truth_v1` in `contracts/trust-api/hash-domain-manifest.v1.yaml`; it does not create a broad free-text production surface.

Added `contracts/trust-api/examples/canonicalization/revenue_claim_semantic_unicode_valid.json` and its expected manifest. Pinned values:

```text
expected_canonical_byte_length=3906
expected_canonical_sha256=sha256:3cb964c81f7798f0c3455f9d09a24c2fcc46d21406cbeece1541360b2c53a0ba
expected_semantic_truth_hash=sha256:6268b78300b0ca7aeb0484694753679c9a177bd00a6d973fbbb556d494a0ef8c
semantic_hash_bearing_field_path=semantic_unicode_probe
expected_canonical_utf8_contains="semantic_unicode_probe":"Café-東京"
```

Strengthened `backend/app/trust/hash_identity.py` with a closed `validate_hash_domain_wrapper()` gate before canonical byte encoding. Allowed wrapper keys are exactly `hash_domain`, `schema_version`, `canonicalization_version`, `hash_algorithm`, and `payload`.

Strengthened `scripts/ci/validate_b25_p2_canonicalization.py` to validate canonical byte length, UTF-8 prefix/fragment expectations, semantic hash-bearing fixture fields, `ensure_ascii=False` enforcement, wrapper closure controls, manual mapper controls, and visible pytest execution.

Added/updated tests in `backend/tests/trust` for semantic Unicode raw UTF-8 bytes, semantic-hash participation, wrapper rejection, and manifest coverage count.

Corrected the original `revenue_claim_valid.expected.json` UTF-8 prefix. The hash and byte length were already correct; the dormant prefix field had not previously been checked.

## Local Corrective Validation

Command:

```text
python scripts/ci/validate_b25_p1_contracts.py --negative-control
```

Output summary:

```text
B25_P1_CONTRACT_VALIDATION_PASS
required_files=14
required_common_trust_fields=32
subject_conditioned_fields=40
max_validity_window_seconds=86400
trust_examples_validated=15
error_examples_validated=11
signing_algorithm_positive_controls_passed=3
typed_negative_controls_passed=110
```

Command:

```text
python scripts/ci/validate_b25_p1_trust_drift.py --negative-control
```

Output summary:

```text
B25_P1_TRUST_DRIFT_VALIDATION_PASS
protected_path_patterns=6
drift_negative_controls_passed=4
```

Command:

```text
python scripts/ci/validate_b25_p2_canonicalization.py --negative-control
```

Output summary:

```text
pytest_command=C:\Python311\python.exe -m pytest backend/tests/trust -q
============================= 38 passed in 29.59s =============================
B25_P2_CANONICALIZATION_VALIDATION_PASS
canonicalization_profile=RFC8785-JCS-Skeldir-v1
canonical_examples_validated=3
golden_byte_fixtures_validated=2
key_order_permutations_passed=1
provenance_order_permutations_passed=1
array_ordering_controls_passed=2
hash_domain_mutations_passed=3
structured_hash_input_controls_passed=1
hash_wrapper_closure_controls_passed=9
version_negative_controls_passed=8
numeric_negative_controls_passed=5
unicode_negative_controls_passed=2
unicode_semantic_controls_passed=4
unsafe_serializer_controls_passed=2
pydantic_serializer_controls_passed=6
ensure_ascii_controls_passed=4
hash_output_format_controls_passed=8
manual_mapper_controls_passed=7
null_presence_controls_passed=4
pytest_trust_tests_passed=38
manifest_field_paths_checked=94
array_field_paths_checked=5
scope_overreach_controls_passed=4
```

Command:

```text
python -m ruff check backend/app/trust/hash_identity.py backend/tests/trust/test_b25_p2_canonicalization.py backend/tests/trust/test_b25_p2_hash_identity.py backend/tests/trust/test_b25_p2_manifest_coverage.py scripts/ci/validate_b25_p2_canonicalization.py
```

Output summary:

```text
All checks passed!
```

Command:

```text
python -m black --check backend/app/trust/hash_identity.py backend/tests/trust/test_b25_p2_canonicalization.py backend/tests/trust/test_b25_p2_hash_identity.py backend/tests/trust/test_b25_p2_manifest_coverage.py scripts/ci/validate_b25_p2_canonicalization.py
```

Output summary:

```text
5 files would be left unchanged.
```

## Local Corrective Remediation II Validation

Command:

```text
python scripts/ci/validate_b25_p1_contracts.py --negative-control
```

Output summary:

```text
B25_P1_CONTRACT_VALIDATION_PASS
required_files=14
required_common_trust_fields=32
subject_conditioned_fields=40
max_validity_window_seconds=86400
trust_examples_validated=15
error_examples_validated=11
signing_algorithm_positive_controls_passed=3
typed_negative_controls_passed=110
```

Command:

```text
python scripts/ci/validate_b25_p1_trust_drift.py --negative-control
```

Output summary:

```text
B25_P1_TRUST_DRIFT_VALIDATION_PASS
protected_path_patterns=6
drift_negative_controls_passed=4
```

Command:

```text
python -m pytest backend/tests/trust -q
```

Output summary:

```text
39 passed in 39.80s
```

Command:

```text
python scripts/ci/validate_b25_p2_canonicalization.py --negative-control
```

Output summary:

```text
B25_P2_CANONICALIZATION_VALIDATION_PASS
canonical_examples_validated=3
golden_byte_fixtures_validated=2
hash_wrapper_closure_controls_passed=10
declarative_hash_wrapper_schema_controls_passed=7
standalone_trust_pytest_ci_step_controls_passed=4
unicode_semantic_controls_passed=4
ensure_ascii_controls_passed=4
pydantic_serializer_controls_passed=6
manual_mapper_controls_passed=7
manifest_field_paths_checked=94
array_field_paths_checked=5
scope_overreach_controls_passed=4
```

Command:

```text
rg -n "python -m pytest backend/tests/trust|pytest backend/tests/trust" .github/workflows Makefile
```

Output summary:

```text
.github/workflows/b2_5-p2-canonicalization.yml:68:        run: python -m pytest backend/tests/trust -q
```

Command:

```text
rg -n "pytest_command|pytest_trust_tests_passed|subprocess\.run|capture_output|returncode|check=True" scripts/ci/validate_b25_p2_canonicalization.py .github/workflows/b2_5-p2-canonicalization.yml Makefile
```

Output summary:

```text
No matches. The P2 validator no longer embeds pytest subprocess execution or emits pytest pass-count proof.
```

Command:

```text
rg -n "@field_serializer|@model_serializer|field_serializer\(|model_serializer\(|PlainSerializer|WrapSerializer|SerializeAsAny|computed_field|json_encoders|ser_json|serialize_|dump_python|dump_json|model_dump|model_dump_json" backend/app/trust scripts/ci/validate_b25_p2_canonicalization.py backend/tests/trust
```

Output summary:

```text
Matches are limited to static negative-control text in `scripts/ci/validate_b25_p2_canonicalization.py` and `backend/tests/trust/test_b25_p2_serializer_boundaries.py`; no trust preimage path uses Pydantic serialization hooks.
```

Command:

```text
python -m ruff check backend/app/trust/hash_identity.py backend/tests/trust/test_b25_p2_hash_identity.py scripts/ci/validate_b25_p2_canonicalization.py
python -m black --check backend/app/trust/hash_identity.py backend/tests/trust/test_b25_p2_hash_identity.py scripts/ci/validate_b25_p2_canonicalization.py
```

Output summary:

```text
All checks passed!
3 files would be left unchanged.
```

## Corrective Remediation II Main CI Proof

Command:

```text
gh run list --branch main --commit d6b37c1c595beb6359b314fc743587647ef7431c
gh run view 28192905146 --json headSha,event,status,conclusion,workflowName,jobs
gh run view 28192905146 --log
```

Output summary:

```text
workflowName=B2.5-P2 Canonicalization
event=push
headSha=d6b37c1c595beb6359b314fc743587647ef7431c
conclusion=success
step 5: Validate B2.5-P1 contract authority preservation -> B25_P1_CONTRACT_VALIDATION_PASS
step 6: Validate B2.5-P1 TrustEnvelope drift tripwire -> B25_P1_TRUST_DRIFT_VALIDATION_PASS
step 7: Run backend trust pytest -> python -m pytest backend/tests/trust -q -> 39 passed in 33.08s
step 8: Validate B2.5-P2 canonicalization and negative controls -> B25_P2_CANONICALIZATION_VALIDATION_PASS
hash_wrapper_closure_controls_passed=10
declarative_hash_wrapper_schema_controls_passed=7
standalone_trust_pytest_ci_step_controls_passed=4
scope_overreach_controls_passed=4
```

Directive II full-main check-run census:

```text
total=134
success=133
skipped_or_neutral=1
pending=0
failed=0
```

## Exit Gates

Gate: P1 baseline preserved
Status: pass
Evidence: P1 contract and drift validators pass unchanged after corrective contract addition locally and in main CI.
Residual risk: none for P2 scope.

Gate: Non-ASCII semantic canonical bytes
Status: pass
Evidence: second golden byte fixture is schema-valid and semantic hash-bearing.
Output summary: `golden_byte_fixtures_validated=2`, `unicode_semantic_controls_passed=4`.
Residual risk: none for P2 scope.

Gate: `ensure_ascii=True` blocked
Status: pass
Evidence: static controls reject `ensure_ascii=True` and approved canonical dumps missing `ensure_ascii=False`.
Output summary: `ensure_ascii_controls_passed=4`.
Residual risk: static scanner is intentionally scoped to trust canonicalization/hash surfaces.

Gate: HashDomainWrapper closure
Status: pass
Evidence: wrapper closure is declaratively validated by `HASH_DOMAIN_WRAPPER_SCHEMA` using JSON Schema Draft 2020-12 with `additionalProperties: false`, exact required keys, domain enum, algorithm const, and object payload before canonical bytes.
Output summary: `hash_wrapper_closure_controls_passed=10`, `declarative_hash_wrapper_schema_controls_passed=7`.
Residual risk: none for P2 scope.

Gate: Manual mapper controls
Status: pass
Evidence: trust hash identity is manifest-projected and wrapper-validated; no forbidden manual trust field map/hash `json.dumps` shortcuts were found.
Output summary: `manual_mapper_controls_passed=7`.
Residual risk: none for P2 scope.

Gate: Pytest CI proof visibility
Status: pass
Evidence: B2.5-P2 workflow has a distinct `Run backend trust pytest` CI step before the P2 validator step; main CI step 7 ran `python -m pytest backend/tests/trust -q` and passed independently.
Output summary: `39 passed in 33.08s`; `standalone_trust_pytest_ci_step_controls_passed=4`.
Residual risk: none for P2 scope.

Gate: Existing P2 regression preservation
Status: pass
Evidence: canonical examples, key ordering, provenance ordering, array ordering, hash-domain mutations, structured hash input, version registry, numeric controls, Unicode surrogate/NFC/NFD controls, serializer boundaries, hash format, null presence, manifest coverage, and scope guard all pass.
Residual risk: none for P2 scope.

Gate: P2 scope boundary
Status: pass
Evidence: no Trust API route, TrustEnvelopeBuilder, signing/verifier/JWKS implementation, agent auth, trust audit/access tables, live source adapters, export projection refactor, MCP behavior, or action authority was added.
Output summary: `scope_overreach_controls_passed=4`.
Residual risk: none for P2 scope.

Gate: Protected-branch merge and final green main CI
Status: pass
Evidence: corrective PR #587 merged to protected `main` at `d6b37c1c595beb6359b314fc743587647ef7431c`; final main check-run census is `134` total, `133` success, `1` skipped/neutral, `0` pending, `0` failed.
Residual risk: none for P2 scope.

## Non-Overclaim Statement

P2 establishes deterministic canonicalization and hash identity tooling over schema-valid TrustEnvelope payloads. It does not implement Trust API routes, TrustEnvelopeBuilder, runtime envelope issuance, signing, verifier/JWKS runtime endpoints, agent auth, trust audit tables, source adapters, export projection refactors, MCP adapters, or autonomous action authority.

## Final Verdict

VERDICT: B2.5-P2 PASS — CORRECTIVE REMEDIATION II COMPLETE

Protected main SHA:
`d6b37c1c595beb6359b314fc743587647ef7431c`

CI status:
`134` total check-runs, `133` success, `1` skipped/neutral, `0` pending, `0` failed.

Declarative wrapper closure proof:
`HASH_DOMAIN_WRAPPER_SCHEMA` plus `Draft202012Validator(HASH_DOMAIN_WRAPPER_SCHEMA)` closes wrapper shape with `additionalProperties: false`; `declarative_hash_wrapper_schema_controls_passed=7`.

Standalone pytest CI step proof:
Main CI job step `Run backend trust pytest` ran `python -m pytest backend/tests/trust -q` and passed with `39 passed`.

P1 baseline proof:
`B25_P1_CONTRACT_VALIDATION_PASS` and `B25_P1_TRUST_DRIFT_VALIDATION_PASS` both pass locally and in main CI.

Existing P2 invariant proof:
`B25_P2_CANONICALIZATION_VALIDATION_PASS` passes with existing markers plus `hash_wrapper_closure_controls_passed=10`.

Pydantic serializer-hook absence proof:
No Pydantic wrapper model was introduced; serializer-hook scan findings are limited to negative-control text, not trust preimage paths.

Scope boundary proof:
`scope_overreach_controls_passed=4`; no Trust API route, TrustEnvelopeBuilder, signer, verifier, JWKS endpoint, agent auth, audit table, source adapter, export refactor, MCP behavior, or action authority was added.

Next authorized phase:
B2.5-P3 — Deterministic Untrusted Text Disposition Matrix
