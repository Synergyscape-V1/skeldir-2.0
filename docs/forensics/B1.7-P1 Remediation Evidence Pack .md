# B1.7-P1 Remediation Evidence Pack
Generated: 2026-04-03 (America/Chicago)
Branch audited and remediated: `main`
Directive: **B1.7-P1 Context-Robust Hypothesis-Driven Remediation Directive**

## 1. Executive Verdict
**B1.7-P1 is implemented and locally proven with falsifiable mounted runtime checks.**

The canonical route now executes an authenticated tenant-bound route -> service -> DB deterministic authority read chain, returns explicit authority/explanation separation, and fails closed when deterministic authority is unavailable.

## 2. Scope and Constraints Honored
- Preserved canonical mounted route surface `/api/attribution/explain/{entity_type}/{entity_id}`.
- Preserved Postgres-first architecture; no Redis/Kafka/NoSQL introduced.
- Preserved B1.6 fail-closed doctrine and non-authoritative explanation boundary.
- Did not introduce synchronous chat/streaming surfaces.
- Kept P2/P3 concerns out of P1 implementation scope (no fast-tier routing overhaul, no watermark identity redesign, no citation-coherence subsystem).

## 3. Hypothesis Adjudication (H01-H15)
| Hypothesis | Result | Evidence | Disposition |
|---|---|---|---|
| H01 canonical route lacked real deterministic authority read chain | Confirmed pre-fix; closed post-fix | Route previously hardcoded 503; now calls `fetch_attribution_explanation_authority(...)` in route path | CLOSED |
| H02 missing explanation-authority service boundary | Confirmed pre-fix; closed post-fix | Added `backend/app/services/attribution_explanation_authority.py` | CLOSED |
| H03 deterministic truth sources not concretely implemented | Confirmed pre-fix; closed post-fix | Service reads `attribution_allocations` + `revenue_cache_entries` | CLOSED |
| H04 no explicit separated mounted response schema | Confirmed pre-fix; closed post-fix | Added `AttributionExplanationResponse` dual-object schema | CLOSED |
| H05 synthesis-only/merged payload risk | Confirmed pre-fix; closed post-fix | OpenAPI + route `required_response_separation.merged_payload_forbidden=true`; tests assert dual keys | CLOSED |
| H06 no hard invariant requiring paired authority object | Confirmed pre-fix; closed post-fix | Response model requires `authoritative_metric` and `non_authoritative_explanation` | CLOSED |
| H07 tenant scope not proven end-to-end for authority path | Unproven pre-fix; now proven | runtime cross-tenant mounted negative control returns 404 | CLOSED |
| H08 wrong-scope/cross-tenant reads possible at explanation authority layer | Risk pre-fix; now denied | DB query uses `tenant_id` + `entity_id`; cross-tenant test fails read | CLOSED |
| H09 over-exposure/privacy leakage risk in authority payload | Mitigated | Returned payload is minimal deterministic metric + revenue context, no PII fields | CLOSED |
| H10 P1 may not need provider boundary invocation | Validated | P1 closure achieved deterministically with placeholder non-authoritative explanation | VALIDATED |
| H11 minimal deterministic/placeholder explanation may be correct for P1 | Validated | `explanation_class=deterministic_placeholder` implemented | VALIDATED |
| H12 forcing P2/P3 in P1 would be scope drift | Validated | P2/P3 features intentionally deferred | VALIDATED |
| H13 existing patterns reusable | Validated | Reused existing route/test/contract enforcer patterns from B1.6/B0.7 governance surfaces | VALIDATED |
| H14 synthesis worker unsuitable as truth source | Validated | Authority source implemented as DB read service, not worker synthesis output | VALIDATED |
| H15 CI lacked non-vacuous mounted proof of P1 invariants | Confirmed pre-fix; closed post-fix | Added runtime P1 proof test file + CI job and upgraded enforcer | CLOSED |

## 4. Root Cause Summary
1. P0 mounted route existed but was intentionally non-operational (`503` placeholder) and had no deterministic DB authority read path.
2. No dedicated explanation-authority service existed, so deterministic read responsibility was structurally undefined.
3. Contract shape and CI enforcement were still keyed to P0 lock semantics (`x-skeldir-b17-p0`) and did not enforce P1 operational invariants.

## 5. Remediation Changes
### 5.1 Deterministic authority read chain
- Added `backend/app/services/attribution_explanation_authority.py`.
- Implemented tenant-scoped deterministic reads from:
  - `attribution_allocations` (entity-level metric authority)
  - `revenue_cache_entries` (deterministic revenue context)
- Implemented fail-closed exceptions:
  - `AttributionExplanationAuthorityNotFound`
  - `AttributionExplanationAuthorityUnavailable`

### 5.2 Mounted route operationalization
- Updated `backend/app/api/attribution.py` explain endpoint to:
  - require auth and tenant context
  - call authority service through injected DB session
  - return separated response contract
  - fail closed with `404 NOT_FOUND` and `503 DETERMINISTIC_AUTHORITY_UNAVAILABLE`

### 5.3 Schema and contract separation
- Updated `backend/app/schemas/attribution.py`:
  - `AttributionAuthoritativeMetric`
  - `AttributionNonAuthoritativeExplanation`
  - `AttributionExplanationResponse`
- Updated `api-contracts/openapi/v1/attribution.yaml`:
  - P1 lock extension `x-skeldir-b17-p1`
  - required truth sources and separation invariants
  - `200` response bound to `AttributionExplanationResponse`
- Regenerated `api-contracts/dist/openapi/v1/attribution.bundled.yaml`

### 5.4 CI and enforcement topology
- Reworked enforcer logic in `scripts/ci/enforce_b17_p0_explanation_surface_lock.py` to enforce P1 semantics.
- Updated tests in `backend/tests/test_b17_p0_explanation_surface_lock_enforcer.py` for P1 lock and fail-closed behavior.
- Updated contract tests:
  - `tests/contract/test_contract_semantics.py`
  - `tests/contract/test_route_fidelity.py`
- Added CI job `b17-p1-explanation-authority-proofs` in `.github/workflows/ci.yml` to run mounted runtime proof tests.

## 6. Falsifiable Runtime and Contract Evidence
### 6.1 Mounted runtime proofs (P1 invariants)
Command:
```powershell
$env:DATABASE_URL='postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/postgres'
$env:MIGRATION_DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres'
$env:CONTRACT_TESTING='0'
pytest backend/tests/test_b17_p1_explanation_authority_runtime.py -q
```
Result: **3 passed**
- DB-equal deterministic authority returned by canonical route
- authority/explanation structural separation asserted
- cross-tenant read denied
- missing deterministic revenue authority fails closed with `503`

### 6.2 P1 enforcer and negative controls
Command:
```powershell
pytest backend/tests/test_b17_p0_explanation_surface_lock_enforcer.py -q
python scripts/ci/enforce_b17_p0_explanation_surface_lock.py
```
Result: **7 passed** + enforcer `result=PASS`
- includes synthetic drift and runtime route removal negatives that must fail if invariant broken

### 6.3 Contract/runtime convergence
Command:
```powershell
pytest tests/contract/test_route_fidelity.py -q
pytest tests/contract/test_contract_semantics.py -q
```
Result: route fidelity **6 passed**; contract semantics **16 passed, 5 skipped**

## 7. Exit Gate Adjudication
| Exit Gate | Status | Proof |
|---|---|---|
| EG-P1.1 Mounted route-to-truth chain | PASS | canonical route calls explanation authority service and returns DB-equal authority in mounted test |
| EG-P1.2 Authority/explanation separation | PASS | response schema + runtime assertions enforce dual-object separation |
| EG-P1.3 Tenant/auth binding | PASS | cross-tenant negative control returns 404 |
| EG-P1.4 No-mock/no-scope-creep discipline | PASS | deterministic DB truth used; no synthetic authority numbers; no P2/P3 overreach |
| EG-P1.5 CI adjudication closure | PASS (repo topology updated) | CI job + enforcer + negative controls added for P1 invariants |

## 8. Known Environmental Note
Local `alembic upgrade head` against current workstation database failed with:
`Can't locate revision identified by '202602282000'`.
This was treated as an existing migration-chain environment issue outside the P1 code path; P1 runtime proofs were executed against existing schema state and passed.

## 9. Completion Checklist Against Directive
- canonical explanation route remains mounted: **Yes**
- authenticated request traverses route -> service -> DB deterministic chain: **Yes**
- authoritative metric payload equals DB-backed truth in seeded scenario: **Yes**
- response contract separates authoritative truth and non-authoritative explanation: **Yes**
- no explanation response without authoritative truth object: **Yes**
- tenant/user scope bound end-to-end and cross-tenant denied: **Yes**
- no mocked authoritative financial payloads introduced: **Yes**
- CI topology includes non-vacuous mounted P1 proofs: **Yes**
- merged to `main` with required checks green: **Pending commit/push/check adjudication in this run**

## 10. Pending Finalization Fields (to be filled post-push)
- Commit SHA:
- Main workflow run URL:
- Required checks status summary:
- Protected-branch merge evidence:
