# B2.4-P5 Remediation Evidence Pack

## Scope

B2.4-P5 is a bounded Bayesian runtime harness. It proves that PyMC, PyTensor, and ArviZ can run in an isolated worker lane with exact runtime identity, native thread caps, process/execution-scoped compiledirs, supervised sampler child termination, child DB/control-plane airgaps, bounded compiledir cleanup, and durable timeout fallback states.

It does not add public attribution APIs, TrustEnvelope output, frontend confidence logic, production attribution semantics, LLM authority, or deterministic-truth mutation.

## Follow-Up Corrective Findings

- The prior P5 evidence was not complete because the live protected branch did not require `B2.4-P5 Bayesian Runtime Harness`; the contract declared the context, but branch protection did not yet enforce it.
- Runtime identity was under-specified: PyTensor was resolver-owned and not directly locked by the Bayesian lane, so the real PyTensor version could drift without a falsifiable runtime check.
- The child sampler environment was not proven as an allowlist. A blacklist/copy-style child environment would allow DB URLs, cloud credentials, or control-plane settings to leak into native sampler children.
- Compiledir isolation was too coarse. Worker-scoped paths did not include parent PID and execution identity, and cleanup lacked a bounded, owner-marker-preserving reaper.
- The parent-death proof was not kernel-real. A supervisor timeout alone does not prove child death when the parent disappears by `SIGKILL`.
- The PyMC sampling policy needed explicit single-process enforcement, not just low default values.
- Timeout fallback proof was log-heavy and did not prove the actual worker helper could persist a tenant-scoped row transition in PostgreSQL.

## Remediations Made

- Pinned `pytensor==2.38.3` beside `pymc==5.28.5`, `arviz==0.23.4`, and `threadpoolctl==3.6.0`.
- Added `backend/app/bayesian/runtime_identity.py` to collect and assert Python 3.11, PyMC 5.28.5, PyTensor 2.38.3, ArviZ 0.23.4, `FAST_RUN`, `cvm`, and compiler presence.
- Reworked PyTensor compiledir policy to use `B24_PYTENSOR_ROOT` plus worker runtime ID, parent PID, and execution ID. Runtime caps now overwrite native thread env vars before scientific imports.
- Added `backend/app/bayesian/child_environment.py` and `backend/app/bayesian/sampler_child.py` for explicit child env allowlisting and DB/control-plane import denial.
- Added `backend/app/bayesian/compiledir_reaper.py` with owner metadata, child PID recording, active/foreign preservation, lock-guarded cleanup, and bounded scan/delete limits.
- Hardened `backend/app/bayesian/sampler_supervisor.py` with `close_fds`, Linux `PR_SET_PDEATHSIG`, process-group kill, Windows process-tree kill, child PID recording, and compiledir cleanup.
- Extended `backend/app/bayesian/runtime_probe.py` with child env/import airgap, compiledir lifecycle, bounded reaper, real parent-death, and behavioral negative-control probes.
- Added sync tenant-bound timeout persistence in `backend/app/bayesian/runtime_state.py` and wired `backend/app/tasks/bayesian.py` so worker fallback writes `public.bayesian_model_fits` when a fit ID is provided.
- Added PostgreSQL runtime proof coverage in `backend/tests/test_b24_p5_postgres_runtime.py` for durable timeout fallback and zero child Postgres sessions.
- Added `B2.4-P5 PostgreSQL Runtime Proof` CI job and declared both P5 contexts in the required-status contract/topology docs.
- Narrowed M2 static governance allowlists only for the new P5 runtime identity/environment files.

## Implemented Artifacts

- `backend/requirements-bayesian.txt`
- `backend/Dockerfile.bayesian`
- `backend/app/bayesian/runtime_policy.py`
- `backend/app/bayesian/runtime_identity.py`
- `backend/app/bayesian/child_environment.py`
- `backend/app/bayesian/sampler_child.py`
- `backend/app/bayesian/compiledir_reaper.py`
- `backend/app/bayesian/sampler_supervisor.py`
- `backend/app/bayesian/runtime_probe.py`
- `backend/app/bayesian/runtime_state.py`
- `backend/app/tasks/bayesian.py`
- `backend/tests/test_b24_p5_runtime_harness.py`
- `backend/tests/test_b24_p5_postgres_runtime.py`
- `.github/workflows/b2_4-gate-dry-run.yml`
- `scripts/ci/validate_b24_p5_runtime_harness.py`
- `scripts/ci/validate_m2_test_feedback_loop.py`
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- `docs/ci/ci_topology_map.md`

## Local Verification

- `python scripts\ci\validate_b24_p5_runtime_harness.py --negative-control`
  - Result: `B24_P5_RUNTIME_HARNESS_VALIDATION_PASS`
- `$env:PYTHONPATH='C:\Users\ayewhy\II SKELDIR II\backend'; python -m pytest backend\tests\test_b24_p5_runtime_harness.py -q`
  - Result: `10 passed`
- Disposable PostgreSQL proof:
  - Prepared `skeldir_b24_p5_ci` in `postgres:15-alpine`, ran `alembic upgrade head`, then executed `python -m pytest backend\tests\test_b24_p5_postgres_runtime.py -q -rA`.
  - Result: `2 passed`
  - Durable proof: worker fallback changed only the tenant A row to `status='timeout'`, `fallback_applied=true`, `fallback_reason='timeout'`, `runtime_seconds=7`; tenant B row with the same fit ID stayed `running`.
  - Child DB proof: sampler child completed with zero `pg_stat_activity` rows for `application_name='b24_p5_child_airgap'`.
- `python -m compileall -q backend\app\bayesian backend\app\tasks\bayesian.py backend\tests\test_b24_p5_runtime_harness.py backend\tests\test_b24_p5_postgres_runtime.py scripts\ci\validate_b24_p5_runtime_harness.py`
  - Result: pass
- `python scripts\ci\validate_m0_scope_lock.py --baseline-sha origin/main`
  - Result: `VERDICT: M0_SCOPE_LOCK_VALID`
- `python scripts\ci\validate_m1_local_dev_authority.py --baseline-sha origin/main`
  - Result: `VERDICT: M1_STATIC_VALID`
- `python scripts\ci\validate_m2_test_feedback_loop.py --baseline-sha origin/main`
  - Result: `VERDICT: M2_STATIC_VALID`
- `python scripts\ci\validate_m3_ci_governance.py --all`
  - Result: `M3_CI_GOVERNANCE_VALIDATION_PASS`
- `python scripts\guard_no_docker.py`
  - Result: `Zero Docker Doctrine check passed: no Docker references found.`

## Docker Runtime Verification

Image: `skeldir-b24-bayesian-worker:followup-p5`

- `docker build -f backend/Dockerfile.bayesian -t skeldir-b24-bayesian-worker:followup-p5 .`
  - Result: pass
- `runtime-report`
  - Result: pass; actual identity was Python `3.11.15`, PyMC `5.28.5`, PyTensor `2.38.3`, ArviZ `0.23.4`, compiler `/usr/bin/gcc`, PyTensor mode `FAST_RUN`, linker `cvm`, all native thread caps `1`.
- `pytensor-compile`
  - Result: pass; `linker=cvm`, `mode=FAST_RUN`, compiled value `[3.0, 5.0, 7.0]`.
- `thread-budget`
  - Result: pass; OpenBLAS reported `num_threads=1`.
- `child-env-airgap` with fake `DATABASE_URL`, fake parent secret, and fake AWS secret
  - Result: pass; child env keys were only the explicit allowlist and `forbidden_env_present=[]`.
- `child-import-airgap`
  - Result: pass; child blocked `sqlalchemy`, `asyncpg`, `psycopg`, `psycopg2`, `celery`, config/secrets/DB modules, Bayesian ORM/runtime-state modules, and `app.tasks`.
- `compiledir-lifecycle`
  - Result: pass; child was killed by supervisor timeout, return code `-9`, and owned compiledir was cleaned.
- `reaper-probe`
  - Result: pass; expired owned compiledir deleted, foreign compiledir preserved.
- `compiledir-concurrency`
  - Result: pass; two isolated PyTensor compiles completed under the execution-scoped root.
- `tiny-benchmark`
  - Result: pass; real PyMC sample used `chains=1`, `cores=1`, `blas_cores=1`, `multiprocessing_policy='single-process'`, and finished under the `60.0s` threshold.
- `supervisor-kill`
  - Result: pass; blocking child killed with return code `-9`, `orphan_reaped=true`.
- `parent-death`
  - Result: pass; observer killed the real parent with `SIGKILL`, verified `child_dead=true`, then reaped the owned compiledir.
- `behavioral-negative-controls`
  - Result: pass; PyMC parallelism and static compiledir controls failed closed.

## Negative Controls

The P5 validator fails closed for:

- missing native thread cap
- missing process group creation
- missing real PyMC benchmark call
- missing durable timeout write
- missing tenant GUC binding
- missing child env allowlist
- missing runtime identity lock
- missing bounded compiledir reaper

The runtime probe fails closed for:

- PyMC `cores>1` / multi-process sampling policy
- static/non-scoped PyTensor compiledir
- child DB/control-plane imports
- parent-death child survival
- foreign compiledir deletion

## Boundary Proof

- No P5 public route or router registration was added.
- No frontend Bayesian confidence threshold logic was added.
- No LLM/provider import was added to the Bayesian runtime path.
- Timeout and stale-running repair write only to `public.bayesian_model_fits`.
- Sampler children receive a positive env allowlist and no DB URL, cloud secret, or app secret keys.
- Child entrypoint blocks DB/control-plane imports even if future code attempts them.
- Runtime identity and scientific imports occur after native thread and compiledir policy is applied.

## Protected-Branch Proof

- Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/512`
- Final PR head: `6b982022d84668dd78cdf942b796392376bbce14`
- Final PR merge state before merge: `CLEAN`
- Live branch-protection required-status verification included both P5 contexts before merge:
  - `B2.4-P5 Bayesian Runtime Harness`
  - `B2.4-P5 PostgreSQL Runtime Proof`
- Required PR checks were green before merge, including:
  - `B2.4-P5 Bayesian Runtime Harness`
  - `B2.4-P5 PostgreSQL Runtime Proof`
  - `B2.4 Gate Dry Run`
  - `B2.3 Composite Proof Harness`
  - `B0.7 P2 Runtime Proof (LLM + Redaction)`
  - `Celery Foundation B0.5.1`
  - `Phase 8 Regression Gate (Full Physics)`
  - `m0-maintainability-scope-lock`
- Merge method: protected GitHub PR squash merge.
- Main merge commit: `85d7789b2bfff6847721ae2ea428733f83969747`
- Merge timestamp: `2026-06-01T18:50:06Z`
- Landed verification: after `git fetch origin main --prune`, `origin/main` resolved to `85d7789b2bfff6847721ae2ea428733f83969747`.

## Main CI Proof

Post-merge main CI was polled for commit `85d7789b2bfff6847721ae2ea428733f83969747` until every listed workflow run completed with a non-failing conclusion. The final poll returned:

- `MAIN_RUNS total=33 pending=0 bad=0`

Representative successful main run IDs:

- `CI`: `26775040053`
- `B2.4 Gate Dry Run`: `26775040018`
- `M0 Maintainability Scope Lock`: `26775039998`
- `M1 Local Development Authority`: `26775040013`
- `M2 Test Feedback Loop`: `26775039980`
- `M3 CI Governance`: `26775040025`
- `R7: Final Winning State`: `26775039985`
- `Phase 8 Regression Gate (Full Physics)`: `26775040011`
- `Empirical Validation - Directive Compliance`: `26775040050`

## Final Non-Overclaim

This pack proves follow-up P5 runtime remediation, live protected-branch required-context enforcement, protected PR merge to `main`, landed-main verification, and one full non-failing post-merge main CI pass. It still does not claim production deployment, full B2.4 business attribution semantics, TrustEnvelope delivery, or LLM authority expansion.
