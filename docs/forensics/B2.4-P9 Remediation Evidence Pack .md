# B2.4-P9 Remediation Evidence Pack

Date opened: 2026-06-08
Scope: `B2.4-P9 - Worker Tenant Hygiene and Process Isolation`
Authoritative branch: `main`
Authoritative merge: PR #527, merged 2026-06-08T15:32:33Z
Implementation PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/527`
Implementation branch: `codex/b24-p9-worker-tenant-hygiene-main-20260608`
Implementation head commit: `612887cfa56d06f5e01371d687bbf3464a5cb05d`
Landed main merge commit: `a866f6d088b251558b8a3d4552bc6a1119965129`

## Executive Verdict

**PASS - AUTHORITATIVE MAIN VALIDATION COMPLETE**

B2.4-P9 worker tenant hygiene remediation landed on `main` through protected PR #527 after required checks passed. The landed merge commit `a866f6d088b251558b8a3d4552bc6a1119965129` subsequently completed a full green main workflow set:

1. PR checks: green; `mergeStateStatus=CLEAN` before merge.
2. Live branch-protection contract enforcement: pass with `B2.4-P9 Worker Tenant Hygiene Proof` listed as required.
3. Main push workflows for landed commit: 36/36 completed with `success`.
4. Main `B2.4-P9 Worker Tenant Hygiene Proof`: success in run `27148605741`, job `80133211017`.

## Follow-Up Corrective Action XVI - 2026-06-21

**PASS - DIRECTIVE XVI RAW DRIVER DATABASE BOUNDARY AND MAIN CI VALIDATION COMPLETE**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive XVI`.

Directive XVI tested the remaining raw database-boundary hypothesis: prior P9 evidence proved cooperative application paths, SQLAlchemy Core paths, schema declarations, and live scheduler recovery, but did not independently prove that a raw runtime-role PostgreSQL client could not bypass RLS, FORCE RLS, dispatch fences, stale lease checks, or direct SECURITY DEFINER function guards.

### Corrective Action XVI Hypothesis Results

| Hypothesis | Classification | Finding |
|---|---|---|
| H-XVI-01 raw psycopg hostile SQL proof is absent | VALIDATED THEN REMEDIATED | Added `backend/tests/test_b24_p9_raw_driver_postgres_runtime.py::test_b24_p9_directive_xvi_raw_psycopg_runtime_role_rejects_hostile_sql`, using `psycopg2.connect(_runtime_dsn())` and raw `cur.execute(...)` only. |
| H-XVI-02 raw asyncpg hostile SQL proof is absent | VALIDATED THEN REMEDIATED | Added `test_b24_p9_directive_xvi_raw_asyncpg_representative_hostile_writes`, using direct `asyncpg.connect(async_dsn)` representative missing-context and wrong-tenant attacks. |
| H-XVI-03 SQLAlchemy Core proof was being treated as raw proof | VALIDATED THEN REMEDIATED | Added a separate raw-driver test file with no SQLAlchemy imports and extended the P9 validator to fail on `from sqlalchemy`, `import sqlalchemy`, `create_engine`, `Session`, `text(`, or dispatch helper substitutions in the raw proof file. |
| H-XVI-04 ORM/helper bypass had not been tested | VALIDATED THEN REMEDIATED | Hostile attack SQL bypasses repository methods, Sessions, tenant helpers, dispatch claim helpers, and artifact helpers; setup and attacks run through raw cursor SQL or direct SECURITY DEFINER calls. |
| H-XVI-05 through H-XVI-07 runtime role hygiene was assumed outside hostile proof | VALIDATED THEN REMEDIATED | The raw psycopg proof asserts `current_user`, `rolsuper=false`, `rolbypassrls=false`, not migration-owner member, no protected table ownership, and no inherited BYPASSRLS role before hostile writes execute. |
| H-XVI-08 through H-XVI-17 raw hostile writes could mutate protected state | VALIDATED THEN REMEDIATED | Raw tests attack missing-context inserts/updates, wrong-tenant updates, artifact writes, dispatch/recovery outbox writes, direct DELETE, stale-lease updates after recoverable failure ACK, stale recovery claims, forged process token, wrong PID, and wrong payload/task semantics. Expected outcomes are PostgreSQL errors or `UPDATE 0`. |
| H-XVI-18 through H-XVI-21 SECURITY DEFINER abuse proof was absent | VALIDATED THEN REMEDIATED | Raw direct calls to `b24_claim_fit_dispatch`, `b24_mark_fit_dispatch_running`, `b24_complete_fit_dispatch`, `b24_fail_fit_dispatch_terminal`, and `b24_fail_fit_dispatch_recoverable` are tested with forged, missing, or stale context. Recovery wake-up remains a bounded reconciler surface, not an arbitrary terminal/artifact mutation surface. |
| H-XVI-22 through H-XVI-24 schema-only RLS/FORCE/trigger proof was insufficient | VALIDATED THEN REMEDIATED | Runtime raw attacks now pair with existing schema validator requirements for RLS/FORCE RLS and dispatch fence triggers. |
| H-XVI-25 through H-XVI-29 CI could pass without raw proof or with evidence overclaim | VALIDATED THEN REMEDIATED | `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py` now requires the raw file, raw driver connect calls, role hygiene assertions, direct SECURITY DEFINER abuse tokens, asyncpg proof, workflow wiring, and negative controls for raw-file removal, SQLAlchemy substitution, role-hygiene removal, asyncpg removal, and workflow omission. |

### Corrective Action XVI Remediations Made

1. Raw psycopg runtime-role proof
   - Added `backend/tests/test_b24_p9_raw_driver_postgres_runtime.py`.
   - Uses `psycopg2.connect(_runtime_dsn())` against the same runtime DSN used by P9 runtime proofs.
   - Uses raw cursor execution only; no SQLAlchemy, ORM Session, repository, tenant helper, dispatch helper, or artifact helper is imported in the raw proof file.
   - Proves missing-context inserts/updates/deletes fail or mutate zero rows.
   - Proves wrong-tenant context cannot mutate tenant A state.
   - Proves stale lease/fence context fails after recoverable failure ACK revokes the old lease.
   - Proves forged process token, wrong PID, wrong payload, and stale recovery generation fail closed before lease minting.

2. Runtime role hygiene proof
   - Queries `pg_roles`, `pg_auth_members`, and `pg_class` in the same raw psycopg proof context.
   - Requires `current_user` to equal `EXPECTED_RUNTIME_DB_USER` or `app_user`.
   - Requires `rolsuper=false`, `rolbypassrls=false`, no migration-owner membership, no protected table ownership, and no inherited BYPASSRLS membership.

3. Raw asyncpg representative proof
   - Adds direct `asyncpg.connect(async_dsn)` coverage.
   - Proves missing-context asyncpg insert is rejected.
   - Proves wrong-tenant asyncpg update returns `UPDATE 0`.

4. CI non-vacuity hardening
   - Added `P9_RAW_DB_TESTS` to the validator required file set.
   - Added raw proof token requirements and forbidden helper-mediated tokens.
   - Added XVI negative controls for raw file removal, SQLAlchemy substitution, role hygiene removal, asyncpg removal, and workflow omission.
   - Updated `.github/workflows/b2_4-gate-dry-run.yml` so `B2.4-P9 Worker Tenant Hygiene Proof` runs `backend/tests/test_b24_p9_raw_driver_postgres_runtime.py`.

### Corrective Action XVI Proof Tier Matrix

| Tier | Status | Evidence |
|---|---|---|
| Live Beat proof | PRESERVED | `test_b24_p9_directive_xv_live_beat_drives_failure_ack_recovery` remains required by validator and workflow. |
| Beat-driven failure-ACK proof | PRESERVED | XV and XIV runtime tests remain required. |
| SQLAlchemy Core proof | PRESERVED BUT NOT RAW | Existing `test_b24_p9_postgres_runtime.py` coverage remains useful but is no longer claimed as raw driver proof. |
| Raw psycopg proof | ADDED | `test_b24_p9_directive_xvi_raw_psycopg_runtime_role_rejects_hostile_sql`. |
| Raw asyncpg proof | ADDED | `test_b24_p9_directive_xvi_raw_asyncpg_representative_hostile_writes`. |
| Runtime role hygiene | ADDED | `_assert_runtime_role_hygiene(...)` in raw psycopg test. |
| RLS/FORCE RLS schema proof | PRESERVED | Existing migration/schema validator tokens remain required. |
| Raw RLS enforcement proof | ADDED | Missing-context and wrong-tenant raw psycopg/asyncpg writes. |
| Trigger enforcement proof | ADDED | Raw artifact/fit writes and stale lease writes exercise dispatch fence rejection. |
| SECURITY DEFINER abuse proof | ADDED | Direct raw calls to claim/running/complete/fail functions under forged, missing, or stale authority. |
| Model A shared recovery | PRESERVED | XIII tests remain required. |
| Secret-free transport | PRESERVED | Existing broker payload and recovery publication validators remain required. |
| Legacy path closure | PRESERVED | Existing P9 validator and unit tests remain required. |

### Corrective Action XVI Local Validation

```text
python -m py_compile backend/tests/test_b24_p9_raw_driver_postgres_runtime.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python -m black --check backend/tests/test_b24_p9_raw_driver_postgres_runtime.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend/tests/test_b24_p9_raw_driver_postgres_runtime.py --collect-only -q
python -m pytest backend/tests/test_b24_p9_raw_driver_postgres_runtime.py -q -rA
git diff --check
```

Local note: raw runtime tests are intentionally skipped outside protected CI unless `SKELDIR_B24_P9_REQUIRE_DB_PROOFS=1` is set. Protected GitHub Actions provisions Postgres and runs the B2.4-P9 proof with that flag.

### Corrective Action XVI Protected Workflow Evidence

- Implementation branch: `codex/b24-p9-directive-xvi-raw-driver`.
- Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/559`.
- Corrective raw-proof validation commit: `6880d0c60c935e8ea0b6da0b347196a82316c2e5`.
- Corrective PR `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27912594017/job/82592189322`, `success`.
- Corrective PR B2.4 gate dry run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27912594017/job/82592189287`, `success`.
- Final corrective PR head SHA: `024ef28d54a81bed8ca58be260bb3a76100fc075`.
- Final corrective PR merge state before merge: `mergeStateStatus=CLEAN`.
- Corrective merge time: `2026-06-21T18:21:04Z`.
- Corrective merge commit / final XVI main SHA: `2033fd4896eb848423d996dc824ad25c41af7941`.
- Final main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27913349870/job/82594206324`, `success`.
- Final main B2.4 gate dry run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27913349870/job/82594206317`, `success`.
- Final main workflow census for merge commit `2033fd4896eb848423d996dc824ad25c41af7941`: 27/27 workflows completed with `success`; 0 active; 0 failed.

### Corrective Action XVI Non-Overclaim Boundary

This evidence iteration claims only the raw-driver database enforcement boundary, runtime role hygiene proof, direct SECURITY DEFINER abuse proof for claim/running/complete/fail functions, asyncpg representative hostile write proof, CI non-vacuity for raw proof, and preservation of prior P9 live scheduler/recovery/secret-free/legacy controls. It does not claim new Bayesian model correctness, new production attribution accuracy, or that the recovery wake-up reconciler is a tenant-fenced artifact/terminal mutation surface; that reconciler remains bounded by the live Beat and durable recovery proofs.

## Follow-Up Corrective Action XV - 2026-06-21

**PASS - DIRECTIVE XV LIVE BEAT SCHEDULER LIVENESS AND MAIN CI VALIDATION COMPLETE**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive XV`.

Directive XV tested the residual scheduler-risk left by prior P9 follow-ups: recovery was production-wired, but the proof surface could still overclaim if it relied on direct `send_task(...)` calls or static Beat registration rather than a real Beat subprocess emitting the recovery reconciler onto the Postgres broker.

### Corrective Action XV Hypothesis Results

| Hypothesis | Classification | Finding |
|---|---|---|
| H-XV-01 live Beat scheduler liveness was not physically proven | VALIDATED THEN REMEDIATED | Existing broker recovery proof used direct `celery_app.send_task(RECOVERY_RECONCILER_TASK_NAME, ...)`; XV added a real `celery beat` subprocess proof and correlates the executed reconciler task id back to a Postgres broker message emitted after a scheduler high-water mark. |
| H-XV-02 worker-only activity could be mistaken for scheduler liveness | VALIDATED THEN REMEDIATED | The live proof now holds the worker absent while Beat writes the reconciler message, verifies no recovery occurs without a worker, then restarts a real Bayesian worker to consume Beat-origin recovery. |
| H-XV-03 disabled scheduler negative control could be confounded by residual broker messages | VALIDATED THEN REMEDIATED | The first XV merge exposed this on main: the disabled-schedule negative control started a worker that could consume a residual reconciler task. PR #557 removed that worker path and made the negative control scheduler-scoped: disabled Beat must emit zero reconciler messages after the broker high-water mark and the DB row must remain `failed_retryable` with zero recovery rows. |
| H-XV-04 evidence scope could overclaim direct scheduler proof from static registration | VALIDATED THEN REMEDIATED | Static/meta tests now require the XV live Beat proof, the disabled Beat negative control, broker high-water correlation, and absence of direct recovery `send_task(...)` inside the live Beat test body. |

### Corrective Action XV Remediations Made

1. Live Beat scheduler runtime proof
   - Added `test_b24_p9_directive_xv_live_beat_drives_failure_ack_recovery`.
   - Starts a real Bayesian worker to create a recoverable failure ACK, then terminates the worker.
   - Starts a real `celery beat` subprocess with `--schedule` and `B24_P9_RECOVERY_RECONCILE_INTERVAL_SECONDS=1`.
   - Captures `public.kombu_message` rows on `QUEUE_BAYESIAN` after a broker high-water mark and requires `RECOVERY_RECONCILER_TASK_NAME` on that queue.
   - Proves Beat alone does not mutate recovery state while no worker is available.
   - Restarts a real Bayesian worker and requires `bayesian_recovery_reconciler_completed` plus `bayesian_fit_intent_executed` for the affected dispatch, with the recovery reconciler task id correlated to a scheduler-origin broker row.

2. Scheduler-disabled negative control
   - Added `test_b24_p9_directive_xv_disabled_beat_schedule_blocks_recovery`.
   - Starts Beat with `SKELDIR_B24_P9_DISABLE_RECOVERY_RECONCILER_JOB=1`.
   - Captures a broker high-water mark and asserts no reconciler broker message is emitted after that mark.
   - Asserts the seeded `failed_retryable` dispatch remains `failed_retryable` with zero recovery rows and no reconciler probe event for the dispatch.

3. Evidence-scope and negative-control hardening
   - Added `_beat_env(...)`, `_wait_for_broker_task_messages(...)`, and `_max_broker_message_id(...)` helpers.
   - Extended `test_b24_p9_directive_xv_live_beat_proof_is_runtime_scoped`.
   - Extended `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py` with XV tokens and negative-control mutations.

### Corrective Action XV Local Validation

The following local validations passed before opening PR #556 and the main-CI corrective PR #557:

```text
python -m py_compile backend/tests/test_b24_p9_postgres_runtime.py backend/tests/test_b24_p9_worker_tenant_hygiene.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python -m black --check backend/tests/test_b24_p9_postgres_runtime.py backend/tests/test_b24_p9_worker_tenant_hygiene.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend/tests/test_b24_p9_worker_tenant_hygiene.py -q -rA
python -m pytest backend/tests/test_b24_p9_postgres_runtime.py --collect-only -q
python -m pytest backend/tests/test_b24_p9_postgres_runtime.py -q -rA
git diff --check
```

Local note: `backend/tests/test_b24_p9_postgres_runtime.py` remains intentionally opt-in outside protected CI and skipped locally when `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is not set. GitHub Actions provisions Postgres and sets `SKELDIR_B24_P9_REQUIRE_DB_PROOFS=1`.

### Corrective Action XV Protected Workflow Evidence

- Implementation PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/556`.
- Implementation PR head SHA: `77fda4f6e20fb94e7d5625b800e37635e131d39f`.
- Implementation PR merge status before merge: `mergeStateStatus=CLEAN`.
- Implementation merge time: `2026-06-21T13:12:14Z`.
- Implementation merge commit: `975e2c651f437abfb375454afd330ba58a921ee3`.
- Implementation PR B2.4 gate run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27904950080`.
- Implementation PR P9 proof job: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27904950080/job/82571856998`.
- Main push after PR #556 falsified the disabled-schedule negative control in `B2.4-P9 Worker Tenant Hygiene Proof`, proving the worker-in-negative-control confounder.
- Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/557`.
- Corrective PR head SHA: `298250dd0365ac22dc7be4075d07fe3a291b1263`.
- Corrective PR merge status before merge: `mergeStateStatus=CLEAN`.
- Corrective merge commit / final XV code main SHA: `b501dae0e9908de5b1fc93530c4d16e56c965687`.
- Final main workflow poll for `b501dae0e9908de5b1fc93530c4d16e56c965687`: 26 workflows, 26 `success`, 0 active, 0 failed.
- Final main `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27906479620`.
- Final main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27906479620/job/82575933154`.
- Final main `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27906479608`.
- Final main `Empirical Validation - Directive Compliance`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27906479654`.

### Corrective Action XV Non-Overclaim Boundary

This evidence iteration claims only B2.4-P9 Directive XV scheduler liveness, Beat-driven failure-ACK recovery, scheduler-disabled negative control, and evidence-scope correction. It does not claim new production Bayesian attribution correctness, P10 completion, or a change to the P9 privacy/tenant-hygiene invariants outside the tested scheduler and recovery proof surface.

## Follow-Up Corrective Action XIV - 2026-06-20

**PASS - DIRECTIVE XIV FAILURE-ACK RECOVERY IMPLEMENTATION AND MAIN CI VALIDATION COMPLETE**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive XIV`.

Corrective implementation branch: `codex/b24-p9-directive-xiv-failure-ack`.

Corrective implementation PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/554`.

Corrective implementation head SHA: `c0c89e30bef3c8b4596b45471e135a9df697b6d8`.

Corrective implementation main merge commit: `4bfaf02cfb379ac4ddd0684104292a5c0575627b`.

Corrective implementation merged at: `2026-06-20T19:32:28Z`.

Directive XIV tested the residual risk left explicit in Directive XIII: a broker-visible worker could acknowledge a recoverable infrastructure failure but still leave stale lease/assignment authority alive, or fail terminally instead of returning the fit to durable recovery. The hypothesis was validated and remediated.

### Corrective Action XIV Hypothesis Results

| Hypothesis | Classification | Finding |
|---|---|---|
| H-XIV-01 failure-ACK path could terminalize recoverable worker infrastructure failures | VALIDATED THEN REMEDIATED | The worker infrastructure-failure branch used terminal failure semantics. It now calls `b24_fail_fit_dispatch_recoverable(...)` and returns `failed_retryable` until max attempts are exhausted. |
| H-XIV-02 failure-ACK could leave stale dispatch authority alive | VALIDATED THEN REMEDIATED | Recoverable failure ACK now clears lease fields, clears `assigned_worker_generation`, expires assignment authority, increments assignment generation, and marks `assignment_reason='failure_ack_recovery_required'`. |
| H-XIV-03 stale SQL after failure ACK could still write fit state | REFUTED AFTER REMEDIATION | Runtime proof verifies stale fenced SQL raises `b24_dispatch_fence_rejected` after recoverable failure ACK revokes the old lease. |
| H-XIV-04 failure-ACK recovery lacked broker-backed proof | VALIDATED THEN REMEDIATED | Added a real Celery worker subprocess proof using `app.tasks.bayesian.probe_recoverable_failure_ack`, Postgres broker transport, no-worker negative phase, and recovery reconciler wake-up publication. |
| H-XIV-05 recovery publication evidence could lose causal task id | VALIDATED THEN REMEDIATED | Recovery publication now records successful `published_task_id` values and reconciler probe events expose `recovery_published_task_ids`; failed attempts remain visible without being counted as published. |
| H-XIV-06 stale publishing quarantine could regress from task-id tracking | REFUTED AFTER REMEDIATION | Publisher helpers return attempted rows for quarantine accounting while counting only rows with `published_task_id` as published. |
| H-XIV-07 canonical schema could drift from migration truth | VALIDATED THEN REMEDIATED | Canonical schema was updated to match the migration-derived normalized function body; protected Schema Validation passed on PR and main. |

### Corrective Action XIV Remediations Made

1. Recoverable failure-ACK database primitive
   - Added Alembic revision `202606201430_b24_p9_directive_xiv_failure_ack_recovery.py`.
   - Added `public.b24_fail_fit_dispatch_recoverable(text)` to transition recoverable worker failures to `failed_retryable`, clear stale lease/assignment authority, and make recovery immediately due.
   - The function terminalizes only when `claim_count >= max_attempts`.

2. Worker execution failure semantics
   - `execute_fit_intent_sync(...)` now routes infrastructure child failures through `fail_dispatch_recoverable_sync(...)` instead of terminal fit failure.
   - Recoverable failure payloads report `status='failed_retryable'` and preserve the broker-independent dispatch authority boundary.

3. Broker-visible failure-ACK proof surface
   - Added `app.tasks.bayesian.probe_recoverable_failure_ack`.
   - Added the task to `REQUIRED_BAYESIAN_TASK_NAMES`.
   - The probe task claims the same DB dispatch authority as real fit execution, marks the dispatch running, acknowledges a recoverable failure, and emits `bayesian_recoverable_failure_ack_probe` with task, dispatch, fit, worker generation, and worker PID evidence.

4. Recovery publication causal evidence
   - `RecoveryOutboxRow` now carries `published_task_id`.
   - `publish_due_recovery_rows(...)` and sync equivalent return attempted rows and annotate successful rows with the Celery task id.
   - `reconcile_fit_recovery_wakeups(...)` emits `recovery_published_task_ids` and counts only successfully published rows.

5. Runtime proof expansion
   - Added `test_b24_p9_directive_xiv_failure_ack_revokes_stale_authority`.
   - Added `test_b24_p9_directive_xiv_broker_backed_failure_ack_recovery`.
   - Extended the static validator and canonical schema authority.

### Corrective Action XIV Proof Tiers

| Proof tier | Coverage | Evidence |
|---|---|---|
| Static wiring proof | failure-ACK task registration, required task set, migration, canonical schema, validator negative controls | `validate_b24_p9_worker_tenant_hygiene.py --negative-control` |
| Database state-machine proof | recoverable ACK clears lease/assignment, revokes stale fence, queues fit for recovery, prevents stale duplicate claim | `test_b24_p9_directive_xiv_failure_ack_revokes_stale_authority` |
| Broker-backed worker proof | no-worker negative phase, Postgres broker transport, real Celery worker subprocess, failure-ACK task, recovery reconciler, replacement fit execution | `test_b24_p9_directive_xiv_broker_backed_failure_ack_recovery` |
| Regression proof | prior XI stale-publishing quarantine remains intact after task-id tracking | `test_b24_p9_directive_xi_stale_publishing_recovery_quarantines` |

### Corrective Action XIV Local Validation

Passed locally before protected PR merge:

```text
python -m py_compile backend/app/tasks/bayesian.py backend/app/bayesian/dispatch_authority.py backend/app/bayesian/dispatch_outbox.py backend/app/bayesian/fit_execution.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py alembic/versions/007_skeldir_foundation/202606201430_b24_p9_directive_xiv_failure_ack_recovery.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m black --check backend/app/tasks/bayesian.py backend/app/bayesian/dispatch_outbox.py backend/tests/test_b24_p9_postgres_runtime.py
python -m pytest backend/tests/test_b24_p9_worker_tenant_hygiene.py -q -rA
python -m pytest backend/tests/test_b24_p9_postgres_runtime.py -q -rA
git diff --check
```

Focused local results:

- P9 validator negative controls: pass.
- P9 worker tenant hygiene suite: 32 passed.
- P9 protected Postgres runtime proof file: 23 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is opt-in outside CI; import/collection succeeded.

### Corrective Action XIV Protected Validation

Protected branch validation completed through PR #554 and main push validation on 2026-06-20.

PR validation before merge:

- PR head: `c0c89e30bef3c8b4596b45471e135a9df697b6d8`.
- PR `mergeStateStatus`: `CLEAN`.
- PR B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881190817`.
- PR `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881190817/job/82508931811`.
- PR Schema Validation: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881190830`.
- PR CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881190833`.

Protected merge validation:

- Merge route: GitHub server-side squash merge API; no admin bypass.
- Merged PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/554`.
- Landed main commit: `4bfaf02cfb379ac4ddd0684104292a5c0575627b`.
- `origin/main` contains the merge commit.

Main validation after merge:

- Main workflow census for `4bfaf02cfb379ac4ddd0684104292a5c0575627b`: 35 total, 35 success, 0 skipped/neutral, 0 failed.
- Main CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881619615`.
- Main B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881619610`.
- Main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881619610/job/82510072804`.
- Main Schema Validation: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881619606`.
- Main R7 Final Winning State: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881619632`.
- Main Production Schema Deployment: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27881642745`.

### Corrective Action XIV Non-Claims

- This remediation does not introduce broker-carried execution authority. Broker messages remain wake-ups only.
- This remediation does not claim transaction-pooler or statement-pooler support; Directive VIII's direct/session-affine PostgreSQL constraint remains in force.
- This remediation does not claim live Beat runtime execution; it proves broker-backed task publication/consumption and preserves the production Beat registration proof from Directive XI.
- This section supersedes the Directive XIII residual non-claim `H-XIII-12 failure-ACK recovery physically unproven`.

## Follow-Up Corrective Action XIII - 2026-06-20

**PASS - DIRECTIVE XIII IMPLEMENTATION AND MAIN CI VALIDATION COMPLETE**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive XIII`.

Corrective implementation branch: `codex/b24-p9-directive-xiii-broker-liveness`.

Corrective implementation PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/552`.

Corrective implementation head SHA: `6dee00676100aefe46ceb427c2eaeb3e51f42879`.

Corrective implementation main merge commit: `73b77115091843157e88bf8eb35f63e6103886e9`.

Corrective implementation merged at: `2026-06-20T13:59:53Z`.

Directive XIII tested whether Directive XI still had two final physics defects: stale-authority rejection was conflated with worker identity blacklisting, and recovery liveness was proven at helper/task-body level rather than through a real broker-backed Celery worker boundary.

### Corrective Action XIII Chosen Model

Directive XIII permits either Model A shared eligible-worker recovery or Model B specific replacement assignment. This remediation chooses **Model A - shared eligible worker recovery**, because recovered dispatch wake-ups are delivered through the shared `bayesian` Celery queue.

The new invariant is:

- Old authority is rejected: stale `attempt_id`, stale `recovery_generation`, forged process token, wrong task/payload/dispatch, expired assignment, and active duplicate lease.
- Worker identity is not blacklisted by default: a live authenticated Bayesian process may claim recovered work when the recovered row is marked `recovery_shared_eligible`.
- Initial/non-recovery dispatches still require a specific `assigned_worker_generation`; `NULL` assignment is accepted only for current recovered rows.

### Corrective Action XIII Hypothesis Results

| Hypothesis | Classification | Finding |
|---|---|---|
| H-XIII-01 prior directive required prior-generation identity rejection | VALIDATED THEN REMEDIATED | Directive XI tests treated prior-generation rejection as required. Tests now distinguish stale old attempt/recovery authority from current authenticated worker authority. |
| H-XIII-02 current recovery model blacklists previous physical worker | VALIDATED THEN REMEDIATED | Recovery publication previously stamped a specific replacement generation. Recovered dispatches now use `assignment_reason='recovery_shared_eligible'` and no named generation. |
| H-XIII-03 same-worker recovery is safe with fresh authority | REFUTED AFTER REMEDIATION | Runtime proof accepts the prior active generation when it presents the current attempt/recovery generation and DB-verified process token, while stale material remains `UNAUTHORIZED`. |
| H-XIII-04 wrong-generation rejection can strand shared-queue work | VALIDATED THEN REMEDIATED | Model B was unsafe without wrong-consumer requeue proof. Model A removes the wrong-generation class for recovered work by making any authenticated Bayesian worker eligible. |
| H-XIII-05 tests conflate stale authority and prior identity | VALIDATED THEN REMEDIATED | New runtime proof separately checks stale claim rejection, forged-token rejection, current eligible claim acceptance, duplicate `ACTIVE_LEASE`, and post-completion collapse. |
| H-XIII-06 eager mode cannot prove production liveness | VALIDATED THEN REMEDIATED | Broker-backed proof asserts `task_always_eager is False` before publishing the recovery task. |
| H-XIII-07 current proof remains helper/in-process level | VALIDATED THEN REMEDIATED | Added a real Celery worker subprocess proof that consumes `app.tasks.bayesian.reconcile_fit_recovery_wakeups` through the configured broker. |
| H-XIII-08 routing errors could pass current proof | VALIDATED THEN REMEDIATED | Broker proof publishes to `QUEUE_BAYESIAN` and requires a worker subscribed to the configured `bayesian` queue to progress. |
| H-XIII-09 worker absence could pass current proof | VALIDATED THEN REMEDIATED | Broker proof first publishes with no worker and verifies no recovery progress, then starts the worker and observes progress. |
| H-XIII-10 broker outage could pass current proof | VALIDATED THEN REMEDIATED | The broker proof uses the configured Postgres/SQLAlchemy Celery broker URL and fails if broker publish/consume is unavailable. |
| H-XIII-11 lost original message recovery physically unproven | VALIDATED THEN REMEDIATED | Test seeds durable recoverable dispatch state without publishing the original fit wake-up, then proves broker-backed recovery publishes and consumes the replacement path. |
| H-XIII-12 failure-ACK recovery physically unproven | PARTIAL | Durable recovery state-machine and stale-publishing release remain proven; the new broker proof covers replacement recovery consumption but does not separately force a broker-level failure-ACK scenario. |
| H-XIII-13 unauthorized recovery consumer handling unproven | REFUTED BY MODEL CHANGE | Under Model A, a valid Bayesian worker is not unauthorized solely by generation. Forged tokens and unassigned non-recovery rows still fail closed. |
| H-XIII-14 duplicate recovery publication through broker unproven | PARTIAL | Duplicate direct-claim collapse remains proven via DB state-machine tests; broker-delivered duplicate wake-ups are not newly exercised. |
| H-XIII-15 broker-backed quarantine unproven | PARTIAL | Quarantine remains proven through direct stale-publishing failure tests; this remediation does not overclaim broker-backed repeated publisher failure. |
| H-XIII-16 CI could pass with eager mode enabled | REFUTED AFTER REMEDIATION | Broker test explicitly sets and asserts eager mode disabled; static validator requires those assertions. |
| H-XIII-17 CI could pass with broker offline | REFUTED AFTER REMEDIATION | Broker test requires `send_task` over a Postgres broker and worker consumption from that broker. |
| H-XIII-18 CI could pass with no worker | REFUTED AFTER REMEDIATION | No-worker negative phase observes no progress before the subprocess worker starts. |
| H-XIII-19 CI could pass with broken recovery routing | REFUTED AFTER REMEDIATION | The proof observes the worker ready on `bayesian` and requires recovery/fit probe events after publishing to that queue. |
| H-XIII-20 wrong-generation ACK/drop could strand work | REFUTED BY MODEL CHANGE | Recovered work is shared-eligible, so a live authenticated Bayesian consumer is allowed to claim; duplicate/active lease protection remains DB-enforced. |
| H-XIII-21 evidence may overclaim production liveness | VALIDATED THEN REMEDIATED | This evidence separates static wiring, DB state-machine proof, broker-backed worker proof, and residual non-claims. |
| H-XIII-22 evidence may overclaim prior-generation rejection | VALIDATED THEN REMEDIATED | This section declares Model A and explicitly rejects stale authority rather than worker identity. |

### Corrective Action XIII Remediations Made

1. Shared eligible-worker recovery
   - Recovery publication no longer calls `b24_next_active_worker_generation()` or stamps `assigned_worker_generation`.
   - Recovered dispatches are marked `assignment_reason='recovery_shared_eligible'` with an unexpired assignment window.
   - `public.b24_claim_fit_dispatch(...)` accepts shared recovery only when `recovery_generation > 0`, the incoming recovery generation is current, assignment is `NULL`, and assignment reason is `recovery_shared_eligible`.
   - Initial/non-recovery dispatches still reject `NULL` assignment via `NOT COALESCE(..., false)`.

2. Stale authority rejection without identity blacklisting
   - Existing stale claim with old `attempt_id`/`recovery_generation` remains `UNAUTHORIZED`.
   - Forged current process-token remains `UNAUTHORIZED`.
   - Current authenticated recovered claim can be acquired by the prior active generation.
   - Concurrent duplicate claim collapses to `ACTIVE_LEASE`; post-completion duplicate collapses to `ALREADY_COMPLETED`.

3. Broker-backed recovery liveness proof
   - Added `test_b24_p9_directive_xiii_broker_backed_recovery_liveness`.
   - The proof publishes `app.tasks.bayesian.reconcile_fit_recovery_wakeups` through `celery_app.send_task(...)` with eager disabled and Postgres broker transport asserted.
   - The proof starts a real Celery worker subprocess subscribed to `bayesian`.
   - The proof observes `bayesian_recovery_reconciler_completed` and `bayesian_fit_intent_executed` probe events, then verifies recovery row publication and terminal dispatch resolution in PostgreSQL.

4. Validator and schema hardening
   - Added Alembic revision `202606201300_b24_p9_directive_xiii_shared_recovery.py`.
   - Updated `db/schema/canonical_schema.sql` with the same shared-recovery claim predicate.
   - Extended `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py` with Directive XIII tokens and negative controls.

### Corrective Action XIII Proof Tiers

| Proof tier | Coverage | Evidence |
|---|---|---|
| Static wiring proof | Recovery task, queue, validator, migration, canonical schema | `validate_b24_p9_worker_tenant_hygiene.py --negative-control` |
| Database state-machine proof | stale authority rejection, shared eligible claim, duplicate lease collapse, null initial assignment rejection | `test_b24_p9_directive_xi_recovery_publication_assignment_runtime`; `test_b24_p9_directive_xiii_shared_recovery_claim_liveness` |
| Broker-backed worker proof | actual broker publish, no-worker negative phase, real worker subprocess, configured `bayesian` queue, replacement consumption | `test_b24_p9_directive_xiii_broker_backed_recovery_liveness` |
| Live Beat proof | Static Beat registration only | `test_b24_p9_directive_xi_recovery_scheduler_is_production_wired`; no live Beat runtime claim is made here. |
| Acknowledgement/retry proof | Stale publishing release and quarantine | Existing `test_b24_p9_directive_xi_stale_publishing_recovery_quarantines`; broker-backed repeated failure is not newly claimed. |

### Corrective Action XIII Local Validation

Passed locally before protected PR creation:

```text
python -m py_compile scripts/ci/validate_b24_p9_worker_tenant_hygiene.py alembic/versions/007_skeldir_foundation/202606201300_b24_p9_directive_xiii_shared_recovery.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m black backend/app/bayesian/dispatch_outbox.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py alembic/versions/007_skeldir_foundation/202606201300_b24_p9_directive_xiii_shared_recovery.py
pytest backend/tests/test_b24_p9_worker_tenant_hygiene.py -q -rA
pytest backend/tests/test_b24_p9_postgres_runtime.py -q -rA
```

Focused local results:

- P9 validator negative controls: pass.
- P9 worker tenant hygiene suite: 32 passed.
- P9 protected Postgres runtime proof file: 21 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is opt-in outside CI; import/collection succeeded.

### Corrective Action XIII Protected Validation

Protected branch validation completed through PR #552 and main push validation on 2026-06-20.

PR validation before merge:

- PR head: `6dee00676100aefe46ceb427c2eaeb3e51f42879`.
- Required PR checks: green via `gh pr checks 552 --required`.
- PR B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873004674`.
- PR `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873004674/job/82487837118`.
- PR Schema Validation: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873004706`.
- PR CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873004678`.

Protected merge validation:

- Merge route: GitHub server-side squash merge API; no admin bypass.
- Merged PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/552`.
- Landed main commit: `73b77115091843157e88bf8eb35f63e6103886e9`.
- `origin/main` contains the merge commit.

Main validation after merge:

- Main CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873348567`.
- Main B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873348570`.
- Main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873348570/job/82488706104`.
- Main Schema Validation: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873348574`.
- Main R7 Final Winning State: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873348547`.
- Production Schema Deployment: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27873377650`.
- Main check-run census for `73b77115091843157e88bf8eb35f63e6103886e9`: 133 total, 132 success, 1 expected skipped, 0 failed.

### Corrective Action XIII Non-Claims

- This remediation does not claim a live Beat runtime exercise; it preserves static Beat registration and adds broker-backed `send_task` recovery proof.
- This remediation does not claim broker-backed duplicate wake-up collapse; duplicate collapse remains DB-state-machine proof.
- This remediation does not claim broker-backed repeated quarantine; stale-publishing quarantine remains direct publisher-failure proof.
- This remediation does not introduce broker-carried execution authority. Broker messages remain wake-ups only.

## Follow-Up Corrective Action XI - 2026-06-19

**PASS - DIRECTIVE XI IMPLEMENTATION AND MAIN CI VALIDATION COMPLETE**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive XI`.

Corrective implementation branch: `codex/b24-p9-directive-xi-durable-recovery`.

Corrective implementation PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/550`.

Corrective implementation head SHA: `ed0fcbd3773158302c5a7d90dbad89ffd41f4cb6`.

Corrective implementation main merge commit: `2f1d9b0707ea22acfeda03cefac822fc0a99d4e0`.

Corrective implementation merged at: `2026-06-19T18:00:27Z`.

Directive XI tested whether the Directive X recovery plane was production-wired end to end. The core hypothesis was validated: recovery detection and recovery publication helpers existed, but no production Beat schedule or equivalent launch surface invoked them. A second liveness issue was also validated: recovery rows moved to `publishing` could remain stuck if the publisher crashed after leasing rows but before publish/marker completion, because the due-row query did not release stale `publishing` rows.

### Corrective Action XI Hypothesis Results

| Hypothesis | Classification | Finding |
|---|---|---|
| H-XI-01 recovery row creation exists but is not production-scheduled | VALIDATED | `b24_create_fit_recovery_wakeups(...)` and Python wrappers existed, but `build_beat_schedule()` had no Bayesian recovery/reconciler entry. |
| H-XI-02 recovery publishing exists but is not production-scheduled | VALIDATED | `publish_due_recovery_rows(...)` existed, but no production task or Beat entry invoked it. |
| H-XI-03 Beat runs but lacks Bayesian recovery entry | VALIDATED | `Procfile` launched `beat`; the schedule lacked a Bayesian recovery key before this remediation. |
| H-XI-04 original broker-message loss can strand work | PARTIAL VALIDATED | Durable DB recovery state existed, but no scheduled publisher guaranteed replacement wake-up publication. |
| H-XI-05 failure-ACK recoverable work can strand | PARTIAL VALIDATED | Recoverable DB states could be produced; without scheduled publication, an acknowledged failed task could remain unrepublished. |
| H-XI-06 replacement assignment validity | PARTIAL VALIDATED | Recovery publisher assigned an active generation, but runtime proof did not yet show prior/wrong generation rejection and replacement-generation claim. |
| H-XI-07 recovery assignment too broad | REFUTED AFTER REMEDIATION | Runtime proof now verifies a valid but wrong generation cannot claim the recovered dispatch. |
| H-XI-08 recovery generation not bound tightly enough | REFUTED AFTER REMEDIATION | Runtime proof now verifies stale recovery-generation messages return `UNAUTHORIZED` before execution. |
| H-XI-09 recovery transition/outbox insertion atomicity | REFUTED | `b24_create_fit_recovery_wakeups(...)` updates dispatch state and inserts the recovery outbox row in one database function/transaction. |
| H-XI-10 publisher crash can strand rows | VALIDATED | Prior due query ignored stale `publishing` rows. Remediation makes them eligible after a bounded stale-publishing interval. |
| H-XI-11 publish-success/marker-failure duplicate safety | REFUTED AFTER REMEDIATION | Duplicate publication is permitted, but the claim layer collapses post-completion replay to `ALREADY_COMPLETED`. |
| H-XI-12 repeated recovery failure unbounded | REFUTED AFTER REMEDIATION | Runtime proof drives repeated publish failure to `quarantined` at the configured attempt bound. |
| H-XI-13 CI lacks broker-message-loss recovery proof | VALIDATED THEN REMEDIATED | P9 runtime proof now models lost original wake-up by relying on recovery detection/publication and replacement claim. |
| H-XI-14 forged-generation rejection proof | PRESERVED | Existing P9 pre-tenant claim proof rejects wrong process-token claims; Directive XI adds wrong-generation recovery proof. |
| H-XI-15 valid-generation/wrong-dispatch rejection proof | REMEDIATED EQUIVALENT | Recovery proof creates prior, replacement, and wrong generations; prior and wrong valid generations fail the recovered claim. |
| H-XI-16 direct stale SQL rejection | PRESERVED | Existing P9 runtime proof still issues direct stale SQL under forged fence context and expects `b24_dispatch_fence_rejected`. |
| H-XI-17 real parent-death behavior | BOUNDED NON-CLAIM | Existing parent-death proof remains the process-trust evidence. Directive XI does not newly claim full Celery prefork parent-kill coverage beyond that bounded model. |
| H-XI-18 evidence equated helper existence with execution | VALIDATED THEN REMEDIATED | This section separates helper existence, Beat registration, runtime proof, CI proof, and residual non-claims. |
| H-XI-19 evidence overstated behavioral coverage | VALIDATED THEN REMEDIATED | Behavioral proofs are identified by exact test names and protected PR/main run IDs. |
| H-XI-20 residual recovery risk underreported | VALIDATED THEN REMEDIATED | The prior absence of scheduled publication is stated explicitly here. |

### Corrective Action XI Remediations Made

1. Production recovery registration
   - Added Beat schedule key `b24-p9-bayesian-recovery-reconciler`.
   - The schedule dispatches `app.tasks.bayesian.reconcile_fit_recovery_wakeups` to the `bayesian` queue with bounded interval, batch size, expiry, and stale-publishing controls.
   - Updated `Procfile` so `worker_bayesian` launches with `SKELDIR_CELERY_WORKER_ROLE=bayesian` and `SKELDIR_CELERY_INCLUDE_BAYESIAN_TASKS=1`.

2. Durable recovery reconciler task
   - Added `RECOVERY_RECONCILER_TASK_NAME` to the required Bayesian task registry.
   - Added `reconcile_fit_recovery_wakeups(...)`, guarded by `assert_bayesian_worker_boot_topology_proven()`.
   - The task uses a disposable Bayesian worker `NullPool` engine, calls `create_recovery_wakeups_sync(...)`, and drains `publish_due_recovery_rows_sync(...)`.

3. Publisher crash release and bounded terminal handling
   - Added sync recovery publisher helpers for the worker task path.
   - `lease_due_recovery_rows(...)` and `lease_due_recovery_rows_sync(...)` now release stale `publishing` rows after `stale_publishing_seconds`.
   - Repeated publish failures continue to use bounded `publish_attempt_count` and transition to `quarantined`.

4. Secret-free recovery transport preserved
   - Recovery broker payload remains limited to `dispatch_id`, `fit_id`, `task_name`, `attempt_id`, `payload_hash`, and `recovery_generation`.
   - No `claim_capability`, `lease_capability`, process token, generation root secret, or reusable execution authority is placed in broker payloads.

5. Non-vacuous proof expansion
   - Added structural proof `test_b24_p9_directive_xi_recovery_scheduler_is_production_wired`.
   - Upgraded the recovery runtime proof to `test_b24_p9_directive_xi_recovery_publication_assignment_runtime`.
   - Added crash/quarantine proof `test_b24_p9_directive_xi_stale_publishing_recovery_quarantines`.
   - Extended `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py` so CI fails if the Beat entry, launch profile, task, sync publisher, or stale-publishing release condition is removed.

### Corrective Action XI Local Validation

Passed locally before protected PR creation:

```text
python -m py_compile backend/app/bayesian/dispatch_outbox.py backend/app/tasks/bayesian.py backend/app/tasks/beat_schedule.py backend/tests/test_b24_p9_worker_tenant_hygiene.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python -m black --check backend/app/bayesian/dispatch_outbox.py backend/app/tasks/bayesian.py backend/app/tasks/beat_schedule.py backend/tests/test_b24_p9_worker_tenant_hygiene.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python -m ruff check backend/app/bayesian/dispatch_outbox.py backend/app/tasks/bayesian.py backend/app/tasks/beat_schedule.py backend/tests/test_b24_p9_worker_tenant_hygiene.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend/tests/test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend/tests/test_b24_p9_postgres_runtime.py -q
python scripts/ci/validate_b24_p6_real_fit_worker.py --negative-control
python -m pytest backend/tests/test_b24_p6_real_fit_worker.py -q
python -m pytest backend/tests/test_b24_p7_postgres_runtime.py backend/tests/test_b24_p8_postgres_runtime.py -q
python -m pytest backend/tests/test_b24_p5_postgres_runtime.py -q
python scripts/ci/validate_m3_ci_governance.py --b24-dry-run
python scripts/ci/validate_m0_scope_lock.py --baseline-sha 44ccc19908eaa0c9587bdd4686fd5c1e5b76017b
python scripts/ci/validate_m1_local_dev_authority.py --baseline-sha 44ccc19908eaa0c9587bdd4686fd5c1e5b76017b
python -m pytest backend/tests/test_b23_p5_composite_proof_negative_controls.py -q
git diff --check
```

Focused local results:

- P9 static validator: pass.
- P9 validator negative controls: pass.
- P9 worker tenant hygiene suite: 32 passed.
- P9 protected Postgres runtime proof file: 19 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is opt-in outside CI; import/collection succeeded.
- P6 real-fit worker validator negative controls: pass.
- P6 focused suite: 5 passed, 2 skipped locally.
- P7/P8 Postgres runtime proofs: 5 skipped locally because protected DB proof flags are CI-scoped.
- P5 Postgres runtime proofs: 2 skipped locally because protected DB proof flags are CI-scoped.
- M3 B2.4 dry-run governance validator: pass.
- M0 scope lock validator: pass against baseline `44ccc19908eaa0c9587bdd4686fd5c1e5b76017b` after exact allowlist update for the required production scheduler and launch files.
- M1 local development authority validator: pass against baseline `44ccc19908eaa0c9587bdd4686fd5c1e5b76017b` after exact allowlist update for the same required production scheduler and launch files.
- B23 composite negative-control suite: 22 passed.
- Whitespace check: pass.

### Corrective Action XI Protected Validation

Authoritative protected workflow evidence:

- PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/550`.
- PR head SHA: `ed0fcbd3773158302c5a7d90dbad89ffd41f4cb6`.
- PR merge status before merge: `mergeStateStatus=CLEAN`; `mergeable=MERGEABLE`.
- Merge time: `2026-06-19T18:00:27Z`.
- Merge commit / landed `main` SHA: `2f1d9b0707ea22acfeda03cefac822fc0a99d4e0`.
- PR `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27840229811`.
- PR `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27840229811/job/82397402306`.
- PR `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27840229833`.
- PR `Contract Semantic Drift Gate`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27840229833/job/82398671230`.
- PR `m0-maintainability-scope-lock`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27840229855/job/82397402204`.
- PR `m1-local-dev-authority`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27840229841/job/82397402123`.
- Main commit check-run census for `2f1d9b0707ea22acfeda03cefac822fc0a99d4e0`: 133 total, 132 `success`, 1 expected `skipped`, 0 active, 0 failed.
- Main `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27841159425`.
- Main `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27841159389`.
- Main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27841159389/job/82400295472`.
- Main `Contract Semantic Drift Gate`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27841159425/job/82401065069`.
- Main `m0-maintainability-scope-lock`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27841159442/job/82400295345`.
- Main `m1-local-dev-authority`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27841159367/job/82400295439`.

### Corrective Action XI Non-Claims

- Local execution did not run the protected Postgres runtime proofs because the repository intentionally gates those tests behind CI/proof environment flags. Protected PR/main CI executed the authoritative DB proof path and passed.
- This remediation does not claim transaction/statement-pooler support; Directive VIII's direct/session-affine PostgreSQL constraint remains in force.
- This remediation does not introduce broker-carried execution authority. Recovery publication remains a wake-up path only.
- Full Celery prefork parent-kill coverage remains bounded by the existing P9 process trust-model proof; this section does not overclaim a new OS-level parent-death proof beyond the existing tested boundary.

## Follow-Up Corrective Action X - 2026-06-18

**PASS - DIRECTIVE X IMPLEMENTATION AND FOLLOW-UP MAIN CI VALIDATION COMPLETE**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive X`.

Corrective implementation branch: `codex/b24-p9-directive-x-broker-independent-authority`.

Corrective implementation PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/545`.

Corrective implementation head SHA: `8816f4a316eaaa73be54396682430208b2c7829f`.

Corrective implementation main merge commit: `3cb0221d32a8a15f9222d8a629491cad3c96951f`.

Corrective implementation merged at: `2026-06-19T00:30:10Z`.

Corrective baseline: `7547313fec2427e41cc4c9e443c2dd00cb974539`.

Final authoritative green main commit after Directive X follow-up CI remediation: `62b67f3962062d39e2475acd3652dc7c926d6959`.

Directive X tested the hypothesis that Directive IX still treated broker delivery as more than a wake-up. The hypothesis was validated. The broker payload still carried `claim_capability`; dispatch and recovery outbox rows stored claim material; `public.b24_claim_fit_dispatch(...)` accepted caller-supplied generation text as part of claim authority; `app.b24_dispatch_fence_required` could disable trigger enforcement; and the production executor still had a fit-id identity helper from the earlier no-dispatch path.

### Corrective Action X Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-DX-01 broker payload is still an execution credential | VALIDATED | `DispatchOutboxRow.queue_payload` included `claim_capability`, allowing broker possession to participate in authorization. |
| H-P9-DX-02 durable outboxes store claim secrets | VALIDATED | Dispatch/recovery outbox paths populated plaintext claim material or retained non-null expectations. |
| H-P9-DX-03 worker generation is caller-authored, not independently authenticated | VALIDATED | The claim function accepted a generation string without a DB-verified process-token binding. |
| H-P9-DX-04 assignment is not exact-dispatch bounded | VALIDATED | No durable `assigned_worker_generation`/assignment expiry existed for the specific dispatch attempt. |
| H-P9-DX-05 stale workers can retain write authority after recovery | VALIDATED | Recovery did not rotate `attempt_id` or clear assignment/lease authority in a way that made old claims fail closed. |
| H-P9-DX-06 fence enforcement can be disabled by GUC omission/manipulation | VALIDATED | `b24_enforce_dispatch_fence()` relied on `app.b24_dispatch_fence_required`. |
| H-P9-DX-07 recovery publication is only partially modeled | VALIDATED | Recovery rows existed, but no publisher path leased recovery rows, assigned a replacement generation, and emitted a secret-free wake-up. |
| H-P9-DX-08 main R2 failure remains open after IX follow-up | VALIDATED | Main run `27766676094` failed because canonical schema application hit unsupported `SET transaction_timeout = 0` on the R2 Postgres image. |

### Corrective Action X Remediations Made

1. Broker-independent dispatch authority
   - `BayesianDispatchClaim` is now a secret-free wake-up envelope: `dispatch_id`, `fit_id`, `task_name`, `attempt_id`, `payload_hash`, and `recovery_generation`.
   - `DispatchOutboxRow.queue_payload` and recovery publication payloads no longer include `claim_capability`.
   - `execute_fit_intent_sync` requires `dispatch_claim` and `BayesianWorkerClaimAuthority`; the unused fit-id identity helper was removed.

2. Process-authenticated claim and assignment
   - Added `public.b24_worker_process_authority` as a digest-only, FORCE-RLS process-authority registry.
   - Added `public.b24_register_worker_process_authority(...)` and `public.b24_next_active_worker_generation()`.
   - Replaced `public.b24_claim_fit_dispatch(...)` with a signature requiring `worker_generation`, `worker_pid`, `worker_process_token`, `recovery_generation`, and lease seconds.
   - Claim success now requires an active registered process-token digest and exact dispatch assignment before the DB mints a fresh lease capability.

3. Mandatory write fencing and stale-worker rejection
   - Removed `app.b24_dispatch_fence_required` from write-context binding and current fence enforcement.
   - Fit/artifact DELETE is rejected by trigger.
   - Recovery rotates `attempt_id`, increments `recovery_generation`, clears lease/capability/assignment state, and makes stale old-attempt claims return `UNAUTHORIZED` without tenant leakage.

4. Durable recovery publication
   - Added recovery row leasing/publishing helpers that assign an active generation, emit a secret-free wake-up, mark success, and quarantine/retry failures.
   - Recovery wake-up payloads use the new attempt and recovery generation rather than a broker-carried secret.

5. Schema, validators, and CI bootstrap
   - Added Directive X Alembic migration and regenerated `db/schema/canonical_schema.sql`.
   - Updated ORM models and exports for assignment columns, nullable legacy claim columns, recovery rows, and worker process authority.
   - Updated P3/P9 validators and tests so capability-bearing broker payloads are a negative-control failure.
   - Hardened R2 canonical bootstrap after protected main validation exposed schema-apply drift: stripped unsupported `SET transaction_timeout = 0`, deferred allocation trigger functions until after referenced tables exist, preserved `public.`-qualified materialized-view source relations, and created bootstrap materialized views as `public.*`.

### Corrective Action X Local Validation

Passed locally before protected PR creation:

```text
python -m py_compile backend/app/bayesian/dispatch_authority.py backend/app/bayesian/dispatch_outbox.py backend/app/bayesian/fit_claim.py backend/app/bayesian/fit_execution.py backend/app/bayesian/worker_boot_probe.py backend/app/tasks/bayesian.py backend/app/bayesian/models.py backend/tests/test_b24_p6_real_fit_worker.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p3_fit_planning.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python -m py_compile backend/app/bayesian/fit_execution.py
python -m black --check backend/app/bayesian/fit_execution.py
python -m ruff check backend/app/bayesian/fit_execution.py
python -m py_compile backend/tests/test_b24_p6_real_fit_worker.py
python -m black --check backend/tests/test_b24_p6_real_fit_worker.py
python -m ruff check backend/tests/test_b24_p6_real_fit_worker.py
python -m py_compile alembic/versions/007_skeldir_foundation/202606181200_b24_p9_directive_x_broker_independent_authority.py
python -m black --check alembic/versions/007_skeldir_foundation/202606181200_b24_p9_directive_x_broker_independent_authority.py
python -m ruff check alembic/versions/007_skeldir_foundation/202606181200_b24_p9_directive_x_broker_independent_authority.py
python scripts/ci/validate_b24_p3_fit_planning.py
python scripts/ci/validate_b24_p3_fit_planning.py --negative-control
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend/tests/test_b24_p3_fit_planning.py backend/tests/test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend/tests/test_b24_p6_real_fit_worker.py -q
python -m pytest backend/tests/test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend/tests/test_b24_p9_postgres_runtime.py -q
python scripts/schema/assert_canonical_schema.py --mode local --no-migrate --artifacts-dir artifacts/schema_authority_directive_x_after --diff-out artifacts/schema_authority_directive_x_after/schema.diff
python scripts/ci/validate_b24_p6_real_fit_worker.py --negative-control
python scripts/ci/validate_m3_ci_governance.py --b24-dry-run
python -m pytest backend/tests/test_b23_p1_schema_authority_enforcer.py -q
python scripts/ci/enforce_b23_p1_schema_authority_lock.py
python scripts/ci/validate_m0_scope_lock.py --baseline-sha 7547313fec2427e41cc4c9e443c2dd00cb974539
python scripts/ci/validate_m1_local_dev_authority.py --baseline-sha 7547313fec2427e41cc4c9e443c2dd00cb974539
python -m pytest backend/tests/test_b23_p5_composite_proof_negative_controls.py -q
git diff --check
```

Focused local results:

- P3 validator positive and negative controls: pass.
- P9 validator positive and negative controls: pass.
- P3/P9 focused unit suite: 63 passed.
- P6 real-fit worker focused suite: 5 passed, 2 skipped locally.
- P9 worker tenant hygiene suite: 31 passed.
- P9 protected Postgres runtime proof file: 18 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is opt-in outside CI; import/collection succeeded.
- Schema authority: pass against scratch database `skeldir_b24p9_directive_x_schema`.
- B23 schema authority and composite negative-control follow-up suites: pass.
- M0/M1 scope validators: pass against merge-base `7547313fec2427e41cc4c9e443c2dd00cb974539`.

### Corrective Action X Protected Validation

Implementation PR #545 reached `mergeStateStatus=CLEAN` before merge. The protected PR check set completed with 131 successful checks, 6 skipped checks, 3 neutral checks, and 0 failed checks. Representative green runs included:

- `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27797085963`
- `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27797085879`
- `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27797085879/job/82259110291`
- `M0 Maintainability Scope Lock`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27797085988`
- `M1 Local Development Authority`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27797085943`

Directive X implementation merged through protected PR #545 at `2026-06-19T00:30:10Z`, landing `3cb0221d32a8a15f9222d8a629491cad3c96951f` on `main`.

Post-merge main validation initially found one remaining non-worker-code failure in R2 canonical schema bootstrap. The corrective follow-up chain was:

| PR | Main merge | Finding | Remediation |
|---|---|---|---|
| #546 | `fa50e0351c2cd344cc30e442ecb5433c87f5b38c` | Main R2 run `27797862663` failed because allocation trigger functions were evaluated before `public.attribution_allocations` existed. | Deferred `public.check_allocation_sum*` functions until after table creation and before trigger binding. |
| #547 | `3fdac84d8eb402bf72ca65e5e81fcf4c5c653619` | Main R2 run `27799034177` failed because the bootstrap materialized-view replacement used unqualified relation names under an empty search path. | Qualified bootstrap view source relations as `public.attribution_allocations`, `public.attribution_events`, and `public.reconciliation_runs`. |
| #548 | `62b67f3962062d39e2475acd3652dc7c926d6959` | Main R2 run `27800035239` failed because the replacement created materialized views without a target schema. | Created bootstrap materialized views as `public.mv_allocation_summary` and `public.mv_reconciliation_status`. |

Final post-remediation main validation on `62b67f3962062d39e2475acd3652dc7c926d6959` completed with 27/27 push workflows successful:

- `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788198`
- `R2: Data-Truth Hardening`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788202`
- `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788219`
- `Empirical Validation - Directive Compliance`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788239`
- `M0 Maintainability Scope Lock`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788144`
- `M1 Local Development Authority`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788201`
- `M2 Test Feedback Loop`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788161`
- `M3 CI Governance`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788165`
- `M4 Operational Runbooks`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27800788176`

R2 proof details for run `27800788202`: canonical schema application passed, EG-R2-ENFORCEMENT and EG-R2-FIX-1 through EG-R2-FIX-9 all executed successfully, and R2 Gate Aggregation completed with `success`.

### Corrective Action X Non-Claims

- Process-authority registration is DB-token-digest backed and generation/assignment bounded. It does not claim support for transaction/statement pooling; Directive VIII's direct/session-affine PostgreSQL constraint remains in force.
- Legacy `claim_capability*` columns remain for migration compatibility, but runtime publisher/claim paths scrub them and no longer use them as broker authority.

## Follow-Up Corrective Action IX - 2026-06-14

**PASS - DIRECTIVE IX IMPLEMENTATION MERGED; POST-MERGE R2 BOOTSTRAP DRIFT REMEDIATED IN FOLLOW-UP**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive IX`.

Corrective implementation branch: `codex/b24-p9-directive-ix-capability-fencing`.

Corrective implementation PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/543`.

Corrective implementation head SHA: `862ad6b297ecdb017d02dfe87c349726b0f7c69a`.

Corrective implementation main merge commit: `ed0e3b51aca39b6dda0ec4e0e9ceb94784074910`.

Corrective implementation merged at: `2026-06-14T21:55:53Z`.

Directive IX closes the remaining capability-bound dispatch gap in B2.4-P9. Before this remediation, a worker could still treat possession of a fit identifier and broker delivery as sufficient execution authority. That left stale, duplicated, or replayed workers outside a database-native lease fence for the fit execution lifecycle and for downstream model-fit/artifact writes. The remediation moves authority from ambient task payload trust into a bounded database capability: claim, lease, running transition, completion, terminal failure, model-fit writes, artifact writes, and stale-lease recovery are now tied to dispatch identity, attempt identity, payload hash, capability digest, expiry, task name, and tenant.

### Corrective Action IX Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-DIX-01 fit-id-only dispatch authority remained sufficient | VALIDATED | The pre-remediation worker path could execute from a fit identifier without proving possession of a current database-bound dispatch capability. |
| H-P9-DIX-02 broker payloads lacked unforgeable attempt authority | VALIDATED | Dispatch payloads did not carry an attempt/capability/hash bundle that the database could independently bind to a live lease. |
| H-P9-DIX-03 stale or duplicate workers could write after supersession | VALIDATED | Model-fit and artifact writes were not protected by a current-dispatch fence tied to the active attempt. |
| H-P9-DIX-04 terminal completion was not atomically tied to dispatch lease state | VALIDATED | Completion and terminal failure needed database-enforced authority checks rather than task-local intent. |
| H-P9-DIX-05 stale lease recovery wakeups were absent | VALIDATED | Claimed but abandoned dispatch rows had no first-class recovery outbox path. |
| H-P9-DIX-06 FORCE RLS blocked pre-tenant claim unless explicitly modeled | VALIDATED | Protected runtime proof showed that the claim aperture needed a narrow capability-digest GUC path before tenant binding is available. |
| H-P9-DIX-07 migration/schema authority drift could mask runtime proof failure | VALIDATED | The new migration required canonical-schema regeneration and migration validator annotations for intentional rollback destructiveness. |
| H-P9-DIX-08 CI runtime dependency drift could expose a docs-only FastAPI app | VALIDATED | Floating FastAPI/Pydantic/Starlette versions changed route-loading behavior in CI; dependency pins restored deterministic runtime surface. |
| H-P9-DIX-09 R2 canonical schema bootstrap assumed unqualified materialized view names | VALIDATED POST-MERGE | Main run `27513259154` failed because canonical pg_dump emitted `public.mv_allocation_summary` and `public.mv_reconciliation_status`, while the R2 bootstrap regex matched only unqualified names. |

### Corrective Action IX Remediations Made

1. Capability-bound dispatch authority
   - Added dispatch outbox columns for `task_name`, `attempt_id`, `payload_hash`, `claim_capability_digest`, capability expiry, lease state, run state, failure metadata, and recovery scheduling.
   - Added `public.b24_claim_fit_dispatch(...)`, `public.b24_current_dispatch_fence_valid(...)`, `public.b24_mark_fit_dispatch_running()`, `public.b24_complete_fit_dispatch()`, and `public.b24_fail_fit_dispatch_terminal(text)`.
   - Added a narrow pre-tenant claim aperture through `app.b24_claim_capability_digest` so FORCE RLS remains enabled while allowing only possession-bound claim attempts.
   - Added `public.b24_fit_recovery_outbox` plus `public.b24_create_fit_recovery_wakeups(integer)` for stale claimed/running dispatch rows.

2. Worker execution fencing
   - `execute_fit_intent` now requires dispatch authority fields: `dispatch_id`, `fit_id`, `attempt_id`, `payload_hash`, and `claim_capability`.
   - Stale fit-id-only or wrong-task payloads fail closed before execution.
   - Fit execution claims a database lease before running and binds the active lease into model-fit and artifact writes.
   - Database triggers reject fit/artifact writes unless `app.current_fit_dispatch_id` and `app.current_fit_dispatch_attempt_id` identify a currently valid dispatch fence.

3. Deterministic payload and recovery semantics
   - Dispatch payload hashes are deterministic over the broker payload body.
   - Claim helpers mint attempt ids and random claim capabilities, store only digests in the database, and propagate only the capability needed by the intended worker.
   - Recovery wakeups are generated separately from execution authority, preserving least privilege for abandoned work detection.

4. Schema, validators, and route/runtime determinism
   - Added the Directive IX Alembic migration and regenerated `db/schema/canonical_schema.sql`.
   - Extended P3/P9 validators, negative controls, ORM models, runtime tests, and Postgres runtime proofs.
   - Pinned `fastapi==0.127.0`, `pydantic==2.12.5`, `pydantic-settings==2.12.0`, and `starlette==0.50.0` to eliminate CI route-surface drift.
   - Hardened the R2 canonical-schema bootstrap matcher to accept both `CREATE MATERIALIZED VIEW mv_*` and `CREATE MATERIALIZED VIEW public.mv_*` declarations.

### Corrective Action IX Local Validation

Passed before implementation merge:

```text
python -m py_compile backend/app/bayesian/dispatch_authority.py backend/app/bayesian/dispatch_outbox.py backend/app/bayesian/fit_claim.py backend/app/tasks/bayesian.py backend/app/bayesian/fit_execution.py backend/tests/test_b24_p3_fit_planning.py backend/tests/test_b24_p9_worker_tenant_hygiene.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p3_fit_planning.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python -m black --check backend/app/bayesian/dispatch_authority.py backend/app/bayesian/dispatch_outbox.py backend/app/bayesian/fit_claim.py backend/app/tasks/bayesian.py backend/app/bayesian/fit_execution.py backend/tests/test_b24_p3_fit_planning.py backend/tests/test_b24_p9_worker_tenant_hygiene.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p3_fit_planning.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python -m ruff check backend/app/bayesian/dispatch_authority.py backend/app/bayesian/dispatch_outbox.py backend/app/bayesian/fit_claim.py backend/app/tasks/bayesian.py backend/app/bayesian/fit_execution.py backend/tests/test_b24_p3_fit_planning.py backend/tests/test_b24_p9_worker_tenant_hygiene.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p3_fit_planning.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts/ci/validate_b24_p3_fit_planning.py --negative-control
python scripts/ci/validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend/tests/test_b24_p3_fit_planning.py backend/tests/test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend/tests/test_b24_p9_postgres_runtime.py::test_b24_p9_directive_ix_pre_tenant_claim_and_fence_runtime backend/tests/test_b24_p9_postgres_runtime.py::test_b24_p9_directive_ix_stale_lease_and_recovery_runtime -q
python scripts/schema/assert_canonical_schema.py --mode local --no-migrate --canonical db/schema/canonical_schema.sql --database-url postgresql+asyncpg://migration_owner:migration_owner@localhost:5432/skeldir_schema_b24p9_ix3 --sync-database-url postgresql://migration_owner:migration_owner@localhost:5432/skeldir_schema_b24p9_ix3
python -m pytest backend/tests/test_b23_p1_schema_authority_enforcer.py backend/tests/test_b23_p5_composite_proof_negative_controls.py::test_p4_remove_telemetry_sql_index_fails -q
python scripts/ci/enforce_b23_p0_typed_boundary_source_alignment.py
python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py
python -m pytest tests/contract/test_route_fidelity.py backend/tests/test_b17_p5_anti_chat_surface_runtime.py -q
python scripts/ci/enforce_b17_p0_explanation_surface_lock.py
python scripts/ci/enforce_b22_p0_webhook_surface_lock.py
python scripts/ci/validate_m0_scope_lock.py --baseline-sha 7942446bf9bd57900106998f49b4d4e83862c69b
python scripts/ci/validate_m1_local_dev_authority.py --baseline-sha 7942446bf9bd57900106998f49b4d4e83862c69b
git diff --check
```

Focused local results:

- P3/P9 static validators and mutation controls: pass.
- P3/P9 focused unit suite: 63 passed.
- Directive IX protected Postgres runtime proofs on scratch database `skeldir_schema_b24p9_ix3`: pass.
- B23 schema authority and semantic freeze proofs: pass.
- Route fidelity and anti-chat surface proofs under pinned dependency surface: 9 passed.
- M0/M1 scope locks with Directive IX allowlists: pass.

Additional follow-up validation for R2 bootstrap hardening:

```text
python -c "import re; from pathlib import Path; text=Path('db/schema/canonical_schema.sql').read_text(encoding='utf-8'); assert len(re.compile(r'CREATE MATERIALIZED VIEW (?:public\\.)?mv_allocation_summary AS\\n.*?\\n\\s*WITH NO DATA;', re.DOTALL).findall(text)) == 1; assert len(re.compile(r'CREATE MATERIALIZED VIEW (?:public\\.)?mv_reconciliation_status AS\\n.*?\\n\\s*WITH NO DATA;', re.DOTALL).findall(text)) == 1"
```

### Corrective Action IX Protected Validation

Implementation PR #543 reached `mergeStateStatus=CLEAN` before merge. The protected PR check set reported 130 successful checks, 6 skipped checks, 3 neutral checks, and 0 failed checks. Representative green runs included:

- `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27512592176`
- `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27512592183`
- `B1.7-P4 Mixed Workload Benchmark`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27512592180`
- `M0 Scope Lock`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27512592164`
- `M1 Local Development Authority`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27512592179`
- `Schema Validation`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27512592159`

Post-merge main validation for `ed0e3b51aca39b6dda0ec4e0e9ceb94784074910` found one additional non-worker-code failure: `R2: Data-Truth Hardening` run `27513259154` could not apply the canonical schema because its bootstrap matcher did not recognize `public.`-qualified materialized view names. This evidence-pack follow-up carries the R2 matcher hardening so final authoritative main validation can be established on the next landed main commit.

### Corrective Action IX Non-Claims

- This evidence does not claim B2.4-P10 completion.
- This evidence does not add transaction-pooling or statement-pooling support; Directive VIII's direct/session-affine PostgreSQL policy remains in force.
- This evidence does not claim production Bayesian model correctness beyond dispatch authority, lease fencing, tenant-bound writes, and stale-lease recovery semantics.

## Follow-Up Corrective Action VIII - 2026-06-13

**PASS - FOLLOW-UP CORRECTIVE ACTION VIII AUTHORITATIVELY VALIDATED ON MAIN**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive VIII`.

Corrective branch: `codex/b24-p9-directive-viii-authority-topology`.

Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/541`.

Corrective implementation head SHA: `68a676f4f9b0d4b77253d56320d08f036103690a`.

Corrective main commit: `d68af57c5d0d2383f007941884cb95de99d1fb8f`.

Directive VIII closes three load-bearing gaps left after Directive VI: child-readable authority claims could still carry the root minting secret; the topology policy named `direct_postgres` without an explicit backend-affinity boundary; and the Bayesian sync DSN path rebuilt URLs in a way that discarded transport/security query parameters. The selected topology policy is Outcome A: Bayesian tenant-sensitive execution supports direct/session-affine PostgreSQL only. Transaction pooling and statement pooling are mechanically rejected unless a future directive adds the full same-transaction proof program.

### Corrective Action VIII Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-DVIII-01 authority payload remains self-authenticating | VALIDATED | The child-readable generation file serialized `authority_secret`, allowing a replaced payload to become its own trust universe. |
| H-P9-DVIII-02 no independent parent-origin anchor exists | VALIDATED | File contents alone could reconstruct generation proof state on spawn paths. |
| H-P9-DVIII-03 parent identity is asserted but not causally verified | PARTIAL | `parent_pid` was token data, but runtime authority did not require the current process to remain a child of that parent. |
| H-P9-DVIII-04 parent death leaves usable child authority | VALIDATED AS RISK | Existing token validation did not check `os.getppid()` against the issuing parent. |
| H-P9-DVIII-05 cross-worker or cross-child replay may succeed | REFUTED AFTER REMEDIATION | Authority remains generation-, parent-, pid-, and topology-fingerprint-bound; parenthood is now checked. |
| H-P9-DVIII-06 root authority material may reach sampler children | REFUTED | Sampler child env remains allowlisted and excludes database/authority env; root authority is not serialized to child-readable claims. |
| H-P9-DVIII-07 supported Bayesian launch surfaces remain inconsistent | VALIDATED | Protected launch surfaces had role/topology wiring but no explicit backend-affinity env. |
| H-P9-DVIII-08 expected Bayesian worker may start incapable | REFUTED | Directive VI role/include registry checks are preserved and remain covered by tests. |
| H-P9-DVIII-10 backend affinity is not explicitly classified | VALIDATED | The policy carried only topology, not the affinity boundary used to justify connect-bound attestation. |
| H-P9-DVIII-13 statement pooling may not be explicitly rejected | VALIDATED | Statement-pooling values were not first-class rejected affinity values. |
| H-P9-DVIII-14 through H-P9-DVIII-18 TOCTOU under transaction pooling | REFUTED BY POLICY | Transaction pooling is not supported; `transaction_lifetime` and statement affinity fail closed before tenant execution. |
| H-P9-DVIII-20 through H-P9-DVIII-25 transaction-pool residue hypotheses | REFUTED BY POLICY | Since transaction pooling is mechanically rejected, pooler reset and shared-backend residue are not claimed supported. |
| H-P9-DVIII-26 TLS server identity not enforced | PARTIAL | This remediation preserves TLS/security query parameters in Bayesian sync DSNs; external CA/hostname mutation proof remains deployment-specific. |
| H-P9-DVIII-27 URL normalization discards security parameters | VALIDATED | `runtime_sync_database_url()` rebuilt DSNs and dropped query parameters; it now delegates to scheme-only normalization. |
| H-P9-DVIII-33 through H-P9-DVIII-36 failure amplification risks | PARTIAL | Child file-only authority now fails closed without DB probes; task retries remain `max_retries=0`; protected runtime proofs validate bounded boot/worker behavior. |
| H-P9-DVIII-39 full P9 regression | REFUTED | P9 validator, negative controls, focused P9 tests, protected PR P9 proof, and post-merge main P9 proof passed. |
| H-P9-DVIII-40 P5 through P8 regression | REFUTED BY PROTECTED CI | Local B0.7 P5 proof is Windows-skipped; protected PR and main B2.4/CI workflows passed on Linux with P5-P9 proof jobs green. |

Validated root causes: `RC-P9-DVIII-05`, `RC-P9-DVIII-06`, `RC-P9-DVIII-07`, and `RC-P9-DVIII-09`. `RC-P9-DVIII-08` remains partially validated because CI still uses direct PostgreSQL rather than a live transaction-pooler negative control; this remediation chooses mechanical rejection of transaction/statement pooling, not support.

### Corrective Action VIII Remediations Made

1. Process-generation authority
   - Kept the root `authority_secret` parent-memory only.
   - Changed the generation authority file to serialize public `BayesianWorkerGenerationClaims` only.
   - Rejects any child-readable payload containing `authority_secret`.
   - File-only/spawn authority now fails closed with `bayesian_worker_generation_anchor_unavailable`; fork-inherited authority remains supported.
   - Runtime authority now verifies that a child process still has the issuing parent as `os.getppid()`, invalidating surviving children after parent death.
   - Backend affinity is now part of the topology authority fingerprint.

2. Topology-affinity policy
   - Added `BayesianWorkerDBBackendAffinity` with `connection_lifetime`, `transaction_lifetime`, `statement_lifetime`, and `unknown`.
   - Protected runtimes must set `SKELDIR_BAYESIAN_DB_BACKEND_AFFINITY=connection_lifetime`.
   - Transaction and statement affinity values fail closed with explicit errors.
   - Added pooler negative-control tokens for transaction/statement/multiplexing forms.

3. Tenant transaction boundary
   - `bind_transaction_local_tenant()` now requires an active transaction.
   - It checks for pre-existing tenant GUC residue before `SET LOCAL`.
   - It records `pg_backend_pid()` before and after binding and fails if backend continuity is lost.
   - Bayesian task-local tenant binding now delegates to the shared guarded helper.

4. Transport/security preservation
   - `runtime_sync_database_url()` now uses `to_sync_postgres_dsn(get_database_url())`, preserving TLS, certificate, channel-binding, timeout, and application-name query parameters while changing only the driver scheme.

5. Launch and CI wiring
   - Added `SKELDIR_BAYESIAN_DB_BACKEND_AFFINITY=connection_lifetime` to `backend/Dockerfile.bayesian`.
   - Added the same affinity contract to protected B2.4 and CI Bayesian runtime jobs.
   - Added the same env to the B0.7 P5 real-worker proof and P9 PostgreSQL runtime helper.

6. Tests and validators
   - Added payload-substitution proof that a child-readable file cannot mint authority.
   - Added parent-death invalidation proof.
   - Added DSN security-query preservation proof.
   - Added topology-affinity rejection proof for transaction and statement pooling.
   - Expanded the P9 static validator and negative controls for authority payload secrets, backend affinity, launch wiring, and preserved security-query normalization.

### Corrective Action VIII Local Validation

Passed:

```text
python -m py_compile backend\app\bayesian\worker_boot_probe.py backend\app\bayesian\db_topology.py backend\app\bayesian\tenant_context.py backend\app\bayesian\db_engine.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m black --check backend\app\bayesian\worker_boot_probe.py backend\app\bayesian\db_topology.py backend\app\bayesian\tenant_context.py backend\app\bayesian\db_engine.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m ruff check backend\app\bayesian\worker_boot_probe.py backend\app\bayesian\db_topology.py backend\app\bayesian\tenant_context.py backend\app\bayesian\db_engine.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python scripts\ci\validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py -q
python scripts\ci\enforce_b21_p4_queue_isolation_semantics_lock.py
python -m pytest backend\tests\test_b21_p4_queue_isolation_semantics_lock_enforcer.py -q
python -m pytest backend\tests\test_db_dsn_normalization.py -q
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py -q
python -m pytest backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py -q
```

Local result details:

- P9 validator and mutation controls: pass.
- P9 unit/static suite: 29 passed.
- B2.1-P4 queue isolation lock: pass; queue-lock test suite: 6 passed.
- DSN normalization suite: 3 passed.
- P9 protected PostgreSQL runtime suite: 16 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is intentionally opt-in outside protected CI.
- B0.7 P5 real-worker proof: skipped on Windows with explicit reason that the Celery prefork timeout proof requires POSIX signals.

### Corrective Action VIII Protected Validation

Passed:

- PR #541 merged through the protected `main` workflow on 2026-06-13T21:24:25Z.
- PR #541 implementation head: `68a676f4f9b0d4b77253d56320d08f036103690a`.
- PR #541 landed on `main` as `d68af57c5d0d2383f007941884cb95de99d1fb8f`.
- PR #541 status rollup before merge: 138 total checks, 129 success, 9 skipped/neutral, 0 failed.
- PR #541 B2.4 Gate Dry Run: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479138059/job/81223697442
- PR #541 B2.4-P9 Worker Tenant Hygiene Proof: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479138059/job/81223697508
- PR #541 CI: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479138015
- PR #541 M0 Maintainability Scope Lock: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479138021/job/81223697149
- PR #541 M1 Local Development Authority: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479138053/job/81223697124
- Post-merge main workflow poll for `d68af57c5d0d2383f007941884cb95de99d1fb8f`: 32 total workflows, 32 success, 0 skipped/neutral, 0 failed, 0 active.
- Main B2.4 Gate Dry Run: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479538210/job/81224767390
- Main B2.4-P9 Worker Tenant Hygiene Proof: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479538210/job/81224767401
- Main CI: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479538183
- Main CI Proof Pack (EG-5): https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27479538183/job/81225779697

### Corrective Action VIII Non-Overclaim Statement

Directive VIII does not claim transaction-pooling support, statement-pooling support, transparent-proxy support, or live failover certification. The landed policy is direct/session-affine PostgreSQL with connection-lifetime backend affinity. Transaction and statement pooling are rejected before Bayesian tenant work. Same-transaction attestation proofs are therefore not claimed applicable in this iteration.

## Follow-Up Corrective Action VI - 2026-06-13

**PASS - FOLLOW-UP CORRECTIVE ACTION VI AUTHORITATIVELY VALIDATED ON MAIN**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive VI`.

Corrective branch: `codex/b24-p9-directive-vi-worker-generation`.

Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/539`.

Corrective implementation head SHA: `dbc07b67f04aa55a8a3816924b33ac6ef2a2285a`.

Corrective main commit: `308cba026ea2cacf4633a33ab516fb6f2e87cf1a`.

Directive VI closes the remaining process-physics defect above Directive V: the old accepted shape still allowed topology environment variables to imply Bayesian task capability and ran the full physical Postgres topology proof in every prefork child. The corrected invariant is explicit role/capability contract plus one physical parent/generation proof. Child processes receive bounded local execution authority derived from that proven generation; they do not open Postgres connections or rerun the physical proof during `worker_process_init`.

### Corrective Action VI Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-DVI-01 expected Bayesian workers can silently start incapable | VALIDATED | Before this remediation, topology env could imply registration, but worker role and required task registry completeness were not governed together. |
| H-P9-DVI-02 worker role and task registration not governed together | VALIDATED | `SKELDIR_CELERY_INCLUDE_BAYESIAN_TASKS` was independent of any explicit worker role; contradictory deployment intent was not fail-closed. |
| H-P9-DVI-03 topology env accidentally controls task capability | VALIDATED | `_bayesian_tasks_registered_for_process()` returned true when any topology env was present. |
| H-P9-DVI-04 every prefork child executes the full physical proof | VALIDATED | `signals.worker_process_init` called `_run_bayesian_worker_boot_topology_probe_if_needed()`, which called the physical DB proof when PID changed. |
| H-P9-DVI-05 no worker generation identity/authenticated child capability | VALIDATED | Proof state was boolean/PID-local, not a generation identity that child task guards could authenticate. |
| H-P9-DVI-06 worker_process_init can exceed lifecycle budget | VALIDATED AS RISK | Child init could run the full DB poison/replacement proof rather than a bounded local operation. |
| H-P9-DVI-07 child probe failure can create respawn loop | VALIDATED | Local Windows spawn surfaced repeated child authority failure before adding the generation authority file bridge; the old design also would have rerun DB work in child failure loops. |
| H-P9-DVI-08 replacement/fleet/degraded DB can amplify connection churn | VALIDATED AS RISK | Full child proofs multiply direct Postgres connections by child count and respawn count. |
| H-P9-DVI-09 task guards do not represent generation authority | VALIDATED | Task entry guards only checked process-local boolean/PID proof, not a generation-bound authenticated authority token. |
| H-P9-DVI-10 CI does not exercise production process physics deeply enough | PARTIAL | The protected P9 DB suite covers physical proof and broker misroute; this remediation adds explicit one-generation/child-authority assertions and preserves POSIX prefork validation in GitHub Actions. |

### Corrective Action VI Remediations Made

1. `backend/app/tasks/bayesian.py`
   - Removed topology env as a Bayesian task capability input.
   - Added explicit `SKELDIR_CELERY_WORKER_ROLE` contract with `bayesian` and `non_bayesian` roles.
   - Preserved `SKELDIR_CELERY_INCLUDE_BAYESIAN_TASKS` as an explicit registration flag, but made role/include contradictions fail closed.
   - Added `REQUIRED_BAYESIAN_TASK_NAMES` and an import-time completeness check when Bayesian tasks are registered.
   - Non-Bayesian workers still structurally omit executable Bayesian task entries; broker-misrouted Bayesian messages remain unregistered.

2. `backend/app/bayesian/worker_boot_probe.py`
   - Replaced boolean/PID proof state with `BayesianWorkerGenerationProof` and `BayesianWorkerExecutionAuthority`.
   - Runs `run_bayesian_worker_boot_topology_probe()` only in the worker parent/generation path.
   - Persists a parent-generated local authority file after the physical proof so spawn-based children can derive authority without DB access; fork children inherit the same proof state in memory.
   - Child init now calls `_derive_bayesian_child_authority_if_needed()` and never calls the physical DB probe.
   - Authority is HMAC-bound to generation id, parent pid, child/current pid, and topology fingerprint.
   - Added `BAYESIAN_CHILD_AUTHORITY_BUDGET_S=0.2` and logs bounded child-authority timing.

3. `backend/app/bayesian/db_boot_probe.py`
   - Extended physical proof results with `worker_connection_count` and `observer_connection_count`.
   - Local evidence from the real-worker probe showed one parent proof, two worker connections, and two observer connections for backend absence checks.

4. Protected launch and CI profiles
   - Updated `backend/Dockerfile.bayesian` to declare the Bayesian worker role explicitly.
   - Updated B2.4 and CI workflow env for Bayesian runtime jobs to set `SKELDIR_CELERY_WORKER_ROLE=bayesian` and `SKELDIR_CELERY_INCLUDE_BAYESIAN_TASKS=1`.
   - Left broad local/e2e launch profiles unchanged because M0/M1 scope guards reserve those surfaces; protected Bayesian CI/runtime profiles carry the Directive VI role contract.
   - Updated the B0.7 P5 real-worker proof to set the same role contract and to skip on Windows because Celery prefork soft/hard timeout semantics require POSIX signals.

5. Tests and validators
   - Added proof that topology env alone does not register Bayesian tasks.
   - Added role/include contradiction fail-closed proof.
   - Updated task-entry guard proof to use generation authority.
   - Added child-init proof that clears in-memory generation state and derives authority from the parent-generated authority file without calling the DB probe.
   - Updated `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py` to reject topology-env registration authority and child physical DB proofs.
   - Updated B2.1-P4 queue isolation lock to require explicit worker role declarations while preserving queue isolation.

### Corrective Action VI Local Validation

Passed:

```text
python -m py_compile backend\app\bayesian\worker_boot_probe.py backend\app\bayesian\db_boot_probe.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py scripts\ci\enforce_b21_p4_queue_isolation_semantics_lock.py backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py
python scripts\ci\validate_b24_p9_worker_tenant_hygiene.py --negative-control
python scripts\ci\enforce_b21_p4_queue_isolation_semantics_lock.py
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b21_p4_queue_isolation_semantics_lock_enforcer.py -q
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py -q
python -m pytest backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py -q
```

Local result details:

- P9 static validator and negative controls: pass.
- B2.1-P4 queue isolation lock: pass.
- P9 unit/static plus B2.1 queue-lock tests: 32 passed.
- P9 protected Postgres runtime suite: 16 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is intentionally opt-in outside protected CI.
- B0.7 P5 real-worker proof: skipped on Windows with explicit reason `Celery prefork soft/hard timeout proof requires POSIX signals`; GitHub Actions Ubuntu remains the authoritative POSIX prefork proof.
- Local probe-log observation from the transient Windows run before the POSIX skip: one parent `bayesian_worker_generation_proof_passed` event with `worker_connection_count=2`, `observer_connection_count=2`, `generation_authority_file=true`, followed by two `bayesian_worker_child_authorized` events and no child physical DB proof events.

### Corrective Action VI Protected Validation

Passed:

- PR #539 merge state before merge: `CLEAN`; merged through GitHub protected-branch workflow on 2026-06-13T18:02:09Z.
- PR #539 status rollup: 138 total contexts; 129 `success`, 9 expected `skipped`/`neutral`, 0 failed or active.
- PR #539 B2.4 Gate Dry Run: run `27474156881`, job `81210285772`, `success`.
- PR #539 `B2.4-P9 Worker Tenant Hygiene Proof`: run `27474156881`, job `81210285760`, `success`.
- PR #539 CI workflow: run `27474156857`, `success`.
- PR #539 M0 scope lock: run `27474156818`, job `81210285543`, `success`.
- PR #539 M1 local development authority: run `27474156880`, job `81210286022`, `success`.
- Post-merge `main` commit: `308cba026ea2cacf4633a33ab516fb6f2e87cf1a`.
- Main B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27474686173`, `success`.
- Main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27474686173/job/81211706991`, `success`.
- Main CI workflow: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27474686138`, `success`.
- Final main workflow poll for landed commit `308cba026ea2cacf4633a33ab516fb6f2e87cf1a`: 32 workflows, 32 `success`, 0 active, 0 failed.

## Follow-Up Corrective Action V - 2026-06-12

**PASS - FOLLOW-UP CORRECTIVE ACTION V AUTHORITATIVELY VALIDATED ON MAIN**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive V`.

Corrective branch: `codex/b24-p9-directive-v-task-authority-20260612`.

Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/537`.

Corrective implementation head SHA: `08f7566bdb720e029da7c3a6c262de09afbeb033`.

Corrective main squash commit: `51b625d2105d2ffd1f35f62ece714cd8fe23faa0`.

Directive V narrows the remaining P9 blocker above the physical DB topology probe: a worker must not be considered safe because of queue arguments or deployment labels. The authoritative invariant is task capability. If a process registers Bayesian task entries, it must complete the physical boot topology proof before it can execute Bayesian work. A non-Bayesian worker may skip that proof only when Bayesian tasks are structurally absent from its registry and broker-misrouted Bayesian messages are rejected as unregistered.

### Corrective Action V Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-DV-01 shared Celery app globally registers Bayesian tasks | VALIDATED | `backend/app/celery_app.py` included `app.tasks.bayesian` for every worker profile before this remediation. |
| H-P9-DV-02 queue arguments controlled boot-probe skipping | VALIDATED | `worker_boot_probe.py` parsed `-Q/--queues` and skipped the physical proof when the queue list did not include `bayesian`. |
| H-P9-DV-03 topology env could act as default-worker capability authority | VALIDATED | The previous default/all-queue decision used presence of topology env values as a probe trigger instead of task registry capability. |
| H-P9-DV-04 Bayesian task entries lacked a proof-state guard | VALIDATED | `run_mcmc_inference`, `execute_fit_intent`, feature-authority tasks, resource contention, and health probe did not assert process-local boot proof at function entry. |
| H-P9-DV-05 prefork child process proof was under-specified | VALIDATED AS RISK | The Directive IV hook covered `worker_init`; Directive V requires process-local proof state, so child process initialization must also be guarded. |
| H-P9-DV-06 launch-profile remediation would breach current scope locks | VALIDATED BY PR CI | The first Directive V PR attempt encoded registry exclusion in shared launch surfaces. M0/M1 rejected that surface, so the final corrective design moves structural registration authority into `backend/app/tasks/bayesian.py`. |
| H-P9-DV-07 broker-misroute behavior was unproven | VALIDATED | There was no real broker proof that a Bayesian message sent to a non-Bayesian worker is rejected as unregistered. |

### Corrective Action V Remediations Made

1. `backend/app/bayesian/worker_boot_probe.py`
   - Removed queue parsing and topology-env based skip authority.
   - Added pid-bound process-local proof state via `bayesian_worker_boot_topology_probe_has_passed()`.
   - Added `assert_bayesian_worker_boot_topology_proven()` and `BayesianWorkerBootTopologyProofMissing`.
   - Registered the physical topology probe on both `signals.worker_init` and `signals.worker_process_init`.
   - Preserved fail-closed `SystemExit("bayesian_worker_boot_topology_probe_failed")` on physical proof failure.

2. `backend/app/tasks/bayesian.py`
   - Added `_bayesian_tasks_registered_for_process()` and `_bayesian_task(...)`.
   - Queue names and worker labels are not inputs to task capability.
   - Processes explicitly setting `SKELDIR_CELERY_INCLUDE_BAYESIAN_TASKS=0` import the module without registering executable Bayesian task entries; broker-misrouted Bayesian messages therefore reject as unregistered.
   - Processes that register Bayesian task entries wire the boot probe and must pass process-local proof.
   - Added `assert_bayesian_worker_boot_topology_proven()` at every externally executable Bayesian task entry before tenant/correlation parsing or DB work.

3. Tests and validators
   - Replaced the old queue-skip proof with registry-exclusion proof.
   - Added explicit enabled/disabled registration subprocess proofs.
   - Added task-entry proof-state tests, including pid-mismatch rejection.
   - Added a protected-DB broker-misroute proof: start a real non-Bayesian worker registry, route `app.tasks.bayesian.health_probe` to its queue, and assert the worker logs an unregistered-task rejection without running the Bayesian health task or boot probe.
   - Updated `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py` to reject queue/env skip authority and require task-module registry gating, child-process hook, task-entry guard, and broker-misroute proof names.
   - Removed the earlier broad launch-profile workaround from the PR diff so M0/M1 scope locks can validate the correction.

### Corrective Action V Local Validation

Passed:

```text
python -m black --check backend/app/tasks/bayesian.py backend/tests/test_b24_p9_worker_tenant_hygiene.py backend/tests/test_b24_p9_postgres_runtime.py scripts/ci/validate_b24_p9_worker_tenant_hygiene.py
python scripts\ci\validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py::test_b24_p9_non_bayesian_registry_rejects_broker_misrouted_bayesian_task -q -rs
```

Local result details:

- P9 static validator and negative controls: pass.
- P9 unit/static proof: 24 passed.
- P9 broker-misroute proof: skipped locally without `SKELDIR_B24_P9_REQUIRE_DB_PROOFS`; this is expected for local runs.
- Forced local protected DB mode cannot run on this workstation because the local Postgres schema is missing `public.bayesian_model_fits`. Protected CI remains the authoritative live broker/runtime proof.
- Formatting check: pass.

### Corrective Action V Protected Validation

- Corrective PR head SHA: `08f7566bdb720e029da7c3a6c262de09afbeb033`.
- PR merge status before merge: `mergeStateStatus=CLEAN`.
- Corrective merge time: `2026-06-12T16:22:42Z`.
- Corrective merge commit / landed `main` SHA: `51b625d2105d2ffd1f35f62ece714cd8fe23faa0`.
- PR `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27427129536`.
- PR `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27427129536/job/81067706496`.
- PR `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27427129462`.
- PR `Governance Guardrails`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27427129462/job/81069284514`.
- Main workflow poll for landed commit: 31 workflows, 31 `success`, 0 active, 0 failed.
- Main `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27428507044`.
- Main `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27428507011`.
- Main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27428507011/job/81072478960`.
- Main `R7: Final Winning State`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27428506948`.

## Follow-Up Corrective Action III - 2026-06-11

**PASS - FOLLOW-UP CORRECTIVE ACTION III AUTHORITATIVELY VALIDATED ON MAIN**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive III`.

Corrective branch: `codex/b24-p9-directive-iii-topology-20260611`.

Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/533`.

Corrective implementation head SHA: `a98f6be5dbd9ce2576d9ff279221478fa8760481`.

Corrective main squash commit: `d26aba78add4e29d667f53357e3a800466641963`.

Chosen topology strategy: Strategy C - hybrid deferred pooler support. Current Bayesian worker DB topology is `direct_postgres` only. External pooler/proxy topologies are unsupported and must fail closed unless a future PR adds live topology-specific poison proof.

### Corrective Action III Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-DIII-01 DSN/hostname classification cannot prove direct PostgreSQL | VALIDATED | Directive II had no topology authority; the worker factory accepted any PostgreSQL endpoint that produced a SQLAlchemy URL. |
| H-P9-DIII-02 NullPool may close only the client-to-proxy socket | VALIDATED AS RISK | NullPool is sufficient for direct PostgreSQL backend replacement, but it cannot prove transparent upstream proxy session destruction. |
| H-P9-DIII-03 obvious pooler keyword rejection may create false safety | VALIDATED AS DESIGN RISK | Keyword checks are useful only as negative controls; absence of keywords cannot prove an opaque hostname is direct PostgreSQL. |
| H-P9-DIII-04 deployment topology may drift from proven CI topology | VALIDATED | The P9 CI job proved `postgres:15-alpine`, but no topology mode/attestation was bound to the protected job. |
| H-P9-DIII-05 external proxy/pooler session state may preserve tenant poison | VALIDATED AS UNSUPPORTED RISK | The repo has no B2.4-P9 live pooler/proxy poison proof; pooler support must remain rejected/deferred. |
| H-P9-DIII-06 defensive DISCARD ALL alone may not prove topology safety | REFUTED BY NON-USE | This remediation does not add `DISCARD ALL` as a completion claim. It rejects unsupported topologies instead. |
| H-P9-DIII-07 runtime proof may prove only CI container topology | VALIDATED | The existing runtime proof is retained and now preceded by explicit direct-postgres topology attestation in the P9 proof job. |
| H-P9-DIII-08 infrastructure truth may be absent from app trust boundary | VALIDATED | No `SKELDIR_BAYESIAN_DB_TOPOLOGY` or attestation source existed before this remediation. |
| H-P9-DIII-09 unknown topology may be allowed by default | VALIDATED IN PROTECTED MODE | Protected runtimes previously had no fail-closed topology mode. |
| H-P9-DIII-10 direct-topology proof may not be bound to deployment governance | VALIDATED | Governance docs and workflow env did not encode topology authority for the P9 job. |
| H-P9-DIII-11 Directive II protections may regress during hardening | VALIDATED AS REQUIRED | P9 and P5/P6/P7/P8 non-regression remain required validation gates. |
| H-P9-DIII-12 older protected CI jobs may invoke the Bayesian worker engine without topology attestation | VALIDATED BY PR CI | PR #533 initially failed `B2.4-P5 PostgreSQL Runtime Proof` with `bayesian_worker_db_topology_missing`; `B2.1-P4 Queue Isolation + Performance Semantics Lock` and `B2.1-P6 Full End-to-End Closure + Downstream Readiness` failed through the same missing topology authority in worker contention artifacts. |

### Corrective Action III Remediations Made

1. `backend/app/bayesian/db_topology.py`
   - Added the canonical Bayesian worker DB topology policy.
   - Supports only `direct_postgres` today.
   - Requires `SKELDIR_BAYESIAN_DB_TOPOLOGY`, `SKELDIR_BAYESIAN_DB_TOPOLOGY_ATTESTATION`, and `SKELDIR_BAYESIAN_DB_TOPOLOGY_SOURCE` in protected/prod-like runtimes.
   - Rejects missing, unknown, unsupported pooler/proxy topology.
   - Treats DSN/hostname tokens only as negative controls through `POOLER_NEGATIVE_CONTROL_TOKENS`; absence of these tokens is not accepted as proof.

2. `backend/app/bayesian/db_engine.py`
   - Calls `resolve_bayesian_worker_db_topology_policy()` before creating the NullPool worker engine.
   - Preserves Directive II `poolclass=NullPool` and non-pooled fail-closed assertion.

3. `.github/workflows/b2_4-gate-dry-run.yml`
   - Binds the protected `B2.4-P9 Worker Tenant Hygiene Proof` job to `SKELDIR_BAYESIAN_DB_TOPOLOGY=direct_postgres`.
   - Binds the protected `B2.4-P5 PostgreSQL Runtime Proof` job to the same explicit direct-postgres topology, because it invokes the Bayesian worker engine through durable timeout fallback.
   - Adds CI attestation `direct_postgres_ci_postgres15`.
   - Adds topology source `github_actions_postgres_15_alpine`.

4. `.github/workflows/ci.yml`
   - Binds the protected `B2.1-P4 Queue Isolation + Performance Semantics Lock` and `B2.1-P6 Full End-to-End Closure + Downstream Readiness` benchmark jobs to explicit direct-postgres topology authority before running Bayesian worker contention tasks.
   - Preserves the runtime policy: CI jobs with direct local PostgreSQL attest directly; unknown/proxy/pooler topologies still fail closed.

5. `backend/tests/test_b24_p9_worker_tenant_hygiene.py`
   - Added static/unit proof that topology policy exists in code and is not DSN proof.
   - Added missing/unknown topology fail-closed proof.
   - Added opaque hostname proof: `db-main.skeldir.internal` is rejected in production unless separate attestation/source is present.
   - Added pooler/proxy fail-closed negative controls.

6. `backend/tests/test_b24_p9_postgres_runtime.py`
   - Added DB-backed proof that direct-postgres topology attestation precedes backend PID replacement proof.
   - Preserved backend PID replacement, session poison, `pg_stat_activity`, dirty invalidation, and representative same-process worker lifecycle proofs.

7. `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py`
   - Validator now requires topology policy tokens, fail-closed errors, CI topology env wiring, and Directive III topology tests.
   - Added topology negative controls for attestation removal, proxy negative-control removal, and removal of CI workflow topology attestation.
   - Requires the B2.4 and CI workflow files to carry explicit direct-postgres topology wiring where protected jobs exercise worker DB paths.

8. `docs/ci/enforcer_registry.yaml` and `docs/ci/gate_subsumption_matrix.yaml`
   - Updated the required P9 invariant to include topology-attested direct PostgreSQL or fail-closed unknown/proxy/pooler topology.

### Corrective Action III Protected Validation Boundary

Authoritative protected workflow evidence:

- PR #533 reached `mergeStateStatus=CLEAN` before merge.
- PR #533 head SHA: `a98f6be5dbd9ce2576d9ff279221478fa8760481`.
- PR #533 merged through protected branch workflow as squash commit `d26aba78add4e29d667f53357e3a800466641963`.
- PR `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27365561262`.
- PR `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27365561262/job/80864008754`.
- PR `B2.4-P5 PostgreSQL Runtime Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27365561262/job/80864008753`.
- PR `B2.1-P4 Queue Isolation + Performance Semantics Lock`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27365561326/job/80864229283`.
- PR `B2.1-P6 Full End-to-End Closure + Downstream Readiness`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27365561326/job/80865795373`.
- Main `B2.4 Gate Dry Run` for landed commit: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27366710754`.
- Main `CI` workflow for landed commit: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27366710748`.
- Main `R6: Worker Resource Governance (Context Gathering)` initially timed out on a Celery prefetch probe, then passed on rerun: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27366710510`.
- Final main workflow poll for landed commit `d26aba78add4e29d667f53357e3a800466641963`: 32 workflows, 32 `success`, 0 active, 0 failed.

Directive III does not claim pooler/proxy support. Poolers/proxies remain explicitly unsupported and deferred pending future live topology-specific poison proof.

## Follow-Up Corrective Action IV - 2026-06-11

**PASS - DIRECTIVE IV AUTHORITATIVELY VALIDATED ON MAIN**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive IV`.

Corrective branch: `codex/b24-p9-directive-iv-boot-probe-20260611`.

Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/535`.

Corrective implementation head SHA: `f6c470d9b8140bfc64967daf80b503ce4afd885b`.

Corrective main merge commit: `79cedfcdc31b6b6f7e3b1e436b9e1682bacee93c`.

Directive IV narrows the remaining P9 blocker to boot-time production-path physics: a Bayesian worker must not become ready or consume Bayesian tasks until it has physically proven that its configured database session boundary behaves like direct PostgreSQL under the accepted `NullPool` strategy.

### Corrective Action IV Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-DIV-01 no boot-time physical topology probe exists | VALIDATED | Prior code had topology policy and pytest DB poison proof, but no Celery worker boot hook that ran the physical proof before readiness or task consumption. |
| H-P9-DIV-02 topology proof currently runs only in pytest | VALIDATED | Session poison injection, backend PID capture, and `pg_stat_activity` inspection were present in `backend/tests/test_b24_p9_postgres_runtime.py`, not in production worker startup code. |
| H-P9-DIV-03 topology policy is enforced at engine creation but not worker boot | VALIDATED | `create_bayesian_worker_engine()` resolved topology, but that could occur during task execution rather than before a Bayesian worker advertised readiness. |
| H-P9-DIV-04 worker can accept Bayesian task after failed topology proof | VALIDATED AS PRIOR RISK | No production startup failure path existed for a failed physical topology proof. |
| H-P9-DIV-05 probe timing may be too late | VALIDATED AS PRIOR RISK | No `worker_init` pre-consumption probe existed; task-level checks would be too late. |
| H-P9-DIV-06 boot probe may not inject meaningful session poison | VALIDATED AS PRIOR GAP | No boot probe injected session-level GUC, search path, advisory lock, and temp object poison. |
| H-P9-DIV-07 boot probe may not observe the old backend independently | VALIDATED AS PRIOR GAP | Old backend PID observation existed only in test helpers. |
| H-P9-DIV-08 unknown/proxy/pooler rejection remains task-level only | VALIDATED AS PRIOR GAP | Unsupported topology could fail on worker engine creation inside task paths, not at Bayesian worker startup. |
| H-P9-DIV-09 full P9 scope may regress during boot-probe remediation | VALIDATED AS REQUIRED | P9 runtime/process/artifact/log hygiene and P5/P6/P7/P8 validators were rerun locally and remain green at this branch boundary. |
| H-P9-DIV-10 B0.7 Phase 8 Bayesian worker proofs may lack topology attestation | VALIDATED DURING PR CI | PR #535 initially made P9 green but failed B0.7 Phase 8 checks because `test_b07_p5_bayesian_timeout_runtime.py` started a Bayesian worker under `CI=true` without Directive III topology attestation env. |
| H-P9-DIV-11 unrelated default-queue proof workers can be misclassified as Bayesian-capable | VALIDATED DURING PR CI | PR #535 exposed `b0545-convergence` starting an unscoped default Celery worker without Bayesian topology authority. Treating every default/all-queue worker as Bayesian-capable incorrectly applied the Bayesian boot gate to a legacy non-Bayesian proof worker. |

### Corrective Action IV Remediations Made

1. `backend/app/bayesian/db_boot_probe.py`
   - Added `run_bayesian_worker_boot_topology_probe()`.
   - Uses the production worker DB path through `create_bayesian_worker_engine()`.
   - Resolves Directive III topology policy before physical proof.
   - Opens worker connection A, captures `old_pid`, injects session-level poison, and closes through the worker path.
   - Poison set: session-level `app.current_tenant_id`, `search_path`, session-level advisory lock, and a unique temporary table.
   - Opens an independent observer connection and polls `pg_stat_activity` until `old_pid` is absent.
   - Opens worker connection B, captures `new_pid`, verifies `new_pid != old_pid`, verifies poison absence, verifies advisory lock release, then observes backend closure.
   - Uses only unique synthetic identifiers; no tenant/customer/provider data, PII, deterministic attribution truth, or durable rows are written.
   - Does not import PyMC, PyTensor, ArviZ, LLM/provider code, object storage, or public API surfaces.

2. `backend/app/bayesian/worker_boot_probe.py` and `backend/app/tasks/bayesian.py`
   - Added a fatal Bayesian boot topology gate registered on Celery `worker_init` through the existing Bayesian task include path.
   - `app.tasks.bayesian` explicitly calls `ensure_bayesian_worker_boot_probe_signal_registered()` at import time, so Bayesian-capable workers load the pre-consumption gate with the task module they already include.
   - Queue parsing treats workers with `--queues=bayesian` / `-Q bayesian` as Bayesian-capable.
   - Default/all-queue workers run the boot probe only when Bayesian topology authority is declared through `SKELDIR_BAYESIAN_DB_TOPOLOGY`, `SKELDIR_BAYESIAN_DB_TOPOLOGY_ATTESTATION`, or `SKELDIR_BAYESIAN_DB_TOPOLOGY_SOURCE`, or when `SKELDIR_BAYESIAN_BOOT_PROBE_REQUIRED` is explicitly set.
   - Explicit non-Bayesian queue workers skip the probe.
   - Probe failure raises `SystemExit("bayesian_worker_boot_topology_probe_failed")` from `worker_init`, before task reservation/consumption.
   - Existing task-time topology policy remains as a secondary guard.

3. `backend/tests/test_b24_p9_worker_tenant_hygiene.py`
   - Added static/unit proof that the boot probe is physical rather than connectivity/config-only.
   - Added proof that the Bayesian task include registers a `worker_init` hook and that the hook is not deferred to `worker_ready` or `task_prerun`.
   - Added queue-scope policy proof for explicit Bayesian workers, topology-declared default workers, forced-probe workers, explicit non-Bayesian workers, and unrelated default workers.

4. `backend/tests/test_b24_p9_postgres_runtime.py`
   - Added DB-backed proof that the boot probe physically proves the session boundary.
   - Added negative-control proof that a failed boot probe is fatal before task consumption.
   - Added proof that explicitly non-Bayesian workers do not run the Bayesian boot probe.

5. `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py`
   - Validator now requires the boot-probe file, poison tokens, independent backend observation, Celery `worker_init` wiring, fatal startup behavior, and new proof names.
   - Added negative controls for removing advisory-lock poison, removing the worker-init boot hook, and removing default-worker topology authority classification.
   - Added topology-attestation requirements for CI jobs that launch Bayesian-capable Celery workers.

6. `.github/workflows/ci.yml`
   - Added explicit direct-Postgres topology attestation to `B0.7 P2 Runtime Proof (LLM + Redaction)` and `Celery Foundation B0.5.1`, because both start real workers that can consume Bayesian tasks.

7. `backend/tests/integration/test_b07_p5_bayesian_timeout_runtime.py`
   - Added explicit direct-Postgres topology attestation to the B0.7 P5 worker proof subprocess that Phase 8 executes.
   - Kept the boot probe fail-closed; the remediation supplies missing physical topology authority rather than weakening the probe.
   - Extended the P9 validator with B0.7 P5 runtime worker topology wiring requirements and a negative control for removing that attestation.

### Corrective Action IV Local Validation

Passed:

```text
python -m py_compile backend\app\bayesian\db_boot_probe.py backend\app\bayesian\worker_boot_probe.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m black --check backend\app\bayesian\db_boot_probe.py backend\app\bayesian\worker_boot_probe.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m ruff check backend\app\bayesian\db_boot_probe.py backend\app\bayesian\worker_boot_probe.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m py_compile backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py
python -m ruff check backend\tests\integration\test_b07_p5_bayesian_timeout_runtime.py
python scripts\ci\validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py -q -rA
python scripts\ci\validate_b24_p5_runtime_harness.py --negative-control
python scripts\ci\validate_b24_p6_real_fit_worker.py --negative-control
python scripts\ci\validate_b24_p7_diagnostics.py --negative-control
python scripts\ci\validate_b24_p8_artifact_lifecycle.py --negative-control
git diff --check
```

Local result details:

- P9 unit/static proof: 21 passed.
- P9 PostgreSQL runtime proof: 14 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is intentionally opt-in outside protected CI. Protected CI sets the flag and provisions Postgres.
- P5/P6/P7/P8 validators: pass.

### Corrective Action IV Protected Validation

Protected PR validation completed:

- PR #535 reached `mergeStateStatus=CLEAN` at head `f6c470d9b8140bfc64967daf80b503ce4afd885b`.
- Required PR checks were green before merge.
- PR B2.4-P9 proof: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27379706074/job/80912972439`.
- PR aggregate CI workflow: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27379706162`.
- PR Phase 8 full physics: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27379706146`.

Protected main validation completed:

- PR #535 landed on protected `main` as `79cedfcdc31b6b6f7e3b1e436b9e1682bacee93c`.
- Main push workflow poll for `79cedfcdc31b6b6f7e3b1e436b9e1682bacee93c`: 32/32 workflows completed successfully.
- Main aggregate CI workflow: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27380538032`.
- Main B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27380538035`.
- Main Phase 8 closure pack: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27380537997`.
- Main R7 final winning state: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27380538050`.

Directive IV claims only B2.4-P9 boot-topology corrective scope. It does not claim P10 completion, full B2.4 completion, TrustEnvelope confidence delivery, production attribution correctness, or pooler/proxy support.

## Follow-Up Corrective Action II - 2026-06-09

**PASS - FOLLOW-UP CORRECTIVE ACTION II AUTHORITATIVELY VALIDATED ON MAIN**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive II`.

Corrective branch: `codex/b24-p9-pool-sanitation-20260609`.

Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/531`.

Corrective implementation commit: `e8295220bb3efa3c8273ba319df2e49bda40e1b5`.

Corrective index commit: `f9ec775c41f0d97bb2583a98c31c9f5eabc12e1a`.

Corrective main merge commit: `46aadeb37317823688a41834b1702c33a1a65ef1`.

Protected merge time: `2026-06-09T17:12:43Z`.

This iteration addresses the remaining worker DB connection physics gap: previous P9 evidence proved transaction-local binding and dirty-check detection, but Bayesian worker task paths still created bounded pooled sync engines (`pool_size=1`, `max_overflow=0`). That was not structural sanitation because the same physical PostgreSQL backend could be returned to later task work with session-level poison intact.

### Corrective Action II Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-FCA-II-01 structural PostgreSQL pool sanitation absent | VALIDATED | Worker task paths used direct `create_engine(... pool_size=1, max_overflow=0)` rather than a non-reuse boundary. |
| H-P9-FCA-II-02 reset may execute inside transaction | VALIDATED AS RISK | No reset strategy existed, so no proof showed reset work occurred outside transaction state. |
| H-P9-FCA-II-03 sanitized connection may be idle-in-transaction | VALIDATED AS RISK | Previous proof did not query `pg_stat_activity` for backend state after failure/cleanup. |
| H-P9-FCA-II-04 session-level GUC poison detected but not remediated | VALIDATED | A dirty pooled checkout raised, but the production path did not structurally remove that backend from reuse. |
| H-P9-FCA-II-05 poison coverage too narrow | VALIDATED | Previous poison coverage focused on tenant GUC and did not cover search path, advisory lock, temp object, and backend transaction state together. |
| H-P9-FCA-II-06 dirty-check detection may not protect production | VALIDATED | Production task engine construction bypassed any enforced non-pooled worker DB factory. |
| H-P9-FCA-II-07 reset failure may return dirty connection | REMEDIATED BY DESIGN | The accepted Pattern D remediation removes the reset/reuse path by closing/replacing physical connections with `NullPool`. |
| H-P9-FCA-II-08 representative worker path insufficient | VALIDATED | Previous same-process proof was mostly helper-level; it did not exercise fit row state, artifact persistence, child supervision, terminal write, cleanup, and repeat in one parent process. |
| H-P9-FCA-II-09 per-task `pool_size=1` mistaken as sanitation | VALIDATED | Single-connection pools still preserve backend session state across checkouts. |
| H-P9-FCA-II-10 P5/P6/P7/P8 non-regression required | VALIDATED AS REQUIRED | Adjacent validators must stay green because P9 touches Bayesian worker runtime plumbing. |

### Corrective Action II Remediations Made

1. `backend/app/bayesian/db_engine.py`
   - Added the canonical sync worker DB engine factory.
   - `create_bayesian_worker_engine()` uses `poolclass=NullPool` and `pool_pre_ping=True`.
   - `assert_bayesian_worker_engine_nonpooled()` fails closed with `bayesian_worker_engine_must_use_nullpool` if the engine can reuse physical connections.
   - `runtime_sync_database_url()` preserves the existing runtime DSN normalization behavior without duplicating it in task modules.

2. `backend/app/tasks/bayesian.py`
   - Replaced direct pooled `create_engine()` construction in Bayesian sync worker paths with `create_bayesian_worker_engine()`.
   - Removed `pool_size=1` / `max_overflow=0` as a false sanitation signal.
   - Preserved explicit `engine.dispose()` on task completion.

3. `backend/tests/test_b24_p9_postgres_runtime.py`
   - Added PostgreSQL proof that the worker engine is structurally `NullPool`.
   - Added poison proof for session-level tenant GUC, `search_path`, advisory lock, temp table, and backend replacement.
   - Added `pg_stat_activity` proof that closed/replaced worker backends are absent or not `idle in transaction`.
   - Added invalidated dirty-connection proof to represent the reset-failure surface under Pattern D.
   - Added representative same-process worker lifecycle proof: Tx1 marks fit running; runtime creates workspace/compiledir, builds child env, supervises and kills/reaps a child; Tx2 persists governed diagnostics artifact; Tx3 marks terminal failure; cleanup executes; the sequence repeats for another tenant in the same parent process with sentinel non-leak checks.

4. `backend/tests/test_b24_p9_worker_tenant_hygiene.py`
   - Added unit/static proof that the worker factory is non-pooled.
   - Added static proof that Bayesian tasks use the worker factory and no longer directly import/use `create_engine` with single-connection pool parameters.

5. `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py`
   - Validator now requires the new worker DB engine file, task factory wiring, `NullPool` token, backend poison proof names, and `pg_stat_activity`/advisory-lock/temp-table proof tokens.
   - Added negative controls for losing `NullPool` and bypassing the worker factory.

### Corrective Action II Local Validation

Passed:

```text
python -m py_compile backend\app\bayesian\db_engine.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m black --check backend\app\bayesian\db_engine.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m ruff check backend\app\bayesian\db_engine.py backend\app\tasks\bayesian.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python scripts\ci\validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py -q -rA
python scripts\ci\validate_b24_p5_runtime_harness.py --negative-control
python scripts\ci\validate_b24_p6_real_fit_worker.py --negative-control
python scripts\ci\validate_b24_p7_diagnostics.py --negative-control
python scripts\ci\validate_b24_p8_artifact_lifecycle.py --negative-control
```

Local result details:

- P9 unit/static proof: 14 passed.
- P9 PostgreSQL runtime proof: 10 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is intentionally opt-in outside protected CI. Protected CI sets the flag and provisions Postgres.
- P5/P6/P7/P8 validators: pass.

### Corrective Action II Protected Main Validation

Authoritative protected workflow evidence:

- PR #531 was `MERGED` through the protected branch workflow after `mergeStateStatus=CLEAN`.
- PR B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27222038464`.
- PR `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27222038464/job/80379093666`.
- PR Governance Guardrails: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27222038635/job/80379397947`.
- Landed main merge commit: `46aadeb37317823688a41834b1702c33a1a65ef1`.
- Main B2.4 Gate Dry Run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27223054919`.
- Main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27223054919/job/80382724818`.
- Main CI: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27223055152`.
- Main Governance Guardrails: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27223055152/job/80383119271`.
- Main R7 final state: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27223054905`.
- Main workflow poll at `2026-06-09T12:42:00-05:00`: 31 completed workflows, 31 `success`, 0 active, 0 failed.

This section claims only Directive II closure for B2.4-P9 worker tenant hygiene/process isolation. It does not claim P10 completion, full B2.4 completion, production Bayesian attribution correctness, or human phase-owner acceptance.

## Follow-Up Corrective Action - 2026-06-08

**PASS - FOLLOW-UP CORRECTIVE ACTION AUTHORITATIVELY VALIDATED ON MAIN**

Corrective directive: `B2.4-P9 Follow-Up Corrective Action Remediation Directive`.

Corrective branch: `codex/b24-p9-follow-up-corrective-20260608`.

Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/529`.

Corrective head commit: `25fbdece51507d984ca7007f1703bcc25ee3756a`.

Corrective main merge commit: `2c922212ddc93822b580ee5652ea11c2a25d3409`.

Follow-up status at this evidence iteration: corrective code landed on protected `main` through PR #529, all PR checks were green before merge, and the landed main merge commit completed a full green main workflow set. This section supersedes any over-broad claim that the original P9 proof alone established same-process, concurrent-tenant, multi-transaction, janitor-race, native-lifecycle, and behavioral log/failure hygiene closure.

### Follow-Up Initial Findings

| Hypothesis | Classification | Finding |
|---|---|---|
| H-P9-FCA-01 same-process sequential tenant proof missing | VALIDATED | Previous P9 proof did not assert Tenant A and Tenant B ran in the same OS parent worker process. |
| H-P9-FCA-02 sequential proof too DB-local | VALIDATED | Previous sequential proof primarily covered SQLAlchemy transaction-local GUC cleanup, not workspace, compiledir, child-env, artifact-ref, cleanup, and failure-payload surfaces. |
| H-P9-FCA-03 multi-transaction tenant rebinding under-proven | PARTIAL | Production code already rebound in several fit transactions, but artifact persistence trusted callers and the proof did not show a failing omitted-bind transaction. |
| H-P9-FCA-04 session-level GUC may poison pool | VALIDATED AS RISK | Normal Bayesian path avoided session-level tenant SET, but there was no behavioral proof that session-level GUC poisoning is detected and rollback-on-return is insufficient. |
| H-P9-FCA-05 multi-transaction task proof missing | VALIDATED | No required P9 test exercised at least three tenant-scoped transactions separated by task-flow boundaries. |
| H-P9-FCA-06 concurrent tenant isolation absent | VALIDATED | No deterministic barrier-synchronized concurrent Tenant A/B proof existed. |
| H-P9-FCA-07 concurrent janitor TOCTOU race can crash worker | VALIDATED | Workspace and compiledir janitor locks used exclusive creation without handling expected `FileExistsError`; deletion raced on `FileNotFoundError`. |
| H-P9-FCA-08 native-memory lifecycle unproven | PARTIAL | P5 proved child runtime controls, but P9 did not assert parent process native-stack airgap and child-per-fit lifecycle. |
| H-P9-FCA-09 runtime proof mostly token/static | VALIDATED | Static validator existed but did not require the load-bearing behavioral proof names. |
| H-P9-FCA-10 log/telemetry hygiene statically checked | VALIDATED | No sentinel-based runtime failure-payload check existed for retained child stream data. |
| H-P9-FCA-11 PyTensor import-order under-proven | PARTIAL | P5 child-env/import proofs existed; P9 did not assert child env compiledir propagation in its same-process worker proof. |
| H-P9-FCA-12 DB proof skip logic too permissive | VALIDATED | `CI=true` could execute the DB proof without failing specifically on missing `SKELDIR_B24_P9_REQUIRE_DB_PROOFS=1`. |

### Follow-Up Remediations Made

1. `backend/app/bayesian/temp_workspace.py`
   - Janitor lock contention now returns bounded `lock_contended` evidence instead of propagating `FileExistsError`.
   - Already-deleted metadata/path races are idempotently accepted.
   - Reaper refuses paths that resolve outside the root or traverse symlinked owned-looking children.
   - Report payloads include `lock_contended` without exposing tenant payload data.

2. `backend/app/bayesian/compiledir_reaper.py`
   - Mirrored the workspace janitor race-safety model for PyTensor compiledirs.
   - Preserved bounded scan/deletion limits and ownership metadata checks.

3. `backend/app/bayesian/fit_execution.py`
   - `_set_tenant_context` now immediately asserts the transaction-local tenant GUC.
   - Added `_sampler_failure_stream_metadata` so parent failure payloads expose byte counts/truncation only, never retained stdout/stderr text.

4. `backend/app/bayesian/artifact_repository.py`
   - Sync artifact persist, verify, and prune paths now require an already-bound transaction-local tenant GUC.
   - This makes omitted tenant binding at artifact-write transaction boundaries fail closed instead of relying on caller convention.

5. `backend/tests/test_b24_p9_worker_tenant_hygiene.py`
   - Added same-process sequential reused-worker proof with equal parent PID, `max_tasks_per_child != 1`, tenant A/B sentinels, workspace/compiledir/artifact/child-env/cleanup coverage, and B-only baseline comparison.
   - Added deterministic concurrent tenant runtime-surface proof for distinct workspaces, compiledirs, artifact refs, and cleanup locality.
   - Added `test_b24_p9_concurrent_janitor_toctou_safe`.
   - Added sentinel-based failure-payload proof that retained child stream bytes are not serialized into parent payloads.
   - Added child-per-fit/native lifecycle proof showing the parent does not import `pymc`, `pytensor`, or `arviz`, launches a distinct child PID, kills/reaps it under supervision, and removes the compiledir.

6. `backend/tests/test_b24_p9_postgres_runtime.py`
   - Hardened protected-CI proof semantics: in `CI=true`, missing `SKELDIR_B24_P9_REQUIRE_DB_PROOFS=1` fails.
   - Added session-level GUC poisoning proof showing rollback-on-return is not treated as sufficient cleanup.
   - Added multi-transaction task-flow proof with a negative omitted-bind artifact transaction.
   - Added barrier-synchronized concurrent Tenant A/B proof using independent DB connections, distinct runtime paths, distinct artifact refs, no cross-tenant row visibility, and clean GUC checkout after commit.

7. `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py`
   - Static validator remains as a supplementary guard.
   - Validator now requires the behavioral proof names, janitor race-safety tokens, artifact transaction-boundary assertions, and DB proof non-skip enforcement.
   - Negative controls now fail when janitor lock handling or artifact tenant-bound assertions are removed.

### Follow-Up Local Validation

Passed:

```text
python -m py_compile backend\app\bayesian\fit_execution.py backend\app\bayesian\artifact_repository.py backend\app\bayesian\temp_workspace.py backend\app\bayesian\compiledir_reaper.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m black backend\app\bayesian\fit_execution.py backend\app\bayesian\artifact_repository.py backend\app\bayesian\temp_workspace.py backend\app\bayesian\compiledir_reaper.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python -m ruff check backend\app\bayesian\fit_execution.py backend\app\bayesian\artifact_repository.py backend\app\bayesian\temp_workspace.py backend\app\bayesian\compiledir_reaper.py backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p9_postgres_runtime.py scripts\ci\validate_b24_p9_worker_tenant_hygiene.py
python scripts\ci\validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py -q -rA
python scripts\ci\validate_b24_p5_runtime_harness.py --negative-control
python scripts\ci\validate_b24_p6_real_fit_worker.py --negative-control
python scripts\ci\validate_b24_p7_diagnostics.py --negative-control
python scripts\ci\validate_b24_p8_artifact_lifecycle.py --negative-control
git diff --check
```

Expected local skip:

```text
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py -q -rA
```

Result: 5 skipped locally because `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is intentionally opt-in outside protected CI. Protected CI sets the flag and provisions Postgres.

Expected negative control:

```text
$env:CI='true'; $env:DATABASE_URL='postgresql://app_user:app_user@127.0.0.1:5432/skeldir_b24_p9_ci'; $env:MIGRATION_DATABASE_URL='postgresql://migration_owner:migration_owner@127.0.0.1:5432/skeldir_b24_p9_ci'; $env:SKELDIR_B24_P9_REQUIRE_DB_PROOFS=''; python -m pytest backend\tests\test_b24_p9_postgres_runtime.py::test_b24_p9_db_proof_requires_explicit_flag_in_ci -q
```

Result: failed as expected because `_require_db_proofs()` was false under `CI=true`.

### Follow-Up Protected Main Validation

- Corrective PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/529`.
- Corrective PR head SHA: `25fbdece51507d984ca7007f1703bcc25ee3756a`.
- PR merge status before merge: `mergeStateStatus=CLEAN`.
- Required-status contract enforcement before merge: `python scripts\ci\enforce_required_status_checks.py` passed.
- Merge time: `2026-06-08T20:13:05Z`.
- Corrective merge commit / final main SHA: `2c922212ddc93822b580ee5652ea11c2a25d3409`.
- Main workflow poll: 31 workflows, 31 `success`, 0 active, 0 failed.
- Main `CI`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27164130464`, 78 successful jobs and 1 expected skip.
- Main `B2.4 Gate Dry Run`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27164130367`.
- Main `B2.4-P9 Worker Tenant Hygiene Proof`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27164130367/job/80186822219`.
- Main `R7: Final Winning State`: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27164129973`.

### Follow-Up Non-Overclaim Boundary

This evidence iteration does not claim P10 completion, full B2.4 completion, TrustEnvelope confidence delivery, production Bayesian attribution correctness, or acceptance by a human phase owner. It claims only that the B2.4-P9 follow-up corrective action landed on protected `main` and that required main CI, including `B2.4-P9 Worker Tenant Hygiene Proof`, was green for the landed corrective merge commit.

## Initial Findings

1. Worker tenant binding existed in the runtime path, but there was no reusable `SET LOCAL` tenant-context helper with a checkout-clean proof.
2. The PyTensor compiledir lease was attempt-scoped, but not explicitly tenant-, fit-, and source-snapshot-hash scoped.
3. Child IPC files lived beneath the compiledir lease, conflating compiled artifacts with transient execution workspace state.
4. Artifact references were fit-scoped (`b24://artifact/{fit_id}/...`) and omitted tenant authority from the durable URI.
5. Child failure payloads exposed retained stderr text to the parent result payload.
6. There was no P9-specific static negative-control validator or Postgres runtime proof gate.
7. Governance files did not list an independent `B2.4-P9 Worker Tenant Hygiene Proof` required context.

## Hypothesis Ledger

| Hypothesis | Initial state | Remediation | Falsifiable proof |
|---|---|---|---|
| H-P9-01: tenant GUC can survive worker connection reuse | Plausible | Added transaction-local tenant binding and clean checkout assertion | P9 Postgres test injects failure, then verifies fresh checkout has no tenant GUC or open transaction |
| H-P9-02: sequential tenant fits can leak through RLS/session state | Unproven | Added `tenant_transaction` helper and sequential-tenant DB proof | P9 Postgres test reads two tenant rows under alternating `app.current_tenant_id` values |
| H-P9-03: compiledir state can cross tenants/fits/sources | Plausible | Compiledir path now includes worker, tenant, fit, source hash, parent pid, execution id | P9 unit proof validates path segments and negative-control validator rejects source-hash removal |
| H-P9-04: temporary IPC/workspace residue can survive attempts | Plausible | Added parent-owned workspace lease and cleanup janitor | P9 unit proof validates cleanup and bounded reaper behavior |
| H-P9-05: artifact refs omit tenant authority | True | Artifact refs now include tenant id and DB constraint was migrated | P9 unit proof and migration/model regex require tenant-bound active refs |
| H-P9-06: failure payload can retain child stderr text | True | Parent payload now reports stderr byte counts/truncation only | P9 validator rejects `stderr_retained` text payload reintroduction |
| H-P9-07: CI has no P9 adjudicator | True | Added workflow job, Make target, enforcer registry, topology, subsumption, required check contract | Governance validators and GitHub CI must pass |

## Remediations

### Runtime Isolation

- Added `backend/app/bayesian/tenant_context.py`.
- Added `backend/app/bayesian/temp_workspace.py`.
- Added `backend/app/bayesian/cleanup.py`.
- Updated `backend/app/bayesian/fit_execution.py` to:
  - run preflight janitor cleanup;
  - assert fresh DB checkout cleanliness before and after execution;
  - bind tenants with transaction-local `set_config(..., true)`;
  - create tenant/fit/source-scoped compiledirs;
  - move IPC under an isolated workspace lease;
  - clean workspace and compiledir in `finally`;
  - avoid returning retained stderr text in failure payloads.

### Durable Artifact Authority

- Updated `backend/app/bayesian/artifact_repository.py` so active artifact refs use:

```text
b24://artifact/{tenant_id}/{fit_id}/{artifact_type}/{hash12}
```

- Updated `backend/app/bayesian/models.py`.
- Added Alembic migration `202606081200_b24_p9_worker_tenant_hygiene.py` to rewrite existing active refs and enforce the tenant-bound regex.

### CI / Governance

- Added `scripts/ci/validate_b24_p9_worker_tenant_hygiene.py`.
- Added unit/static tests in `backend/tests/test_b24_p9_worker_tenant_hygiene.py`.
- Added opt-in Postgres runtime proofs in `backend/tests/test_b24_p9_postgres_runtime.py`.
- Added workflow job `B2.4-P9 Worker Tenant Hygiene Proof` in `.github/workflows/b2_4-gate-dry-run.yml`.
- Added `validate-b24-p9-worker-tenant-hygiene` Make target.
- Added P9 gate entries in:
  - `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
  - `docs/ci/enforcer_registry.yaml`
  - `docs/ci/ci_topology_map.md`
  - `docs/ci/gate_subsumption_matrix.yaml`

## Local Validation

The following local validations passed on the implementation branch:

```text
python -m py_compile backend\app\bayesian\fit_execution.py backend\app\bayesian\compiledir_reaper.py backend\app\bayesian\tenant_context.py backend\app\bayesian\temp_workspace.py backend\app\bayesian\cleanup.py backend\app\bayesian\artifact_repository.py backend\app\bayesian\models.py
alembic heads
python scripts\ci\validate_b24_p9_worker_tenant_hygiene.py --negative-control
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py -q
python -m pytest backend\tests\test_b24_p9_postgres_runtime.py -q -rA
python scripts\ci\validate_b24_p8_artifact_lifecycle.py --negative-control
python scripts\ci\validate_b24_p5_runtime_harness.py --negative-control
python scripts\ci\validate_b24_p6_real_fit_worker.py --negative-control
python -m pytest backend\tests\test_b24_p8_artifact_lifecycle.py backend\tests\test_b24_p5_runtime_harness.py::test_b24_p6_parent_recomputes_after_db_failure_without_staging -q
python scripts\ci\validate_m3_ci_governance.py --registry
python scripts\ci\validate_m3_ci_governance.py --subsumption
python scripts\ci\validate_m3_ci_governance.py --topology
python scripts\ci\validate_m3_ci_governance.py --b24-dry-run
python -m pytest backend\tests\test_b24_p9_worker_tenant_hygiene.py backend\tests\test_b24_p8_artifact_lifecycle.py -q
python -m pytest backend\tests\test_b24_p6_real_fit_worker.py::test_b24_p6_parent_keeps_pymc_child_only backend\tests\test_b24_p6_real_fit_worker.py::test_b24_p6_fit_id_only_resolution_is_explicit_rls_capability backend\tests\test_b24_p6_real_fit_worker.py::test_b24_p6_ci_validator_negative_controls -q
python -m pytest backend\tests\test_b24_p5_runtime_harness.py::test_b24_p5_reaper_preserves_foreign_and_deletes_expired_owned -q
```

Local note: `backend/tests/test_b24_p9_postgres_runtime.py` is intentionally opt-in outside CI and skipped locally when `SKELDIR_B24_P9_REQUIRE_DB_PROOFS` is not set. The workflow sets `SKELDIR_B24_P9_REQUIRE_DB_PROOFS=1` and provisions Postgres before running the proof.

## CI / Merge Evidence

Authoritative protected workflow evidence:

- PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/527`
- PR head SHA: `612887cfa56d06f5e01371d687bbf3464a5cb05d`
- PR merge status before merge: `mergeStateStatus=CLEAN`
- Merge time: 2026-06-08T15:32:33Z
- Merge commit / landed `main` SHA: `a866f6d088b251558b8a3d4552bc6a1119965129`
- Branch-protection contract verifier: `python scripts\ci\enforce_required_status_checks.py` passed against `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- PR aggregate CI run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27147550732`
- PR B2.4 gate run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27147549868`
- PR P9 proof job: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27147549868/job/80129381429`
- Main full workflow poll: 36 workflows, 36 `success`, 0 active, 0 failed
- Main CI run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27148605845`
- Main B2.4 gate run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27148605741`
- Main P9 proof job: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27148605741/job/80133211017`
- Main R7 final winning state run: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/27148605458`

## Exit Gate Status

| Gate | Status | Evidence |
|---|---|---|
| Tenant transaction hygiene | Pass | P9 unit and Postgres proofs; main P9 proof job `80133211017` |
| Worker process isolation | Pass | workspace/compiledir tests and static negative controls; main B2.4 gate run `27148605741` |
| Artifact authority | Pass | artifact ref tests, model regex, Alembic migration; main CI run `27148605845` |
| Failure residue control | Pass | cleanup tests and stderr-airgap negative control; main P9 proof job `80133211017` |
| Governance adjudication | Pass | workflow, required-check contract, registry/topology/subsumption validators; live branch-protection verifier passed |
| Protected `main` closure | Pass | PR #527 merged; landed main SHA `a866f6d088b251558b8a3d4552bc6a1119965129`; 36/36 main workflows green |

## Non-Claims

1. This remediation does not introduce Redis, Kafka, NoSQL, alternate Celery brokers, identity stitching, or PII persistence.
2. This remediation does not let LLMs compute statistical or financial outputs.
3. This remediation does not claim local Postgres DB closure when the opt-in DB proof is skipped; CI is the authoritative DB proof surface.
