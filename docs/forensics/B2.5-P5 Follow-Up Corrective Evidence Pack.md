# B2.5-P5 Follow-Up Corrective Evidence Pack

## Verdict

VERDICT: B2.5-P5 FOLLOW-UP PASS — HARDENING VALIDATION COMPLETE

Protected main SHA: `b0230d0f9b0e7065f8102a33bcbdf75d70106026`

CI status: main workflow census for `b0230d0f9b0e7065f8102a33bcbdf75d70106026` was `30` total, `30` success, `0` pending, `0` failed.

Next authorized phase: B2.5-P6 - Refusal, Degraded, and Reason-Code Truth Matrix.

## Identification

- Directive: B2.5-P5 Follow-Up Corrective Action Directive.
- Starting main SHA: `cf7b2639e8e832e21569cc8a4ca07c1c9c97e54d`.
- Implementation branch: `codex/b25-p5-follow-up-hardening`.
- Final branch SHA: `0c145c81e48427896fdd58ff1def948997366444`.
- PR: #594, https://github.com/Synergyscape-V1/skeldir-2.0/pull/594.
- Protected main SHA after corrective merge: `b0230d0f9b0e7065f8102a33bcbdf75d70106026`.
- Evidence finalization branch: `codex/b25-p5-follow-up-evidence`.

## Files Changed

- `backend/tests/trust/test_b25_p5_builder.py`
- `scripts/ci/validate_b25_p5_builder.py`
- `docs/forensics/B2.5-P5 Follow-Up Corrective Evidence Pack.md`
- `docs/forensics/B2.5-P5 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`

## Initial Findings

The baseline P5 implementation on `main` was substantially correct for the original builder scope: match-verdict only, read-only source access, unsigned payloads, P3 text disposition, P4 money authority, no routes/signing/audit/export/MCP/action authority, and P1-P4 preservation.

The follow-up audit was also correct: the P5 validator did not yet emit executable controls for three fully iterated hardening classes:

- policy and benchmark default request isolation;
- Pydantic `.model_construct()` / `.construct()` validation-bypass bans;
- native async/thread/process/background dispatch bans.

The defect was proof incompleteness, not a required P5 reimplementation.

## Hypothesis Ledger

### H-P5-FU-01 - Defaults May Not Be Mechanically Request-Isolated

Status: CONFIRMED.

Evidence: `policy_defaults.py` and `benchmark_defaults.py` returned fresh-looking dicts, but tests and CI did not mutate Request A outputs and prove Request B remained clean.

Root cause: original P5 validator checked semantic values only.

Remediation: added direct factory isolation checks and builder-level request isolation tests. The tests mutate returned policy/benchmark objects aggressively, including nested scope lists and fake tenant/provider keys.

Post-remediation proof:

```text
immutable_default_controls_passed=4
default_mutation_isolation_controls_passed=11
shared_mutable_default_meta_negative_controls_passed=1
```

Residual risk: P5 still materializes dict/list projections because the TrustEnvelope payload is JSON-shaped; CI now proves those projections are fresh per call/request.

### H-P5-FU-02 - Pydantic Validation-Bypass Constructors May Not Be Banned

Status: CONFIRMED.

Evidence: no production usage was found, but CI did not explicitly fail on `.model_construct()` or `.construct()` in the P5 trust construction path.

Root cause: P5 did not use Pydantic directly, so the original validator relied on absence rather than a regression tripwire.

Remediation: added AST scanning across reachable `app.trust` dependencies for `Attribute.attr in {"model_construct", "construct"}`.

Post-remediation proof:

```text
pydantic_construct_ban_controls_passed=728
pydantic_construct_meta_negative_controls_passed=2
```

Residual risk: the ban is intentionally conservative and may reject non-Pydantic methods named `construct` inside the P5 trust path. That is acceptable for this boundary.

### H-P5-FU-03 - Native Background Dispatch May Not Be Banned

Status: CONFIRMED.

Evidence: existing controls banned Celery/outbox/export/artifact/fit surfaces and dynamic imports, but did not explicitly ban `asyncio.create_task`, executors, thread/process constructors, FastAPI/Starlette background tasks, anyio/trio task groups, or `subprocess.Popen`.

Root cause: the original no-compute proof focused on Skeldir dispatch surfaces and SQL write behavior.

Remediation: added AST import/call scanning across reachable P5 trust dependencies with alias resolution for native dispatch symbols.

Post-remediation proof:

```text
native_dispatch_ban_controls_passed=728
native_dispatch_meta_negative_controls_passed=2
```

Residual risk: `asyncio.gather` is banned conservatively in P5 trust-path modules even if used synchronously; P5 builder scope does not require it.

### H-P5-FU-04 - P5 Validator Non-Vacuity Covered Only Original Standard

Status: CONFIRMED.

Evidence: baseline `meta_negative_controls_passed=4` covered original properties only.

Root cause: marker coverage was not updated after the hardening standard changed.

Remediation: added meta-negative controls for shared mutable defaults, Pydantic construct bypass, and native dispatch.

Post-remediation proof:

```text
shared_mutable_default_meta_negative_controls_passed=1
pydantic_construct_meta_negative_controls_passed=2
native_dispatch_meta_negative_controls_passed=2
```

Residual risk: none for P5 scope; later phases must add their own route/signing/auth/audit proof layers.

## Root-Cause Findings

- RC-P5-FU-01: CONFIRMED. The original P5 directive did not require these hardening markers.
- RC-P5-FU-02: CONFIRMED. Structural reasoning existed but executable failure proofs were absent.
- RC-P5-FU-03: CONFIRMED. Dynamic import and LLM scans did not cover native dispatch.
- RC-P5-FU-04: CONFIRMED. Fresh dict factories existed, but nested mutation isolation was not tested.
- RC-P5-FU-05: CONFIRMED. Pydantic bypass was a future regression path, not a current production use.

## Local Proof Commands

All commands were run from `C:\Users\ayewhy\II SKELDIR II`.

```text
python -m pytest backend/tests/trust/test_b25_p5_builder.py -q
```

Output summary:

```text
7 passed in 14.30s
```

```text
python -m ruff check backend/app/trust backend/tests/trust/test_b25_p5_builder.py scripts/ci/validate_b25_p5_builder.py
```

Output summary:

```text
All checks passed!
```

```text
python scripts/ci/validate_b25_p5_builder.py --negative-control
```

Output summary:

```text
B25_P5_BUILDER_VALIDATION_PASS
pydantic_construct_ban_controls_passed=728
native_dispatch_ban_controls_passed=728
immutable_default_controls_passed=4
default_mutation_isolation_controls_passed=11
shared_mutable_default_meta_negative_controls_passed=1
pydantic_construct_meta_negative_controls_passed=2
native_dispatch_meta_negative_controls_passed=2
```

Baseline preservation:

```text
python scripts/ci/validate_b25_p1_contracts.py --negative-control
python scripts/ci/validate_b25_p1_trust_drift.py --negative-control
python scripts/ci/validate_b25_p2_canonicalization.py --negative-control
python scripts/ci/validate_b25_p3_text_disposition.py --negative-control
python scripts/ci/validate_b25_p4_money_authority.py --negative-control
python -m pytest backend/tests/trust -q
```

Output summary:

```text
B25_P1_CONTRACT_VALIDATION_PASS
B25_P1_TRUST_DRIFT_VALIDATION_PASS
B25_P2_CANONICALIZATION_VALIDATION_PASS
B25_P3_TEXT_DISPOSITION_VALIDATION_PASS
B25_P4_MONEY_AUTHORITY_VALIDATION_PASS
68 passed in 67.77s
```

Governance:

```text
python scripts/ci/validate_m3_ci_governance.py --registry
python scripts/ci/validate_m3_ci_governance.py --subsumption
GITHUB_EVENT_NAME=pull_request GITHUB_BASE_REF=main python scripts/ci/enforce_forensics_index.py
```

Output summary:

```text
M3_CI_GOVERNANCE_VALIDATION_PASS
M3_CI_GOVERNANCE_VALIDATION_PASS
Forensics INDEX enforcement passed.
```

## Protected Branch and CI Evidence

Corrective implementation PR:

- PR: #594
- URL: https://github.com/Synergyscape-V1/skeldir-2.0/pull/594
- Head SHA: `0c145c81e48427896fdd58ff1def948997366444`
- Merge commit: `b0230d0f9b0e7065f8102a33bcbdf75d70106026`
- Merged at: `2026-06-29T21:51:37Z`

PR checks:

- B2.5-P5 Trust Builder: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28404158819
- CI: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28404158770

Main checks for `b0230d0f9b0e7065f8102a33bcbdf75d70106026`:

- Main B2.5-P5 Trust Builder: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28405062509
- Main CI: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28405062476
- Main B2.4 Gate Dry Run: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/28405062612
- Main workflow census: `30` total, `30` success, `0` pending, `0` failed.

## Exit Gates

Gate: Protected Main and Baseline Preservation.
Status: PASS.
Evidence: PR #594 merged to protected `main`; P1/P2/P3/P4/P5 validators and `backend/tests/trust` passed locally and in hosted CI.
Residual risk: none for P5 follow-up scope.

Gate: Default Immutability and Mutation Isolation.
Status: PASS.
Evidence: direct factory and builder-level mutation isolation tests; `immutable_default_controls_passed=4`; `default_mutation_isolation_controls_passed=11`; shared mutable default meta-negative passed.
Residual risk: JSON payload projections remain mutable after emission, but future builds cannot be contaminated by mutating prior outputs.

Gate: Pydantic Validation-Bypass Ban.
Status: PASS.
Evidence: AST scan across reachable trust dependencies; meta-negative fixtures for `.model_construct()` and `.construct()`.
Residual risk: conservative false positives on non-Pydantic `.construct()` names inside the trust path.

Gate: Native Dispatch Ban.
Status: PASS.
Evidence: AST import/call scan with alias handling for native dispatch mechanisms; meta-negative fixtures for `asyncio.create_task` and `ThreadPoolExecutor().submit`.
Residual risk: intentionally conservative ban on `asyncio.gather`.

Gate: CI Non-Vacuity.
Status: PASS.
Evidence: validator fails under shared mutable default, Pydantic bypass, and native dispatch synthetic violations.
Residual risk: none identified.

Gate: P5 Scope Preservation.
Status: PASS.
Evidence: no production P5 files changed; validator still bans routes, signing/JWKS, machine auth, audit persistence, export, MCP, action authority, LLM/provider imports, and dynamic imports.
Residual risk: later B2.5 phases must own their own authority surfaces.

## Scope and Non-Overclaim Statement

This follow-up does not implement B2.5-P6 through B2.5-P14. It only hardens P5 validation and tests for default isolation, Pydantic construct bypass, and native background dispatch bans.

## Final PASS Summary

P1 baseline proof: `B25_P1_CONTRACT_VALIDATION_PASS` and `B25_P1_TRUST_DRIFT_VALIDATION_PASS`.
P2 baseline proof: `B25_P2_CANONICALIZATION_VALIDATION_PASS`.
P3 baseline proof: `B25_P3_TEXT_DISPOSITION_VALIDATION_PASS`.
P4 baseline proof: `B25_P4_MONEY_AUTHORITY_VALIDATION_PASS`.
P5 baseline proof: `B25_P5_BUILDER_VALIDATION_PASS`.
Default immutability proof: `immutable_default_controls_passed=4`.
Default mutation isolation proof: `default_mutation_isolation_controls_passed=11`.
Pydantic construct ban proof: `pydantic_construct_ban_controls_passed=728`.
Native dispatch ban proof: `native_dispatch_ban_controls_passed=728`.
Meta-negative proof: shared mutable default `1`, Pydantic construct `2`, native dispatch `2`.
Scope boundary proof: P5 validator scope-overreach controls remain green.
