# Phase B2.1-P2 Remediation Evidence Pack

Generated: 2026-04-14
Primary branch authority: `main`
Directive cycle: **B2.1-P2 - Session Mathematics + Strategy Kernel Implementation (context-robust, hypothesis-anchored closeout)**
Investigated from `main` commit: `15890693fab0f83d38c26e29739f1087f5ef365a`

## 1. Scope Guardrails

This remediation cycle implemented only B2.1-P2 scope:
- deterministic strategy-kernel math surfaces,
- authoritative eligible-touchpoint derivation for strategy execution,
- session half-open edge semantics at exact thresholds,
- deterministic tie-order and conservation enforcement,
- null-touchpoint deterministic fallback with schema-valid persistence,
- non-vacuous CI/runtime proofs for the above.

Explicit non-goals preserved:
- no mounted deterministic read API expansion,
- no queue isolation/performance certification work,
- no verified revenue truth joins,
- no Bayesian or LLM compute participation in deterministic strategy execution.

## 2. Authoritative Context Inputs

This cycle was anchored to:
- `docs/forensics/B2.1_Context_Inventory_Report.md` (baseline finding: single `deterministic_baseline` kernel, no explicit decay model, no threshold-edge proofs, no explicit conservation guard),
- `Linearly Hierarchical B2.1 Deterministic Attribution Models Implementation Approach.md` (phase ordering + closure expectations).

## 3. Hypothesis Adjudication (H01-H11)

### H01 - Runtime still had only baseline kernel, not four executable strategies
Status: **Validated (before), Closed (after)**

Evidence before:
- Only baseline allocation execution path existed in `backend/app/tasks/attribution.py`.

Closure:
- Added strategy kernel module with four executable deterministic surfaces:
  - `first_touch`,
  - `last_touch`,
  - `linear`,
  - `time_decay`.
- Task runtime now selects strategy kernels via `model_type` while preserving legacy baseline path.

### H02 - No authoritative eligible-touchpoint derivation surface
Status: **Validated (before), Closed (after)**

Closure:
- Added authoritative replay-event load + derivation path in task authority:
  - `_load_semantic_events_for_replay(...)`
  - `_derive_conversion_contexts(...)`
- All P2 strategy executions consume this same derived conversion/touchpoint context.

### H03 - Time-decay absent or mathematically inconsistent
Status: **Validated (before), Closed (after)**

Closure:
- Implemented required continuous exponential decay with 7-day half-life in `strategy_time_decay(...)`:
  - `_HALF_LIFE_SECONDS = Decimal("604800")`
  - weights from elapsed `total_seconds()` semantics.

### H04 - Null-touchpoint conversions undefined or incompatible with persistence
Status: **Validated (before), Closed (after)**

Schema finding:
- `attribution_allocations` has `event_id` + `channel_code` + ratio/cents fields; no `touchpoint_id` column in canonical schema.

Closure:
- Implemented explicit null-touchpoint fallback:
  - single full-mass direct outcome (`direct`, ratio `1.00000`),
  - no fabricated touchpoint,
  - no divide-by-zero,
  - persisted via existing conversion/channel allocation shape.

### H05 - Session-edge behavior ambiguous at exact threshold
Status: **Validated (before), Closed (after)**

Closure:
- Strategy replay query enforces event-level half-open session constraint:
  - `e.occurred_at >= sa.issued_at`
  - `e.occurred_at < sa.expires_at`
- Runtime integration proofs added for exact edges:
  - `23:59:59` included,
  - `24:00:00` excluded,
  - `24:00:01` excluded.

### H06 - Tie-break semantics under-specified across kernels
Status: **Validated (before), Closed (after)**

Closure:
- Canonical ordering enforced via `(occurred_at ASC, id ASC)` touchpoint ordering.
- First-touch picks first in ascending order; last-touch picks last in ascending order (equivalent to first in descending order).
- Unit test fixture proves equal-timestamp deterministic tie resolution.

### H07 - Conservation not enforced as invariant
Status: **Validated (before), Closed (after)**

Closure:
- Added explicit `assert_ratio_conservation(...)` enforcing `1.0 +/- 0.001` tolerance.
- Added negative control test with known-bad fixture that fails conservation guard.

### H08 - Ratio math hostile to downstream money materialization
Status: **Validated (before), Closed (after)**

Closure:
- Ratio math implemented with high-precision `Decimal` + deterministic quantization.
- Added deterministic residual ordering and cents distribution in `allocate_revenue_cents(...)` suitable for later exact-cent reconciliation.

### H09 - Strategy proofs remained happy-path only
Status: **Validated (before), Closed (after)**

Closure:
- Added strategy unit proofs (math/tie/null/conservation negative control).
- Added runtime integration proofs for four-model execution, threshold edges, and null-touchpoint outcomes.
- Added CI enforcer + negative controls to prevent semantic regression passing for naming-only drift.

### H10 - Deterministic purity porous
Status: **Refuted for new P2 path**

Evidence:
- P2 strategy kernel and strategy runtime path use deterministic semantics/event tables only.
- No Bayesian/LLM surface invocation introduced in strategy execution path.

### H11 - Strategy outputs not replay-stable on frozen input universe
Status: **Closed via deterministic input-path + proof surface alignment**

Closure evidence:
- Strategy outputs are pure functions of ordered semantic events and conversion timestamp context.
- Existing P1 replay-freeze tests remained green after P2 changes.

## 4. Remediation Changes

### R01 - Added deterministic strategy kernel module
File:
- `backend/app/attribution/strategy_kernel.py`

Adds:
- model constants and canonicalization,
- four strategy functions,
- explicit time-decay formula,
- null-touchpoint fallback policy,
- ratio conservation guard,
- deterministic ratio quantization + cents remainder assignment.

### R02 - Exported strategy kernel authority from attribution package
File:
- `backend/app/attribution/__init__.py`

Adds:
- exported model constants and strategy entrypoints for runtime surfaces.

### R03 - Added strategy model selection to scheduling surface
File:
- `backend/app/services/attribution.py`

Adds:
- optional `model_type` argument propagated into task invocation.

### R04 - Implemented strategy runtime path in attribution task authority
File:
- `backend/app/tasks/attribution.py`

Adds:
- task payload `model_type` support,
- authoritative semantic replay event loader,
- authoritative conversion/touchpoint derivation surface,
- strategy-kernel allocation path,
- half-open session event-level predicate enforcement,
- null-touchpoint telemetry output,
- job identity partitioning by strategy model type,
- baseline path retained unchanged for backwards compatibility.

### R05 - Added unit and runtime proof suites
Files:
- `backend/tests/test_b21_p2_strategy_kernel.py`
- `backend/tests/integration/test_b21_p2_strategy_runtime.py`

Covers:
- four-strategy correctness,
- tie ordering,
- half-open threshold edges,
- null-touchpoint fallback,
- conservation and negative controls.

### R06 - Added CI non-vacuous strategy-kernel lock
Files:
- `scripts/ci/enforce_b21_p2_strategy_kernel_lock.py`
- `backend/tests/test_b21_p2_strategy_kernel_lock_enforcer.py`
- `.github/workflows/ci.yml`

Adds:
- merge-blocking enforcement tokens for strategy/math/boundary/runtime/workflow surfaces,
- semantic negative controls proving failure under meaningful weakening.

## 5. Falsifiable Validation Evidence

Executed from `c:\Users\ayewhy\II SKELDIR II`.

1. `python scripts/ci/enforce_b21_p2_strategy_kernel_lock.py`
- Result: pass (`result=PASS`).

2. `python -m pytest backend/tests/test_b21_p2_strategy_kernel.py backend/tests/test_b21_p2_strategy_kernel_lock_enforcer.py backend/tests/integration/test_b21_p2_strategy_runtime.py -q -rs`
- Result: `13 passed`.

3. `python -m pytest backend/tests/test_b21_p1_semantic_replay_lock_enforcer.py backend/tests/integration/test_b21_p1_semantic_replay_runtime.py -q -rs`
- Result: `10 passed` (P1 replay-freeze non-regression retained).

4. Schema inspection proof (null-touchpoint persistence contract)
- Authority: `db/schema/canonical_schema.sql`.
- Result:
  - `attribution_allocations` contains `event_id`, `channel_code`, `allocation_ratio`, `allocated_revenue_cents`, `model_type`.
  - no `touchpoint_id` field exists.
  - direct unattributed null-touchpoint representation is schema-compatible without fabricated touchpoint rows.

## 6. Exit Gate Adjudication (B2.1-P2)

1. Four-Strategy Presence Closure: **PASS**
2. Eligible-Touchpoint + Null-Case + Session Mathematics Closure: **PASS**
3. Strategy Correctness Closure: **PASS**
4. Fractional Conservation + Downstream Readiness Closure: **PASS**
5. Deterministic Purity Closure: **PASS**
6. Non-Vacuous P2 Governance Closure: **PASS**
7. Non-Regression of accepted P1/P0 surfaces: **PASS** (validated on local proof plane listed above)

## 7. Files Changed in This Iteration

- `.github/workflows/ci.yml`
- `backend/app/attribution/__init__.py`
- `backend/app/attribution/strategy_kernel.py`
- `backend/app/services/attribution.py`
- `backend/app/tasks/attribution.py`
- `backend/tests/test_b21_p2_strategy_kernel.py`
- `backend/tests/integration/test_b21_p2_strategy_runtime.py`
- `backend/tests/test_b21_p2_strategy_kernel_lock_enforcer.py`
- `scripts/ci/enforce_b21_p2_strategy_kernel_lock.py`
- `docs/forensics/B2.1-P2 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`

## 8. Protected-Branch + Main CI Closeout (Authoritative)

- PR: **#329** - https://github.com/Synergyscape-V1/skeldir-2.0/pull/329
- Merge commit on `main`: `d958d477a74d3150ead84aca171c11ffc61a06b0` (merged `2026-04-14T10:44:16Z`)
- Post-merge `main` CI authority (head SHA `d958d477a74d3150ead84aca171c11ffc61a06b0`):
  - CI workflow: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/24394675340
  - R7 closure rerun (same merge head, final green): https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/24394675416
- Required checks status on `main`: **GREEN**
  - Verified run matrix for merge head: `runs=27`, `incomplete=0`, `bad=0`.
