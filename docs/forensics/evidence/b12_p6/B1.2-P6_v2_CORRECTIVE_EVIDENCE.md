# B1.2-P6 v2 Corrective Evidence

Date: 2026-03-05  
Branch: `b12-p6-r1-r2-schema-fix`  
Scope: RBAC scopes, default-deny enforcement, refresh role authority, tenant divergence, hot-path boundedness, CI adjudication.

## Initial finding summary
- H01 supported at baseline: admin enforcement was route-by-route, not structural default-deny.
- H02 supported at baseline: admin RBAC scopes were not contract-visible.
- H03 partially refuted at baseline: refresh used DB role source, but no explicit non-vacuous negative control.
- H04 supported at baseline: no explicit tenant divergence proof.
- H05 supported at baseline: required-check drift from governance contract.

## Remediation implemented

### 1) Contract-visible RBAC scopes (EG6.C1)
- Files:
  - `api-contracts/openapi/v1/_common/base.yaml`
  - `api-contracts/openapi/v1/auth.yaml`
- Changes:
  - `bearerAuth` now OAuth2 with scopes `admin|manager|viewer`.
  - Admin operations now require `["admin"]` scope in contract.

### 2) Structural default-deny admin namespace (EG6.A1)
- File: `backend/app/api/auth.py`
- Changes:
  - Added `admin_router` with router-level dependency:
    - `Security(get_auth_context, scopes=["admin"])`
  - Mounted all `/api/auth/admin/*` endpoints under this router.
  - Added negative-control route behind env toggle:
    - `SKELDIR_B12_P6_ENABLE_NEGATIVE_ADMIN_UNSCOPED_ROUTE`

### 3) Scope-aware auth context semantics (EG6.A1, EG6.D1)
- File: `backend/app/security/auth.py`
- Changes:
  - Switched bearer extraction to `OAuth2PasswordBearer` (scheme name `bearerAuth`).
  - Added scope enforcement with role precedence (`admin >= manager >= viewer`).
  - Enforced expected semantics:
    - invalid/missing/revoked token -> 401
    - insufficient role scope -> 403

### 4) Refresh re-mint DB-authoritative + negative control (EG6.R1)
- File: `backend/app/services/auth_tokens.py`
- Changes:
  - Refresh role claims minted from DB membership roles for `(user_id, tenant_id)`.
  - Added non-vacuous failure toggle:
    - `SKELDIR_B12_P6_NEGATIVE_COPY_ROLE_FORWARD`

### 5) Tenant membership resolution reliability fix
- File: `backend/app/services/auth_tokens.py`
- Change:
  - `resolve_tenant_membership()` now binds tenant GUC before querying RLS-protected membership tables:
    - `await set_tenant_guc_async(session, requested_tenant_id, local=True)`

### 6) P6 proof suite expansion + harness hardening
- File: `backend/tests/test_b12_p6_rbac_enforcement.py`
- Added/validated:
  - route-lint default-deny scope test
  - route-lint negative control
  - tenant divergence proof
  - refresh-after-downgrade DB-authoritative proof
  - refresh copy-forward negative control proof
  - hot-path zero role-table read proof
- Harness stabilization:
  - seeding/login hash alignment with auth runtime pepper path
  - runtime identity guard accepts `app_user` and `postgres` (CI DSN compatibility)

## Exit-gate mapping
- EG6.C1: scope-bearing OpenAPI contracts (`auth.yaml`, `_common/base.yaml`)
- EG6.A1: router-level admin scope + route-lint + negative control
- EG6.D1: viewer token receives deterministic 403 on admin endpoint
- EG6.T1: same user diverges by tenant role and authorization outcome
- EG6.R1: refresh after downgrade does not re-mint admin claim (or denies refresh); negative control proves violation mode
- EG6.P1: protected endpoint authorization performs zero role-table reads after warmup
- EG6.CI1: `B1.2 P6 RBAC Proofs` present in required checks on `main`

## Local adjudication evidence
- Clean local proof environment:
  - fresh DB: `skeldir_p6_closure_20260305`
  - migrations: `alembic upgrade head`
- Proof command:
  - `pytest backend/tests/test_b12_p6_rbac_enforcement.py -q`
- Result:
  - `11 passed`

## Completion status
- Branch-level technical closure: complete (code + contracts + local proof suite green).
- Governance closure condition remaining at document timestamp:
  - merge SHA on `main` must show required checks green (including `B1.2 P6 RBAC Proofs`).
