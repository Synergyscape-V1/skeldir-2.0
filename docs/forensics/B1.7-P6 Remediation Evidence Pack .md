# B1.7-P6 Remediation Evidence Pack

Generated: 2026-04-08  
Branch: `main`  
Phase: **B1.7-P6 — Full End-to-End System Testing and Validation**

## Initial Findings (pre-remediation)

1. `B1.7 Explanation Runtime Adjudication` in `.github/workflows/ci.yml` validated mounted P1-P5 proofs, but did not run a merge-blocking mixed-workload P6 benchmark adjudication path.
2. `.github/workflows/b17-p4-mixed-workload-benchmark.yml` produced baseline/prewarm artifacts, but did not assert literal P6 closure gates in one fail-closed adjudication step.
3. `scripts/benchmarks/b17_p4_mixed_workload.py` emitted latency and execution-path diagnostics, but did not emit explicit cache-hit ratio in the summary payload.
4. No composed `backend/tests/test_b17_p6_end_to_end_runtime.py` existed to prove full mounted correctness in one dedicated P6 runtime suite.
5. No dedicated structural enforcer bound PR + `main` benchmark topology to prevent future vacuous P6 CI wiring regressions.

## Remediations Applied

1. Added `backend/tests/test_b17_p6_end_to_end_runtime.py` with mounted end-to-end correctness checks:
   - authoritative truth + bounded explanation,
   - numeric mismatch injection fail-closed,
   - stale replay rejection with provider reentry blocked.
2. Extended `scripts/benchmarks/b17_p4_mixed_workload.py` to emit `cache_hit_rate` and optional fail-closed assertion flags for:
   - overall p95 threshold,
   - cache-hit threshold,
   - warm/cold diagnostics presence.
3. Added `scripts/ci/enforce_b17_p6_benchmark_adjudication.py` to adjudicate literal P6 benchmark gates from baseline/prewarm artifacts.
4. Added `scripts/ci/enforce_b17_p6_adjudication_plane.py` and negative-control tests to lock PR + `main` adjudication topology.
5. Updated `.github/workflows/ci.yml`:
   - added P6 enforcer negative controls in contract-semantic-drift job,
   - added `test_b17_p6_end_to_end_runtime.py` into required B1.7 runtime context,
   - added baseline/prewarm mixed-workload benchmark runs plus P6 adjudication step and artifact upload.
6. Updated `.github/workflows/b17-p4-mixed-workload-benchmark.yml`:
   - added `push` trigger for `main` (kept `schedule` + `workflow_dispatch`, no PR trigger),
   - added explicit P6 benchmark adjudication step.
7. Tightened `scripts/ci/enforce_b17_p5_merge_blocking_adjudication.py` required CI tokens so P6 adjudication-plane lock cannot be silently dropped.
8. Added negative-control suites:
   - `backend/tests/test_b17_p6_benchmark_adjudication_enforcer.py`
   - `backend/tests/test_b17_p6_adjudication_plane_enforcer.py`

## Falsifiability and Non-Vacuity

P6 now fails closed if any of the following regress:
- overall endpoint p95 is not below 500ms,
- cache-hit ratio is not above 60%,
- warm/cold diagnostics are missing,
- prewarm efficacy is absent (no assisted hits, no cold-exposure reduction, or no overall/cache benefit),
- mounted P6 runtime correctness protections regress,
- required PR/`main` adjudication topology is altered.

## Authoritative Evidence

### Local verification evidence
- [ ] P6 runtime test suite passes locally.
- [ ] P6 enforcer negative controls pass locally.
- [ ] Mixed-workload baseline and prewarm artifacts generated locally.

### CI authoritative evidence (`main`)
- [ ] Commit SHA merged to `main`: `TBD`
- [ ] Required context green: `B1.7 Explanation Runtime Adjudication` (`TBD run URL`)
- [ ] Required context green: `Contract Semantic Drift Gate` (`TBD run URL`)
- [ ] Protected-branch enforcement verified from CI required checks contract and live run (`TBD`)
- [ ] Post-merge benchmark run green with adjudication gate (`TBD run URL`)

## Exit Gate Mapping (EG-P6.1 .. EG-P6.7)

- **EG-P6.1 End-to-end correctness:** covered by `backend/tests/test_b17_p6_end_to_end_runtime.py`.
- **EG-P6.2 Overall latency p95 <500ms:** enforced by `scripts/ci/enforce_b17_p6_benchmark_adjudication.py` over `prewarm_enabled.json`.
- **EG-P6.3 Cache hit rate >60%:** enforced by same adjudicator via `cache_hit_rate.ratio`.
- **EG-P6.4 Warm/cold diagnostics:** enforced by benchmark payload structure and adjudicator checks.
- **EG-P6.5 Prewarm efficacy:** enforced by baseline vs prewarm comparison checks (assisted hits, reduced cold exposure, improved overall/cache behavior).
- **EG-P6.6 CI adjudication topology:** enforced by `scripts/ci/enforce_b17_p6_adjudication_plane.py` + CI workflow wiring in PR and `main`.
- **EG-P6.7 No-regression:** preserved by existing P1-P5 runtime proofs and anti-chat checks retained in required context.
