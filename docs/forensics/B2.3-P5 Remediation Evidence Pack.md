# B2.3-P5 Remediation Evidence Pack

Generated: 2026-05-06

Primary branch authority target: `main`

Directive cycle: **Phase B2.3-P5 - Non-Vacuous Proof Harness + Merge-Blocking CI Adjudication**

## 1. Initial Findings

P5-FH01/P5-FH02 were confirmed. P0-P4 enforcers existed, and `ci.yml` already executed those surfaces inside `Contract Semantic Drift Gate`, but no canonical B2.3-P5 composite manifest or single named composite harness existed.

P5-FH03 was confirmed as a missing guard. There was no P5-owned manifest-level rule rejecting self-exemption of required P0-P4 proof classes.

P5-FH04/P5-FH05/P5-FH06 were partially confirmed. Existing phase negative controls were present, but P3/P4 enforcer tests included function-level patched reads. P5 now adds a separate fixture-root suite that runs real enforcers against isolated copied source trees and semantic mutations.

P5-FH07 was confirmed at local inspection time. Live branch protection for `main` did not yet list `B2.3 Composite Proof Harness` as a required status check.

P5-FH11 found a context conflict: the B2.3 approach document freezes `b23_p0_semantic_authority_freeze.main.json` at `1.2.0`, while the checked-out manifest had drifted to `1.4.0`. P5 restores the manifest metadata to `1.2.0` and adds a structural negative control for version regression without changing P0 semantic fields.

P5-FH12 was confirmed as missing from B2.3 composite adjudication. Typegen drift existed in other CI jobs, but P5 did not have an explicit composite drift gate.

## 2. Remediations

Added `contracts-internal/governance/b23_p5_composite_proof.main.json` as the canonical P5 manifest. It enumerates required P0-P4 enforcers, required pytest suites, required fixture-root negative controls, frontend typegen drift commands, runtime proof-mode variables, branch-protection check name, no-self-exemption policy, and forbidden P6/B2.4/B2.6 scope.

Added `contracts-internal/governance/b23_p5_pytest_suite.main.json` mapping B2.3 pytest files to required gate classes: extraction correctness, arrival-window guard, match quality, transition timing, exception creation, refund/chargeback adjustment, P4 benchmark, queue isolation, DB pool isolation, telemetry SQL, frontend typegen drift, branch protection, and fixture-root negative controls.

Added `scripts/ci/enforce_b23_p5_composite_proof.py`. The harness loads both manifests, validates required artifacts, executes required phase enforcers and pytest suites, parses JUnit skip results, runs frontend typegen drift commands, invokes branch-protection verification, emits `artifacts/b23_p5_composite_proof_summary.json`, and exits non-zero on partial proof.

Added `scripts/ci/verify_b23_p5_branch_protection.py`. It queries GitHub branch protection for `main`, confirms the exact required status check, confirms the workflow job name matches, supports fixture JSON for negative controls, and fails closed on missing token/API failure.

Added `backend/tests/test_b23_p5_composite_proof_negative_controls.py`. The suite contains fixture-root negative controls for P1 schema status/constraint/taxonomy drift, P2 extraction/timing/LLM/refund-handler drift, P3 transition/exception/read-surface drift, P4 queue/worker/pool/benchmark/telemetry drift, frontend generated type drift, manifest self-exemption, branch-protection required-check absence, and P0 contract-version regression.

Hardened `scripts/ci/enforce_b23_p4_queue_performance.py` to detect vacuous benchmark timing where benchmark fixture insertion moves inside the timed execution region.

Added a dedicated CI job named `B2.3 Composite Proof Harness` with Postgres, migrations, B2.3 proof-mode variables, composite harness execution, and summary artifact upload.

Updated `contracts-internal/governance/b03_phase2_required_status_checks.main.json` to require `B2.3 Composite Proof Harness`.

## 3. Local Validation

Local structural P5 harness:

`python scripts/ci/enforce_b23_p5_composite_proof.py --structural-only --summary-file artifacts/b23_p5_structural_summary.json` -> PASS

Fixture-root negative controls:

`python -m pytest backend/tests/test_b23_p5_composite_proof_negative_controls.py -q` -> PASS, 22 passed

P0-P4 enforcers:

`python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py --skip-baseline-git-check` -> PASS

`python scripts/ci/enforce_b23_p0_typed_boundary_source_alignment.py` -> PASS

`python scripts/ci/enforce_b23_p1_schema_authority_lock.py` -> PASS

`python scripts/ci/enforce_b23_p2_match_engine_kernel.py` -> PASS

`python scripts/ci/enforce_b23_p3_verdict_persistence.py` -> PASS

`python scripts/ci/enforce_b23_p4_queue_performance.py` -> PASS

Live branch-protection pre-update check:

`python scripts/ci/verify_b23_p5_branch_protection.py` with `GH_TOKEN=$(gh auth token)` -> FAIL as expected before hardware update: `required_check_missing_from_branch_protection:B2.3 Composite Proof Harness`.

Live branch-protection post-update check:

`python scripts/ci/verify_b23_p5_branch_protection.py` with `GH_TOKEN=$(gh auth token)` -> PASS. Observed `B2.3 Composite Proof Harness` in `main` required status checks.

Required-status contract live check:

`python scripts/ci/enforce_required_status_checks.py` with `GH_TOKEN=$(gh auth token)` -> PASS.

## 4. Protected-Branch Evidence

Implementation PR:

`https://github.com/Synergyscape-V1/skeldir-2.0/pull/443`

Implementation head SHA:

`47e28b966ae18569ff103cfbb29691e14e1583c0`

Protected merge SHA on `main`:

`268c924ac38d1cfdd663c9ba0b46d940252104bd`

PR required-check evidence:

`B2.3 Composite Proof Harness` -> PASS

`https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25455167833/job/74686438397`

`Contract Semantic Drift Gate` -> PASS after rerunning a transient P4 planner assertion

`https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25455167833/job/74686438643`

Post-merge `main` CI evidence for merge SHA `268c924ac38d1cfdd663c9ba0b46d940252104bd`:

`CI` -> PASS

`https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25456546200`

`B2.3 Composite Proof Harness` on `main` -> PASS

`https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25456546200/job/74687919227`

`Contract Semantic Drift Gate` on `main` -> PASS

`https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25456546200/job/74688076990`

Live post-merge branch-protection verifier:

`GH_TOKEN=$(gh auth token) python scripts/ci/verify_b23_p5_branch_protection.py` -> PASS

Observed source: GitHub branch-protection API.

Observed required check: `B2.3 Composite Proof Harness`.

Observed workflow job id: `b23-p5-composite-proof`.

Live required-status contract verifier:

`GH_TOKEN=$(gh auth token) python scripts/ci/enforce_required_status_checks.py` -> PASS.

Main-run inventory for merge SHA `268c924ac38d1cfdd663c9ba0b46d940252104bd` showed all listed workflow runs completed with `success`, including `CI`, `Contract Semantic Drift Gate`, `b07-phase8-closure-pack`, B0.6 adjudication, B0.5.4 gates, B1.7 mixed-workload benchmark, B11 readiness gates, and the B0.5.7/B0.5.7 chain workflows.

## 5. Closure Statement

B2.3-P5 was completed without implementing P6/B2.4/B2.6/dashboard/frontend product UI/LLM explanation scope. The merged changes are limited to proof manifests, proof orchestration, branch-protection verification, fixture-root negative controls, skip/self-exemption adjudication, frontend typegen drift inclusion, and composite CI binding.
