# B1.3-P8 Remediation Evidence Pack

Date: 2026-03-12  
Scope: B1.3-P8 only (Failure Taxonomy + Graceful Degradation + Provider Baseline Stabilization)

## Executive Verdict

**FAIL (local remediation complete, authoritative `main` adjudication incomplete in this workspace context).**

Local code/test/CI remediation for P8 is implemented, but `main`-branch CI success and live branch-protection enforcement cannot be proven from this local run alone.

## Hypothesis Ledger

| Hypothesis | Status | Evidence |
|---|---|---|
| H-P8-01 provider failures still coarse at runtime surfaces | VALIDATED -> REMEDIATED | Added canonical runtime failure translation and explicit `/refresh-state` surface mapping in `backend/app/services/provider_oauth_runtime.py`, `backend/app/api/platform_oauth.py`. |
| H-P8-02 internal/external taxonomy not aligned | VALIDATED -> REMEDIATED | Added failure-class mapping table and externalized code mapping (`provider_expired/revoked/scope_insufficient/rate_limited/transport_failure`). |
| H-P8-03 terminal failures may still churn refresh | VALIDATED -> REMEDIATED | Terminal `invalid_client` classification and revocation path enforced; due-selection excludes revoked and clears `next_refresh_due_at`. |
| H-P8-04 reconnectable degradation not first-class | VALIDATED -> REMEDIATED | `refresh-state` and runtime status now emit explicit `reconnect_required` when connection exists without durable credentials. |
| H-P8-05 provider baseline suite unstable/red | PARTIALLY VALIDATED | Added supported-tranche proof tests and dedicated P8 suite; full green-on-`main` proof pending authoritative CI execution. |
| H-P8-06 degraded outputs too weak | VALIDATED -> REMEDIATED | Added typed refresh-state response with last attempt/success/error code fields and explicit error responses. |
| H-P8-07 request-path vs worker-path divergence | VALIDATED -> REMEDIATED | Worker failure classes map to canonical external codes in runtime refresh-state surface. |
| H-P8-08 degraded paths may leak upstream bodies | VALIDATED -> REMEDIATED | Refresh-state/ProblemDetails remain sanitized and token-free; tests assert no token leakage in failure outputs. |
| H-P8-09 supported tranche ambiguous | VALIDATED -> REMEDIATED | P8 proof enforces tranche from P0/P5 governance contracts (`stripe` runtime-backed, `dummy` internal only). |
| H-P8-10 no P8-specific adjudication gate | VALIDATED -> REMEDIATED | Added `scripts/ci/enforce_b13_p8_failure_baseline.py`, CI job `B1.3 P8 Failure & Baseline Proofs`, and required-check contract enrollment. |

## Root Causes

1. P1 contract taxonomy existed, but runtime lacked a dedicated refresh-state surface and canonical mapping bridge.
2. P7 orchestration correctly handled refresh mechanics, but P8-level external degradation semantics were not yet surfaced.
3. Supported-tranche governance existed (P0/P5), but no P8 adjudication consumed it directly.
4. Independent P8 gate was absent from workflow and required-context contract.

## Remediations Performed

1. Added explicit provider `invalid_client` terminal classification in lifecycle adapters:
   - `backend/app/services/provider_oauth_lifecycle.py`
2. Added P8 runtime failure translation and refresh-state runtime model:
   - `backend/app/services/provider_oauth_runtime.py`
3. Added runtime API route for refresh-state with correlation + scope enforcement:
   - `backend/app/api/platform_oauth.py`
4. Added contract-aligned schema classes for refresh-state and lifecycle error code:
   - `backend/app/schemas/attribution.py`
5. Added P8 structural enforcement script:
   - `scripts/ci/enforce_b13_p8_failure_baseline.py`
6. Added P8 runtime/provider proof suite:
   - `backend/tests/test_b13_p8_failure_baseline.py`
7. Added independent CI adjudication job:
   - `.github/workflows/ci.yml` (`name: B1.3 P8 Failure & Baseline Proofs`)
8. Added required-context contract enrollment:
   - `contracts-internal/governance/b03_phase2_required_status_checks.main.json`

## Proof Pointers

- Runtime paths:
  - `backend/app/api/platform_oauth.py`
  - `backend/app/services/provider_oauth_runtime.py`
  - `backend/app/services/provider_token_refresh.py`
- Provider baseline paths:
  - `backend/app/services/provider_oauth_lifecycle.py`
  - `contracts-internal/governance/b13_p0_provider_capability_matrix.main.json`
  - `contracts-internal/governance/b13_p5_oauth_adapter_capabilities.main.json`
- Tests:
  - `backend/tests/test_b13_p8_failure_baseline.py`
- CI job:
  - `.github/workflows/ci.yml` -> `B1.3 P8 Failure & Baseline Proofs`
- Required-check contract:
  - `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- Live branch-protection snapshot (2026-03-12):
  - `docs/forensics/evidence/b13_p8/runtime/main_required_status_checks_20260312.json`

## Local Verification Executed

1. `python scripts/ci/enforce_b13_p6_runtime_lifecycle.py` -> pass
2. `python scripts/ci/enforce_b13_p7_refresh_orchestration.py` -> pass
3. `python scripts/ci/enforce_b13_p8_failure_baseline.py` -> pass
4. `pytest backend/tests/test_b13_p8_failure_baseline.py -q -rs`
   - pass: 3
   - skipped: 3 (local DB role lacks create-database privilege for isolated runtime DB)
5. `pytest backend/tests/test_b13_p6_runtime_lifecycle.py::test_b13_p6_gate_passes_repo_state -q` -> pass
6. `pytest backend/tests/test_b13_p7_refresh_orchestration.py::test_b13_p7_gate_passes_repo_state -q` -> pass

## Exit Gates

| Gate | Status | Notes |
|---|---|---|
| Exit Gate 1 Failure-Taxonomy Lock | PASS (local) | Explicit internal/external mapping implemented and enforced by P8 gate script/tests. |
| Exit Gate 2 Graceful-Degradation Lock | PASS (local) | Terminal invalid-client revocation + due-selection suppression and reconnect-required outputs proved locally. |
| Exit Gate 3 Baseline-Stability Lock | PARTIAL | Supported-tranche proofs added; authoritative green-on-`main` CI not provable in local workspace alone. |
| Exit Gate 4 Non-Leak Failure-Path Lock | PASS (local) | Failure payload assertions and sanitized ProblemDetails behavior verified locally. |
| Exit Gate 5 P8 Adjudication Lock | PARTIAL | Job/context + contract enrollment implemented; live branch-protection enforcement and green `main` run pending authoritative GitHub state. |

## Blockers

1. Live branch-protection snapshot does not yet require `B1.3 P8 Failure & Baseline Proofs`.
2. Live `main` CI run showing `B1.3 P8 Failure & Baseline Proofs` success is not available from this local-only execution.
3. Full runtime DB-backed P8 tests are skipped locally due insufficient DB privilege to create isolated databases.

## Non-Blocking Observations

1. P6/P7 gate scripts remain green after P8 changes.
2. Supported tranche is now explicitly consumed in P8 proofs and no provider-breadth expansion was introduced.

## Authorization Statement

**B1.3 is not yet authorized to advance from P8 to P9** until:

1. `B1.3 P8 Failure & Baseline Proofs` is green on authoritative `main`.
2. Live branch protection requires `B1.3 P8 Failure & Baseline Proofs`.
3. Runtime DB-backed P8 proofs run without skip in the authoritative CI environment.
