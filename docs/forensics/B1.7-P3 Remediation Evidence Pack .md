# B1.7-P3 Remediation Evidence Pack
Generated: 2026-04-04 (America/Chicago)  
Branch audited and remediated: `main` (work completed from a short-lived change branch)  
Directive: **B1.7-P3 Context-Robust Hypothesis-Driven Remediation Directive**

## 1. Executive Verdict
**B1.7-P3 is implemented with falsifiable cache-correctness proofs on the canonical mounted explanation route.**

The canonical route now:
- derives deterministic truth snapshot watermark/version from DB-backed authority before replay acceptance,
- uses determinant-complete cache identity (not prompt-hash-only),
- rejects stale replay without provider reentry on the canonical B1.7 surface,
- exposes structured truth snapshot fields in both authoritative metric and explanation metadata,
- and records replay classification as machine-observable metadata.

## 2. Initial Findings (Pre-Remediation)
- Cache key derivation was effectively prompt-seed-centric and did not require explicit determinant identity object.
- Watermark mismatch was handled as an opaque cache miss (`None`) and could flow into provider generation.
- Stale replay classification was not explicit for the canonical route.
- Provider reentry on stale mismatch was not blocked for canonical B1.7 replay safety closure.
- Structured truth snapshot metadata was not present in both authoritative metric payload and explanation payload.
- Contract/runtime proof topology lacked explicit B1.7-P3 stale-replay and citation-coherence assertions.

## 3. Remediation Summary
### 3.1 Deterministic truth snapshot watermark/version from DB-backed authority
- `backend/app/services/attribution_explanation_authority.py`
  - Added deterministic truth snapshot derivation from authority rows.
  - Added `truth_snapshot_version` + `truth_snapshot_watermark` + `truth_snapshot_as_of` to authority record.

### 3.2 Determinant-complete cache identity and authoritative watermark-first replay gate
- `backend/app/api/attribution.py`
  - Canonical route now passes explicit `cache_identity` determinants and DB-derived `cache_watermark`.
  - Added route-level `truth_snapshot` payload propagation into provider boundary metadata.
  - Added canonical stale-replay policy signal (`reject_provider_reentry_on_stale`) for B1.7 surface.
- `backend/app/llm/provider_boundary.py`
  - Added cache probe classification (`miss`, `hit`, `stale_mismatch`) instead of opaque hit/miss only.
  - Added stale replay rejection path for canonical endpoint before provider call.
  - Added replay-state metadata (`cache_replay_state`, `provider_reentry_blocked`) for observability.

### 3.3 No provider reentry on stale replay rejection (canonical B1.7)
- `backend/app/llm/provider_boundary.py`
  - Enforced canonical endpoint stale-replay rejection before provider invocation.
  - Persisted blocked stale-replay outcome into `llm_api_calls` metadata and audit reason.

### 3.4 Structured citation/truth coherence in contract and runtime payloads
- `backend/app/api/attribution.py`
  - Added `truth_snapshot` object to both `authoritative_metric` and `non_authoritative_explanation`.
  - Added explanation metadata fields: `cache_replay_state`, `provider_reentry_blocked`, `explanation_contract_version`.
  - Added explicit stale replay synthesis state mapping.
- `backend/app/schemas/attribution.py`
  - Added typed `AttributionTruthSnapshot` schema and new explanation metadata fields/enums.
- `api-contracts/openapi/v1/attribution.yaml`
  - Added typed truth snapshot contract fields and B1.7-P3 lock metadata.
- `api-contracts/dist/openapi/v1/attribution.bundled.yaml`
  - Rebundled from source contract after P3 updates.
- `frontend/src/types/api/attribution.ts`, `frontend/src/types/api/index.ts`
  - Regenerated from canonical bundled contracts.

### 3.5 CI proof-plane extension for P3
- `backend/tests/test_b17_p3_cache_correctness_runtime.py`
  - Added determinant identity seam proof (prompt-text mutation does not alter key; determinant mutations do).
  - Added structured truth-snapshot coherence proof on cold path + cache hit.
  - Added stale replay rejection proof with provider call count asserted `0`.
- `tests/contract/test_route_fidelity.py`
  - Added B1.7-P3 contract/runtime lock assertions and truth snapshot schema checks.
- `.github/workflows/ci.yml`
  - Extended B1.7 proof job to include P3 runtime/seam proofs.

## 4. Hypothesis Closure Snapshot (H01-H20)
| Hypothesis Group | Status |
|---|---|
| H01-H04 cache identity topology | CLOSED |
| H05-H08 watermark truth binding | CLOSED |
| H09-H11 replay rejection semantics | CLOSED |
| H12-H15 citation/truth coherence + contract authority | CLOSED |
| H16-H20 CI/proof topology | CLOSED (B1.7 proof job now executes P3 checks) |

## 5. Local Falsifiable Proof Runs
Executed during remediation:

```powershell
$env:DATABASE_URL='postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/postgres'
$env:MIGRATION_DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres'
$env:PYTHONPATH="$PWD;$PWD/backend"

pytest -q backend/tests/test_b17_p3_cache_correctness_runtime.py
# 3 passed

pytest -q backend/tests/test_b17_p1_explanation_authority_runtime.py `
         backend/tests/test_b17_p2_explanation_fastpath_runtime.py `
         tests/contract/test_route_fidelity.py::test_b17_canonical_explain_route_mounted_and_runtime_openapi_converged
# 12 passed

pytest -q tests/contract/test_contract_semantics.py tests/contract/test_b15_p2_contract_addendum.py
# 25 passed, 5 skipped
```

## 6. Exit Gate Mapping (Directive EG-P3.1 ... EG-P3.6)
| Exit Gate | Status | Evidence |
|---|---|---|
| EG-P3.1 Authoritative watermark-first cache topology | PASS | Route resolves deterministic authority first and passes DB-derived watermark before cache probe acceptance. |
| EG-P3.2 Determinant-complete cache identity | PASS | Explicit `cache_identity` determinant object + seam test mutating determinants. |
| EG-P3.3 Watermark-based stale replay rejection | PASS | Runtime stale-change test rejects replay after authority mutation. |
| EG-P3.4 No provider reentry on stale mismatch | PASS | Runtime proof asserts provider spy call count remains `0` on stale rejection. |
| EG-P3.5 Structured citation/truth coherence | PASS | Structured `truth_snapshot` equality asserted across authoritative + explanation payloads (cold and cached). |
| EG-P3.6 CI adjudication closure | PASS | Workflow updated to execute B1.7-P3 proof file in canonical B1.7 proof job. |

## 7. Completion Fields
- Remediation commit on feature branch: `e228daab39f11c83e1a871dd10fe39ddc854a19a`
- Pull request: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/293` (merged 2026-04-04)
- Merge commit on `main`: `3e3357adaf5d1fbc9563002a42d590745ecfbb1b`
- Main CI run URL with required checks green:
  - `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23988345521` (`CI`, success)
  - `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23988345488` (`R7: Final Winning State`, success after rerun)
- Protected-branch merge evidence:
  - PR metadata shows `merged=true`, `merged_at=2026-04-04T21:52:17Z`, `merge_commit_sha=3e3357adaf5d1fbc9563002a42d590745ecfbb1b`.
  - Post-merge main push-run set for merge SHA audited to completion (`INFLIGHT=0`, `BAD=0`).
