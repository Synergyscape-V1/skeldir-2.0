# B1.4-P3 Remediation Evidence Pack

Date: 2026-03-21  
Scope: B1.4-P3 Attribution Locality Rebinding - Terminal Infrastructure Corrective Directive

## 1) Corrective Status

This corrective pass is merged on `main` and validated with DB-backed proofs plus one full green `main` CI run.

- Structural enforcer: `PASS`
- Enforcer tests: `3 passed`
- Runtime locality suite: `9 passed`
- Migration: applied locally through `alembic upgrade head` including the new terminal corrective revision.
- PR merge: `#223` -> merge commit `9e6cde8e707aea17acaac901d050806db46a3775`
- PR checks: green on `b14-p3-terminal-corrective`
- Main CI: green push run on merge commit (see Section 12)

## 2) Corrective Hypothesis Ledger

### H01 - Aggregate export destroyed historical ROAS via authority TTL gate
Status: **Validated pre-fix, remediated**

Findings:
- `backend/app/api/export.py` previously enforced `sa.expires_at > :authority_now` in the export query path even when no session header was provided.

Remediation:
- Export query is now explicitly partitioned:
  - Aggregate mode (`session_scope is None`): no `session_authority` TTL predicate.
  - Session-scoped raw mode (`X-Attribution-Session-ID` provided): still enforces active `session_authority`.

Proof:
- Runtime test `test_b14_p3_runtime_export_partition_preserves_aggregate_and_session_scoped_reporting` now expires session authority rows and still validates non-zero aggregate reporting.

### H02 - Webhook continuity was Stripe metadata-bound and structurally incomplete
Status: **Validated pre-fix, remediated**

Findings:
- Non-Stripe webhook paths generated fresh session IDs and had no shared stateful continuity layer.

Remediation:
- Introduced ephemeral order-resolution substrate (`order_id -> session_id`, 24h TTL) and wired it into ingress resolution before durable persistence.
- Added `order_id` continuity key propagation in webhook event payload assembly for Shopify, Stripe, PayPal, WooCommerce.

Proof:
- Runtime test `test_b14_p3_runtime_universal_webhook_order_resolution_adopts_active_browser_session` proves cookieless PayPal webhook adopts the browser session through the ephemeral cache.

### H03 - Deterministic click telemetry degraded due no transient click substrate
Status: **Validated pre-fix, remediated**

Findings:
- No transient store existed for `click_id` continuity after durable minimization.

Remediation:
- Introduced ephemeral click-resolution substrate (`click_id -> session_id`, 24h TTL).
- Ingress now resolves candidate session via active authority, then ephemeral order/click continuity, then authority issuance.
- Ingress upserts ephemeral click continuity at write-time without persisting click identifiers in durable payload.

Proof:
- Runtime test `test_b14_p3_runtime_ephemeral_click_resolution_routes_and_expires_after_25h` proves:
  - click continuity routes conversion to the original browser session, and
  - expired click rows are physically hard-deleted by GC.

## 3) Root-Cause Adjudication

- R01 (export implementation conflated raw session locality with aggregate reporting): **Confirmed**
- R02 (no universal transient continuity substrate for cookieless webhooks): **Confirmed**
- R03 (no transient click continuity cache after durable minimization): **Confirmed**
- R04 (proof plane lacked direct coverage for these infrastructure truths): **Confirmed**

## 4) Exact Files Changed

- `alembic/versions/007_skeldir_foundation/202603211810_b14_p3_terminal_ephemeral_resolution_substrate.py`
- `backend/app/models/ephemeral_resolution.py`
- `backend/app/models/__init__.py`
- `backend/app/privacy/ephemeral_resolution.py`
- `backend/app/ingestion/event_service.py`
- `backend/app/api/webhooks.py`
- `backend/app/api/export.py`
- `backend/app/tasks/maintenance.py`
- `backend/app/tasks/beat_schedule.py`
- `backend/tests/integration/test_b14_p3_attribution_locality_runtime.py`
- `scripts/ci/enforce_b14_p3_attribution_locality.py`
- `db/schema/canonical_schema.sql`
- `docs/forensics/B1.4-P3 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`

## 5) Export SQL (Authoritative)

### Aggregate reporting branch (`session_scope is None`)

```sql
SELECT
    date_trunc('day', e.occurred_at)::date AS export_date,
    aa.channel_code AS channel_code,
    COALESCE(SUM(aa.allocated_revenue_cents), 0)::bigint AS revenue_cents,
    COUNT(DISTINCT aa.event_id)::bigint AS conversion_count,
    COALESCE(AVG(aa.confidence_score), 0)::numeric AS confidence_score
FROM attribution_allocations aa
JOIN attribution_events e
  ON e.id = aa.event_id
 AND e.tenant_id = aa.tenant_id
WHERE aa.tenant_id = :tenant_id
  AND e.occurred_at >= :start_ts
  AND e.occurred_at < :end_ts
  AND (
        :channels_is_empty
        OR lower(aa.channel_code) = ANY(CAST(:channels AS text[]))
  )
GROUP BY export_date, aa.channel_code
ORDER BY export_date ASC, aa.channel_code ASC
```

### Session-scoped raw branch (`X-Attribution-Session-ID` provided)

```sql
SELECT
    date_trunc('day', e.occurred_at)::date AS export_date,
    aa.channel_code AS channel_code,
    COALESCE(SUM(aa.allocated_revenue_cents), 0)::bigint AS revenue_cents,
    COUNT(DISTINCT aa.event_id)::bigint AS conversion_count,
    COALESCE(AVG(aa.confidence_score), 0)::numeric AS confidence_score
FROM attribution_allocations aa
JOIN attribution_events e
  ON e.id = aa.event_id
 AND e.tenant_id = aa.tenant_id
JOIN session_authority sa
  ON sa.tenant_id = e.tenant_id
 AND sa.session_id = e.session_id
WHERE aa.tenant_id = :tenant_id
  AND e.session_id = :session_id
  AND e.occurred_at >= :start_ts
  AND e.occurred_at < :end_ts
  AND sa.invalidated_at IS NULL
  AND sa.expires_at > :authority_now
  AND (
        :channels_is_empty
        OR lower(aa.channel_code) = ANY(CAST(:channels AS text[]))
  )
GROUP BY export_date, aa.channel_code
ORDER BY export_date ASC, aa.channel_code ASC
```

## 6) Ephemeral Infrastructure Delivered

### Schema (Alembic)

Revision:
- `202603211810_b14_p3_terminal_ephemeral_resolution_substrate.py`

Created tables:
- `public.ephemeral_order_resolution`
- `public.ephemeral_click_resolution`

Properties:
- tenant-scoped, RLS enabled/forced
- unique keys:
  - `(tenant_id, order_id)`
  - `(tenant_id, click_id)`
- hard 24h bounds via CHECK constraints
- role grants include DELETE for runtime GC

### Runtime Hard-Delete GC

Implemented:
- task: `app.tasks.maintenance.gc_expired_ephemeral_resolution`
- async worker routine: `_delete_expired_ephemeral_resolution_rows`
- beat schedule entry: `b14-p3-ephemeral-resolution-gc` every 10 minutes (disable via env flag).

## 7) Ingress Resolution Wiring

`backend/app/ingestion/event_service.py` now:
- extracts bounded continuity keys (`order_id`, `click_id`/`gclid`/`fbclid`),
- resolves candidate session via `resolve_session_candidate_with_ephemeral_substrate(...)`,
- resolves authority via `resolve_session_authority(...)`,
- upserts ephemeral order/click continuity links via `upsert_ephemeral_resolution_links(...)`.

All webhook ingress paths now provide `order_id` continuity keys in `backend/app/api/webhooks.py`.

## 8) DB-Backed Proofs

Executed commands:

```powershell
python scripts/ci/enforce_b14_p3_attribution_locality.py
pytest backend/tests/test_b14_p3_attribution_locality_enforcer.py -q
pytest backend/tests/integration/test_b14_p3_attribution_locality_runtime.py -q -rs
```

Observed results:
- Enforcer script: `result=PASS`
- Enforcer tests: `3 passed`
- Runtime integration proofs: `9 passed`

New runtime proofs added:
- `test_b14_p3_runtime_universal_webhook_order_resolution_adopts_active_browser_session`
- `test_b14_p3_runtime_ephemeral_click_resolution_routes_and_expires_after_25h`

Updated runtime proof:
- `test_b14_p3_runtime_export_partition_preserves_aggregate_and_session_scoped_reporting`
  - now explicitly validates historical aggregate reporting after session-authority expiry.

## 9) CI Proof-Lane Extensions

`scripts/ci/enforce_b14_p3_attribution_locality.py` now additionally enforces:
- export branch partitioning and aggregate TTL-decoupling,
- event-service ephemeral resolution tokens,
- maintenance GC substrate tokens,
- terminal corrective migration presence/tokens,
- new runtime proof test tokens.

## 10) P0/P1/P2/P3 Preservation Checks

Preserved:
- session-local recompute and no-reconstruction gates
- tenant-scoped enforcement patterns
- bounded telemetry minimization and durable forbidden-key behavior
- merge-blocking P3 structural proof lane semantics

No durable cross-session bridge join was introduced.

## 11) Corrective Closeout Verdict (Branch Truth)

This terminal corrective directive is **implemented, merged to `main`, and falsifiably validated** by schema, code, DB-backed tests, PR required-check topology, and one full green `main` CI run on the merge commit.

**B1.4-P3 status:** **PASSING on `main`**.

## 12) Mainline Merge + Protected CI Evidence

Authoritative merge and CI anchors:

- PR: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/223`
- PR state: `MERGED` at `2026-03-21T18:30:18Z`
- Merge commit on `main`: `9e6cde8e707aea17acaac901d050806db46a3775`

Protected-check proof on PR head:

- PR check sweep (green): `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23385718595`
- Schema authority gate (green): `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23385718577/job/68032322967`
- P3 lane on PR (green): `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23385718595/job/68032410019`

Full `main` CI proof (post-merge):

- CI run (push on merge SHA, green): `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23385964517`
- `B1.4 P3 Attribution Locality Proofs` job (green): `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23385964517/job/68033051215`

Final production-truth statement:

- **B1.4-P3 is passing on `main` with required checks green and merge-blocking proof lane intact.**
