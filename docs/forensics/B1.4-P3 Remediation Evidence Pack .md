# B1.4-P3 Remediation Evidence Pack

Date: 2026-03-20  
Scope: B1.4-P3 Attribution Locality Rebinding (compute + read/export + CI proof lane)

## 1) Outcome Summary

This remediation pass rebounded attribution-bearing computation from tenant+window-only selection to session-local authority constraints and added a merge-blocking P3 proof lane scaffold.

Implemented surfaces:
- Worker locality rebinding in `backend/app/tasks/attribution.py`
- Ingestion-to-worker session propagation in `backend/app/ingestion/event_service.py` and `backend/app/api/webhooks.py`
- Session-local export read path in `backend/app/api/export.py`
- Dedicated P3 structural enforcer + negative controls
- Dedicated P3 runtime proof suite + CI lane wiring + required-check contract update

## 2) Hypothesis Ledger (H01-H11)

### H01 - Authoritative attribution substrate still tenant + time-window oriented
Status: **Validated (pre-remediation), remediated**.

Evidence:
- Previous worker query selected by `tenant_id + occurred_at window` only.
- Remediated worker now enforces `session_id` predicate and active `session_authority` join.

### H02 - Time-window logic acting as identity surrogate
Status: **Validated (pre-remediation), remediated**.

Evidence:
- Window remains as temporal bound, but compute now requires session-local scope resolution and session-bound event selection.

### H03 - Conversion-bearing webhook events structurally unattributable under current session model
Status: **Refuted for current deterministic baseline model**.

Evidence:
- Current conversion computation is event-local (no clickstream stitching path in this worker).
- Webhook conversions carry bounded telemetry (`utm_source`, `utm_medium`, `campaign_id`, `channel`, `external_event_id`) and are now processed only within session-local scope.

### H04 - Boundary between forbidden cross-session joins and approved bounded commerce joins unadjudicated
Status: **Adjudicated**.

Decision:
- P3 implementation forbids durable cross-session reconstruction joins in worker/read paths.
- No bounded commerce bridge join is required for current deterministic baseline compute path in this phase.

### H05 - Worker can still allocate across sessions with durable surrogates
Status: **Validated (pre-remediation), remediated**.

Evidence:
- Worker now computes allocations per session scope and rejects stale/invalid session scopes.
- Telemetry guard checks forbidden identifier/proxy keys before allocation.

### H06 - Read/export surfaces may violate locality even if worker fixed
Status: **Validated risk, remediated**.

Evidence:
- Export endpoints now query attribution data only under explicit `X-Attribution-Session-ID` scope and active `session_authority`.

### H07 - Bounded attribution telemetry visibility under-specified
Status: **Validated (pre-remediation), remediated**.

Evidence:
- `ALLOWED_BOUNDED_TELEMETRY_KEYS` inventory introduced in worker.
- Forbidden key guard leverages privacy authority banned identifier set.

### H08 - Hidden helper/query-builder paths preserve old locality assumptions
Status: **Partially validated, remediated on authoritative P3 surfaces**.

Evidence:
- Rebound authoritative worker query path, webhook scheduling path, and export attribution query path.

### H09 - Deterministic replay may stay true in privacy-wrong way
Status: **Addressed with dedicated P3 runtime proofs**.

Evidence:
- P3 runtime tests include session-local replay determinism and cross-session non-linking assertions.

### H10 - Tenant safety / tenantless fail-closed could regress
Status: **No regression introduced in touched surfaces**.

Evidence:
- Tenant task envelope and tenant GUC flows preserved.
- Session-local rebinding occurs inside tenant-scoped DB context only.

### H11 - Missing merge-blocking P3 proof lane
Status: **Remediated in repository topology**.

Evidence:
- Added `b14-p3-attribution-locality-proofs` job in `.github/workflows/ci.yml`.
- Added required context `B1.4 P3 Attribution Locality Proofs` to required-check contract.

## 3) Root Cause Adjudication (R01-R06)

- R01 (historical optimization for enough events in window): **Confirmed**
- R02 (session authority added before compute rebinding): **Confirmed**
- R03 (locality assumptions distributed across worker/read surfaces): **Confirmed**
- R04 (no explicit bounded-commerce-join model yet): **Confirmed**, but not required for current deterministic baseline in P3
- R05 (telemetry minimization completed before compute visibility inventory): **Confirmed**
- R06 (CI proofing stronger on storage/substrate than compute locality): **Confirmed**

## 4) Files Changed

- `.github/workflows/ci.yml`
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- `scripts/ci/enforce_b14_p3_attribution_locality.py`
- `backend/tests/test_b14_p3_attribution_locality_enforcer.py`
- `backend/tests/integration/test_b14_p3_attribution_locality_runtime.py`
- `backend/app/tasks/attribution.py`
- `backend/app/ingestion/event_service.py`
- `backend/app/api/webhooks.py`
- `backend/app/services/attribution.py`
- `backend/app/api/export.py`

## 5) P3 Gate Coverage Mapping

- Exit Gate 1 (Query/Read locality): worker session-local query predicates + export session-local query predicates + structural enforcer.
- Exit Gate 2 (Runtime no-reconstruction): P3 runtime test `test_b14_p3_runtime_query_locality_blocks_cross_session_reconstruction_attempt`.
- Exit Gate 3 (Conversion-path adjudication): P3 runtime test `test_b14_p3_runtime_conversion_paths_are_session_local_without_durable_bridge_join`.
- Exit Gate 4 (Bounded telemetry visibility): worker telemetry inventory + forbidden-key guard.
- Exit Gate 5 (Session-local deterministic replay): P3 runtime test `test_b14_p3_runtime_session_local_replay_is_deterministic`.
- Exit Gate 6 (Tenant safety preservation): tenant envelope/GUC flows retained; no tenant bypass introduced.
- Exit Gate 7 (Merge-blocking proof gate): new CI lane + required-check contract context.

## 6) Verification Evidence (This Iteration)

Local commands executed:
- `python scripts/ci/enforce_b14_p3_attribution_locality.py` -> PASS
- `pytest backend/tests/test_b14_p3_attribution_locality_enforcer.py -q` -> PASS (3 passed)
- `pytest backend/tests/integration/test_b14_p3_attribution_locality_runtime.py -q -rs` -> SKIPPED locally because local DB schema was not at alembic head (`session_authority` absent)

Notes:
- Runtime suite is CI-wired to run after `alembic upgrade head` in `b14-p3-attribution-locality-proofs`.
- Existing local environment mismatch does not represent authoritative CI runtime proof state.

## 7) Conversion-Path Adjudication Statement

For current B1.4-P3 scope and deterministic baseline worker:
- Conversion-bearing webhook events are attributable session-locally without durable cross-session bridge joins.
- Revenue allocation remains event-local and session-bounded.
- No B2.2/B2.3 commerce bridge productization was introduced in this phase.

## 8) Completion Statement (Current Iteration)

Repository remediation artifacts for B1.4-P3 are implemented and wired.

Authoritative completion requirements still pending in this iteration:
- Protected-branch merge to `main`
- One full green `main` CI run showing `B1.4 P3 Attribution Locality Proofs` executed and passing
- Runtime artifact capture from that authoritative main run

Until those governance events are complete, **B1.4-P3 cannot be declared finally passing on main**.
