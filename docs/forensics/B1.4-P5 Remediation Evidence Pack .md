# B1.4-P5 Remediation Evidence Pack

Date: 2026-03-23
Repo: `c:\Users\ayewhy\II SKELDIR II`
Phase: `B1.4-P5 - Export, Logging, and Artifact No-Leak Closure`

## 1. Scope and Constraints

This remediation covered only P5 output-surface no-leak closure (exports, logs, failure surfaces, artifacts, CI lane enforcement).

Out of scope by directive:
- Branch-protection API hardware enforcement (deferred to P7)
- Re-opening P1/P2/P3/P4 architecture semantics except where required to preserve compatibility

## 2. Hypothesis Ledger (H01-H10)

| ID | Hypothesis | Adjudication | Evidence |
|---|---|---|---|
| H01 | Export output not fully allowlisted | **Validated** (pre-fix), then **closed** | Added strict `EXPORT_ROW_ALLOWLIST` and payload leak checks in `backend/app/api/export.py`; runtime export test passes. |
| H02 | Export correctness relies on implicit behavior | **Validated** (pre-fix), then **closed** | Added explicit allowlist + fail-closed enforcement in `export.py`; OpenAPI tightened in `api-contracts/openapi/v1/export.yaml`. |
| H03 | Logging redaction scope too narrow | **Validated** (risk material), then **closed** | `backend/app/observability/logging_config.py` now applies P5 output redaction for records/args; runtime log canary proof passes. |
| H04 | Dead-letter/quarantine/debug surfaces can leak | **Validated** (pre-fix), then **closed** | `backend/app/ingestion/dlq_handler.py` now sanitizes and drops forbidden proxy keys in failure payload surfaces; runtime failure-surface proofs pass. |
| H05 | CI artifacts/evidence bundles may leak | **Validated** (no dedicated P5 scanner), then **closed** | Added `scripts/ci/scan_b14_p5_artifacts.py`; non-vacuity (`--simulate-regression`) and pass scan both proven. |
| H06 | Redaction may miss proxy identifiers | **Validated** (pre-fix), then **closed** | Added `backend/app/privacy/output_redaction.py` and integrated into logging/celery/export/dlq surfaces. |
| H07 | Error paths may bypass redaction | **Validated** (pre-fix risk), then **closed** | `backend/app/celery_app.py` failure serialization now redacts exception/traceback/args/kwargs. |
| H08 | Missing dedicated merge-blocking P5 lane | **Validated** (pre-fix), then **closed in repo config** | Added CI job `b14-p5-export-log-artifact-no-leak` and required context in governance contract JSON. |
| H09 | B1.3 artifact discipline not extended with B1.4 canaries | **Validated** (pre-fix), then **closed** | Added P5 artifact scanner + seeded canary enforcement and negative control tests. |
| H10 | Evidence/process artifacts can include illegal fields | **Validated** (pre-fix risk), then **closed** | Artifact scanner now blocks seeded canaries/pattern leaks; runtime and scanner tests prove detection. |

## 3. Root-Cause Adjudication (R01-R05)

| ID | Root Cause | Adjudication |
|---|---|---|
| R01 | Incremental hardening without one output invariant | **Confirmed** |
| R02 | Logging controls at selected choke points only | **Confirmed** |
| R03 | Export shape from serializer convenience vs privacy allowlist | **Confirmed** |
| R04 | Secret scanning existed but not privacy-canary scanning | **Confirmed** |
| R05 | Failure/debug surfaces were previously under-scoped | **Confirmed** |

## 4. Remediations Applied

### 4.1 Unified output redaction boundary
- Added `backend/app/privacy/output_redaction.py`.
- New helpers:
  - `output_forbidden_key_set(...)`
  - `redact_output_text(...)`
  - `sanitize_output_payload(...)`
  - `find_output_leaks(...)`
- Added key-drop mode (`drop_forbidden_keys=True`) for failure surfaces.

### 4.2 Export allowlist closure (Gate 1)
- Updated `backend/app/api/export.py`:
  - explicit row/top-level/date-range allowlists
  - no-leak checks before emit
  - fail-closed response on leak detection
  - tenant_id included in JSON export payload
- Updated `api-contracts/openapi/v1/export.yaml`:
  - tightened schema (`additionalProperties: false`)
  - explicit `ExportRow`
  - `tenant_id` in export payload schema

### 4.3 Logging and exception no-leak closure (Gate 2)
- Updated `backend/app/observability/logging_config.py`:
  - RedactionFilter + JsonFormatter now enforce P5 output redaction.
- Updated `backend/app/celery_app.py`:
  - sanitize task args/kwargs and redact exception/traceback on worker failure paths.

### 4.4 Failure-surface closure (Gate 4)
- Updated `backend/app/ingestion/dlq_handler.py`:
  - `_sanitize_failure_surface_payload(...)` now drops forbidden proxy keys and redacts direct identifiers.
  - Applied to both `dead_events` and `dead_events_quarantine` writes.

### 4.5 Artifact secrecy closure (Gate 3)
- Added `scripts/ci/scan_b14_p5_artifacts.py`.
- Added fail-closed synthetic regression mode: `--simulate-regression`.

### 4.6 Merge-blocking lane and governance (Gate 5)
- Updated `.github/workflows/ci.yml` with job:
  - `b14-p5-export-log-artifact-no-leak`
  - runs enforcer, negative controls, runtime proofs, scanner non-vacuity, artifact upload
- Updated required-check contract:
  - `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
  - required context: `B1.4 P5 Export Log Artifact No-Leak`

### 4.7 Authority contract alignment
- Updated `contracts-internal/governance/b14_p0_privacy_authority.main.json` with explicit P5 forbidden output/proxy fields.

## 5. Files Changed

- `.github/workflows/ci.yml`
- `api-contracts/openapi/v1/export.yaml`
- `backend/app/api/export.py`
- `backend/app/celery_app.py`
- `backend/app/ingestion/dlq_handler.py`
- `backend/app/observability/logging_config.py`
- `backend/app/privacy/__init__.py`
- `backend/app/privacy/output_redaction.py` (new)
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- `contracts-internal/governance/b14_p0_privacy_authority.main.json`
- `scripts/ci/enforce_b14_p5_export_log_artifact_no_leak.py` (new)
- `scripts/ci/scan_b14_p5_artifacts.py` (new)
- `backend/tests/test_b14_p5_export_log_artifact_no_leak_enforcer.py` (new)
- `backend/tests/integration/test_b14_p5_export_log_artifact_no_leak_runtime.py` (new)

## 6. Proof Execution (Falsifiable)

### 6.1 P5 enforcer + runtime + scanner
Executed on 2026-03-23 with:
- `DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/skeldir_b14_p5_local`
- `MIGRATION_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/skeldir_b14_p5_local`

Results:
- `python scripts/ci/enforce_b14_p5_export_log_artifact_no_leak.py` -> **PASS**
- `pytest backend/tests/test_b14_p5_export_log_artifact_no_leak_enforcer.py -q` -> **4 passed**
- `pytest backend/tests/integration/test_b14_p5_export_log_artifact_no_leak_runtime.py -q -rs` -> **4 passed**
- `python scripts/ci/scan_b14_p5_artifacts.py --simulate-regression` -> **FAIL as expected** (non-vacuity)
- `python scripts/ci/scan_b14_p5_artifacts.py --artifacts-dir artifacts/b14_p5 --canary p5_artifact_canary@test.invalid` -> **PASS**

Artifacts:
- `artifacts/b14_p5/runtime.log`
- `artifacts/b14_p5/junit.runtime.xml`

### 6.2 Preservation of prior-phase guarantees (Gate 6)
Executed on 2026-03-23:
- `pytest backend/tests/integration/test_b14_p1_ingress_privacy_runtime.py backend/tests/integration/test_b14_p2_session_authority_runtime.py backend/tests/integration/test_b14_p3_attribution_locality_runtime.py backend/tests/integration/test_b14_p4_retention_deletion_runtime.py -q -rs`
- Result: **23 passed**

Additional redaction regression check:
- `pytest backend/tests/test_b11_p6_log_redaction_integrity.py -q -rs` -> **5 passed**

## 7. Exit Gate Status

- Gate 1 Export allowlist: **PASS**
- Gate 2 Logging no-leak: **PASS**
- Gate 3 Artifact secrecy: **PASS**
- Gate 4 Failure-surface no-leak: **PASS**
- Gate 5 Merge-blocking proof lane (repo configuration): **PASS**
- Gate 6 Prior-phase preservation: **PASS**

## 8. Mainline Closure Status

Code/config/test evidence for B1.4-P5 is complete and locally green with falsifiable proofs.

Protected-branch landing status must be verified by authoritative remote PR merge and required-check execution on `main`.

At this point in this pack revision:
- Local implementation/proofs: **COMPLETE**
- Remote protected-branch merge + required-check run-history evidence on `main`: **PENDING final push/merge execution evidence**

## 9. Direct Determination

B1.4-P5 is **technically closed by implementation and proofs in this workspace**.

Final program-level statement "B1.4-P5 passing on `main`" is conditioned on protected-branch merge and green required checks on the remote authoritative `main` branch.
