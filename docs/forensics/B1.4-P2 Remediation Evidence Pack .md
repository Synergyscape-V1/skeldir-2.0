# B1.4-P2 Remediation Evidence Pack

Date: 2026-03-20  
Scope: B1.4-P2 follow-up corrective closeout (expired-session severance ambiguity)

## 1) Corrective Scope and Outcome

This pass was bounded to the remaining P2 ambiguity only: whether retained invalidated `session_authority` rows plus FK-bound `attribution_events` preserve forbidden cross-session linkage semantics after expiry.

Implemented and merged on `main`:

- Added DB-backed persisted-row severance proof: expired authority session rotates, historical rows remain non-bridgeable to new session.
- Added DB-backed stale-session insert rejection proof at DB trigger layer (not app filter).
- Hardened P2 enforcer to require these runtime proofs and assert `ON DELETE RESTRICT` FK topology remains explicit.

Result: **B1.4-P2 corrective objective is satisfied on `main`** with no unresolved expired-row linkage ambiguity.

## 2) Corrective Hypothesis Ledger (H01-H07)

### H01 - Retained invalidated rows may still preserve forbidden durable linkage
Status: **Refuted for cross-session linkage**.

Evidence:
- Runtime proof `test_b14_p2_runtime_persisted_expired_rows_cannot_bridge_rotated_session` demonstrates that event under expired authority row (`event_a`) and event after rotation (`event_b`) do not join on session substrate.
- Query assertions in the test prove zero bridge rows for both event-session and authority-mediated joins.

### H02 - Model may pass forward non-linkability but fail physical post-expiry severance
Status: **Refuted**.

Evidence:
- Existing runtime rotation logic in `resolve_session_authority` invalidates stale candidate row then issues fresh `uuid4()` session.
- New persisted-row severance proof confirms post-expiry persisted history cannot be used to bridge to successor session.

### H03 - Runtime proof surface too narrow on persisted historical rows
Status: **Validated pre-fix, remediated**.

Remediation:
- Added persisted historical-row severance proof test.
- Added direct DB-level stale-insert rejection proof test.
- Enforcer now requires both proof tokens, so removal/regression fails CI.

### H04 - Schema/trigger enforce valid inserts but under-specify expired-row lifecycle
Status: **Partially validated pre-fix, remediated for P2 scope**.

Remediation:
- No schema redesign required.
- Added explicit runtime and DB-trigger proofs that define and lock expected expired-row behavior: stale row invalidation + rotation + insert-time stale rejection.

### H05 - Async webhook singleton-session behavior may be real but out of P2 scope
Status: **Validated as real; out-of-scope for P2 blocker set**.

Evidence:
- Webhook handlers generate privacy sessions and do not provide a P2 ephemeral continuity bridge.
- This does not reintroduce cross-session durable identity linkage; therefore it is not a P2 substrate failure.

### H06 - `click_id` / `gclid` / `fbclid` removal may be out-of-scope
Status: **Validated as out-of-scope for P2 failure**.

Evidence:
- No canonical durable columns for these IDs in schema; minimization stands.
- P2 contract allows removal; bounded replacement envelope belongs to later phase if needed for continuity goals.

### H07 - Hidden helper/query paths may still preserve legacy linkage assumptions
Status: **No blocking P2 contradiction found**.

Evidence:
- Canonical ingestion write path remains authority-resolved (`resolve_session_authority`) before event persistence.
- Worker/query surfaces continue time-window attribution reads, but no alternate durable cross-session bridge write path was found.

## 3) Root-Cause Adjudication

- R01 (optimized first for authority binding/rotation, not explicit persisted-row severance proof): **Confirmed**.
- R02 (referential integrity preference left severance interpretation ambiguous): **Confirmed**.
- R03 (proof plane emphasized bridge non-adoption/rotation over persisted-row severance): **Confirmed**.
- R04 (later-phase continuity concerns mixed into P2 review noise): **Confirmed**.

## 4) Exact Files Changed (Corrective Pass)

Code/tests/enforcer merged via PR #218:

- `backend/tests/integration/test_b14_p2_session_authority_runtime.py`
- `scripts/ci/enforce_b14_p2_session_authority.py`

Evidence artifacts added in this closeout iteration:

- `docs/forensics/evidence/b14_p2/runtime/pr_218_merge_20260320.json`
- `docs/forensics/evidence/b14_p2/runtime/main_commit_d33c2d23_run_list_20260320.json`
- `docs/forensics/evidence/b14_p2/runtime/main_commit_d33c2d23_run_list_20260320.txt`
- `docs/forensics/evidence/b14_p2/runtime/main_commit_d33c2d23_ci_jobs_20260320.json`
- `docs/forensics/B1.4-P2 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`

## 5) Expired-Session Severance Design (Authoritative Behavior)

- `session_authority` row may persist for historical integrity/audit context.
- Expired/invalidated authority rows are not reusable by runtime.
- New session issuance after stale candidate is always fresh UUID4.
- `attribution_events` insert trigger blocks stale/invalidated session references (`session authority violation`).
- Therefore, persisted historical rows do not grant a bridge to subsequent sessions.

## 6) DB-Backed Proof Harnesses (Corrective)

Added runtime tests:

- `test_b14_p2_runtime_persisted_expired_rows_cannot_bridge_rotated_session`
- `test_b14_p2_runtime_db_rejects_stale_session_insert_despite_historical_rows`

Existing runtime controls retained:

- stale invalidation + rotation
- durable candidate non-adoption

Enforcer hardening:

- `scripts/ci/enforce_b14_p2_session_authority.py` now requires:
  - `ON DELETE RESTRICT` schema token
  - both new runtime proof test names

## 7) Scope Discipline Adjudication

- Async webhook continuity remains a documented system condition, but no direct contradiction of P2 privacy substrate contract was found.
- Missing bounded click telemetry replacement remains later-phase concern, not a P2 blocker.
- No P3 locality rebinding or B2 revenue-matching implementation was introduced in this corrective pass.

## 8) Merge and CI Evidence

### Protected-branch merge

- PR merged: `#218`  
  https://github.com/Synergyscape-V1/skeldir-2.0/pull/218
- Merge commit on `main`: `d33c2d23248d38c75fd7f07b7a0d45d8bd6a4430`
- Merge timestamp: `2026-03-20T17:08:28Z`

### Full `main` CI run set for merge commit

All workflows for commit `d33c2d23248d38c75fd7f07b7a0d45d8bd6a4430` completed with no failed conclusions.

Key runs:

- CI: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23353935825
- Empirical Validation - Directive Compliance: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23353935806
- R1: Contract/Runtime Viability: https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23353935839

Dedicated P2 lane in merged main CI:

- `B1.4 P2 Session Authority Proofs` (success):  
  https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/23353935825/job/67940002352

Governance required-check contract still includes the P2 context:

- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`

## 9) Exit Gate Status (Corrective Directive)

- Exit Gate 1 (Expired-Session Severance): **Pass**
- Exit Gate 2 (Session Substrate Integrity): **Pass**
- Exit Gate 3 (Scope Discipline): **Pass**
- Exit Gate 4 (Merge-Blocking Proof): **Pass**
- Exit Gate 5 (P0/P1 Preservation): **Pass**

## 10) Final Corrective Adjudication

**B1.4-P2 is fully passing on `main` without unresolved expired-row linkage ambiguity.**

The corrective pass validated and remediated the only remaining P2 blocker space while preserving P0/P1 invariants and keeping scope bounded away from P3/B2 implementation.
