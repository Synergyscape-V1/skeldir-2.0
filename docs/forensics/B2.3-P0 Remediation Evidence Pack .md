# B2.3-P0 Remediation Evidence Pack

Generated: 2026-04-25
Primary branch authority target: `main`
Directive cycle: **B2.3-P0 Corrective Action Remediation Directive (clean baseline worktree execution)**

## 1. Baseline Convergence

1. Corrective execution was performed from a clean secondary worktree rooted to `origin/main` (no conflict-contaminated lineage).
2. Authoritative merges landed via PRs **#400**, **#401**, and **#402** into `main`.
3. Latest corrective merge commit on `main`: **`93dc2bce2d7c7d525e83534879bc032198c854e2`**.

## 2. Initial Findings (Validated)

1. Governance/runtime semantic authority was already substantive and enforceable (not decorative).
2. Residual blocker remained governed runtime Neon topology proof due credential-plane/API auth failure.
3. Lifecycle migration hard-failing unconditionally for `pg_cron` availability breaks non-governed CI Postgres runners and cascades false failure; production strictness must be gated to governed context.
4. Governed direct DB URL sources still resolved localhost DSNs in secrets manager, forcing API fallback path.

## 3. Remediations Implemented in This Iteration

### R01 - Governed-only `pg_cron` hard-fail semantics

Files:
- `alembic/versions/007_skeldir_foundation/202604241815_b23_p0_activity_independent_identity_lifecycle.py`
- `.github/workflows/schema-deploy-production.yml`

Changes:
1. Added explicit governed strictness toggle in lifecycle migration:
   - `current_setting('skeldir.require_pg_cron', true)`
   - If toggle is on: fail on missing `pg_cron`/`cron` schema.
   - If toggle is off: emit notice and skip scheduler registration (non-governed environments only).
2. Bound production deploy job to strict mode by setting:
   - `PGOPTIONS: -c skeldir.require_pg_cron=on`

Outcome:
- Production path remains fail-closed for activity-independent lifecycle requirements.
- Non-governed CI schema validation remains runnable.

### R02 - Governed secret fallback expansion for DSN resolution

Files:
- `.github/workflows/schema-deploy-production.yml`

Changes:
1. Added allowlisted GitHub secret env bindings:
   - `GH_NEON_DATABASE_URL`
   - `GH_NEON_MIGRATION_DATABASE_URL`
2. Added fallback resolution chain to accept these values only when valid non-local Postgres DSNs and when AWS-resolved DSNs are absent/localhost.

Outcome:
- Reduced dependency on Neon API path when valid governed DSNs are available from GitHub secret plane.

### R03 - Enforcer/proof hardening for governed lifecycle toggle

Files:
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`
- `backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py`

Changes:
1. Enforcer now requires lifecycle migration tokens for governed strictness contract:
   - `current_setting('skeldir.require_pg_cron', true)`
   - `missing_extension:pg_cron`
   - `missing_schema:cron`
   - explicit scheduled-registration skip marker (non-governed context)
2. Added negative control test proving regression when governed toggle token is removed.

Outcome:
- Lifecycle strictness model is machine-enforced and non-vacuous.

### R04 - Explicit secret DSN locality preference hardening

Files:
- `.github/workflows/schema-deploy-production.yml`
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`
- `backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py`

Changes:
1. Added deploy-workflow helper `resolve_secret_manager_database_dsn_value()` that prefers non-local Postgres DSNs across explicit secret candidates before accepting localhost values.
2. Switched explicit `NEON_DATABASE_URL` and `NEON_MIGRATION_DATABASE_URL` resolution paths to this helper.
3. Extended enforcer required tokens and added negative-control test proving regression fails if non-local preference logic is removed.

Outcome:
- Deploy workflow no longer silently prefers localhost DSNs when a non-local explicit candidate exists in the enumerated secret IDs.

## 4. Hypothesis Adjudication (H01 / H02)

### H01: "Did remediation avoid native source alignment and only add governance theater?"

Adjudication: **Refuted**.

Evidence:
1. Native source alignment enforcer remains active:
   - `scripts/ci/enforce_b23_p0_typed_boundary_source_alignment.py`
2. Route/spec/codegen boundary enforcers continue to pass:
   - `scripts/ci/enforce_b15_p3_runtime_route_binding.py`
   - `scripts/ci/enforce_b15_p4_mock_sdk_boundary.py`
3. Non-vacuous negative controls continue to fail under mutation.

### H02: "Does downstream typed mapping create free-form translation risk by design?"

Adjudication: **Not supported by current evidence**.

Evidence:
1. Mapping remains typed/deterministic with pre-dispatch validation.
2. No free-form translation path introduced in this cycle.
3. Existing downstream mapping proofs remain green.

## 5. Falsifiable Local Validation (This Iteration)

All executed from clean corrective branch before merge:

1. `python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py --skip-baseline-git-check` -> PASS
2. `python scripts/ci/enforce_b23_p0_typed_boundary_source_alignment.py` -> PASS
3. `python scripts/ci/enforce_b15_p7_ci_adjudication_closure.py` -> PASS
4. `python scripts/ci/enforce_b15_p3_runtime_route_binding.py` -> PASS
5. `python scripts/ci/enforce_b15_p4_mock_sdk_boundary.py` -> PASS
6. `pytest backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py -q` -> PASS (18)
7. `pytest backend/tests/test_b23_p0_semantic_authority.py -q` -> PASS (16)
8. `pytest backend/tests/test_b23_p0_typed_boundary_source_alignment_enforcer.py -q` -> PASS (5)
9. `pytest backend/tests/test_b15_p7_ci_adjudication_closure_enforcer.py -q` -> PASS (12)

## 6. Mainline Landing + CI Evidence

1. PR merged: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/400`  
   - merge SHA: `2d89c97a4416772d6845ce9c68ea66bf603831a5`
2. PR merged: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/401`  
   - merge SHA: `8f2f3178c621b62ea925289e2e26393ed14bfa36`
3. PR merged: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/402`  
   - merge SHA: `93dc2bce2d7c7d525e83534879bc032198c854e2`
4. `main` CI run after PR #401 merge: **24930367466** -> success
5. `main` CI run after PR #402 merge: **24930972998** -> success

## 7. Governed Production Deployment Evidence

Triggered on `main` and executed through the actual governed workflow.

Runs:
1. `24929452673` (Production Schema Deployment) -> **failure**
2. `24929469414` (Production Schema Deployment) -> **failure**
3. `24931248563` (Production Schema Deployment, `workflow_dispatch` on `main` after PR #402) -> **failure**

Failure point:
- Step: `Get Neon connection string`
- Error: `Unable to resolve Neon connection URI for governed production deploy.`

Audit artifact evidence (`schema-deployment-audit-24931248563/deployment.log`):
- `Neon credential source: api=github_secret_allowlisted, project=github_variable, db_url=aws_secrets_manager_explicit, migration_db_url=aws_secrets_manager_explicit`
- `Ignoring governed direct database URL secret/parameter because it targets localhost and no non-local migration URL fallback was available; falling back to Neon API connection resolution.`
- `diagnostic=supplied credentials do not pass authentication`

Interpretation:
- Code/workflow path is fail-closed and executing correctly.
- The new DSN-preference hardening is active, but no non-local DSN candidate exists in the enumerated explicit secret set and Neon API authentication still fails.
- Remaining blocker is governed secret-plane/runtime credential validity (external control-plane data), not missing schema/migration logic in repo.

## 8. Current Phase Closure Status

1. **Completed in repo/mainline**:
   - semantic authority enforcement,
   - dual-sided identity law + typed boundary proof plane,
   - lifecycle strictness model with governed fail-closed toggle,
   - protected-branch merges and repeated green `main` CI runs.
2. **Still open**:
   - governed Neon runtime deployment proof remains blocked by secret/auth mismatch in the target environment.

B2.3-P0 cannot be asserted fully closed until governed Neon credentials/project path resolves and the production schema deployment workflow completes successfully through migration + runtime topology verification steps.

## 9. 2026-04-28 Follow-up Corrective Delta (Post-#402)

### Mainline corrective merges already landed

1. PR `#407` merged to `main` (`d6cb8ae53e38e183ce3ecffbf314c26ace1a5066`):
   - Removed pooled-connection `PGOPTIONS` startup-parameter injection that blocked governed migration execution.
2. PR `#408` merged to `main` (`56be2157d76202479a8609da9f169c1f29da12d1`):
   - Added fail-closed platform encryption material resolution for governed production deploy.
   - Hardened baseline ancestor enforcement checkout depth (`fetch-depth: 0`) in CI.

### Hypothesis check: “Neon API key may be required”

Adjudication: **Validated as necessary but not sufficient**.

Evidence:
1. Repository secret `NEON_API_KEY` rotated on `2026-04-28T18:43:23Z`.
2. Governed deploy rerun after rotation:
   - Run `25071273807` (Production Schema Deployment, `main`, `workflow_dispatch`)
   - Still fails fail-closed at `Guard Neon control-plane secrets (fail closed when missing)`
   - Terminal error: `Missing required platform encryption material for governed production deploy migrations.`

Interpretation:
1. Neon API secret validity is required and now present.
2. Decisive remaining blocker is **missing `PLATFORM_TOKEN_ENCRYPTION_KEY`** in governed secret sources (`AWS Secrets Manager`/`SSM`/GitHub allowlist).

### Current governed runtime proof status

1. Full `main` CI is green:
   - Run `25071048265` -> `success`.
2. Governed production deploy remains blocked:
   - `25071054178` -> failure (missing platform encryption material)
   - `25071273807` -> failure (same blocker after Neon API key rotation)

### Typed-boundary gate status (current evidence)

1. B1.5 P4 job passes in `main` CI run `25071048265` (job `73451681217`).
2. Logged TypeScript errors are from intentional negative-control compilation paths and are accompanied by successful gate completion, not silent pass-through of unexpected type drift.

### Open requirement for final B2.3-P0 closure

B2.3-P0 remains open pending governed secret-plane completion for:
1. `PLATFORM_TOKEN_ENCRYPTION_KEY` (and key-id if required by deployed payload shape),
2. successful governed production deployment to Neon through migration + topology/lifecycle verification steps.

## 10. 2026-04-28 Final Follow-up Closure Iteration (PR #410-#413)

Generated: 2026-04-28  
Primary branch authority target: `main`  
Latest closure merge on `main`: `33c8a545a44abcc863748b0a31dc19c80ccbaee0`

### 10.1 Initial Findings for This Iteration

1. Governed deployment reached Neon and applied migrations, but failed at lifecycle job registration with:
   - `permission denied for function schedule_in_database`
   - Evidence: Production Schema Deployment run `25078254133`.
2. After registration-path correction, governed deployment failed at runtime lifecycle verification with:
   - `syntax error at or near ":"` for `AND database = :'target_db'` inside a `DO $$ ... $$` block
   - Evidence: Production Schema Deployment run `25079907972`.
3. CI boundary proof plane was strong but ancestry-sensitive; PR branch ancestry had to be aligned with `origin/main` to satisfy the semantic drift gate fail-closed policy.

### 10.2 Remediations Implemented

1. PR `#412` merged (`1554e9317f95747b4bc745f50201eba41fc182c9`):
   - file: `.github/workflows/schema-deploy-production.yml`
   - changed cron job registration logic to:
     - use `cron.schedule(...)` when `cron.database_name == target_db`
     - use `cron.schedule_in_database(...)` only when cross-database registration is required
   - objective: eliminate `schedule_in_database` permission-path failure in same-db Neon runtime mode.
2. PR `#413` merged (`33c8a545a44abcc863748b0a31dc19c80ccbaee0`):
   - file: `.github/workflows/schema-deploy-production.yml`
   - replaced invalid `DO`-block psql-variable interpolation with explicit fail-closed SQL checks for:
     - `pg_cron` extension existence
     - `cron` schema existence
     - active scheduled lifecycle job presence for target DB and command shape
   - objective: make lifecycle substrate verification executable and syntactically valid under governed runtime execution.
3. Baseline contamination prevention was preserved:
   - branch ancestry was explicitly corrected to satisfy `baseline_authority_main_ancestor_check`.

### 10.3 Mainline Landing Evidence (Protected Workflow)

1. PR `#410` merged at `2026-04-28T20:34:05Z`:
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/410`
   - merge SHA: `2ab6d08514aa7bbc3f68f76a7f9b3ad8ef1da4f8`
2. PR `#411` merged at `2026-04-28T21:19:05Z`:
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/411`
   - merge SHA: `3f0b87ace6d8262da7f3d7e2931b7e6fda922bda`
3. PR `#412` merged at `2026-04-28T21:59:59Z`:
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/412`
   - merge SHA: `1554e9317f95747b4bc745f50201eba41fc182c9`
4. PR `#413` merged at `2026-04-28T22:39:18Z`:
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/pull/413`
   - merge SHA: `33c8a545a44abcc863748b0a31dc19c80ccbaee0`

### 10.4 Falsifiable Governed Runtime Proof (Neon)

1. Final governed production deployment run on `main`:
   - Run ID: `25081345154`
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25081345154`
   - Result: `success`
2. Required lifecycle/runtime objects verified in governed path:
   - `attribution_commerce_identities`
   - lifecycle index `idx_attr_commerce_identity_last_observed`
   - prune function `fn_b23_p0_prune_attribution_commerce_identities`
   - `SECURITY DEFINER` wrapper `fn_b23_p0_prune_attribution_commerce_identities_trigger`
   - trigger `trg_b23_p0_prune_attribution_commerce_identities`
   - `pg_cron` extension + `cron` schema + active scheduled job
3. Positive runtime verifier evidence line captured in deploy log:
   - `verified_extension=pg_cron verified_schema=cron verified_job=b23_p0_prune_attribution_commerce_identities_hourly target_db=neondb`

### 10.5 Full Green `main` CI Proof

1. Full `CI` workflow explicitly executed on `main` post-closure merge:
   - Run ID: `25081424764`
   - URL: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25081424764`
   - Result: `success`
   - Head SHA: `33c8a545a44abcc863748b0a31dc19c80ccbaee0`
2. Typed-boundary gate in this run:
   - Job: `B1.5 P4 Mock Plane, SDK, and Typed Boundary Enforcement`
   - Job URL: `https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/25081424764/job/73487713894`
   - Result: `success`

### 10.6 Hypothesis Adjudication (H01/H02, Follow-up)

1. H01 ("governance theater instead of native source alignment"):
   - **Refuted** for this cycle.
   - Evidence: fixes were applied directly to the governed deploy source workflow and merged to `main`; failures moved from permission-path defect -> SQL verifier defect -> full governed runtime pass.
2. H02 ("downstream projection layer guarantees LLM retry-loop/type-failure risk"):
   - **Not validated as a current blocker in this cycle.**
   - Evidence: decisive blockers were runtime deployment/verification defects; typed boundary and CI closure gates passed on final `main` run.

### 10.7 Closure Status

For this corrective directive scope, closure criteria are now satisfied with falsifiable evidence:
1. changes landed on `main` via protected PR merges,
2. governed production deploy to Neon completed and verified lifecycle substrate in runtime,
3. full `main` CI was run once and is green,
4. evidence pack updated with exact run IDs, SHAs, and defect-to-fix traceability.
