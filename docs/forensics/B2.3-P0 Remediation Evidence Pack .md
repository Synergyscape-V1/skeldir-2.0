# B2.3-P0 Remediation Evidence Pack

Generated: 2026-04-24  
Primary branch authority target: `main`  
Directive cycle: **B2.3-P0 Corrective Action Remediation Directive (Fail-Closed Lifecycle + Governed Deploy Proof)**

## 1. Baseline Convergence

1. Previous workspace was conflict-contaminated and non-authoritative (`AA` merge conflicts, divergent `main`).
2. Corrective work was executed in a clean secondary worktree rooted at `origin/main` commit `a3a0a096b`.
3. All findings/remediations below were produced from this clean baseline.

## 2. Initial Findings (Validated)

1. **Validated**: delayed-arrival lifecycle remained write-activity dependent (`database_trigger_pruning`) and not activity-independent under quiescent periods.
2. **Validated**: governed production deploy workflow still had a soft-skip path when Neon control-plane secrets were absent.
3. **Validated**: B2.3 typed-boundary source alignment and B1.5-P3/P4 enforcers were already substantive, but this cycle still needed explicit canonical proof continuity.
4. **Validated**: B0.6 false-authority exclusions and threshold guards were already present and had to be preserved.

## 3. Remediations Implemented

### R01 - Activity-independent DB-native lifecycle enforcement

Files:
- `alembic/versions/007_skeldir_foundation/202604241815_b23_p0_activity_independent_identity_lifecycle.py`
- `backend/app/revenue_verification/semantic_authority.py`
- `contracts-internal/governance/b23_p0_semantic_authority_freeze.main.json`
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`
- `backend/tests/test_b23_p0_semantic_authority.py`
- `backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py`

Changes:
1. Added migration `202604241815` to install/require `pg_cron` and register scheduled pruning job:
   - job name: `b23_p0_prune_attribution_commerce_identities_hourly`
   - cron schedule: `0 * * * *`
   - command: `SELECT public.fn_b23_p0_prune_attribution_commerce_identities(1000);`
2. Hardened trigger wrapper privilege path by replacing wrapper with `SECURITY DEFINER` function.
3. Preserved immediate bootstrap prune (`500000`) for migration-time backfill cleanup.
4. Upgraded semantic authority lifecycle mode from `database_trigger_pruning` to `database_scheduled_pruning` and added explicit lifecycle constants:
   - schedule,
   - job name,
   - activity-independent enforcement requirement.
5. Extended contract lifecycle binding with:
   - `pruning_schedule`,
   - `pruning_job_name`,
   - `activity_independent_enforcement`.

Outcome:
- Lifecycle authority is no longer write-path dependent by policy or implementation intent.

### R02 - Governed production deploy path made fail-closed

Files:
- `.github/workflows/schema-deploy-production.yml`
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`
- `contracts-internal/governance/b15_p7_ci_adjudication_closure.main.json`

Changes:
1. Replaced Neon secret guard soft-skip with fail-closed behavior:
   - old behavior: green no-op when missing secrets,
   - new behavior: explicit failure (`exit 1`) with required message.
2. Removed downstream conditional execution (`if: steps.neon-guard.outputs.enabled == 'true'`) so deploy stages no longer silently skip.
3. Extended Neon runtime topology verification to assert:
   - table/index/function/trigger presence,
   - trigger-wrapper `SECURITY DEFINER` status,
   - `pg_cron` extension presence,
   - scheduled job presence (`b23_p0_prune_attribution_commerce_identities_hourly`).
4. Enforcer now fails if workflow tokens for fail-closed deploy semantics are missing.
5. B1.5-P7 contract deploy-gate required commands updated to include these fail-closed/runtime-proof markers.

Outcome:
- Workflow-level green now requires actual governed deploy prerequisites; missing secret-plane cannot produce a successful no-op.

### R03 - Canonical schema and lifecycle authority sync

Files:
- `db/schema/canonical_schema.sql`

Changes:
1. Added `pg_cron` extension declaration to canonical schema authority surface.
2. Updated `fn_b23_p0_prune_attribution_commerce_identities_trigger()` to `SECURITY DEFINER` in canonical schema.

Outcome:
- Canonical schema reflects lifecycle privilege hardening and scheduler dependency.

## 4. Hypothesis Adjudication (User H01/H02)

### H01
**Conjecture:** remediation avoided real contract/source alignment and only added governance theater.

**Adjudication:** **Refuted on objective current evidence.**

Evidence:
1. Canonical source alignment enforcer (`scripts/ci/enforce_b23_p0_typed_boundary_source_alignment.py`) executes against:
   - OpenAPI sources,
   - mounted FastAPI runtime routes,
   - generated artifacts,
   - contract-consumption gate operation IDs.
2. `B1.5-P3`, `B1.5-P4`, and native source-alignment enforcers all pass on this cycle.
3. Negative controls in tests fail when alignment tokens are intentionally broken.

Conclusion:
- This is not “test acknowledges drift”; the enforcement proves/fails source alignment directly.

### H02
**Conjecture:** preserving the downstream mapping layer guarantees future Pydantic failures/retry loops.

**Adjudication:** **Not supported by current implementation evidence.**

Evidence:
1. Mapping remains typed/deterministic (`B23DownstreamSemanticProjection`, `extra="forbid"`, strict validators).
2. Free-form values fail pre-dispatch (`test_b23_p0_downstream_mapping_payload_requires_pre_dispatch_validation`).
3. Mapping path does not introduce new business semantics beyond enum-token projection.

Conclusion:
- Current layer behaves as a typed adapter, not free-form semantic translation. Risk is controlled under current proofs.

## 5. Falsifiable Local Validation (This Cycle)

Executed from clean worktree `b23p0-corrective-final`:

1. `pytest backend/tests/test_b23_p0_semantic_authority.py -q`  
Result: **PASS (16 passed)**

2. `pytest backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py -q`  
Result: **PASS (21 passed)**

3. `pytest backend/tests/test_b23_p0_typed_boundary_source_alignment_enforcer.py -q`  
Result: **PASS (5 passed)**

4. `pytest backend/tests/test_b15_p7_ci_adjudication_closure_enforcer.py -q`  
Result: **PASS (12 passed)**

5. `python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`  
Result: **PASS**

6. `python scripts/ci/enforce_b23_p0_typed_boundary_source_alignment.py`  
Result: **PASS**

7. `python scripts/ci/enforce_b15_p7_ci_adjudication_closure.py`  
Result: **PASS**

8. `python scripts/ci/enforce_b15_p3_runtime_route_binding.py`  
Result: **PASS**

9. `python scripts/ci/enforce_b15_p4_mock_sdk_boundary.py`  
Result: **PASS**

## 6. Non-Vacuous Break Cases Now Covered

Proof harnesses now fail for:

1. lifecycle mode/schedule/job-name drift,
2. lifecycle activity-independence drift,
3. lifecycle migration proof token removal (`pg_cron` scheduling path),
4. deploy workflow soft-skip regression,
5. trigger-wrapper privilege hardening regression,
6. typed-boundary source alignment regressions,
7. threshold drift,
8. false-authority/B0.6 leakage.

## 7. Files Changed (This Iteration)

- `.github/workflows/schema-deploy-production.yml`
- `alembic/versions/007_skeldir_foundation/202604241815_b23_p0_activity_independent_identity_lifecycle.py`
- `backend/app/revenue_verification/semantic_authority.py`
- `backend/tests/test_b23_p0_semantic_authority.py`
- `backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py`
- `contracts-internal/governance/b15_p7_ci_adjudication_closure.main.json`
- `contracts-internal/governance/b23_p0_semantic_authority_freeze.main.json`
- `db/schema/canonical_schema.sql`
- `docs/forensics/B2.3-P0 Remediation Evidence Pack .md`
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`

## 8. Landing + Main CI Status

Status at time of this evidence update: **In progress in this corrective cycle.**

Pending final closure actions:
1. land branch to `main` via protected workflow,
2. verify required checks green on `main`,
3. record governed production deploy run evidence from the real workflow path.
