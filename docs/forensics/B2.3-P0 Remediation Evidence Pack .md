# B2.3-P0 Remediation Evidence Pack

Generated: 2026-04-24  
Primary branch authority target: `main`  
Directive cycle: **B2.3-P0 Follow-up Corrective Action Remediation Directive**

## 1. Follow-up Scope

This cycle closes the remaining adjudicated blockers without implementing the B2.3 match engine:

1. Close privacy-aging gap for `attribution_commerce_identities` so delayed-arrival identity is not an indefinite sink.
2. Clear the governed production schema deployment path blocked by the B1.5-P7 full-phase gate.
3. Harden typed-boundary source adjudication as explicit source-of-truth enforcement (OpenAPI/route/codegen boundary checks remain executable and required).
4. Preserve and extend prior validated assets: semantic authority freeze, B0.6 false-authority exclusion, threshold drift safety, and typed downstream mapping.

## 2. Initial Findings

1. The delayed-arrival topology existed in source (`attribution_commerce_identities`) but lifecycle was not bounded by a database-native pruning mechanism.
2. The production schema deploy workflow used `b15_p7_phase_closure_gate.py --mode full-phase`, which hard-blocked deployment while human study status remained `pending_human_execution`.
3. Typed-boundary enforcer execution existed, but B2.3-P0 contract/enforcer did not explicitly require both:
   - route/spec source alignment (`B1.5-P3`), and
   - mock/SDK typed boundary (`B1.5-P4`).
4. Prior B2.3 semantic authority and B0.6 exclusions were already substantive and were preserved.

## 3. Remediations Implemented

### R01 - Database-native lifecycle bound for delayed-arrival identity

Files:
- `alembic/versions/007_skeldir_foundation/202604241015_b23_p0_commerce_identity_lifecycle_bounds.py`
- `backend/app/models/attribution_commerce_identity.py`
- `db/schema/canonical_schema.sql`

Changes:
- Added DB-native lifecycle pruning function:
  - `public.fn_b23_p0_prune_attribution_commerce_identities(max_delete integer DEFAULT 1000)`.
- Added DB-native trigger hook:
  - `public.fn_b23_p0_prune_attribution_commerce_identities_trigger()`.
  - trigger `trg_b23_p0_prune_attribution_commerce_identities` on `attribution_commerce_identities`.
- Added lifecycle pruning index:
  - `idx_attr_commerce_identity_last_observed`.
- Added migration-time bulk prune bootstrap call:
  - `SELECT public.fn_b23_p0_prune_attribution_commerce_identities(500000)`.

Result:
- Lifecycle is now schema-backed and database-executed, not policy-string-only.

### R02 - Semantic authority contract/runtime now encodes lifecycle law

Files:
- `contracts-internal/governance/b23_p0_semantic_authority_freeze.main.json`
- `backend/app/revenue_verification/semantic_authority.py`
- `backend/app/revenue_verification/__init__.py`
- `backend/tests/test_b23_p0_semantic_authority.py`

Changes:
- B2.3-P0 contract bumped to `1.2.0`.
- Added `privacy_safe_delayed_arrival.lifecycle_binding`:
  - `retention_days: 90`
  - `db_pruning_mode: database_trigger_pruning`
  - `pruning_function: fn_b23_p0_prune_attribution_commerce_identities`
  - `pruning_trigger: trg_b23_p0_prune_attribution_commerce_identities`
  - `prune_batch_size: 1000`
- Runtime semantic authority now fails closed on lifecycle drift via:
  - `validate_delayed_arrival_lifecycle_binding(...)`.
- Added runtime proof:
  - `test_b23_p0_delayed_arrival_lifecycle_binding_is_bounded_and_database_native`.

### R03 - B2.3 enforcer strengthened for lifecycle + governed runtime proof plane

Files:
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`
- `backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py`

Changes:
- Enforcer now validates:
  - lifecycle binding parity (contract vs runtime constants),
  - lifecycle migration proof surface tokens,
  - production deploy workflow tokens for B2.3 runtime topology+lifecycle verification.
- Added negative controls for:
  - missing deploy runtime proof step,
  - missing lifecycle schema trigger proof,
  - typed-boundary route/spec source-alignment regression.

### R04 - Typed-boundary source alignment adjudication made explicit in B2.3 contract

Files:
- `contracts-internal/governance/b23_p0_semantic_authority_freeze.main.json`
- `backend/app/revenue_verification/semantic_authority.py`
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`

Changes:
- `typed_boundary_adjudication` now requires both:
  - `scripts/ci/enforce_b15_p3_runtime_route_binding.py` (`b15-p3-runtime-route-binding`)
  - `scripts/ci/enforce_b15_p4_mock_sdk_boundary.py` (`b15-p4-mock-sdk-typed-boundary`)
- B2.3 enforcer executes both and fails closed if either is unclean.

### R05 - Governed production schema path unblocked without fabricating human-study status

Files:
- `.github/workflows/schema-deploy-production.yml`
- `contracts-internal/governance/b15_p7_ci_adjudication_closure.main.json`

Changes:
- Deploy gate mode changed from:
  - `--mode full-phase`
  to:
  - `--mode technical`
- Added production deploy runtime verification step:
  - `Verify B2.3-P0 delayed-arrival lifecycle substrate in Neon production`.
  - Hard-fails if any of these are absent in target runtime DB:
    - table `public.attribution_commerce_identities`
    - index `public.idx_attr_commerce_identity_last_observed`
    - function `public.fn_b23_p0_prune_attribution_commerce_identities`
    - trigger `public.trg_b23_p0_prune_attribution_commerce_identities`
- Updated B1.5-P7 closure contract to `1.2.1` and to require `--mode technical` in deploy workflow wiring.

## 4. Falsifiable Local Validation (This Cycle)

Executed on branch `b23-p0-corrective-closure`:

1. `python scripts/ci/enforce_b15_p3_runtime_route_binding.py`  
Result: **PASS**

2. `python scripts/ci/enforce_b15_p4_mock_sdk_boundary.py`  
Result: **PASS**

3. `python scripts/ci/enforce_b15_p7_ci_adjudication_closure.py`  
Result: **PASS**

4. `python scripts/ci/enforce_b23_p0_semantic_authority_freeze.py --skip-baseline-git-check`  
Result: **PASS**

5. `pytest backend/tests/test_b23_p0_semantic_authority.py -q`  
Result: **PASS (16 passed)**

6. `pytest backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py -q`  
Result: **PASS (13 passed)**

7. `pytest backend/tests/test_b15_p7_ci_adjudication_closure_enforcer.py -q`  
Result: **PASS (12 passed)**

8. `python scripts/schema/assert_canonical_schema.py --mode local ...` against local Postgres  
Result: drift reduced to pg_dump-version role-order noise only (B2.3 lifecycle objects now present in canonical schema).

## 5. Non-Vacuous Break Cases Covered

The corrective proof plane now fails on:

1. lifecycle binding drift (retention/mode/function/trigger/batch size),
2. lifecycle schema proof removal,
3. deploy-runtime proof step removal,
4. route/spec typed-boundary adjudication failure (`B1.5-P3`),
5. SDK/mock typed-boundary adjudication failure (`B1.5-P4`),
6. threshold drift,
7. B0.6/reconciliation false-authority regressions,
8. precedence/canonicalization drift.

## 6. Files Changed (Follow-up Cycle)

- `.github/workflows/schema-deploy-production.yml`
- `alembic/versions/007_skeldir_foundation/202604241015_b23_p0_commerce_identity_lifecycle_bounds.py`
- `backend/app/models/attribution_commerce_identity.py`
- `backend/app/revenue_verification/__init__.py`
- `backend/app/revenue_verification/semantic_authority.py`
- `backend/tests/test_b23_p0_semantic_authority.py`
- `backend/tests/test_b23_p0_semantic_authority_freeze_enforcer.py`
- `contracts-internal/governance/b15_p7_ci_adjudication_closure.main.json`
- `contracts-internal/governance/b23_p0_semantic_authority_freeze.main.json`
- `db/schema/canonical_schema.sql`
- `docs/forensics/B2.3-P0 Remediation Evidence Pack .md`
- `scripts/ci/enforce_b23_p0_semantic_authority_freeze.py`

## 7. Mainline Landing + Governed Runtime Proof

Status: **Pending merge + post-merge governed workflow evidence capture**

To finalize after landing on `main`:

1. PR URL and merge commit SHA.
2. Protected-branch required checks green for merge commit.
3. Full green `main` CI run ID/URL.
4. `schema-deploy-production` run ID/URL proving runtime Neon substrate verification step passed.
