# B2.4-P8 Remediation Evidence Pack

## Phase

`B2.4-P8 - Artifact Persistence, Lifecycle, and Storage Governance`

## Initial Findings

P8 began with a real blocker, not a documentation gap:

- `backend/app/bayesian/artifact_repository.py` was P1-era read-only and explicitly stated artifact generation, pruning, and storage lifecycle were out of scope.
- `bayesian_model_fits.artifact_ref` and `artifact_hash` could be populated by `fit_execution.py` without a corresponding governed artifact row.
- The current artifact taxonomy still allowed `posterior_trace`, `object_storage`, `local_fs`, and `zstd`, which conflicts with the corrected P8 directive.
- `bayesian_artifacts` had metadata fields but no stored payload bytes, exact stored-byte hash verification path, lifecycle status, tombstone metadata, tenant quota row, or bounded pruning operation.
- P7 correctly blocked ungoverned full trace persistence, but P8 had not yet supplied the only allowed governed artifact path.

## Remediation Summary

Implemented P8 as a Postgres-native small-artifact governance phase:

- Added `backend/app/bayesian/artifacts.py` with versioned P8 policy, JSON canonicalization, exact byte hashing, hard byte/JSON caps, PII-key rejection, internal ref validation, and bounded gzip decompression via `decompressobj`.
- Replaced the read-only artifact repository with governed persistence, verification, idempotent replay, per-fit WAL budget checks, tenant quota accounting, and bounded pruning with tombstone metadata.
- Added Alembic migration `202606061200_b24_p8_artifact_lifecycle.py` for `payload_bytes`, `payload_json`, `payload_byte_count`, lifecycle fields, `pruned_metadata`, and `bayesian_artifact_storage_quotas` with RLS.
- Tightened current enums and ORM constraints: no `posterior_trace`, no object/local storage backend, no `zstd`, active payloads capped at 64 KiB, and active internal URI refs constrained to `b24://artifact/...`.
- Updated `fit_execution.py` so sampled result summaries persist through `persist_artifact_sync()` before linking `bayesian_model_fits.artifact_ref/hash`.
- Added P8 static and DB proof tests plus `scripts/ci/validate_b24_p8_artifact_lifecycle.py` with negative controls.
- Wired `B2.4-P8 Artifact Lifecycle Proof` into the B2.4 workflow, Makefile target, required-status contract, enforcer registry, subsumption matrix, and CI topology map.

## Scope Preserved

P8 remains internal-only:

- No public FastAPI route, OpenAPI artifact schema, frontend artifact field, TrustEnvelope link, LLM/provider import, cloud SDK, object-store abstraction, PostgreSQL Large Object usage, durable worker-local file path, or deterministic revenue/attribution/match-truth mutation was added.
- Full posterior traces remain rejected by policy rather than stored in bytea.
- P7 diagnostic and interval invariants remain intact.

## Local Validation

Executed on `codex/b24-p8-artifact-lifecycle` after fast-forwarding to `origin/main`.

| Validation | Result |
| --- | --- |
| `python scripts/ci/validate_b24_p8_artifact_lifecycle.py --negative-control` | PASS |
| `python -m pytest backend/tests/test_b24_p8_artifact_lifecycle.py backend/tests/test_b24_p8_postgres_runtime.py -q -rA` | PASS: 4 passed, 1 local DB proof skipped |
| `python -m ruff check ...touched Python files...` | PASS |
| `python scripts/ci/validate_b24_p1_authority_schema.py --negative-control` | PASS |
| `python scripts/ci/validate_b24_p7_diagnostics.py --negative-control` | PASS |
| `python scripts/ci/validate_b24_p6_real_fit_worker.py --negative-control` | PASS |
| `python -m pytest backend/tests/test_b24_p6_real_fit_worker.py -q -rA` | PASS: 5 passed, 2 local DB proofs skipped |
| `python -m pytest backend/tests/test_b24_p1_authority_schema_enforcer.py backend/tests/test_b24_p7_diagnostic_semantics.py backend/tests/test_b24_p8_artifact_lifecycle.py -q` | PASS: 35 passed |
| `python scripts/ci/validate_m3_ci_governance.py --all` | PASS |
| `python scripts/ci/run_ci_governance_cohort.py --cohort b2-4-dry-run --dry-run --summary-path artifacts/b24_gate_dry_run_summary.json` | PASS: 12 gates, 0 failed |
| `git diff --check` | PASS |

Local note: `make` is not installed in this Windows shell, so Makefile targets were validated through the underlying Python commands. The DB-backed P8 proof is mandatory in CI via `SKELDIR_B24_P8_REQUIRE_DB_PROOFS=1`.

## Protected Workflow Evidence

Pending GitHub PR and protected `main` validation.

| Evidence Item | Status |
| --- | --- |
| PR branch | Pending |
| PR number | Pending |
| PR CI run ID proving `B2.4-P8 Artifact Lifecycle Proof` | Pending |
| Protected merge to `main` | Pending |
| Main commit | Pending |
| Main CI run ID proving required checks green | Pending |

## Completion Statement

Local remediation is complete for P8 implementation and falsifiable local validation. Directive-level completion remains pending until GitHub protected-branch PR CI is green, the branch is merged to `main`, and main CI is verified green with run IDs recorded above.
