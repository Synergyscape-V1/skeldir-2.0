# B2.4-P1 Remediation Evidence Pack

## Corrective Finding

B2.4-P1 was implemented but not complete. Two database-authority blockers remained:

- `bayesian_artifacts.fit_id` referenced `bayesian_model_fits(id)` without tenant-bound relational integrity.
- `bayesian_model_fits` uniqueness omitted `source_window_start` and `source_window_end`.

Two guardrails were also missing: machine-enforced Bayesian zero-PII/identity-linkage protection and explicit storage-physics readiness for lifecycle updates.

## Remediation Summary

- Added `uq_bayesian_model_fits_tenant_id_id` to support composite artifact FK authority.
- Replaced artifact fit relationship with `fk_bayesian_artifacts_tenant_fit` on `(tenant_id, fit_id) -> bayesian_model_fits(tenant_id, id)`.
- Replaced hash-only fit identity uniqueness with `uq_bayesian_model_fits_tenant_model_window_snapshot`.
- Set `bayesian_model_fits` storage parameter `fillfactor=90`.
- Recorded pre-P3/P5 partition decision gate in migration comment and canonical YAML.
- Hardened `validate_b24_p1_authority_schema.py` with non-vacuous negative controls for FK, uniqueness, privacy, identity linkage, and fillfactor.
- Extended runtime DB proof to include cross-tenant artifact insertion failure and physical fit identity behavior.

## Evidence Map

| Gate | Evidence |
| --- | --- |
| Tenant-bound referential integrity | Migration `202605201430`, ORM `ForeignKeyConstraint`, canonical SQL/YAML, runtime `pg_constraint` assertion |
| Cross-tenant negative control | `test_b24_p1_artifact_fk_is_tenant_bound` |
| Physical fit identity | `uq_bayesian_model_fits_tenant_model_window_snapshot`; `test_b24_p1_fit_identity_is_unique_over_window_and_hash` |
| Privacy/identity guardrail | `validate_b24_p1_authority_schema.py --negative-control` PII/FK/import negative controls |
| Storage physics | `ALTER TABLE public.bayesian_model_fits SET (fillfactor = 90)`; runtime `pg_class.reloptions` assertion |
| Existing P1 regression guard | B2.4 validator, enforcer tests, runtime DB proof, canonical schema assertion, M5/M6/M7 validators |

## Local Proof Results

```text
python scripts/ci/validate_b24_p1_authority_schema.py --negative-control
PASS: B24_P1_AUTHORITY_SCHEMA_VALIDATION_PASS

python -m pytest -q backend/tests/test_b24_p1_authority_schema_enforcer.py
PASS: 10 passed

python -m pytest -q backend/tests/test_b24_p1_authority_schema_runtime.py -rA
PASS: 6 passed

python scripts/schema/assert_canonical_schema.py --mode local --no-migrate --diff-out artifacts/schema_authority/b24_p1_corrective.diff
PASS: canonical schema matches migrations-derived schema

python -m alembic downgrade 202605201200
python -m alembic upgrade head
PASS on fresh empty migrated schema

python scripts/ci/validate_m5_b24_readiness_design.py
PASS: M5_B24_READINESS_VALIDATION_PASS

python scripts/ci/validate_m6_llm_boundary.py
PASS: M6_LLM_BOUNDARY_VALIDATION_PASS

python scripts/ci/validate_m7_b24_readiness.py
PASS: M7_B24_READINESS_VALIDATION_PASS

python scripts/ci/validate_m4_ops_runbooks.py
PASS: M4_OPS_RUNBOOKS_VALIDATION_PASS
```

PR CI surfaced one unrelated M4 proof-harness defect: the local Stripe tamper helper could produce an unchanged HMAC when the valid digest already started with `0`. The correction makes the tamper deterministic by choosing a different first hex character. This preserves the existing webhook runtime invariant and does not alter B2.4 behavior.

## Scope Boundary

This evidence pack covers P1 corrective closure only. It does not authorize or implement B2.4-P2. No source snapshot computation, fit planner, Bayesian runtime, convergence diagnostic computation, confidence projection, public API route, LLM/provider behavior, or artifact lifecycle job is introduced.

## Protected-Branch Status

Baseline main SHA `cbf9cd28ab45cd77b903afddfc3e42671039b26f` was green before corrective work. The corrective merge SHA, PR number, and final main CI links are verified after protected-branch merge and recorded in the final operational response.
