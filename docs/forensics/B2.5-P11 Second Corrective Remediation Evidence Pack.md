# B2.5-P11 Second Corrective Remediation Evidence Pack

Status: **COMPLETE.** The second corrective implementation merged through protected PR #619. The dedicated P11 workflow is green on both the final PR head and the exact implementation merge SHA. The full main CI result is recorded below.

## 1. Starting main SHA

The isolated implementation worktree was created from clean `origin/main@749743dea615ce8bc1248f8d80b07b3c1178f479`. No divergent primary-worktree state entered the branch.

## 2. Branch and protected pull request

- Implementation branch: `agent/b25-p11-corrective-ii`.
- Protected pull request: [#619](https://github.com/Synergyscape-V1/skeldir-2.0/pull/619).
- Merged at `2026-08-09T23:20:37Z` without administrative bypass.

## 3. Final implementation head

The GitHub implementation head is `5492a3b41a5a2dd2a19ee95cdddc9f8f782677f2`. Its tree is `b75c375a17d4b193de3b0fbb3b72d59fe1305f26`, byte-for-byte equal to the validated local tree.

## 4. Merge SHA and parentage

PR #619 merged as `601e05f0f814c0b59ffe085ee3f05f6669eee1b5`, with parents `749743dea615ce8bc1248f8d80b07b3c1178f479` and `5492a3b41a5a2dd2a19ee95cdddc9f8f782677f2`. Git ancestry verification proves the implementation head is contained by authoritative main.

## 5. Final authoritative implementation main

The implementation closure SHA is `601e05f0f814c0b59ffe085ee3f05f6669eee1b5`. The evidence-only publication that carries this document does not alter export runtime, contract, hash, serializer, database, or authority physics.

## 6. Changed-file inventory

The implementation changes exactly 17 files: the P11 workflow; public and bundled export OpenAPI; export runtime; artifact runtime; three affected test files; CSV evolution policy and two export contracts; hash-domain manifest; generated frontend export types; the P11 validator; the repository error-model auditor; and the M0/M1 allowlists. No migration, P7 schema, P9 schema, financial source table, cache, materialized view, summary table, or reserved subject type was added.

## 7. Hypothesis ledger

| Hypothesis | Verdict | Discriminating evidence | Resolution |
| --- | --- | --- | --- |
| H-P11-C2-01 enriched CSV silently broke positional consumers | confirmed | default runtime and historical baseline had been changed from five to seven columns | preserve five-column default; require explicit v2 opt-in |
| H-P11-C2-02 runtime 503s were absent from authority contracts | confirmed | four handlers could emit two deadline reasons but OpenAPI declared neither | model all four operations and both nested FastAPI wires |
| H-P11-C2-03 signer metadata polluted artifact identity | confirmed | key ID and algorithm were framed into artifact-hash bytes | remove signer metadata from artifact identity |
| H-P11-C2-04 rotation semantics were not falsifiable | confirmed | no same-artifact/two-key metamorphic test existed | add rotation proof and public-only verification |
| H-P11-C2-05 asyncio semaphore bounded coroutines, not physical serializers | confirmed | `to_thread` continued after HTTP timeout | retain task and permit through physical completion |
| H-P11-C2-06 cancellation could release capacity early | confirmed | prior context-manager lifetime ended at coroutine timeout | release only in serializer task done callback |
| H-P11-C2-07 memory evidence measured Python allocations only | confirmed | `tracemalloc` omitted native `openpyxl`/zlib/process memory | sample `psutil.Process().memory_info().rss` |
| H-P11-C2-08 corrected concurrency required fresh maximum proof | confirmed | lifetime repair changes simultaneous physical work | remeasure two real serializers at 1,000 rows |
| H-P11-C2-09 semantic mutation coverage was incomplete | confirmed | token/static controls did not falsify all accepted defects | add seven second-corrective production mutations |
| H-P11-C2-10 historical baseline had been overwritten | confirmed | the versioned baseline contained the new seven-column wire | restore true 2.0.0 five-column evidence |

Additional audit claims were discriminated, not assumed. A generic Track-2 “later refusal” claim was refuted because the complete accepted set is tenant/exact-reference/money preflighted before P5, leaving no reachable deterministic later refusal. A cross-page single-snapshot claim was also refuted: each page is its own signed, time-bound snapshot, and the contract makes no cross-page single-snapshot promise.

## 8. Root-cause adjudication

The defects came from conflating richer schema with compatible wire evolution, coroutine cancellation with thread cancellation, a signer choice with artifact identity, and Python allocation tracing with process working set. The corrective design changes those physical boundaries without inventing financial semantics or widening P7/P9 authority.

## 9. CSV compatibility model

Existing routes default to `legacy-v1` with the exact five-column header `date,channel,revenue,conversions,confidence`. The detached self-identifying seven-column representation is available only through `csv_schema_version=b25-p11-export-csv-v2`.

## 10. CSV wire examples and media identity

Legacy record one remains:

```text
date,channel,revenue,conversions,confidence
```

The explicit v2 profile begins:

```text
projection_authority,projection_schema_version,date,channel,revenue,conversions,confidence
```

V2 returns `text/csv; profile="https://api.skeldir.com/profiles/export-csv-v2"`; every data row repeats `non_authoritative_display` and `b25-p11-export-csv-v2`.

## 11. CSV version and baseline governance

The public API contract moved from 3.1.0 to 4.0.0 and the standalone export contract from 2.1.0 to 3.0.0. `contracts/export/baselines/v1.0.0/export.yaml` is restored to the true 2.0.0 five-column wire. `contracts/export/CSV_EVOLUTION.md` records the compatibility and migration rules.

## 12. Runtime/OpenAPI 503 parity

Revenue, CSV, JSON, and Excel operations all reference one `ExportDeadlineExceeded` response. Its schema exactly admits the two runtime wires:

```json
{"detail":{"status":"refused","reason_code":"legacy_export_database_deadline_exceeded"}}
{"detail":{"status":"refused","reason_code":"legacy_export_handler_deadline_exceeded"}}
```

ASGI tests validate both live responses against the canonical OpenAPI JSON Schema.

## 13. Bundling, generation, and error-model governance

The canonical bundle and frontend types were regenerated and contain the query enum and both 503 reason enums. Because bundling dereferences response components, `x-skeldir-shared-error-component: true` preserves shared-component provenance for the non-RFC7807 FastAPI wire. The repository error-model auditor recognizes only that explicit marker, and the P11 validator requires it. This closes the B0.1 failure observed on the first PR attempt.

## 14. Contract/runtime falsification

Removing any operation’s 503 response, either reason enum, the shared-error provenance marker, or either runtime validation case makes the governed validation chain fail. Both database-deadline and handler-deadline runtime tests pass on PR and exact main.

## 15. Artifact-hash input list

The artifact identity frames only: artifact schema version, canonicalization version, artifact signing domain, generated instant, tenant hash, envelope count, and each ordered envelope’s artifact hash. `signing_key_id`, `signing_algorithm`, detached signature, and signature hash are excluded. The exact list is governed in `contracts/trust-api/hash-domain-manifest.v1.yaml`.

## 16. Signature input list

Detached signature material frames the signature domain, artifact hash, signing key ID, and signing algorithm. `signature_hash` is the P2-governed hash of those exact bytes, and Ed25519 signs those same bytes. This binds the signer and algorithm without changing unsigned artifact identity.

## 17. Key-rotation mutation matrix

| Same unsigned artifact, different active key | Expected and observed |
| --- | --- |
| artifact hash | unchanged |
| signing key ID | changed |
| signature hash | changed |
| signature bytes | changed |
| public verification through old registry entry | succeeds |
| public verification through new registry entry | succeeds |
| substitute key ID without resigning | rejected |

## 18. Public-only verification

The verifier recomputes artifact identity and signature material, resolves the declared key from a public-only registry, verifies every embedded envelope, and verifies the detached signature. No private seed is required or exposed. Wrong-domain, tampered-field, wrong-key, and unsupported-algorithm cases reject.

## 19. Physical serializer lifetime

Track 1 manually acquires the two-slot serializer semaphore, starts a task around `asyncio.to_thread`, awaits it through `asyncio.shield`, and retains the task after timeout or cancellation. A done callback consumes the outcome and releases the permit only after the physical serializer has terminated.

## 20. Physical concurrency proof

Two deliberately blocked serializers time out at the HTTP layer while remaining active in their worker threads. A third request cannot begin physical serialization. Observed peak physical serializers is exactly two.

## 21. Timeout accounting proof

Exact-main metrics are `active_after_http_timeout=2`, `capacity_after_http_timeout=0`, `third_serializer_started_while_saturated=false`, and `capacity_after_physical_completion=2`. Thus timed-out work remains capacity-accounted until it is physically gone.

## 22. RSS measurement method

Track-1 proof uses a process sampler over `psutil.Process().memory_info().rss`; `tracemalloc` is not accepted as the authoritative measurement. Sampling spans two simultaneous maximum CSV+XLSX serializer pairs.

## 23. RSS and artifact-byte evidence

Exact-main Track-1 serialization measured baseline RSS `120,512,512`, peak RSS `121,618,432`, and delta `1,105,920` bytes, below the declared aggregate `67,108,864` bytes. Each 1,000-row CSV was `88,985` bytes and each XLSX `31,709` bytes.

## 24. Cleanup and capacity restoration

The timeout test proves retained tasks are removed after completion, exceptions are consumed, permits return from zero to two, and no third serializer starts while saturation is physical. No orphaned permit or unobserved-task exception remains.

## 25. PostgreSQL scaling and deadline evidence

PostgreSQL 15 exact-main metrics hold the 31-day/1,000-row capability fixed while source rows scale 100→1,000→10,000 and output rows 100→900→992. Execution was 0.527→3.924→26.599 ms; temp blocks remained zero. Production returned 992 rows with `statement_timeout=1250ms`, `work_mem=4096KiB`, parallel gather disabled, and forced cancellation in 51.04 ms.

## 26. Track-2 resource and P7 issuance evidence

Two concurrent maximum requests accept 50 references but evaluate/sign only the two-reference page. Exact-main metrics show 10,848-byte maximum envelope, two 22,386-byte artifacts, 5,373,952-byte RSS delta, 1,223.85 ms maximum latency under the 1,500 ms deadline, and `signature_hash != artifact_hash`. Valid issuance remains routed through the existing P5/P7/P8 chain.

## 27. P9 and accepted-work boundary

P9 authentication, tenant scope, replay, and rate limits are unchanged. The full ≤50 exact-reference set is prefetched and money-preflighted before any page-one issuance. Missing/wrong-tenant-equivalent and non-exportable-money inputs yield one non-disclosing refusal with zero build/audit calls.

## 28. P1–P10 preservation

All P1–P10 validators passed with their prior negative controls intact. Exact-main P11 ran 252 trust tests with PostgreSQL proof flags active and zero skips; the P10 validator still reports all 34 P10 negative controls firing. No upstream ceiling was raised.

## 29. Second-corrective negative-control matrix

| Control | Production mutation | Required failure |
| --- | --- | --- |
| NC-C2-01 | change legacy default columns | CSV compatibility validator fails |
| NC-C2-02 | remove an operation’s 503 response | contract parity validator fails |
| NC-C2-03 | put signer key back into artifact identity | hash-domain validator fails |
| NC-C2-04 | remove key ID from signature binding | rotation/signature validator fails |
| NC-C2-05 | release semaphore at HTTP timeout | physical lifetime validator fails |
| NC-C2-06 | replace process RSS with `tracemalloc` | measurement validator fails |
| NC-C2-07 | mutate frozen historical CSV baseline | baseline validator fails |

## 30. Negative-control totals

Exact-main P11 reports `negative_controls_fired=15`, `corrective_negative_controls_fired=11`, and `second_corrective_negative_controls_fired=7`: 33 production-enforcement mutations, all detected. No prior negative control stopped firing.

## 31. Local and isolated test counts

- Focused P11 unit/runtime set: 30 passed, zero skipped.
- Full local trust suite without PostgreSQL flags: 241 passed, 11 PostgreSQL-gated skips.
- Isolated PostgreSQL 15 P11 suite: 3 passed, zero skipped; the dedicated container was removed afterward.
- Exact-main full trust suite with proof flags: 252 passed, zero skipped.
- Error-model audit: 14 bundled contract files checked, zero issues.
- M0/M1 local governance: 93/93 M0 checks and `M1_STATIC_VALID`.

## 32. Protected-PR aggregate checks

At merge, PR #619 reported 170 contexts: 160 success, six intentional skips, three neutral external statuses, one nonblocking external status without a conclusion, and zero failures. GitHub reported `mergeStateStatus=CLEAN` before the normal merge operation.

## 33. PR-head P11 and full-CI proofs

- [P11 run `31340327473`, job `93312925796`](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/31340327473/job/93312925796): success on exact PR head `5492a3b41a5a2dd2a19ee95cdddc9f8f782677f2`.
- [Full CI run `31340327354`](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/31340327354): success on the same head; 79 terminal jobs, 78 success, one intentional skip, zero failures.

## 34. Exact-main P11 proof

[P11 run `31341657633`, job `93316340082`](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/31341657633/job/93316340082) completed successfully on exact merge SHA `601e05f0f814c0b59ffe085ee3f05f6669eee1b5`. It records the 252-test trust suite, all maximum-resource experiments, P1–P10 preservation, 30 focused P11 tests, all 33 P11 negative controls, and `M3_CI_GOVERNANCE_VALIDATION_PASS`.

## 35. Exact-main full CI proof

[Full CI run `31341657640`](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/31341657640) is the canonical push-triggered complete main matrix on `601e05f0f814c0b59ffe085ee3f05f6669eee1b5`. Final result: **SUCCESS**; 79 terminal jobs, 78 successful and one intentional skip, with zero failures or cancellations.

## 36. Falsifiable exit-gate mapping

Gates A–L are bound respectively to: protected lineage; five-column default/v2 opt-in; exact runtime 503 schema; signer-independent artifact identity; key-bound detached signatures; public rotation verification; retained physical serializer permits; process-RSS proof; fixed PostgreSQL limits/deadlines; preserved P1–P10 controls; 33 firing production mutations; and exact-main P11 plus full-CI success. Every gate has a code test, mutation, measured metric, or authoritative GitHub run above.

## 37. Evidence publication boundary

This document and its `INDEX.md` entry are the only evidence-publication changes. They are published separately after implementation closure so evidence cannot mutate the environment being measured. The implementation main SHA and its run IDs remain the scientific subject.

## 38. Binary verdict

`COMPLETE` — the accepted independent-audit defects are remediated without architecture bleed, authority expansion, or ceiling inflation. The implementation is on protected main; the exact merge SHA has a green dedicated P11 proof and a full green CI matrix; CSV compatibility, 503 parity, artifact/signature identity, rotation, physical concurrency, process RSS, PostgreSQL bounds, prior-phase preservation, and semantic negative controls are all falsifiably demonstrated.
