# B2.5-P13 Corrective Action IX — Remediation Evidence

**Directive**: B2.5-P13 Context-robust, Hypothesis-anchored Follow-Up Corrective Action IX
**Contextual framing**: Independent Audit Report 44 (Nicholas), Independent Audit Report 45 (Trey)
**Branches**: `b25-p13-c9-truth-under-concurrency`, `b25-p13-c9-credential-entropy`,
`b25-p13-c9-ess-bound`
**Pull requests**: [#669](https://github.com/Synergyscape-V1/skeldir-2.0/pull/669),
[#670](https://github.com/Synergyscape-V1/skeldir-2.0/pull/670),
[#671](https://github.com/Synergyscape-V1/skeldir-2.0/pull/671)

**Merged to main**: `2ad6482275e618b211c1899a5186618f2ea9b322` (the remediations),
then `804cf41047f49b527195aa32c1babaecabf8b5ad` and
`a960cf9ca40364250938db1fab65d02f6485e39c`. All enqueued through the
`main-merge-queue` ruleset (ALLGREEN, `enforce_admins: true`) rather than
bypassed with `--admin`.

**Exact-main verification** on `a960cf9ca`: **39 workflow runs, 39 success, 0
failure.** The `CI` workflow alone: 78 jobs success, 1 skipped, 0 failed. Both
P13 contexts green on that lineage
([run 32669375181](https://github.com/Synergyscape-V1/skeldir-2.0/actions/runs/32669375181)):
`B2.5-P13 E2E Trust Closure` and `B2.5-P13 C9 Positive Confidence Composition`.

Counters pinned to literals and confirmed on main:

```
c7_invariant_groups_passed=11
c7_invariant_witnesses=155
c7_static_negative_controls_fired=38
c7_semantics_preserving_controls_held=1
c7_behavioral_obligations_bound=12
```

Behavioural suites executing on main: C6 3 passed, C7 conservation 12 passed,
C8 identity/window 9 passed, C8 live transport 1 passed, **C9 cardinality 10
passed**, **C9 containment 2 passed**, **C9 degradation 4 passed**, **C9
supersession 2 passed**, C8-N contiguous journey 2 passed (309.26s), and **C9
positive composition 2 passed**.

Falsifiers fired on that lineage rather than being declared: NC-C7-B01/B02 each
`1 failed, 11 deselected`, NC-C7-B03 `2 failed, 10 deselected`, NC-C8-B05
`1 failed in 90.96s`, **NC-C9-01** `1 failed, 9 passed`, **NC-C9-06**
`2 failed`, and **NC-C9-08** `1 failed, 1 passed` — each restored green
afterwards.

### Two corrections exact-main CI made to this work

Both are recorded because CI caught them and I did not:

- **A fixture feeding a unique constraint two characters of entropy.**
  `token_prefix` is the first eight characters of a credential token and is
  unique; every P13 fixture built tokens as a six-character literal plus hex,
  leaving 256 possibilities. Several steps run the E2E suite against one
  database, so a collision was a birthday problem rather than bad luck. Entropy
  moved to the front.
- **An overstated mathematical claim of mine.** I asserted that effective sample
  size *cannot exceed* the number of draws. Exact-main measured 65.66 against 64
  draws, because NUTS can sample antithetically. The assertion was corrected
  rather than the observation explained away; F-11's substance is about
  magnitude — 400 against 64 — not a strict bound.

---

## 1. What the audits said, and what was true

Corrective Action VIII connected the chain. Both auditors credited that and both
rejected completion, for the same reason stated two ways: **a chain that
conducts is not yet a chain that stays truthful when reality is messy.**

Three of their findings were already in my own C8 report as open items. One was
not, and it was mine:

> The feature-authority producer derives the source hash and the P2 preflight
> from one PostgreSQL snapshot, and provider and campaign cardinality from a
> later transaction, yet persists all of it under the first snapshot's hash as
> `fresh`.

My C8 report had claimed the opposite in plain words — "the producer recomputes
the source hash inside one repeatable-read transaction and writes only when the
hash it observes is the hash the request named." The hash and the preflight were
in that transaction. Provider and campaign width were not. The sentence was true
about half the row and I wrote it as though it were true about all of it.

The test I shipped alongside it could not catch the difference. It compared the
caller's expected hash against a value the *first* transaction had already
returned, and that comparison is structurally blind to anything happening after
it. A guard that runs before the gap cannot see into the gap.

---

## 2. Findings

| ID | Finding | Source | Status |
|----|---------|--------|--------|
| **F-08** | Feature authority measured across **two** transactions. A commit landing between them changed provider/campaign width without changing the hash, so a row could be written `fresh` describing a snapshot that never existed. | Audit 44 §6 | **closed** |
| **F-05b** | The planner's own governed degradation write was refused by the C5 dispatch fence — a Postgres trigger raising into a Python call stack with no handler for it. | C8 self-disclosed; Audit 44 §11 | **closed** |
| **F-07** | The planner leased a tenant batch and iterated it with no `except`, so one tenant's exception left every tenant behind it leased by an owner that had stopped running. | C8 self-disclosed; both audits | **closed** |
| **F-10** | Obsolete feature-authority requests retried every 60 seconds forever. "Not yet" and "never again" were the same state. | Audit 44 §7 | **closed** |
| **F-09** | *Discovered building C9-J.* The fit-claim granted every fit `max_samples = 0`. Every fit the planner has ever claimed was refused `policy_rejected` before compute started. | **new** | **closed** |
| **F-11** | *Discovered building C9-J.* The sampler runs one chain — its validator *requires* it — so R-hat is undefined; and it draws 64 samples against an ESS threshold of 400. Either arm alone makes acceptance impossible. No fit this system can produce can pass its own diagnostics. | **new** | **open, pinned** |

---

## 3. Remediations

### F-08 — one snapshot, five measurements

The producer now measures the hash, the P2 preflight (which supplies channel and
currency width) and both walked dimensions **inside one
`REPEATABLE READ READ ONLY` transaction**. `compute_source_snapshot_hash` gained
one optional parameter, `within_snapshot`, which runs an additional measurement
inside the transaction it already holds and carries the result back on the
result object. Every existing caller is unaffected.

The point is not that the producer now *detects* a mid-flight mutation. It is
that under one snapshot there is nothing to detect: a commit made after the
transaction begins is not visible to any read within it, so the five
measurements agree because they are one observation.

**The proof stands in the old defect's window and pushes.** A real settlement —
introducing a provider and a campaign that exist in no earlier state — is
committed from a *separate connection* at exactly the moment the old code was
between transactions. The measurement must not see it.

Reintroducing the split makes it fail with the hybrid row itself:

```
provider_count            2  (S1)   expected 1  (S0)
campaign_or_feature_count 7  (S1)   expected 6  (S0)
hash                      S0
```

That is the row Audit 44 described, produced on demand.

### F-05b — a narrow door for planner-owned degradation

The dispatch fence has always admitted two planner-authored states without
dispatch authority — `queued` and `pending` — because scheduling a fit is not
the same act as producing one. Declaring that a fit *cannot* happen belongs to
the same category and was simply missing from the list.

The state is added narrowly. It is admitted only for a row carrying **no
computational consequence whatsoever**: no artifact, no artifact hash, no
confidence evidence hash, no sampling/fit/completion timestamps, no runtime, no
sample or chain count, no r-hat, no ESS, no divergences, no credible interval,
and a confidence bucket that is explicitly `unavailable`.

Three forgeries were attempted directly against the database and all three were
refused:

| Forged row | Result |
|-----------|--------|
| `fallback_only` carrying an artifact | `b24_dispatch_fence_rejected` |
| `fallback_only` carrying `available` confidence | `b24_dispatch_fence_rejected` |
| `fallback_only` carrying sampling evidence | `b24_dispatch_fence_rejected` |
| the planner's genuine no-compute declaration | persisted, 0 dispatch rows |

The forgery boundary underneath the trigger is the table grant, and it is
stronger than the trigger: `app_user` — the API runtime role — holds **SELECT
only** on `bayesian_model_fits`. Nothing reachable from an HTTP request can write
a fit row of any shape.

### F-07 — failure isolation without losing the failure

Containment is per-tenant, and the disposal in `finally` still runs, so C7's
residual-authority logic decides what happens to the failed obligation rather
than this loop discarding it. `Exception` and not `BaseException`, deliberately:
a hard worker time limit is not a per-tenant fault and must keep propagating.
What was contained is reported in the task result — `failed_tenant_count` and
`failure_classes` — because a planner that silently absorbed exceptions would
trade one invisible defect for another.

Two properties are proven, and the second does not follow from the first:

- **containment** — a failing tenant does not prevent its batch-mates from being
  planned in the same pass;
- **bounded progress** — a tenant that fails on *every* pass, seeded oldest so it
  is leased first every time, still does not prevent healthy tenants from
  progressing across four consecutive cycles.

### F-10 — impossible work stops being work

A request naming a snapshot that has been superseded is not unavailable, it is
historically impossible, and no horizon resolves it. The bounded-retry columns
for exactly this were already on the table and never consulted — `retry_count`,
`max_retries`, `terminal_reason`, `terminal_at` — and the dirty-event vocabulary
already contained `authority_retry_superseded`. The pieces were present and
unconnected, which is what Audit 44 predicted.

Past its own retry horizon the request terminates as `authority_superseded`, the
schedule is cleared, and the dirty evidence waiting on it is released. A request
naming the snapshot that *does* exist still completes on its first cycle and is
charged no retry — otherwise this would have traded infinite retry for silent
starvation.

### F-09 — every fit was granted a budget of zero samples

The fit-claim INSERT hard-coded its execution budget: `max_runtime_seconds 60`,
`max_samples 0`, `max_cores 1`. The sampler refuses any fit whose sample budget
is below what the sampling policy intends to draw, and zero is below every
policy there could be.

**So every fit the planner has ever claimed was structurally unexecutable.** The
worker authenticated, took its lease, loaded the row, and refused it
`policy_rejected` before compute started.

Nothing caught it because the only proof that a fit can execute writes its own
fit row, with its own budget, and never asks the planner for one — the same
fixture substitution that hid F-06, one layer further down. The budget is now
read from the policy that spends it.

---

## 4. F-11 — the wall, and why I did not climb over it

Building C9-J revealed something neither audit found and no component is
individually wrong about. It has two arms, and each is independently sufficient.

**Arm one: R-hat cannot exist.** The B2.4-P6 sampler runs inside a
single-process cage — `chains = 1` — and its own validator *requires* it:

```python
if self.blas_cores != 1 or self.cores != 1 or self.chains != 1:
    raise ValueError("P6 preserves P5 single-process runtime cage")
```

R-hat compares variance *between* chains. With one chain there is nothing to
compare, so it is NaN, and the diagnostics reject the fit as
`nonfinite_diagnostic` before sample size is considered at all.

**Arm two: ESS cannot reach its threshold.**

```
b24-p6-sampling-policy-v1     draws 64 x chains 1  =  64 posterior samples
b24-p7 diagnostic policy      requires effective sample size >= 400
```

Effective sample size tracks the number of draws: usually below it, and modestly
above it when NUTS samples antithetically. The observed run measured **65.7** and
an earlier assertion of mine that ESS "cannot exceed" the draw count was too
strong — exact-main CI caught it at 65.66 against 64 draws, and the assertion was
corrected rather than the observation explained away. The substantive point is
unchanged and is about magnitude, not a strict bound: a threshold of 400 against
64 draws is a 6.25x gap that no sampler quality closes. Only drawing more samples
would.

**Observed on the real journey**, which is how this was found rather than
reasoned:

```
status                            succeeded
n_samples_actual                  64
ess_min                           68.68
r_hat_max                         None
diagnostic_status                 failed
diagnostic_failure_reason         nonfinite_diagnostic
confidence_bucket                 unavailable
artifact_ref                      b24://artifact/.../diagnostics/c7faa625af8d
confidence_evidence_snapshot_hash == source_snapshot_hash
```

A real posterior, real retained diagnostic evidence, a real measured ESS — and
no R-hat, because there cannot be one.

**No fit this system can produce is capable of passing its own diagnostics**,
whatever the data. Every real sampled fit is truthfully reported `nonconverged`,
and a usable confidence is unreachable through the production sampling path.

This is why no proof in this repository has ever shown an available confidence
from real sampling. The B2.4-P6 proof samples successfully and asserts nothing
about diagnostics; every other positive-confidence test writes the confidence it
then reads. Three governed, separately versioned policies — the P5 runtime cage,
the P6 sampling policy and the P7 diagnostic policy — that have never been asked
to hold at the same time.

I did not resolve it, and the reason is the one I got wrong in C8 and was
corrected on: **this is a modelling decision with an owner.** Lifting the
single-chain cage changes the P5 process-isolation guarantee. Raising the draw
count spends compute against a 30-second soft limit. Lowering the ESS threshold
weakens a published convergence criterion. The directive is explicit that
governed thresholds may not be moved to make this journey pass, and picking a
number here to turn a proof green would be exactly that.

It is pinned rather than described.
`test_c9_the_sampling_policy_cannot_satisfy_its_own_diagnostics` asserts **both
arms** and goes red the moment either lifts, naming the journey that must then be
extended. The journey itself asserts `r_hat_max is None` and
`ess_min <= 64 < 400` for the same reason — if either changes, the proof says so
rather than silently passing.

## 5. C9-J — what the journey does prove

The journey writes **no feature authority, no fit, and no dispatch claim**. One
settlement run, and at each stage exactly what the previous stage produced:

```
real settlements committed
  -> production invalidation trigger writes the dirty evidence
    -> production planner judges it fittable and asks for an authority
      -> production cardinality producer measures the exact snapshot
        -> production planner claims the fit and mints a dispatch lease
          -> production relay leases that row
            -> production worker executes its payload
              -> a real posterior, real diagnostics, persisted confidence
                -> production Trust route, signed and JWKS-verified
```

Possession of a dispatch id is **not** authority to execute, and the system said
so: reading the outbox row directly and executing it returns `UNAUTHORIZED` from
the fence. That refusal is how F-09 was found. The row is leased through the
production relay, and the only bridge is the broker hop — proven separately by
the live transport proof.

**C9-J is recorded PARTIAL.** Composition and correspondence are proven: the
chain reaches a real posterior with no test-written authority at any seam,
diagnostics are genuinely computed on it, and Trust reports exactly the verdict
the pipeline persisted. The *available-confidence branch* is **BLOCKED by F-11**
and no completion claim rests on it.

---

## 6. Proof surface

| Suite | Tests | Establishes |
|-------|-------|-------------|
| `c9_feature_cardinality_physics` | 10 | Exact-snapshot measurement, including the deliberate MVCC race and its persisted counterpart |
| `c9_tenant_containment` | 2 | Containment, and bounded progress across four failure cycles |
| `c9_degradation_matrix` | 4 | Three negative states through the real planner to a signed envelope |
| `c9_authority_supersession` | 2 | Obsolete terminates; current still completes, charged no retry |
| `c9_positive_confidence` | 2 | Real-posterior composition; F-11 pinned in arithmetic |

Behavioural falsifiers wired into CI:

| Control | Reintroduces |
|---------|--------------|
| **NC-C9-01** | Splitting the feature-authority measurement back across two transactions |
| **NC-C9-06** | Removing the per-tenant containment boundary |
| **NC-C9-08** | Substituting a test-created feature authority at the C9-J seam |

Every remediation was verified to fail before it was fixed — the split
measurement, the fence rejection, the batch abort, and the zero-sample budget
were each reproduced on demand.

---

## 7. Honest accounting

Audit 44 §18 found an internal contradiction in the C8 report: the header said
38 fired static negative controls and §4.3 said 28. CI confirmed 38. The prose
was wrong by ten — I updated the pin when the count changed and did not update
the narrative. That is a documentation-integrity defect of exactly the kind the
directive warns about, and it is corrected here rather than explained away.

C9 counters, taken from the gate's own output:

```
c7_invariant_groups_passed=11
c7_invariant_witnesses=155
c7_static_negative_controls_fired=38
c7_semantics_preserving_controls_held=1
c7_behavioral_obligations_bound=12
```

Distinguished as the directive requires: **expected** cases are what the workflow
pins; **observed passed** is what the suite reports; a test that begins and fails
before its invariant is not counted as a behavioural witness anywhere in this
document.

Also noted, from Audit 44 §9: the `ALLOWED_SOURCE_READ_MODELS` duplication the
auditor could not find a gate for **is** gated —
`validate_source_invalidation_contract` compares `input_contract`'s literal
against `source_contract_authority.allowed_source_read_models()` and fails on
divergence. The auditor was right that it looked unguarded and right to say so.

---

## 8. What is not claimed

- **C9-J's available-confidence branch is not proven, and cannot be until F-11
  is resolved.** Recorded PARTIAL.
- **F-11 is open.** It is a live incompatibility between two governed policies,
  not a latent one: it affects every fit the system produces today.
- The historical-freshness transition (C9-L) at the *envelope* level, and the
  duplicate-delivery idempotency injections (C9-D), are exercised at the database
  tier by existing C8 proofs but are not assembled as end-to-end journeys here.
