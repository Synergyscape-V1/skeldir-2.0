# B1.2-P5 GC Singleflight Baseline

## Scope
- Directive: Follow-up corrective action for denylist GC fleet singleflight.
- Authority baseline: `main` state at merge `cffdb530bff42a39c1ede733cba429086d7b7e4a`.

## 1) GC entrypoint and invocation mechanism
- Task entrypoint:
  - `backend/app/tasks/maintenance.py`
  - `gc_expired_access_token_denylist(...)`
- Scheduler registration:
  - `backend/app/tasks/beat_schedule.py`
  - key: `auth-denylist-gc`
  - task: `app.tasks.maintenance.gc_expired_access_token_denylist`
  - cadence: `crontab(minute="*/10")`

## 2) Current delete SQL and batch semantics
- Delete implementation path:
  - `backend/app/tasks/maintenance.py`
  - `_delete_expired_denylist_rows(batch_size)`
- Batched path used:
  - tenant loop over `public.tenants`
  - CTE `doomed` selects expired rows ordered by `expires_at` with `LIMIT :batch_size`
  - delete joins back on `(tenant_id, user_id, jti)`
- Mutation path:
  - `SKELDIR_B12_P5_GC_UNBOUNDED_DELETE=1` switches to unbounded tenant delete (negative control).

## 3) Lock inventory proof (pre-corrective)
- Search targets:
  - `pg_try_advisory_xact_lock`
  - `advisory_lock`
  - `FOR UPDATE SKIP LOCKED`
- Baseline verdict:
  - no fleet singleflight primitive found in GC path (`_delete_expired_denylist_rows`).
  - no lease-row locking strategy found.

## 4) Existing CI tests/negative controls touching GC (pre-corrective)
- Positive tests present:
  - `backend/tests/test_b12_p5_revocation_substrate.py::test_denylist_gc_is_bounded_and_makes_progress`
  - `backend/tests/test_b12_p5_revocation_substrate.py::test_denylist_gc_job_is_registered_in_beat_schedule`
- Negative controls present:
  - `scripts/contracts/run_negative_controls.sh` had:
    - `SKELDIR_B12_P5_GC_UNBOUNDED_DELETE=1`
    - `SKELDIR_B12_P5_DISABLE_DENYLIST_GC_JOB=1`
- Missing before this corrective:
  - no deterministic two-invocation concurrency proof
  - no `SKELDIR_B12_P5_DISABLE_GC_SINGLEFLIGHT` non-vacuous control.
