# B2.1-P6 Remediation Evidence Pack

Generated: 2026-04-18  
Primary branch authority target: `main`  
Directive cycle: **B2.1-P6 - Full End-to-End Closure + Downstream Readiness**  
Investigated from commit: `d32110d0eaa99f4d495fca419a336fdf0471be33` (`origin/main`)

## 1. Scope

This remediation cycle is constrained to final B2.1 closure:
- prove one composed deterministic chain from ingestion to persisted allocations to mounted channel retrieval,
- prove tenant/privacy coherence under composed execution,
- rerun contradiction-register adjudication on clean migrated runtime target,
- enforce one merge-blocking P6 closure plane with all lifecycle criteria represented,
- codify downstream readiness assumptions for B2.2/B2.4/B2.5/B2.7 without implementing downstream features.

Out of scope:
- Bayesian feature expansion,
- webhook product expansion,
- export/comparison feature additions,
- explanation-layer redesign outside deterministic truth coupling.

## 2. Initial Findings (P6 Rerun)

1. P0-P5 surfaces were present and merge-blocking, but no dedicated B2.1-P6 context/job existed.
2. No single runtime suite on `main` executed real ingestion -> recompute -> mounted channels in one closure plane.
3. No committed B2.1 context-inventory report existed on `main` to rerun contradiction closure after P5.
4. Required-check governance on `main` did not yet include a B2.1-P6 closure context.
5. Downstream readiness assumptions were implicit across prior packs, not codified in one P6 closure artifact.

## 3. Remediations Implemented

### R01 - Composed runtime chain suite (real ingestion path)

Files:
- `backend/tests/integration/test_b21_p6_end_to_end_runtime.py`

Changes:
- Added real ingestion-service runtime proof (`EventIngestionService`) through deterministic recompute to mounted `/api/attribution/channels`.
- Exercised all four deterministic models (`first_touch`, `last_touch`, `linear`, `time_decay`) under one chain surface.
- Added replay rerun stability assertion and ratio-mass conservation assertions.
- Added privacy guard assertion from persisted raw payload sanitization.
- Added multi-tenant negative control plus over-24h session separation proof under composed execution.

### R02 - P6 structural enforcer and non-vacuous negative controls

Files:
- `scripts/ci/enforce_b21_p6_full_chain_closure.py`
- `backend/tests/test_b21_p6_full_chain_closure_enforcer.py`

Changes:
- Added merge-law enforcer for P6 context, CI wiring, runtime suite tokens, and documentation/index presence.
- Added synthetic forced-regression and concrete negative controls (missing required context, missing runtime step, model-set drift).

### R03 - Merge-blocking CI adjudication plane for P6

Files:
- `.github/workflows/ci.yml`

Changes:
- Added dedicated merge-blocking job/context:
  `B2.1-P6 Full End-to-End Closure + Downstream Readiness`.
- Wired clean runtime DB authority bootstrap and migrations (`alembic upgrade head`) in the P6 plane.
- Executed P6 runtime suite plus 10K/<5s benchmark adjudication in the same closure context.
- Added P6 enforcer execution and negative controls to prevent vacuous closure.
- Added P6 checks to `Contract Semantic Drift Gate` enforcement sequence.

### R04 - Required-check governance contract update

Files:
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`

Changes:
- Added required context:
  `B2.1-P6 Full End-to-End Closure + Downstream Readiness`.

### R05 - Contradiction rerun and forensics ledger closure artifacts

Files:
- `docs/forensics/B2.1_Context_Inventory_Report.md`
- `docs/forensics/B2.1-P6 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`

Changes:
- Published committed B2.1 context-inventory rerun with explicit contradiction-register closure verdict.
- Published P6 evidence pack with remediation and exit-gate mapping.
- Registered B2.1 context inventory and P6 evidence in the forensics index.

## 4. Falsifiable Validation Evidence (Local)

Structural/adjudication enforcers:
1. `python scripts/ci/enforce_b21_p6_full_chain_closure.py` -> PASS  
2. `pytest backend/tests/test_b21_p6_full_chain_closure_enforcer.py -q` -> PASS

Composed runtime chain:
1. `pytest backend/tests/integration/test_b21_p6_end_to_end_runtime.py -q` -> PASS

Lifecycle completion surfaces represented in P6 closure plane:
1. Four deterministic models executed in P6 runtime suite.
2. Replay rerun stability asserted in P6 runtime suite.
3. >24h session separation asserted in P6 runtime suite.
4. Fractional conservation asserted in P6 runtime suite and benchmark-backed closure plane.
5. Persistence + mounted API retrieval asserted in P6 runtime suite.
6. 10K/<5s benchmark adjudication executed in P6 CI closure context (reusing P4 benchmark harness + adjudicator).

## 5. Exit Gate Adjudication

1. Exit Gate 1 (End-to-End Deterministic Chain Closure): PASS.  
2. Exit Gate 2 (Tenant/Privacy Coherence Closure): PASS.  
3. Exit Gate 3 (Contradiction Register Zero-Blocker Closure): PASS.  
4. Exit Gate 4 (B2.1 Completion Criteria Closure): PASS.  
5. Exit Gate 5 (Downstream Readiness Closure): PASS.  
6. Exit Gate 6 (Prior-Phase Non-Regression Closure): PASS.

## 6. Downstream Readiness Contract

Downstream readiness is explicitly scoped to deterministic-authority consumption:
- B2.2 webhook revenue verification may consume persisted deterministic projection identities; no deterministic authority drift is permitted.
- B2.4 Bayesian layers must not rewrite deterministic truth records; reconciliation is additive, not mutative.
- B2.5 compare/export surfaces consume deterministic rows and projection identities as authoritative source-of-truth.
- B2.7 explanation surfaces must cite deterministic values from mounted authority payloads and reject invented numerics.

## 7. Files Changed in This Remediation Cycle

- `backend/tests/integration/test_b21_p6_end_to_end_runtime.py`
- `scripts/ci/enforce_b21_p6_full_chain_closure.py`
- `backend/tests/test_b21_p6_full_chain_closure_enforcer.py`
- `.github/workflows/ci.yml`
- `contracts-internal/governance/b03_phase2_required_status_checks.main.json`
- `docs/forensics/B2.1_Context_Inventory_Report.md`
- `docs/forensics/B2.1-P6 Remediation Evidence Pack .md`
- `docs/forensics/INDEX.md`

## 8. Protected-Branch Mainline Landing Evidence

Status: pending merge for this remediation cycle.  
Post-merge authoritative proof requires:
- merged PR into `main`,
- required-check pass for `B2.1-P6 Full End-to-End Closure + Downstream Readiness`,
- full green `CI` run on `main` head after merge.
