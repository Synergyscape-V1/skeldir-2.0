# B2.5-P11 Remediation Evidence Pack

Corrective status (2026-08-10): the original P11 closure below is superseded for
final acceptance by the corrective lineage, in order:

1. [B2.5-P11 Corrective Remediation Evidence Pack.md](B2.5-P11%20Corrective%20Remediation%20Evidence%20Pack.md)
   — artifact-level `signature_hash`, detached CSV authority, Track-1 database
   and serialization physics, concurrency, P7 preservation, mixed-reference
   lifecycle.
2. [B2.5-P11 Second Corrective Remediation Evidence Pack.md](B2.5-P11%20Second%20Corrective%20Remediation%20Evidence%20Pack.md)
   — positional CSV wire restoration, 503 contract modelling, artifact/signature
   domain separation, current-protocol key rotation, process-RSS measurement.
3. **[B2.5-P11 Third Corrective Remediation Evidence Pack.md](B2.5-P11%20Third%20Corrective%20Remediation%20Evidence%20Pack.md)
   — the authoritative record for final acceptance.** It closes the three
   blocking findings of the 26th independent audit: the default CSV surface
   could not identify itself as non-authoritative once detached (P11-G4);
   one `v1` artifact marker denoted two incompatible hash algorithms, so a
   legitimately signed pre-corrective artifact failed as indistinguishable
   `artifact_hash_mismatch`; and the repository-wide error-model validator
   accepted any 4xx/5xx response that asserted
   `x-skeldir-shared-error-component: true`. It also closes the two
   non-blocking forward obligations (413 contract parity and the
   `/api/export/csv` description contradiction).

Read the third corrective pack for the current phase verdict. The status line
immediately below describes the original closure only and is retained for
historical lineage, not as the present adjudication.

Status: COMPLETE. The implementation merged through protected `main`, the exact merge SHA passed the dedicated P11 workflow and the full main CI graph, and all fixed ceilings and negative controls remained intact.

Forensic phase-isolation finding: the independent audit correctly found no P11 footprint on protected `main`. GitHub PR file inventories for #612, #613, and #614 contain only P10 work, and the P11 working tree starts at the final P10 merge `ec0e27d8d` on a separate `agent/b25-p11-*` branch. No P10 history was rewritten or contaminated. The audit prompt's contrary characterization of P11 artifacts inside a P10 PR was rejected by direct GitHub evidence.

## 1. Starting and final main SHA

- Starting authority: `origin/main@ec0e27d8d179d381da94b7aab92961f0d6c3e22c`.
- Clean-room branch: `agent/b25-p11-export-compatibility-projection`.
- Implementation head: `6f005ca9d542a57ed08d8f112c01026a313a23f5`.
- Protected-branch implementation merge SHA: `b7385e04e0c66add56ffd36eabf86ec92cdd3429` (PR #615).
- Authoritative implementation `origin/main`: `b7385e04e0c66add56ffd36eabf86ec92cdd3429` at closure validation.
- Ancestry proof (`git merge-base --is-ancestor 6f005ca9d542a57ed08d8f112c01026a313a23f5 origin/main`): PASS (exit 0).

## 2. Route inventory

| Route | Caller | Authority | Output |
| --- | --- | --- | --- |
| `GET /api/export/json` | human `viewer` | non-authoritative display | closed JSON projection |
| `GET /api/export/csv` | human `viewer` | non-authoritative display | RFC 4180 CSV |
| `GET /api/export/revenue?format=xlsx` | human `viewer` | non-authoritative display | real XLSX workbook |
| `GET /api/export/excel` | human `viewer` | non-authoritative display | real XLSX workbook |
| `POST /api/trust/v1/exports/match-verdicts` | P9 machine identity with `trust.export.create_limited` | signed machine artifact | full P8-signed `match_verdict` envelopes |

The human routes remain Track 1 display projections. The machine route is Track 2 and does not reach the legacy attribution aggregation path.

## 3. Field-to-authority matrix

| Field | Source / transformation | Authoritative | Envelope source | Text | Money | Tenant | Signature | Artifact hash |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `artifact_schema_version` | P11 constant | yes | artifact | trusted enum | n/a | none | yes | yes |
| `canonicalization_version` | P11 constant | yes | artifact | trusted enum | n/a | none | yes | yes |
| `artifact_signing_domain` | P11 constant | yes | artifact | trusted enum | n/a | none | yes | yes |
| `envelopes[]` | full P5/P7/P8 signed envelopes | yes | `match_verdict` | disposition-controlled | envelope minor units | envelope hash only | each envelope and artifact | yes |
| `artifact_hash` | `compute_artifact_hash(framed_identity_bytes)` | yes | artifact | lowercase hex | n/a | none | signature input | self-excluded identity digest |
| `signature` | P8 active Ed25519 key | yes | artifact | base64 | n/a | none | self | no |
| `signing_key_id` | P8 active-key registry | yes | artifact | trusted identifier | n/a | none | bound | yes |
| `signing_algorithm` | P8 key metadata | yes | artifact | `Ed25519` | n/a | none | bound | yes |
| `generated_at` | UTC issuance instant | yes, artifact-only | artifact | RFC 3339 | n/a | none | yes | yes |
| `tenant_id_hash` | P2 tenant hash | yes | request tenant | lowercase hash | n/a | hashed only | yes | yes |
| `projection_authority` | P11 display constant | no | none | trusted enum | n/a | none | no | no |
| `projection_schema_version` | P11 display constant | no | none | trusted enum | n/a | none | no | no |
| `date_range.start/end` | admitted request dates | no | none | ISO date | n/a | none | no | no |
| `rows[].date` | legacy query; projection ordered and neutralized by format | no | none | display-only | n/a | none | no | no |
| `rows[].channel` | legacy query; projection ordered and neutralized by format | no | none | display-only | n/a | none | no | no |
| `rows[].revenue_minor` | legacy aggregate converted to integer minor units | no | none | n/a | integer minor | none | no | no |
| `rows[].revenue_display` | `Decimal(revenue_minor) / 100`, fixed string | no | none | display-only | string rendering | none | no | no |
| `rows[].conversions` | legacy aggregate integer | no | none | n/a | non-money integer | none | no | no |
| `rows[].confidence_display` | decimal/string display rendering | no | none | display-only | non-money string | none | no | no |

## 4. H-P11-01 through H-P11-22 adjudication

| H | Working-tree verdict | Falsifiable evidence / closure |
| --- | --- | --- |
| 01 | CONFIRMED | Legacy `_fetch_reporting_rows` aggregates; Track 2 call graph has no aggregate SQL. NC-01 covers regression. |
| 02 | CONFIRMED | Legacy rows are channel/day summaries while P5 supports `match_verdict`; Architecture A separates the tracks. |
| 03 | CONFIRMED | Legacy raw tenant output was removed; schemas and recursive guards require only `tenant_id_hash`. NC-02. |
| 04 | CONFIRMED | Legacy floats were removed; integer minor units and display strings are closed by schema/runtime. NC-03. |
| 05 | CONFIRMED | Human output was unsigned; Track 2 embeds signed envelopes and adds artifact signature/hash. |
| 06 | CONFIRMED (latent) | No prior export identity existed; P11 exclusively reuses `compute_artifact_hash(bytes)`. |
| 07 | CONFIRMED (latent) | P11 adds a distinct export signing domain and reuses only the P8 active key registry. |
| 08 | CONFIRMED (latent) | Artifact embeds complete signed envelopes; no reference-only semantics were invented. NC-04. |
| 09 | CONFIRMED | Every display response requires `projection_authority=non_authoritative_display`. NC-08. |
| 10 | CONFIRMED | Both mock XLSX bodies were replaced by `openpyxl` workbooks. NC-09. |
| 11 | CONFIRMED | Manual CSV joining was replaced with `csv.writer`, CRLF and `QUOTE_MINIMAL`. NC-10. |
| 12 | CONFIRMED (latent) | Spreadsheet injection was not a P3 class; isolated display-only neutralization was added. NC-11. |
| 13 | CONFIRMED | Missing date/row/byte limits are now exact admission constraints. NC-12. |
| 14 | CONFIRMED | Fetch-all was replaced by SQL `LIMIT n+1` and bounded `fetchmany`. NC-13. |
| 15 | CONFIRMED (forward) | Semantic envelope hashes remain stable across issuance times; artifact hashes bind `generated_at`. |
| 16 | REJECTED (SQL) / CONFIRMED (projection) | SQL already ordered; projection independently sorts and shuffled fixtures prove DB-order independence. |
| 17 | CONFIRMED | Track 1 retains `viewer`; Track 2 requires P9 machine scope `trust.export.create_limited`. |
| 18 | CONFIRMED | Track 2 builds with `access_log_only=False`, producing issuance audit authority. |
| 19 | REJECTED (current) / PRESERVED | Legacy export was SELECT-only; snapshot proof permits only trust access/issuance audit writes. |
| 20 | CONFIRMED | All four export contract copies now agree with the runtime display schema. |
| 21 | CONFIRMED (risk) | `attribution_result` has no adapter and is rejected, never activated. NC-15. |
| 22 | CONFIRMED | P11 adds a dedicated non-vacuous validator and production negative controls. |

## 5. Validated root causes RC-01 through RC-08

- RC-01: the legacy export SQL independently computed financial summaries, creating parallel truth.
- RC-02: the requested `attribution_result` subject had no P5 authority adapter.
- RC-03: runtime/contracts exposed raw tenant identity instead of the P2 hash boundary.
- RC-04: float calculation conflated machine money with human display.
- RC-05: export artifacts lacked canonical identity, lineage, a signature, and a distinct domain.
- RC-06: handwritten CSV, fake XLSX, and unneutralized cells made format claims unsafe.
- RC-07: unbounded date spans, fetch-all, and post-query limiting made resource use attacker-controlled.
- RC-08: machine authorization, route governance, and phase preservation gates did not cover exports.

## 6. Chosen authority model per format

Architecture A is implemented. JSON, CSV and XLSX human routes are explicitly `non_authoritative_display`. Only the machine `match_verdict` export contains authoritative truth, and that truth is the embedded P5/P7/P8 envelope rather than a recomputation.

## 7. Supported subject coverage and attribution gap

`SUPPORTED_EXPORT_SUBJECT_TYPES == {"match_verdict"}`. `attribution_result` remains reserved and receives a typed refusal. Architectures B and C were prohibited because manufacturing an adapter or treating legacy aggregate output as signed truth would widen the authority surface.

## 8. Static call graph

`trust_export.create_match_verdict_export_artifact` -> bounded body -> P9 authentication/tenant checks -> subject admission -> bounded source lookup (`LIMIT n+1`) -> `build_unsigned_trust_envelope_with_audit(access_log_only=False)` -> `sign_trust_envelope` -> `build_export_artifact` -> `sign_export_artifact`. Neither the route nor artifact module calls aggregate SQL, attribution recomputation, LLM/provider code, Celery, or Bayesian code.

## 9. Final schemas

- Machine artifact: `contracts/trust-api/export-artifact.v1.yaml`, closed with `additionalProperties: false`.
- Display projection: `contracts/trust-api/export-projection.schema.json`, closed at the top level and row level.
- Machine route: `contracts/trust-api/trust-api.openapi.yaml` operation `createMatchVerdictExportArtifact`.
- Human contract copies: canonical, baseline, API source, and bundled distribution updated coherently.

## 10. Privacy reconciliation

Raw tenant UUIDs are excluded from JSON fields, CSV columns, XLSX Export and Metadata sheets, filenames, and the machine artifact. The recursive output guard and schema require only the P2-derived `tenant_id_hash`.

## 11. Money mapping

Machine authority remains envelope integer minor units. Display rows expose `revenue_minor: integer` plus `revenue_display: string`, rendered using `Decimal` with two fixed fractional digits. Float values are rejected recursively.

## 12. Artifact canonicalization and hash design

`b25-p11-artifact-framing-v1` length-frames the ordered artifact identity fields and canonical envelope payloads. The only final digest function is P2 `compute_artifact_hash(bytes)`. `generated_at` is included in artifact identity while envelope `semantic_truth_hash` remains independent of issuance metadata.

## 13. Signature-domain design

Artifact signatures use the distinct byte domain `b"skeldir:b25-p11:export-artifact:v1\x00"`. They use the P8 active Ed25519 key and public-key registry. Cross-verification with the TrustEnvelope domain is rejected in both directions.

## 14. External verification result

The artifact verifier resolves the public key independently, verifies every embedded envelope, recomputes the artifact hash, and verifies the artifact signature. Local unit result: PASS. Authoritative P11 CI result: PASS in pre-merge run `31279996557` / job `93159656057` and exact post-merge run `31280838192` / job `93161723259`.

## 15. Tamper tests

Tests mutate each authoritative top-level field, one embedded authoritative value, one identity byte without a new hash, the artifact domain, and the envelope/artifact domain direction. Each mutation is rejected.

## 16. Adversarial CSV parse evidence

Fixtures cover comma, quote, LF, CR, Unicode, empty and maximum-length channel values. `csv.reader` round-trips the emitted RFC 4180 rows exactly after the intentional display-only formula neutralization.

## 17. XLSX parser evidence

Both XLSX routes emit ZIP/Open XML workbooks with `Export` and `Metadata` worksheets. `openpyxl.load_workbook` opens the bytes and verifies cells and metadata. The former mock prefix is absent.

## 18. Injection evidence

Values beginning with `=`, `+`, `-`, `@`, tab, or CR are apostrophe-prefixed only at CSV/XLSX display serialization. Formula fixtures include `=1+1`, `+SUM(A1:A2)`, `-1+2`, `@SUM(A1:A2)`, and `=HYPERLINK(...)`; XLSX cell types are text. Passing a machine-authority path to the neutralizer raises.

## 19. Exact limits

| Boundary | Ceiling |
| --- | ---: |
| request body | 65,536 bytes |
| accepted subject references | 50 |
| evaluated/signed references per call | 2 |
| artifact bytes | 1,048,576 |
| concurrent export executions per worker | 2 |
| DB rows examined | `LIMIT n+1` only |
| whole-handler deadline | 1,500 ms |
| display date span | 31 days |
| display output rows | 1,000 |
| display serialized bytes | 1,048,576 |
| display distinct channels | 32 |
| display channel length | 256 characters |

No ceiling was raised to fit an implementation.

## 20. Maximum-valid measurements

The PostgreSQL maximum-valid test drives the maximum admitted request through concurrent two-envelope executions and emits `P11_RESOURCE_METRICS=`. PostgreSQL 15 local measurement: request 3,268 bytes; 50 accepted references; two evaluated per request; concurrency two; artifacts 22,295 bytes each; maximum latency 646.914 ms; RSS delta 233,472 bytes; maximum response amplification 6.823; 50 seeded source rows. Exact post-merge authoritative CI measurement: request 3,268 bytes; 50 accepted references; two evaluated per request; concurrency two; artifacts 22,295 bytes each; maximum latency 1,188.757 ms; RSS delta 5,292,032 bytes; maximum response amplification 6.822216; 50 seeded source rows; handler deadline 1,500 ms. Every local and authoritative measurement remained below the fixed ceiling.

## 21. Wrong-tenant tests

Direct two-tenant RLS and route-level wrong-tenant reference tests returned only the authorized tenant row and a non-disclosing typed refusal for the other tenant. Raw UUIDs were absent from both responses. Local PostgreSQL 15 result: PASS.

## 22. Read-only before/after

The PostgreSQL snapshot records relevant row counts before and after the export. Source financial/event tables did not change except for the test's deliberate unrelated-history seed; route execution changed only the permitted trust access/issuance audit surfaces. Local PostgreSQL 15 result: PASS.

## 23. No-LLM and no-compute proof

AST/source validation rejects aggregate SQL and imports/calls involving LLM providers, Celery, Bayesian inference, or attribution recomputation on the Track 2 path. NC-01 and NC-14 prove the checks fail under production mutations.

## 24. Negative-control matrix

| NC | Production mutation | Expected gate |
| --- | --- | --- |
| 01 | aggregate attribution SQL on authoritative path | P11-A |
| 02 | raw `tenant_id` output | P11-C |
| 03 | float `revenue_minor` | P11-D |
| 04 | remove embedded signed envelopes | P11-E |
| 05 | mutate authoritative value after signing | P11-E |
| 06 | mutate bytes without matching artifact hash | P11-E |
| 07 | cross-use artifact and envelope signature domains | P11-E |
| 08 | remove display authority marker | P11-F |
| 09 | restore mock XLSX body | P11-G |
| 10 | restore comma joining | P11-G |
| 11 | remove spreadsheet neutralizer | P11-G |
| 12 | remove admission ceiling | P11-H |
| 13 | materialize before budget | P11-H |
| 14 | add LLM/provider/recompute dependency | P11-I |
| 15 | accept `attribution_result` | P11-B |

Local validator: `negative_controls_fired=15`, PASS. Exact post-merge P11 CI: `negative_controls_fired=15`, PASS.

## 25. CI commands, exit codes, and reproduction contract

Reproduction contract: PostgreSQL 15; database `skeldir_b25_p11_ci`; flags `SKELDIR_TEST_POSTGRES_DSN`, `SKELDIR_RUN_P7_POSTGRES_TESTS=1`, `SKELDIR_RUN_P9_POSTGRES_TESTS=1`, `SKELDIR_RUN_P10_POSTGRES_TESTS=1`, `SKELDIR_RUN_P11_POSTGRES_TESTS=1`; dependencies from `backend/requirements-lock.txt` plus `pytest`, `jsonschema`, and `PyYAML`.

| Command | Exit |
| --- | ---: |
| `python scripts/ci/validate_b25_p11_export_compatibility.py --negative-control` | 0 |
| `python scripts/ci/validate_b25_p1_contracts.py --negative-control` through P10 preservation sweep | 0 for every phase |
| `python scripts/ci/enforce_b14_p5_export_log_artifact_no_leak.py` | 0 |
| `python scripts/ci/validate_m3_ci_governance.py --all` | 0 |
| `pytest backend/tests/trust -q` with the reproduction contract | 0 (`241 passed`) |
| Required B1.4 runtime export node against the reproduction database | 0 (`1 passed`) |

## 26. Test node IDs and pass/skip counts

- `backend/tests/trust/test_b25_p11_export_projection.py`: 5 passed.
- `backend/tests/trust/test_b25_p11_export_artifact.py`: 15 passed.
- `backend/tests/trust/test_b25_p11_postgres_physics.py`: 2 passed on PostgreSQL 15, zero skipped.
- `backend/tests/integration/test_b14_p5_export_log_artifact_no_leak_runtime.py::test_b14_p5_runtime_export_allowlist_blocks_identity_fields`: preserved and passed against the reproduction database.
- Full local trust run: 241 passed, zero skipped. Exact post-merge P11 trust regression: 241 passed, zero skipped (one warning).

## 27. P11 workflow run and job IDs

- Pull-request P11 workflow run/job on implementation head `6f005ca9d542a57ed08d8f112c01026a313a23f5`: `31279996557` / `93159656057`, SUCCESS.
- Post-merge P11 workflow run/job on exact merge SHA `b7385e04e0c66add56ffd36eabf86ec92cdd3429`: `31280838192` / `93161723259`, SUCCESS.
- Full main CI run on exact merge SHA `b7385e04e0c66add56ffd36eabf86ec92cdd3429`: `31280838255`, SUCCESS; 79-job graph, 78 successful, one intentional skip, zero failures.
- Required-check rollup before merge: PR #615 `mergeStateStatus=CLEAN`; strict protected-branch checks, including `B2.5-P11 Export Compatibility`, M0, M1, P1-P10, and the full CI graph, were successful. Merge used the ordinary protected workflow with no admin bypass.

## 28. P1 through P10 preservation results

The P11 workflow invokes all ten prior phase validators, including P10, with their negative controls. No P7/P8 authority module or reserved-scope trigger was changed. Local P1-P10 preservation sweep: PASS for every validator, with negative controls still firing. The pre-merge and exact post-merge P11 workflows both passed the complete preservation sweep; the post-merge log records every phase counter as passed and the prior P10 negative-control aggregate still firing.

## 29. Binary verdict

`COMPLETE` — implementation head `6f005ca9d542a57ed08d8f112c01026a313a23f5` merged through protected `main` as `b7385e04e0c66add56ffd36eabf86ec92cdd3429`; ancestry is proven; every required check was green; exact post-merge P11 run `31280838192` and full main CI run `31280838255` both succeeded without a ceiling increase or negative-control loss.

## Appendix A: P1-P10 scope-exclusion widening register

| ID | Decision executed |
| --- | --- |
| X-1 | Added `trust.export.create_limited` only to P9's design-partner scope set; reserved scopes unchanged. |
| X-2 | Preserved `EXPORT_ROW_ALLOWLIST`, `_enforce_export_row_no_leak`, `_enforce_export_payload_no_leak`, and literal `tenant_id` prohibition text. |
| X-3 | Preserved the required B1.4-P5 test node name while updating its body. |
| X-4 | Added the four P11 trust runtime paths to the P1 drift allowlist. The preservation sweep then falsified the plan's assumption that tests were outside the protected glob: `backend/**/trust*.py` also matches the three mandatory `backend/tests/trust/test_b25_p11_*` paths. Those exact phase tests were registered in this dedicated P11 change; no wildcard or non-P11 path was added. |
| X-5 | Used `trust_export` / `export_artifact`; no forbidden `export_trust`; no P7/P8 module edits. |
| X-6 | Added a new migration widening only the design-partner CHECK and restoring the original on downgrade; reserved trigger untouched. |
| X-7 | Registered the new route and operation ID in contract scope. |
| X-8 | Updated all four export contract authorities with an explicit version. |
| X-9 | Added this exact evidence path to M0 and M1. |
| X-10 | Added P11 workflow, Make target and `.PHONY`, required-status contract, topology, registries, matrix and forensics index. |
| X-11 | Made no CODEOWNERS change; existing patterns cover every new file. |
| X-12 | Verified P10 does not enforce an exact trust route set; no widening was made. |

## Appendix B: Forensic preservation corrections discovered by execution

The full preservation sweep falsified two omissions in the pre-adjudicated interaction register. Both corrections are contained exclusively in this dedicated P11 change and preserve the prior gates' negative controls:

1. P1's `backend/**/trust*.py` glob matches the three mandatory P11 test paths as well as runtime paths. The exact three test paths were registered; no wildcard was widened.
2. P2's scope guard allowed the prior `trust_keys.py` and `trust_api.py` phase routes by exact path but rejected the mandatory P11 `trust_export.py` route. That one exact route was registered; the forbidden route/signer/JWKS patterns remain unchanged and their negative controls still fire.
3. The authoritative PR run falsified X-9's assumption that M1 needed only the evidence path. M1's baseline-diff gate rejected ten mandatory P11 workflow, migration, API, dependency-lock, export-contract, and validator paths. Only the ten paths printed by job `93153625005` were registered; broad directories and prohibited patterns were not weakened.
4. Running M0 in its CI baseline-diff mode revealed the same omitted P11 surfaces. The same ten exact paths were registered in M0; migration and dependency prohibitions still apply to every path not explicitly phase-authorized.
5. The first P11 job proved the maximum-valid workload passed but its anchored evidence grep failed because pytest prefixed the printed metric with the test node ID. The metric emission now begins on a fresh line, preserving the exact `^P11_RESOURCE_METRICS=` machine-readable contract without weakening the grep.
6. Schema Authority run `31277812415`, job `93154162884`, proved the P11 migration-derived CHECK differed from the committed canonical SQL dump by exactly the new design-partner scope. The canonical SQL constraint was advanced to the migration-derived form; its reserved-scope `ALL` clause is byte-for-byte unchanged.
7. Full CI run `31278029710` exposed stale consumers of the intentionally versioned Track 1 response schema. B0.2 response parity still counted `.data`; B1.4-P3's structural/runtime proofs still required the former branch spelling and float-money fields; B1.4-P4 and B1.4-P7 runtime proofs still indexed the former raw-tenant/data shape; and three frontend/composite gates proved the generated export client was stale. The correction preserves P3 aggregate-versus-session-local behavior, rewrites the proof assertions to `projection_authority`, `tenant_id_hash`, `rows`, and integer `revenue_minor`, updates mock parity to the same contract, and checks in deterministically regenerated clients. No authority surface, allowlist wildcard, resource ceiling, or prior negative control was widened. Focused PostgreSQL validation after correction: P3 enforcer PASS; P3 and P4 export nodes 2 passed; P7 composed suite 3 passed, zero skipped; P11 validator 20 passed.
8. Corrected-head P11 run `31279063329`, job `93157320074`, and full CI run `31279063359` both passed, but the separately invoked M0/M1 required contexts then identified the two newly corrected contract-consumer paths as absent from their exact baseline-diff registries. Only `frontend/src/types/api/export.ts` and `scripts/test-response-parity.sh` were added to both registries; no directory, wildcard, prohibited surface, or authority path was broadened. The same exact-baseline local invocations passed after this correction.
9. Final candidate head `6f005ca9d542a57ed08d8f112c01026a313a23f5` passed M0 run `31279996407`, M1 run `31279996489`, P11 run `31279996557` / job `93159656057`, and full CI run `31279996434` (78 successful, one intentional skip, zero failures). PR #615 was `CLEAN` and merged without bypass as `b7385e04e0c66add56ffd36eabf86ec92cdd3429`.
10. Exact-merge post-main proof closed the phase independently of PR state: P11 run `31280838192` / job `93161723259` succeeded with 241 trust tests, 20 focused P11 tests, all 15 P11 production negative controls, the complete P1-P10 preservation sweep, and maximum-valid resource evidence below every fixed ceiling. Full main CI run `31280838255` succeeded across its 79-job graph with 78 successful jobs, one intentional skip, and zero failures.
