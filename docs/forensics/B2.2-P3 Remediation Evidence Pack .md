# B2.2-P3 Remediation Evidence Pack

Date: 2026-04-20  
Branch: `main` (local workspace)  
Scope: Canonical provider-preserving ingress identity envelope + explicit first-class verified-commerce ingress state

## 1) Initial findings (hypothesis validation)

- H01/H02/H03/H04/H05/H06/H08 were **validated** on ingress representation: webhook handlers emitted provider-fragmented identity fields and durable verified-commerce state was not represented as a dedicated first-class substrate.
- Monetary representation was not fully canonical as a dedicated ingress envelope substrate (provider-native formats were normalized for `revenue_cents`, but not as one explicit identity envelope contract).
- Existing durability and idempotency remained correct (`(tenant_id, idempotency_key)` authority), and no B2.3 matching logic was introduced.

## 2) Remediations implemented

- Added first-class durable table/model `webhook_ingress_identities` for canonical ingress envelope persistence:
  - `provider`
  - `provider_native_event_reference`
  - `provider_native_commerce_reference`
  - `normalized_commerce_reference_kind`
  - `normalized_commerce_reference_value`
  - `verified_amount_minor`
  - `verified_amount_currency`
  - `verified_amount_scale`
  - `event_timestamp`
  - `idempotency_key`
  - `verified_commerce_ingress_state`
- Added migration `202604201200_b22_p3_webhook_ingress_identity_envelope.py`:
  - creates table/constraints/indexes
  - enables + forces RLS
  - creates tenant isolation policy
  - grants runtime roles
- Updated webhook normalization in `backend/app/api/webhooks.py`:
  - emits typed normalized reference kinds:
    - Shopify: `shopify_order_id`
    - WooCommerce: `woocommerce_order_id`
    - Stripe: `stripe_payment_intent_id`
    - PayPal: `paypal_transaction_id`
  - emits canonical monetary fields (`verified_amount_minor`, `verified_amount_currency`, `verified_amount_scale`)
  - emits explicit verified ingress state (`verified_commerce_ingress_state=authenticity_verified`)
  - preserves provider-native references distinctly
- Updated ingestion persistence in `backend/app/ingestion/event_service.py`:
  - validates required canonical envelope fields for webhook sources
  - persists `WebhookIngressIdentity` as first-class durable substrate
  - compatibility guard: if table is not migrated yet, ingestion still works without runtime failure (to avoid breaking existing suites before migration is applied)
- Added CI follow-up compatibility and authority-sync fixes:
  - restored Stripe canonical-route `external_event_id` compatibility expected by B0.5.7-P3 webhook proof (`external_event_id=payment_intent_id`)
  - updated `db/schema/canonical_schema.sql` with `webhook_ingress_identities` DDL/constraints/FKs/indexes/RLS/policy so migration-derived schema authority checks remain deterministic
- Added merge-blocking P3 governance:
  - contract: `contracts-internal/governance/b22_p3_canonical_commerce_identity_envelope.main.json`
  - enforcer: `scripts/ci/enforce_b22_p3_canonical_commerce_identity_envelope.py`
  - enforcer tests: `backend/tests/test_b22_p3_canonical_identity_envelope_enforcer.py`
  - runtime proofs: `backend/tests/test_b22_p3_canonical_identity_envelope.py`
  - CI wiring in `.github/workflows/ci.yml`

## 3) Proof outcomes (local)

- `pytest backend/tests/test_b22_p3_canonical_identity_envelope_enforcer.py -q` -> **4 passed**
- `pytest backend/tests/test_b22_p3_canonical_identity_envelope.py -q` -> **1 passed, 2 skipped**
  - skips were expected locally because `webhook_ingress_identities` was not yet present in the current runtime DB.
  - in CI, `SKELDIR_B22_P3_REQUIRE_DB_PROOFS=1` + migrations make missing table a hard failure.
- Regression suites remained green:
  - `pytest backend/tests/test_b045_webhooks.py -q` -> **12 passed**
  - `pytest backend/tests/test_b046_integration.py -q` -> **8 passed**
  - `pytest backend/tests/test_b12_p8_error_contract_normalization.py -q` -> **14 passed**

## 4) Exit gate status (P3)

- Exit Gate 1 (canonical identity envelope): **Implemented in first-class durable substrate**
- Exit Gate 2 (explicit verified-commerce state): **Implemented as first-class durable column (`verified_commerce_ingress_state`)**
- Exit Gate 3 (canonical monetary representation): **Implemented (`verified_amount_minor/currency/scale`)**
- Exit Gate 4 (phase boundary integrity): **Maintained (no B2.3 matching/discrepancy logic added)**
- Exit Gate 5 (merge-blocking adjudication): **Implemented locally (contract + enforcer + CI wiring + proofs)**

## 5) Remaining external completion requirements

- Land commit(s) on remote `main` through protected-branch workflow.
- Run authoritative CI on `main` with migrations applied and all required checks green.
- Record the final green run URL/SHA as branch-protection completion evidence.
